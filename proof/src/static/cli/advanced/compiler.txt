Usage: python -m prove compiler [OPTIONS] FILE

  Convert between .prv lookup types and PDAT binary format.

Options:
  --load             Compile .prv lookup to PDAT binary.
  --dump             Dump PDAT binary to .prv source.
  -o, --output PATH  Output path.
  --help             Show this message and exit.


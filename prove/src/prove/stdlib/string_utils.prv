module StringUtils
  narrative: """Handles string operations"""

transforms int_to_string(n Integer) String
from
    to_string(n)

transforms bool_to_string(b Boolean) String
from
    to_string(b)

transforms string_length(s String) Integer
from
    len(s)

module Json
  narrative: """Handles JSON encoding operations"""

transforms encode_string(value String) String
from
    value

transforms encode_int(value Integer) String
from
    to_string(value)

transforms encode_bool(value Boolean) String
from
    to_string(value)

transforms encode_decimal(value Decimal) String
from
    to_string(value)

transforms encode_float(value Float) String
from
    to_string(value)

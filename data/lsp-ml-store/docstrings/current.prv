// Docstring knowledge base for LSP intent completions
// keyword | module | function | verb | docstring
// Auto-generated by scripts/ml_store.py — do not edit by hand
type DocstringMap:[Lookup] is String String String String String where
    r00000 | "absolute" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r00001 | "absolute" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r00002 | "absolute" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00003 | "accept" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00004 | "accept" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00005 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00006 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00007 | "add" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00008 | "add" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00009 | "add" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r00010 | "add" | "Time" | "date" | "transforms" | "Add days to a date"
    r00011 | "add" | "Simple_math" | "add" | "transforms" | "add(1, 2) == 3"
    r00012 | "added" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00013 | "added" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00014 | "added" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00015 | "added" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00016 | "against" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00017 | "all" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r00018 | "all" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r00019 | "all" | "Store" | "version" | "inputs" | "List all versions of a table"
    r00020 | "all" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r00021 | "all" | "Table" | "values" | "reads" | "Get all values in a table"
    r00022 | "all" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r00023 | "alphabetic" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00024 | "alphanumeric" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00025 | "and" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00026 | "and" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00027 | "and" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00028 | "and" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00029 | "and" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00030 | "and" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00031 | "and" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00032 | "and" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r00033 | "and" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00034 | "and" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00035 | "and" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00036 | "and" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00037 | "and" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00038 | "and" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00039 | "and" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00040 | "and" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00041 | "and" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r00042 | "and" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00043 | "and" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00044 | "and" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00045 | "append" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r00046 | "append" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00047 | "appending" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r00048 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00049 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00050 | "apply" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00051 | "are" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00052 | "arguments" | "System" | "process" | "inputs" | "Get command line arguments"
    r00053 | "array" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00054 | "array" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00055 | "array" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00056 | "array" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00057 | "array" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00058 | "array" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r00059 | "array" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00060 | "array" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00061 | "array" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00062 | "array" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00063 | "array" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r00064 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00065 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00066 | "array" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00067 | "array" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00068 | "array" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00069 | "array" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00070 | "array" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00071 | "array" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00072 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00073 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00074 | "array" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00075 | "array" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00076 | "array" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00077 | "arrays" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00078 | "await" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00079 | "back" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00080 | "backed" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00081 | "base" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r00082 | "base" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00083 | "base" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00084 | "base" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r00085 | "base" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00086 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00087 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00088 | "between" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00089 | "between" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00090 | "between" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00091 | "between" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00092 | "between" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00093 | "binary" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r00094 | "bind" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00095 | "blake" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00096 | "blake" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r00097 | "blake" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00098 | "blank" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00099 | "blocking" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00100 | "boolean" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r00101 | "boolean" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00102 | "boolean" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00103 | "boolean" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r00104 | "boolean" | "Types" | "bool" | "validates" | "Check if a Value is a boolean"
    r00105 | "both" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r00106 | "bounds" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00107 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00108 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00109 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00110 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00111 | "bounds" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00112 | "bounds" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00113 | "bounds" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00114 | "bounds" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00115 | "build" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00116 | "builder" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00117 | "builder" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r00118 | "builder" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00119 | "builder" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00120 | "builder" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00121 | "byte" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00122 | "byte" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00123 | "byte" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00124 | "byte" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r00125 | "byte" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00126 | "byte" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00127 | "byte" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00128 | "byte" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00129 | "byte" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00130 | "byte" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00131 | "byte" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00132 | "bytearray" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00133 | "bytearray" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00134 | "bytearray" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r00135 | "bytearray" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00136 | "bytes" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00137 | "bytes" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00138 | "bytes" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00139 | "called" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00140 | "ceiling" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r00141 | "centers" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r00142 | "character" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00143 | "character" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00144 | "character" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00145 | "character" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00146 | "character" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00147 | "character" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00148 | "character" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r00149 | "character" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00150 | "character" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00151 | "character" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00152 | "character" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00153 | "check" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00154 | "check" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00155 | "check" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00156 | "check" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r00157 | "check" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r00158 | "check" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00159 | "check" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00160 | "check" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00161 | "check" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00162 | "check" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00163 | "check" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00164 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r00165 | "check" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r00166 | "check" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00167 | "check" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00168 | "check" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r00169 | "check" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00170 | "check" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00171 | "check" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r00172 | "check" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00173 | "check" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00174 | "check" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00175 | "check" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r00176 | "check" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00177 | "check" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00178 | "check" | "Types" | "string" | "validates" | "Check if a value is a string"
    r00179 | "check" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r00180 | "check" | "Types" | "float" | "validates" | "Check if a value is a float"
    r00181 | "check" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00182 | "check" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00183 | "check" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00184 | "check" | "Types" | "text" | "validates" | "Check if a Value is text"
    r00185 | "check" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r00186 | "check" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00187 | "check" | "Types" | "bool" | "validates" | "Check if a Value is a boolean"
    r00188 | "check" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00189 | "check" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r00190 | "check" | "Types" | "null" | "validates" | "Check if a Value is null"
    r00191 | "check" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r00192 | "check" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00193 | "check" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00194 | "check" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r00195 | "checks" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00196 | "checks" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00197 | "checks" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00198 | "checks" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00199 | "checks" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00200 | "checks" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00201 | "chunk" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00202 | "chunk" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00203 | "chunk" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00204 | "chunks" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00205 | "clamp" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00206 | "clamp" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00207 | "clock" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00208 | "clock" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r00209 | "clock" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00210 | "clock" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00211 | "close" | "Network" | "socket" | "outputs" | "Close a connection"
    r00212 | "close" | "System" | "close" | "outputs" | "Close an open file handle"
    r00213 | "code" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r00214 | "code" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00215 | "code" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00216 | "column" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00217 | "combined" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00218 | "command" | "System" | "system" | "inputs" | "Execute a system command"
    r00219 | "command" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r00220 | "command" | "System" | "process" | "inputs" | "Get command line arguments"
    r00221 | "compile" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r00222 | "compiled" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r00223 | "component" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r00224 | "component" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r00225 | "component" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r00226 | "components" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r00227 | "components" | "Path" | "join" | "transforms" | "Join path components together"
    r00228 | "components" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00229 | "components" | "Time" | "date" | "creates" | "Create a date from components"
    r00230 | "components" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00231 | "components" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00232 | "compute" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00233 | "compute" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r00234 | "compute" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00235 | "concatenate" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00236 | "concurrency" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r00237 | "conflict" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r00238 | "conflict" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r00239 | "conflict" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00240 | "conflict" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r00241 | "conflict" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r00242 | "conflicted" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00243 | "conflicts" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00244 | "conflicts" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00245 | "connection" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00246 | "connection" | "Network" | "socket" | "outputs" | "Close a connection"
    r00247 | "connection" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00248 | "connection" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00249 | "connections" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00250 | "console" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00251 | "console" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00252 | "console" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00253 | "console" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00254 | "console" | "System" | "console" | "outputs" | "Print text to the console"
    r00255 | "console" | "System" | "console" | "inputs" | "Read a line from the console"
    r00256 | "console" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00257 | "console" | "System" | "console" | "validates" | "Validate console input"
    r00258 | "console" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00259 | "construct" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00260 | "consumed" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00261 | "contains" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00262 | "content" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r00263 | "content" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r00264 | "content" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r00265 | "content" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r00266 | "content" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r00267 | "content" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r00268 | "content" | "System" | "file" | "outputs" | "Write content to a file"
    r00269 | "content" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00270 | "contents" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00271 | "contents" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00272 | "contents" | "System" | "file" | "inputs" | "Read contents from a file"
    r00273 | "convert" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r00274 | "convert" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r00275 | "convert" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r00276 | "convert" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r00277 | "convert" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r00278 | "convert" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r00279 | "convert" | "Types" | "string" | "reads" | "Convert a float to a string"
    r00280 | "convert" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r00281 | "converts" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r00282 | "converts" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r00283 | "converts" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r00284 | "cooperative" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r00285 | "copy" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00286 | "copy" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00287 | "count" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00288 | "count" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r00289 | "create" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00290 | "create" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r00291 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00292 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00293 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00294 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00295 | "create" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00296 | "create" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00297 | "create" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00298 | "create" | "System" | "dir" | "outputs" | "Create a directory"
    r00299 | "create" | "Table" | "new" | "creates" | "Create a new empty table"
    r00300 | "create" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00301 | "create" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00302 | "create" | "Time" | "date" | "creates" | "Create a date from components"
    r00303 | "create" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00304 | "create" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00305 | "create" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00306 | "csv" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r00307 | "csv" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r00308 | "csv" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r00309 | "ctrl" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00310 | "current" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00311 | "current" | "Time" | "time" | "inputs" | "Get current time"
    r00312 | "data" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r00313 | "data" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r00314 | "data" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00315 | "data" | "Network" | "message" | "inputs" | "Read data from a socket"
    r00316 | "data" | "Network" | "message" | "outputs" | "Write data to a socket"
    r00317 | "date" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r00318 | "date" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r00319 | "date" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00320 | "date" | "Time" | "date" | "reads" | "Extract date from a time"
    r00321 | "date" | "Time" | "date" | "creates" | "Create a date from components"
    r00322 | "date" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00323 | "date" | "Time" | "date" | "transforms" | "Add days to a date"
    r00324 | "date" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00325 | "date" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r00326 | "date" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00327 | "datetime" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r00328 | "datetime" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r00329 | "datetime" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00330 | "datetime" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r00331 | "datetime" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00332 | "datetime" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00333 | "datetime" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r00334 | "days" | "Time" | "date" | "transforms" | "Add days to a date"
    r00335 | "days" | "Time" | "days" | "reads" | "Get number of days in a month"
    r00336 | "debug" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00337 | "decimal" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r00338 | "decimal" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r00339 | "decimal" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r00340 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00341 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r00342 | "decimal" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00343 | "decimal" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00344 | "decode" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00345 | "decode" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00346 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00347 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00348 | "default" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00349 | "default" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00350 | "default" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r00351 | "default" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r00352 | "diff" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00353 | "diff" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00354 | "difference" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00355 | "diffs" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00356 | "digest" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00357 | "digest" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00358 | "digest" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00359 | "digit" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00360 | "directory" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r00361 | "directory" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00362 | "directory" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r00363 | "directory" | "System" | "dir" | "inputs" | "List entries in a directory"
    r00364 | "directory" | "System" | "dir" | "outputs" | "Create a directory"
    r00365 | "directory" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r00366 | "double" | "Simple_math" | "double" | "transforms" | "double(5) == 10"
    r00367 | "doubles" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00368 | "duration" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r00369 | "duration" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r00370 | "duration" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00371 | "duration" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r00372 | "duration" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00373 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00374 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00375 | "each" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00376 | "each" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00377 | "each" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00378 | "echo" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00379 | "echo" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00380 | "echo" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00381 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00382 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r00383 | "element" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r00384 | "element" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r00385 | "element" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00386 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00387 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00388 | "element" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00389 | "element" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00390 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00391 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00392 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00393 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00394 | "element" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00395 | "element" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00396 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00397 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00398 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00399 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00400 | "element" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00401 | "element" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00402 | "element" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00403 | "element" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00404 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00405 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00406 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00407 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00408 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00409 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00410 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00411 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00412 | "empty" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00413 | "empty" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00414 | "empty" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00415 | "empty" | "Table" | "new" | "creates" | "Create a new empty table"
    r00416 | "encode" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00417 | "encode" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00418 | "end" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r00419 | "end" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r00420 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00421 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00422 | "end" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r00423 | "ends" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00424 | "ends" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r00425 | "entries" | "System" | "dir" | "inputs" | "List entries in a directory"
    r00426 | "entries" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r00427 | "eof" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00428 | "eof" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00429 | "error" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00430 | "error" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00431 | "events" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r00432 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00433 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00434 | "every" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00435 | "every" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00436 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00437 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00438 | "exactly" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00439 | "exactly" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00440 | "exclusive" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r00441 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00442 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00443 | "exclusive" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r00444 | "execute" | "System" | "system" | "inputs" | "Execute a system command"
    r00445 | "exists" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00446 | "exists" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r00447 | "exists" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r00448 | "exists" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00449 | "exists" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r00450 | "exists" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r00451 | "exists" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00452 | "exit" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r00453 | "exit" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r00454 | "exit" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00455 | "exits" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00456 | "exits" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00457 | "exits" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00458 | "exp" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r00459 | "expected" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r00460 | "expected" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r00461 | "expected" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00462 | "extension" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r00463 | "extension" | "Path" | "extension" | "reads" | "Get the file extension"
    r00464 | "extract" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00465 | "extract" | "Time" | "date" | "reads" | "Extract date from a time"
    r00466 | "extract" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r00467 | "extract" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r00468 | "fails" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r00469 | "falls" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00470 | "falls" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00471 | "fields" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r00472 | "fields" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r00473 | "file" | "Path" | "extension" | "reads" | "Get the file extension"
    r00474 | "file" | "System" | "file" | "inputs" | "Read contents from a file"
    r00475 | "file" | "System" | "file" | "outputs" | "Write content to a file"
    r00476 | "file" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00477 | "file" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r00478 | "file" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00479 | "file" | "System" | "close" | "outputs" | "Close an open file handle"
    r00480 | "file" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r00481 | "file" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r00482 | "file" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00483 | "file" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00484 | "filename" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r00485 | "filename" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r00486 | "final" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00487 | "find" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r00488 | "find" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r00489 | "find" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r00490 | "find" | "Text" | "index" | "reads" | "Find the index of a substring"
    r00491 | "fire" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r00492 | "first" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r00493 | "first" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r00494 | "float" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r00495 | "float" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r00496 | "float" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00497 | "float" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r00498 | "float" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r00499 | "float" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r00500 | "float" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r00501 | "float" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r00502 | "float" | "Types" | "float" | "validates" | "Check if a value is a float"
    r00503 | "float" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r00504 | "float" | "Types" | "float" | "creates" | "Parse a string as a float"
    r00505 | "float" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r00506 | "float" | "Types" | "string" | "reads" | "Convert a float to a string"
    r00507 | "floats" | "Math" | "min" | "reads" | "Minimum of two floats"
    r00508 | "floats" | "Math" | "max" | "reads" | "Maximum of two floats"
    r00509 | "floor" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r00510 | "for" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r00511 | "for" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00512 | "for" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00513 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00514 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00515 | "for" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r00516 | "for" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r00517 | "forget" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r00518 | "format" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r00519 | "format" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r00520 | "format" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r00521 | "format" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r00522 | "format" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00523 | "format" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00524 | "format" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00525 | "format" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r00526 | "formats" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r00527 | "formats" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r00528 | "found" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r00529 | "framed" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00530 | "from" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00531 | "from" | "Network" | "message" | "inputs" | "Read data from a socket"
    r00532 | "from" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00533 | "from" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r00534 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00535 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r00536 | "from" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r00537 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00538 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00539 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00540 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00541 | "from" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r00542 | "from" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r00543 | "from" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00544 | "from" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r00545 | "from" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00546 | "from" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r00547 | "from" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r00548 | "from" | "System" | "console" | "inputs" | "Read a line from the console"
    r00549 | "from" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00550 | "from" | "System" | "file" | "inputs" | "Read contents from a file"
    r00551 | "from" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00552 | "from" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r00553 | "from" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r00554 | "from" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r00555 | "from" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00556 | "from" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00557 | "from" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r00558 | "from" | "Time" | "date" | "reads" | "Extract date from a time"
    r00559 | "from" | "Time" | "date" | "creates" | "Create a date from components"
    r00560 | "from" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r00561 | "from" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00562 | "from" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r00563 | "from" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r00564 | "from" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00565 | "from" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00566 | "from" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00567 | "from" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00568 | "fully" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00569 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00570 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00571 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00572 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00573 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00574 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00575 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r00576 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r00577 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00578 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r00579 | "generate" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00580 | "get" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r00581 | "get" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r00582 | "get" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r00583 | "get" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r00584 | "get" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r00585 | "get" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r00586 | "get" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r00587 | "get" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r00588 | "get" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r00589 | "get" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r00590 | "get" | "Path" | "extension" | "reads" | "Get the file extension"
    r00591 | "get" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r00592 | "get" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r00593 | "get" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r00594 | "get" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r00595 | "get" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r00596 | "get" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r00597 | "get" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r00598 | "get" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00599 | "get" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r00600 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00601 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00602 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00603 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00604 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00605 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00606 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00607 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00608 | "get" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r00609 | "get" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00610 | "get" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r00611 | "get" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00612 | "get" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r00613 | "get" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r00614 | "get" | "System" | "process" | "inputs" | "Get command line arguments"
    r00615 | "get" | "Table" | "get" | "reads" | "Get a value by key"
    r00616 | "get" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r00617 | "get" | "Table" | "values" | "reads" | "Get all values in a table"
    r00618 | "get" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r00619 | "get" | "Text" | "length" | "reads" | "Get the length of a string"
    r00620 | "get" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r00621 | "get" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00622 | "get" | "Time" | "time" | "inputs" | "Get current time"
    r00623 | "get" | "Time" | "days" | "reads" | "Get number of days in a month"
    r00624 | "get" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r00625 | "get" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00626 | "gets" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r00627 | "given" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00628 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00629 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00630 | "given" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00631 | "given" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00632 | "given" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00633 | "handle" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r00634 | "handle" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00635 | "handle" | "System" | "close" | "outputs" | "Close an open file handle"
    r00636 | "handle" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r00637 | "handle" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r00638 | "hash" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00639 | "hash" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r00640 | "hash" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r00641 | "hash" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00642 | "hash" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r00643 | "hash" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r00644 | "hash" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00645 | "hash" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r00646 | "hash" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00647 | "hash" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r00648 | "have" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r00649 | "have" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00650 | "header" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00651 | "hex" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r00652 | "hex" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r00653 | "hex" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r00654 | "hexadecimal" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00655 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00656 | "hexadecimal" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r00657 | "hexadecimal" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r00658 | "hmac" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r00659 | "hmac" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r00660 | "host" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00661 | "host" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00662 | "host" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r00663 | "idx" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00664 | "idx" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00665 | "inclusive" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00666 | "inclusive" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00667 | "inclusive" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r00668 | "incoming" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00669 | "index" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r00670 | "index" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00671 | "index" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r00672 | "index" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r00673 | "index" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r00674 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00675 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00676 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00677 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00678 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00679 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00680 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00681 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00682 | "index" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00683 | "index" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00684 | "index" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00685 | "index" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00686 | "index" | "Text" | "index" | "reads" | "Find the index of a substring"
    r00687 | "info" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00688 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00689 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00690 | "inner" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r00691 | "input" | "System" | "console" | "validates" | "Validate console input"
    r00692 | "integer" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r00693 | "integer" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r00694 | "integer" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00695 | "integer" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r00696 | "integer" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r00697 | "integer" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r00698 | "integer" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r00699 | "integer" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r00700 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r00701 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r00702 | "integer" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00703 | "integer" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r00704 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r00705 | "integer" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r00706 | "integer" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r00707 | "integer" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r00708 | "integer" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r00709 | "integer" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r00710 | "integer" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r00711 | "integer" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r00712 | "integer" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00713 | "integers" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00714 | "integers" | "Math" | "min" | "reads" | "Minimum of two integers"
    r00715 | "integers" | "Math" | "max" | "reads" | "Maximum of two integers"
    r00716 | "integers" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r00717 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00718 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00719 | "integrity" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r00720 | "into" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r00721 | "into" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r00722 | "into" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r00723 | "into" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r00724 | "into" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00725 | "into" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r00726 | "into" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r00727 | "into" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r00728 | "into" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r00729 | "into" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r00730 | "into" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00731 | "into" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00732 | "iteration" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00733 | "iteration" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00734 | "iteration" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00735 | "its" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r00736 | "join" | "Path" | "join" | "transforms" | "Join path components together"
    r00737 | "join" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r00738 | "json" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r00739 | "json" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r00740 | "json" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r00741 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00742 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00743 | "key" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00744 | "key" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r00745 | "key" | "Table" | "get" | "reads" | "Get a value by key"
    r00746 | "key" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r00747 | "keys" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r00748 | "largest" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r00749 | "last" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r00750 | "leap" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r00751 | "left" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r00752 | "length" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r00753 | "length" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r00754 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00755 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00756 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00757 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00758 | "length" | "Text" | "length" | "reads" | "Get the length of a string"
    r00759 | "length" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00760 | "length" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00761 | "line" | "System" | "console" | "inputs" | "Read a line from the console"
    r00762 | "line" | "System" | "process" | "inputs" | "Get command line arguments"
    r00763 | "line" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00764 | "line" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r00765 | "line" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00766 | "line" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00767 | "line" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00768 | "line" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00769 | "lines" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00770 | "list" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00771 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00772 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r00773 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r00774 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r00775 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00776 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00777 | "list" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00778 | "list" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00779 | "list" | "Store" | "version" | "inputs" | "List all versions of a table"
    r00780 | "list" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00781 | "list" | "System" | "dir" | "inputs" | "List entries in a directory"
    r00782 | "list" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r00783 | "listen" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00784 | "listening" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00785 | "load" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r00786 | "load" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r00787 | "local" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r00788 | "logarithm" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r00789 | "logs" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r00790 | "lookup" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r00791 | "lookup" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r00792 | "lookup" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00793 | "loop" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r00794 | "loop" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00795 | "lowercase" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00796 | "lowercase" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r00797 | "many" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00798 | "match" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r00799 | "match" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r00800 | "match" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r00801 | "match" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r00802 | "matched" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r00803 | "matches" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r00804 | "matches" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r00805 | "matches" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00806 | "matches" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00807 | "matches" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00808 | "matches" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00809 | "matches" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r00810 | "matches" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r00811 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00812 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00813 | "maximum" | "Math" | "max" | "reads" | "Maximum of two integers"
    r00814 | "maximum" | "Math" | "max" | "reads" | "Maximum of two floats"
    r00815 | "merge" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00816 | "merge" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r00817 | "merge" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00818 | "merge" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r00819 | "merge" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00820 | "merged" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r00821 | "message" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00822 | "message" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00823 | "message" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00824 | "message" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00825 | "message" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r00826 | "message" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00827 | "minimum" | "Math" | "min" | "reads" | "Minimum of two integers"
    r00828 | "minimum" | "Math" | "min" | "reads" | "Minimum of two floats"
    r00829 | "month" | "Time" | "days" | "reads" | "Get number of days in a month"
    r00830 | "mutable" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00831 | "mutable" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00832 | "mutable" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00833 | "mutable" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00834 | "mutable" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00835 | "mutable" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00836 | "mutable" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00837 | "mutable" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00838 | "mutable" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00839 | "mutable" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00840 | "mutation" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00841 | "mutation" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00842 | "name" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r00843 | "name" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r00844 | "name" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r00845 | "name" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00846 | "name" | "System" | "process" | "validates" | "Validate a process name"
    r00847 | "natural" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r00848 | "nearest" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r00849 | "new" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00850 | "new" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00851 | "new" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00852 | "new" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r00853 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00854 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00855 | "new" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00856 | "new" | "Sequence" | "to_sequence" | "creates" | "Copy array contents into a new List"
    r00857 | "new" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00858 | "new" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00859 | "new" | "Table" | "new" | "creates" | "Create a new empty table"
    r00860 | "new" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r00861 | "new" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r00862 | "new" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00863 | "next" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00864 | "next" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00865 | "none" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00866 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00867 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00868 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00869 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00870 | "none" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00871 | "none" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00872 | "none" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00873 | "none" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00874 | "normalize" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r00875 | "not" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r00876 | "not" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r00877 | "not" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00878 | "null" | "Types" | "null" | "validates" | "Check if a Value is null"
    r00879 | "number" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r00880 | "number" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r00881 | "number" | "Time" | "days" | "reads" | "Get number of days in a month"
    r00882 | "number" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r00883 | "numeric" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r00884 | "object" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r00885 | "object" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r00886 | "occurrences" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r00887 | "occurrences" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r00888 | "octal" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r00889 | "one" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00890 | "one" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00891 | "one" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00892 | "one" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00893 | "open" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00894 | "open" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00895 | "open" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r00896 | "open" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00897 | "open" | "System" | "close" | "outputs" | "Close an open file handle"
    r00898 | "open" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r00899 | "open" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r00900 | "optimistic" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r00901 | "option" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00902 | "option" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r00903 | "option" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r00904 | "option" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r00905 | "option" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r00906 | "out" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00907 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00908 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00909 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00910 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00911 | "out" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00912 | "out" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r00913 | "out" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00914 | "out" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r00915 | "pads" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r00916 | "pads" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r00917 | "pair" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r00918 | "parameters" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00919 | "parent" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r00920 | "parse" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r00921 | "parse" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r00922 | "parse" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r00923 | "parse" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r00924 | "parse" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r00925 | "parse" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r00926 | "parse" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r00927 | "parse" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r00928 | "parse" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r00929 | "parse" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r00930 | "parse" | "Types" | "float" | "creates" | "Parse a string as a float"
    r00931 | "parses" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00932 | "past" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r00933 | "path" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00934 | "path" | "Path" | "join" | "transforms" | "Join path components together"
    r00935 | "path" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r00936 | "path" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r00937 | "path" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00938 | "path" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r00939 | "path" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00940 | "path" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r00941 | "pattern" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r00942 | "pattern" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r00943 | "pattern" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r00944 | "pattern" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r00945 | "pattern" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r00946 | "pattern" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r00947 | "pattern" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r00948 | "pattern" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r00949 | "pattern" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r00950 | "pattern" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r00951 | "pattern" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r00952 | "pattern" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r00953 | "pattern" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r00954 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00955 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r00956 | "place" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00957 | "place" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r00958 | "places" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r00959 | "places" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r00960 | "point" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00961 | "point" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00962 | "port" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00963 | "port" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00964 | "port" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r00965 | "position" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r00966 | "position" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r00967 | "position" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00968 | "positive" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00969 | "power" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r00970 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00971 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00972 | "prefix" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r00973 | "previous" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r00974 | "print" | "System" | "console" | "outputs" | "Print text to the console"
    r00975 | "print" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00976 | "process" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r00977 | "process" | "System" | "process" | "validates" | "Validate a process name"
    r00978 | "process" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00979 | "processes" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r00980 | "producing" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00981 | "prompt" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00982 | "query" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00983 | "raised" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r00984 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r00985 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r00986 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00987 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r00988 | "random" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00989 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00990 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r00991 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r00992 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r00993 | "range" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00994 | "range" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r00995 | "range" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00996 | "range" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r00997 | "reach" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r00998 | "reach" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r00999 | "read" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r01000 | "read" | "Network" | "message" | "inputs" | "Read data from a socket"
    r01001 | "read" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r01002 | "read" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r01003 | "read" | "System" | "console" | "inputs" | "Read a line from the console"
    r01004 | "read" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01005 | "read" | "System" | "file" | "inputs" | "Read contents from a file"
    r01006 | "read" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01007 | "read" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r01008 | "read" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01009 | "read" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01010 | "read" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01011 | "read" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01012 | "readable" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r01013 | "reading" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01014 | "reads" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01015 | "regex" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r01016 | "regex" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r01017 | "remote" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r01018 | "remote" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r01019 | "remove" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01020 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r01021 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r01022 | "repeat" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r01023 | "replace" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r01024 | "replace" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r01025 | "replaced" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01026 | "replaced" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01027 | "replacement" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r01028 | "resolve" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r01029 | "resolver" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r01030 | "result" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01031 | "result" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01032 | "result" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01033 | "result" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r01034 | "result" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r01035 | "return" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r01036 | "return" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01037 | "return" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01038 | "return" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01039 | "return" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r01040 | "return" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r01041 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01042 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01043 | "returning" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r01044 | "returns" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r01045 | "returns" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r01046 | "returns" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r01047 | "returns" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r01048 | "returns" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r01049 | "returns" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r01050 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01051 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01052 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01053 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01054 | "returns" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01055 | "returns" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01056 | "returns" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01057 | "returns" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r01058 | "returns" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01059 | "reverse" | "Sequence" | "reverse" | "transforms" | "Reverse a sequence"
    r01060 | "right" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r01061 | "rollback" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r01062 | "root" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r01063 | "round" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01064 | "row" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r01065 | "rows" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r01066 | "rows" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r01067 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01068 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01069 | "same" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r01070 | "same" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r01071 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01072 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01073 | "save" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01074 | "scheme" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01075 | "search" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01076 | "seconds" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r01077 | "send" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r01078 | "send" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01079 | "send" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01080 | "send" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01081 | "separator" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r01082 | "separator" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r01083 | "separator" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01084 | "sequence" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r01085 | "sequence" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r01086 | "sequence" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r01087 | "sequence" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r01088 | "sequence" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r01089 | "sequence" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r01090 | "sequence" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r01091 | "sequence" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01092 | "sequence" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r01093 | "sequence" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01094 | "sequence" | "Sequence" | "reverse" | "transforms" | "Reverse a sequence"
    r01095 | "sequence" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r01096 | "sequence" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r01097 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01098 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01099 | "serialize" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r01100 | "serialize" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r01101 | "serialize" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r01102 | "set" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r01103 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01104 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01105 | "set" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01106 | "set" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01107 | "set" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r01108 | "set" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds"
    r01109 | "set" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r01110 | "set" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r01111 | "severity" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r01112 | "severity" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01113 | "severity" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01114 | "severity" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01115 | "sha" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r01116 | "sha" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r01117 | "sha" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01118 | "sha" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r01119 | "sha" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r01120 | "sha" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01121 | "sha" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r01122 | "sha" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r01123 | "signature" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r01124 | "signature" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r01125 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01126 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01127 | "size" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01128 | "size" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01129 | "skips" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01130 | "slice" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01131 | "smallest" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r01132 | "socket" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01133 | "socket" | "Network" | "message" | "inputs" | "Read data from a socket"
    r01134 | "socket" | "Network" | "message" | "outputs" | "Write data to a socket"
    r01135 | "sort" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r01136 | "sort" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r01137 | "source" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r01138 | "source" | "Types" | "value" | "validates" | "Validate a source string"
    r01139 | "spawn" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r01140 | "specific" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r01141 | "specified" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r01142 | "specified" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r01143 | "specified" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r01144 | "specified" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r01145 | "specified" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r01146 | "specified" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r01147 | "split" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r01148 | "split" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r01149 | "square" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r01150 | "start" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r01151 | "start" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01152 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01153 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01154 | "start" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01155 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01156 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01157 | "starts" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r01158 | "status" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01159 | "stdin" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01160 | "stdin" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01161 | "stdin" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01162 | "stdout" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r01163 | "stdout" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01164 | "stdout" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01165 | "stdout" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01166 | "step" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01167 | "store" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01168 | "store" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01169 | "store" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01170 | "store" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01171 | "store" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r01172 | "store" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r01173 | "string" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r01174 | "string" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r01175 | "string" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r01176 | "string" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r01177 | "string" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r01178 | "string" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r01179 | "string" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r01180 | "string" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r01181 | "string" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r01182 | "string" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r01183 | "string" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r01184 | "string" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r01185 | "string" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r01186 | "string" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r01187 | "string" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r01188 | "string" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r01189 | "string" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r01190 | "string" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r01191 | "string" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r01192 | "string" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r01193 | "string" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r01194 | "string" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r01195 | "string" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r01196 | "string" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r01197 | "string" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r01198 | "string" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r01199 | "string" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r01200 | "string" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r01201 | "string" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r01202 | "string" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r01203 | "string" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r01204 | "string" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r01205 | "string" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r01206 | "string" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r01207 | "string" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01208 | "string" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r01209 | "string" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01210 | "string" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r01211 | "string" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r01212 | "string" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r01213 | "string" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r01214 | "string" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01215 | "string" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r01216 | "string" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r01217 | "string" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r01218 | "string" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01219 | "string" | "Text" | "length" | "reads" | "Get the length of a string"
    r01220 | "string" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r01221 | "string" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r01222 | "string" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r01223 | "string" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r01224 | "string" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r01225 | "string" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r01226 | "string" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r01227 | "string" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r01228 | "string" | "Text" | "builder" | "creates" | "Create a new string builder"
    r01229 | "string" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r01230 | "string" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r01231 | "string" | "Types" | "string" | "validates" | "Check if a value is a string"
    r01232 | "string" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r01233 | "string" | "Types" | "float" | "creates" | "Parse a string as a float"
    r01234 | "string" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r01235 | "string" | "Types" | "string" | "reads" | "Convert a float to a string"
    r01236 | "string" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r01237 | "string" | "Types" | "value" | "validates" | "Validate a source string"
    r01238 | "string" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r01239 | "strings" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r01240 | "strings" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r01241 | "structural" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r01242 | "sub" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r01243 | "substring" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01244 | "substring" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r01245 | "substring" | "Text" | "index" | "reads" | "Find the index of a substring"
    r01246 | "substring" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r01247 | "succeeded" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01248 | "successful" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01249 | "suffix" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r01250 | "system" | "System" | "system" | "inputs" | "Execute a system command"
    r01251 | "table" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r01252 | "table" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01253 | "table" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01254 | "table" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r01255 | "table" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r01256 | "table" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r01257 | "table" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01258 | "table" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r01259 | "table" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r01260 | "table" | "Store" | "version" | "inputs" | "List all versions of a table"
    r01261 | "table" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01262 | "table" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r01263 | "table" | "Table" | "new" | "creates" | "Create a new empty table"
    r01264 | "table" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r01265 | "table" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r01266 | "table" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01267 | "table" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r01268 | "table" | "Table" | "values" | "reads" | "Get all values in a table"
    r01269 | "table" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r01270 | "tables" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r01271 | "tag" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01272 | "tcp" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r01273 | "tcp" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r01274 | "termination" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01275 | "test" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r01276 | "test" | "Example" | "user" | "transforms" | "test"
    r01277 | "test" | "Example" | "ensure_user" | "matches" | "test2"
    r01278 | "text" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r01279 | "text" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01280 | "text" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r01281 | "text" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01282 | "text" | "System" | "console" | "outputs" | "Print text to the console"
    r01283 | "text" | "Types" | "text" | "validates" | "Check if a Value is text"
    r01284 | "that" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01285 | "that" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r01286 | "that" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r01287 | "that" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r01288 | "that" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01289 | "the" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r01290 | "the" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r01291 | "the" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r01292 | "the" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r01293 | "the" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r01294 | "the" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r01295 | "the" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r01296 | "the" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01297 | "the" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01298 | "the" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r01299 | "the" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r01300 | "the" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r01301 | "the" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r01302 | "the" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r01303 | "the" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r01304 | "the" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r01305 | "the" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r01306 | "the" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r01307 | "the" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r01308 | "the" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01309 | "the" | "Path" | "extension" | "reads" | "Get the file extension"
    r01310 | "the" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01311 | "the" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01312 | "the" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r01313 | "the" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r01314 | "the" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r01315 | "the" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r01316 | "the" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r01317 | "the" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r01318 | "the" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r01319 | "the" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01320 | "the" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r01321 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01322 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01323 | "the" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01324 | "the" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01325 | "the" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r01326 | "the" | "Sequence" | "set" | "transforms" | "In-place mutation - returns the same array (for Array<T>:[Mutable])"
    r01327 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01328 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01329 | "the" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01330 | "the" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01331 | "the" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01332 | "the" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r01333 | "the" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r01334 | "the" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r01335 | "the" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01336 | "the" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01337 | "the" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01338 | "the" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01339 | "the" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01340 | "the" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r01341 | "the" | "System" | "console" | "outputs" | "Print text to the console"
    r01342 | "the" | "System" | "console" | "inputs" | "Read a line from the console"
    r01343 | "the" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01344 | "the" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01345 | "the" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01346 | "the" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r01347 | "the" | "Text" | "length" | "reads" | "Get the length of a string"
    r01348 | "the" | "Text" | "index" | "reads" | "Find the index of a substring"
    r01349 | "the" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r01350 | "the" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r01351 | "the" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r01352 | "the" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r01353 | "the" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r01354 | "the" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01355 | "the" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01356 | "the" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01357 | "them" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01358 | "then" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01359 | "three" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r01360 | "three" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r01361 | "time" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r01362 | "time" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r01363 | "time" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r01364 | "time" | "Time" | "time" | "inputs" | "Get current time"
    r01365 | "time" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r01366 | "time" | "Time" | "date" | "reads" | "Extract date from a time"
    r01367 | "time" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r01368 | "time" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r01369 | "time" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01370 | "time" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01371 | "time" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01372 | "times" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r01373 | "times" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r01374 | "timestamp" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r01375 | "timestamp" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01376 | "timestamp" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01377 | "timestamp" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01378 | "timestamp" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r01379 | "together" | "Path" | "join" | "transforms" | "Join path components together"
    r01380 | "toml" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r01381 | "toml" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r01382 | "toml" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r01383 | "total" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r01384 | "trim" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r01385 | "truncates" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01386 | "two" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01387 | "two" | "Math" | "min" | "reads" | "Minimum of two integers"
    r01388 | "two" | "Math" | "min" | "reads" | "Minimum of two floats"
    r01389 | "two" | "Math" | "max" | "reads" | "Maximum of two integers"
    r01390 | "two" | "Math" | "max" | "reads" | "Maximum of two floats"
    r01391 | "two" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r01392 | "two" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r01393 | "two" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r01394 | "type" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01395 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01396 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01397 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01398 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01399 | "unicode" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r01400 | "unicode" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01401 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01402 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01403 | "unit" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r01404 | "unit" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r01405 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01406 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01407 | "until" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r01408 | "unwrap" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r01409 | "unwrap" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r01410 | "unwrap" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r01411 | "unwrap" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r01412 | "unwrap" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r01413 | "uppercase" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r01414 | "uppercase" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r01415 | "url" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r01416 | "url" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01417 | "url" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r01418 | "url" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r01419 | "url" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r01420 | "url" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r01421 | "using" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r01422 | "using" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r01423 | "using" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r01424 | "using" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r01425 | "using" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r01426 | "using" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r01427 | "using" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r01428 | "using" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r01429 | "val" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01430 | "val" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01431 | "valid" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r01432 | "valid" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r01433 | "valid" | "Time" | "date" | "validates" | "Check if a date is valid"
    r01434 | "valid" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r01435 | "valid" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r01436 | "validate" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r01437 | "validate" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r01438 | "validate" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r01439 | "validate" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r01440 | "validate" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01441 | "validate" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r01442 | "validate" | "System" | "console" | "validates" | "Validate console input"
    r01443 | "validate" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r01444 | "validate" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r01445 | "validate" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r01446 | "validate" | "System" | "process" | "validates" | "Validate a process name"
    r01447 | "validate" | "Types" | "value" | "validates" | "Validate a source string"
    r01448 | "value" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r01449 | "value" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r01450 | "value" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01451 | "value" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r01452 | "value" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r01453 | "value" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r01454 | "value" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r01455 | "value" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r01456 | "value" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01457 | "value" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r01458 | "value" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r01459 | "value" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r01460 | "value" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r01461 | "value" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r01462 | "value" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r01463 | "value" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r01464 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01465 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01466 | "value" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01467 | "value" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01468 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01469 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01470 | "value" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01471 | "value" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01472 | "value" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r01473 | "value" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r01474 | "value" | "Table" | "get" | "reads" | "Get a value by key"
    r01475 | "value" | "Types" | "string" | "validates" | "Check if a value is a string"
    r01476 | "value" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r01477 | "value" | "Types" | "float" | "validates" | "Check if a value is a float"
    r01478 | "value" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r01479 | "value" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r01480 | "value" | "Types" | "character" | "validates" | "Check if a value is a character"
    r01481 | "value" | "Types" | "text" | "validates" | "Check if a Value is text"
    r01482 | "value" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r01483 | "value" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r01484 | "value" | "Types" | "bool" | "validates" | "Check if a Value is a boolean"
    r01485 | "value" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r01486 | "value" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r01487 | "value" | "Types" | "null" | "validates" | "Check if a Value is null"
    r01488 | "value" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r01489 | "value" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r01490 | "value" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r01491 | "values" | "Table" | "values" | "reads" | "Get all values in a table"
    r01492 | "variant" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01493 | "variant" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01494 | "variant" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01495 | "variant" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01496 | "variant" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r01497 | "variant" | "Sequence" | "set_safe" | "transforms" | "Set element at index; returns None if out of bounds - mutable variant"
    r01498 | "variant" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01499 | "verify" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01500 | "verify" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01501 | "verify" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01502 | "verify" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r01503 | "version" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r01504 | "versions" | "Store" | "version" | "inputs" | "List all versions of a table"
    r01505 | "waiting" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r01506 | "warning" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01507 | "way" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r01508 | "way" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r01509 | "weekday" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r01510 | "weekend" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r01511 | "when" | "Main" | "read_chunks" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01512 | "when" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01513 | "whether" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01514 | "whitespace" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r01515 | "whitespace" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r01516 | "width" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r01517 | "width" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r01518 | "width" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r01519 | "with" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r01520 | "with" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r01521 | "with" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r01522 | "with" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r01523 | "with" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01524 | "with" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01525 | "with" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01526 | "with" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01527 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01528 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01529 | "with" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01530 | "with" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01531 | "with" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01532 | "with" | "Sequence" | "set" | "transforms" | "Return a new array with element at idx replaced by val"
    r01533 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01534 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01535 | "with" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01536 | "with" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r01537 | "with" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01538 | "with" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r01539 | "with" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r01540 | "with" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r01541 | "with" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01542 | "within" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r01543 | "within" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r01544 | "within" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r01545 | "within" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r01546 | "without" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01547 | "without" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01548 | "without" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r01549 | "write" | "Network" | "message" | "outputs" | "Write data to a socket"
    r01550 | "write" | "System" | "file" | "outputs" | "Write content to a file"
    r01551 | "write" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r01552 | "write" | "Main" | "write_chunks" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01553 | "writing" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01554 | "year" | "Time" | "days" | "validates" | "Check if a year is a leap year"

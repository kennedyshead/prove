# V1.0 Feature Gap Analysis

Comparison of documented/planned features vs actual implementation.
Generated 2026-03-08. Checked against docs, plans (v0.1–v0.9.7), and source code.

---

## Legend

- [x] Implemented and working
- [~] Partially implemented (details noted)
- [ ] Not implemented / parsed only

---

## 1. Comptime Execution (v0.9.3 plan — incomplete)

The interpreter (`interpreter.py`, 368 lines) exists and is called by the optimizer for
constant-folding pure calls. However, full comptime block execution as described in docs
is incomplete.

- [x] `comptime` keyword parsed in lexer/parser
- [x] `comptime` expressions type-checked in checker (E410–E422 codes)
- [x] Tree-walking interpreter for pure constant expressions (literals, binary ops, unary ops, match)
- [x] `read()` function for file I/O in comptime
- [x] Optimizer calls interpreter for compile-time evaluation of pure calls with constant args
- [ ] **Comptime blocks in user code** — `comptime { ... }` blocks are parsed but NOT executed
      at compile time; they emit no C code. No working example exists. (`module_features_demo`
      is expected-fail.)
- [ ] **Build dependency tracking** — files read via comptime `read()` should become build deps;
      not wired into builder
- [ ] **Comptime match for conditional compilation** — documented in `compiler.md` but no
      evidence of working in emitter

**Refs:** `docs/compiler.md` (Comptime section), `prove-py/archive/0.9.3-PLAN.md`,
`interpreter.py`, `optimizer.py:_ct_eval_pure_calls`

---

## 2. Linear Types & Ownership (v0.9.4/v0.9.7 plans — partial)

- [x] `Own` and `Mutable` type modifiers defined in `types.py`
- [x] Basic use-after-move detection via `_moved_vars` set in checker (E340)
- [x] `BorrowType` defined and used for compiler-inferred read-only parameter borrows
- [x] `_infer_param_borrows()` in checker detects read-only parameters
- [~] **Move tracking** — tracks moves through function call arguments (`_check_calls.py:432`)
      and assignment (`checker.py:1535–1538`); does NOT track:
  - [x] Assignment moves (`x = y` where y is Own)
  - [ ] Complex expression moves (chained operations on owned values)
  - [ ] Nested moves (fields of owned values)
  - [ ] Return-position moves
- [x] **Borrow inference** — `_infer_param_borrows()` is wired and active
      (`checker.py:840–845`); infers read-only parameters and wraps their types as
      `BorrowType` to avoid unnecessary copies

**Refs:** `docs/types.md` (Ownership Lite), `prove-py/archive/0.9.4-PLAN.md`,
`prove-py/archive/0.9.7-PLAN.md`, `checker.py:2115–2210`, `_check_contracts.py:430`

---

## 3. Memory Regions for Local Temporaries (v0.9.5/v0.9.7 plans — partial)

The region runtime exists and is initialized, but is NOT used for local temporary allocation.

- [x] `prove_region.c/h` runtime with `enter`/`exit`/`alloc` (133 lines total)
- [x] `_global_region` created in `prove_runtime.c` init
- [x] **Per-function region scoping** — `prove_region_enter/exit` emitted in ALL
      functions via `_emit_function()` (`c_emitter.py:595,619`), not just main
- [ ] **`prove_region_alloc` for temporaries** — no temporary allocations routed through regions;
      all allocation still via `prove_arena` or malloc

**Refs:** `docs/design.md` (Zero-Cost Abstractions), `prove-py/archive/0.9.5-PLAN.md`,
`prove-py/archive/0.9.7-PLAN.md`, `c_emitter.py:595,619`, `prove_region.c`

---

## 4. AI-Resistance Features — Parsed but Not Enforced

These keywords are lexed, parsed into AST nodes, and formatted — but the checker/emitter
do nothing with them beyond basic warnings. Additionally, the module-level AI-resistance
keywords (`domain`, `temporal`, `invariant_network`) have incomplete parsing support that
causes syntax highlighting issues and spurious linting errors. The `module_features_demo`
example is expected-to-fail for this reason.

### 4a. Counterfactual Annotations (`why_not`, `chosen`)

- [x] Lexed and parsed into `FunctionDef.why_not` (list) and `FunctionDef.chosen` (str)
- [x] Formatter preserves them
- [x] LSP provides keyword completion with docs
- [ ] **No semantic checking** — not verified against code, not used by prover
- [ ] **No documentation generation** — not surfaced in `prove check` output

**Refs:** `docs/ai-resistance.md` (Parsed section), `parser.py:492–496`

### 4b. Temporal Effect Ordering (`temporal:`)

- [~] Parsed in module declaration (`parser.py:1226`)
- [ ] **Incomplete parsing** — causes syntax highlighting and linting errors when used
- [ ] **No enforcement** — declared operation ordering is never checked; the checker
      ignores `temporal` entirely

**Refs:** `docs/syntax.md` (temporal), `docs/ai-resistance.md` (Parsed section)

### 4c. Intent Annotations (`intent:`)

- [x] Parsed into `FunctionDef.intent` (str)
- [x] Formatter preserves it
- [~] **Warning only** — W311 if `intent` declared without `ensures`/`requires`
- [ ] **No deeper verification** — intent not compared to actual behavior

**Refs:** `docs/syntax.md` (intent), `_check_contracts.py:168–176`

### 4d. Invariant Networks (`invariant_network`, `satisfies`)

- [~] `invariant_network` blocks parsed with rules (`parser.py:1307`)
- [x] `satisfies` parsed on function definitions
- [x] Checker validates `satisfies` references an existing type (E382)
- [ ] **Incomplete parsing** — causes syntax highlighting and linting errors when used
- [ ] **No invariant verification** — rules inside `invariant_network` blocks are never
      checked against function implementations
- [ ] **No cross-function coherence checking**

**Refs:** `docs/syntax.md` (invariant_network), `docs/ai-resistance.md`,
`_check_contracts.py`

### 4e. Domain Declarations (`domain`)

- [~] Keyword lexed and parsed
- [ ] **Incomplete parsing** — causes syntax highlighting and linting errors when used
- [ ] **No enforcement or use** — domain tag is stored but never used for anything

**Refs:** `docs/ai-resistance.md` (Parsed section)

---

## 5. Optimizer Passes — Documented but Not Implemented

The `docs/compiler.md` and `archive/0.5-PLAN.md` describe 6 optimizer passes. Only some
are in the actual `optimizer.py`.

**Implemented passes (in `optimizer.py:optimize()`):**
- [x] Runtime dependency collection
- [x] Tail call optimization
- [x] Dead branch elimination
- [x] Compile-time evaluation of pure calls
- [x] Small function inlining
- [x] Dead code elimination
- [x] Memoization candidate identification
- [x] Match compilation (consecutive match merge)

**Documented/planned but NOT implemented:**
- [ ] **Iterator fusion** — `map(filter(...))` fusing into single pass (described in
      `0.5-PLAN.md` Part 2 Phase 2). No code in `optimizer.py`.
- [ ] **Copy elision** — avoid copies when source not reused (described in
      `0.5-PLAN.md` Part 2 Phase 3). No code in `optimizer.py`.
- [ ] **Match to jump tables** — compile match to efficient decision trees/jump tables
      (described in `0.5-PLAN.md` Part 2 Phase 4). Current match compilation only merges
      consecutive matches, does NOT generate jump tables.

**Refs:** `docs/compiler.md` (Optimizer Passes), `prove-py/archive/0.5-PLAN.md`,
`optimizer.py:107–116`

---

## 6. Concurrency / Parallelism

Documented in `docs/design.md` but nothing is implemented.

- [ ] **`par_map`** — parallel map operation (mentioned in design.md)
- [ ] **Structured concurrency** — typed, no data races (mentioned in design.md)
- [ ] **Effect types for concurrency** — `Async` effect mentioned in syntax.md naming
      conventions but no implementation exists

**Refs:** `docs/design.md` (Concurrency section)

---

## 7. Proposed AI-Resistance (Not Yet Parsed)

These are in `docs/ai-resistance.md` under "Proposed" — not even parsed yet.

- [ ] **Refutation challenges** — compiler generates plausible-but-wrong alternatives,
      programmer must explain why they fail
- [ ] **Semantic commit verification** — compiler verifies commit messages match changes
- [ ] **Context-dependent syntax** — syntax adapts based on module's `domain`
- [ ] **Non-local coherence enforcement** — module must tell coherent "story"

**Refs:** `docs/ai-resistance.md` (Proposed section)

---

## 8. Future AI-Resistance Research (Post-1.0)

Not expected for V1.0 but documented for completeness.

- [ ] **Binary `.prv` format** — structured source, not human-readable text
- [ ] **Semantic normalization** — canonicalize code, strip names, store map separately
- [ ] **Fragmented source** — function definition split across `.prv`, `.explain`, `.intent` files
- [ ] **Identity-bound compilation** — cryptographic signature chain
- [ ] **Project-specific grammars** — syntactic extensions via `prove.toml`

**Refs:** `docs/ai-resistance.md` (Future Research section)

---

## 9. Type System Gaps

- [x] Refinement types with `where` constraints (range, regex, comparison)
- [x] Type modifiers (`Integer:[16 Unsigned]`, `String:[ASCII 255]`, `Mutable`, `Own`)
- [x] Generic types (`List<T>`, `Option<T>`, `Result<T, E>`, `Table<V>`)
- [x] Lookup types (single-type bidirectional)
- [x] Binary lookup types (multi-column)
- [ ] **Multi-type lookup** — `type X:[Lookup] is String | Integer where ...` documented
      as planned v1.X in `types.md`
- [ ] **Row polymorphism** — mentioned in `types.md` algebraic types section, no
      implementation evidence
- [~] **Refinement type static rejection** — docs say "compiler rejects invalid values
      statically"; actual implementation inserts runtime checks (see `_emit_stmts.py`
      refinement validation). True static analysis not implemented.

**Refs:** `docs/types.md`, `_emit_stmts.py` (refinement validation emission)

---

## 10. Contract / Verification Gaps

- [x] `ensures` / `requires` parsed, type-checked, generate property tests
- [x] `explain` blocks with strict/loose mode, named entries, `when` conditions
- [x] `believe` requires `ensures` (E393), generates 3x adversarial tests
- [x] `know` / `assume` parsed and type-checked; `assume` emits runtime assertion
- [x] `terminates` required for recursive functions (E366)
- [x] `near_miss` generates boundary tests
- [x] Mutation testing (`mutator.py`)
- [ ] **Formal proof** — `know` claims the "compiler can prove it" (docs) but the compiler
      does NOT actually prove anything; it just type-checks that the expression is boolean.
      No SMT solver, no proof engine.
- [ ] **Verification chain propagation** — docs say `ensures` propagates through call graph
      and warns when called functions lack `ensures`. Current implementation: `prove check`
      reports statistics but does NOT emit per-call-site warnings for unverified callees.
- [ ] **`near_miss` distinct boundary verification** — docs say "compiler verifies each
      exercises a distinct boundary". Implementation generates test cases but doesn't verify
      distinctness.

**Refs:** `docs/contracts.md`, `docs/syntax.md`, `prover.py`, `testing.py`

---

## 11. C Runtime Gaps

### Missing dedicated test files

- [ ] `test_time_runtime_c.py` — Time module has no C runtime tests (exercised via e2e only)
- [ ] `test_random_runtime_c.py` — Random module has no C runtime tests
- [ ] `test_hash_crypto_runtime_c.py` — Hash module has no C runtime tests
- [ ] `test_bytes_runtime_c.py` — Bytes module has no C runtime tests

**Refs:** `prove-py/tests/` (compare existing `test_*_runtime_c.py` files to stdlib modules)

---

## 12. Documentation Claims Not Matching Implementation

| Claim (in docs) | Reality |
|------------------|---------|
| "Effects: CamelCase (IO, Fail, Async)" (`syntax.md`) | No effect types exist in the type system |
| "Row polymorphism" (`types.md`) | Not implemented |
| "Compiler rejects invalid values statically" (`types.md`, `index.md`) | Runtime checks emitted, not static rejection |
| "`know:` — compiler can prove it" (`syntax.md`) | Just type-checked as boolean, no proof engine |
| "Pure functions get automatic memoization and parallelism" (`types.md`) | Memoization candidates identified, tables emitted; parallelism not implemented |
| "Lambdas: No closures — cannot reference enclosing scope" (`syntax.md`) | Enforced (E364), but docs imply this is a future feature; it's a permanent design choice |
| "Reference counting only where ownership shared" (`design.md`) | Basic RC in emitter but not ownership-aware; scope-based release only |

---

## 13. CLI / Tooling Gaps

- [x] All 7 CLI commands work (`new`, `build`, `check`, `test`, `format`, `view`, `lsp`)
- [x] `--md` flag for checking/formatting Markdown code blocks
- [x] `--strict` promotes warnings to errors
- [x] `--stdin` for formatter
- [ ] **`prove export`** — `export.py` exists but is NOT wired as a CLI command
- [ ] **`prove lint`** — mentioned in archived README plans (`v0.4.2-PLAN.md`) but never
      implemented as a separate command; linting is part of `prove check`

---

## Summary: Critical V1.0 Blockers

These are the features that V1.0 claims to include (per `docs/roadmap.md`) but are missing:

| Feature | Status | Priority |
|---------|--------|----------|
| Comptime execution | Interpreter exists, blocks not wired | High |
| Linear types (comprehensive moves) | Assignment + call-arg moves tracked; no nested/return-position moves | Medium |
| Memory regions (temporaries via region) | Per-function scoping wired; temporaries still use arena | Medium |
| AI-resistance enforcement (temporal, invariant_network) | Parsed only | Medium |
| Iterator fusion / copy elision | Not implemented | Low |
| Formal verification (`know` proofs) | Not implemented | Low |
| Concurrency (`par_map`) | Not implemented | Low (may defer to post-1.0) |
| C runtime test coverage (4 modules) | Missing test files | Medium |

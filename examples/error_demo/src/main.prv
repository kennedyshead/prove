module Main
  narrative: """Demonstrates Error module operations.

  Missing runtime dependencies (build fails). Expected to fail: check, check_md, check_strict, build, build_debug, build_mutate, format_check, format_check_md, view.
  """
  System outputs console

main() Result<Unit, Error>!
from
    console("Error module demo")

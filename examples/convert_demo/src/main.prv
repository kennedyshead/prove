module Main
  narrative: """Demonstrates Types module operations.
  """
  System outputs console
  Types reads string code

main() Result<Unit, Error>!
from
    console(f"string(42) = {string(42)}")
    console(f"string(true) = {string(true)}")
    console(f"code('A') = {code('A')}")

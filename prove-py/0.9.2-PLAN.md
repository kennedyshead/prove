# Prove v0.9.2 — Documentation Parity

## Goal

Make the documentation accurately reflect the current implementation state.
Every doc page should clearly distinguish between implemented features and
planned/upcoming features. No doc page should claim a feature is implemented
when it isn't.

---

## Changes

### `docs/diagnostics.md` — Add missing diagnostic codes

6 codes implemented in the checker but missing from docs:

| Code | Description |
|------|-------------|
| E321 | Type mismatch in constant/variable definition |
| E322 | Return type mismatch |
| E352 | Function calls not allowed in `where` constraints |
| E381 | `requires` expression must be Boolean |
| E384 | `know` expression must be Boolean |
| E385 | `assume` expression must be Boolean |

### `docs/compiler.md` — Document optimizer passes

6 optimizer passes exist in `optimizer.py` but are undocumented:

1. Tail call optimization
2. Dead branch elimination
3. Small function inlining
4. Match compilation (merge consecutive matches)
5. Copy elision (placeholder)
6. Iterator fusion (placeholder)

### `docs/roadmap.md` — Fix stale numbers + add 0.9.x roadmap

- Update test count: 506 → 612
- Add v0.9.1 (doc parity) entry
- Add v0.9.2–0.9.5 entries for future features
- Move mutation testing to planned

### `docs/ai-resistance.md` — Reorganize by implementation status

Replace Phase 1 / Phase 2 / Phase 3 structure with status-based sections:

- **Implemented**: explain verification, verb purity enforcement, exhaustive
  match, refinement types, near_miss testing, epistemic checking (basic)
- **Parsed (syntax ready, enforcement upcoming)**: why_not, chosen, temporal,
  intent, invariant_network, satisfies, domain
- **Upcoming**: refutation challenges, semantic commit verification,
  context-dependent syntax behavior, non-local coherence enforcement
- **Future research (post-1.0)**: binary .prv format, semantic normalization,
  fragmented source, identity-bound compilation, project-specific grammars

### `docs/design.md` — Fix unimplemented claims

Mark as planned:

- "Pure functions auto-memoized and inlined"
- "Region-based memory for short-lived allocations"
- "Reference counting only where ownership is shared"

### `docs/types.md` — Mark unimplemented type features

- `Own` (linear ownership) → upcoming
- `Mutable` type modifier → upcoming
- "Compiler-inferred borrows" → upcoming

### `docs/syntax.md` — Mark comptime as upcoming

- comptime keyword is parsed but not executed at compile time
- Mark IO-during-comptime and build-dependency tracking as upcoming

### `docs/cli.md` — Fix mutation testing claim

- `--mutate` flag: add "planned — not yet implemented" note

### `docs/contracts.md` — Fix mutation testing references

- Level 4 mutation testing section: mark as upcoming

---

## Verification

1. `mkdocs build --strict` — no broken links
2. All 6 new diagnostic codes match existing format
3. AI-resistance page clearly separates implemented vs upcoming
4. Roadmap version numbers consistent across all docs
5. No doc page claims a feature is implemented when it isn't

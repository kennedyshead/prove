// Docstring knowledge base for LSP intent completions
// Auto-generated by prove-py/scripts/build_stores.py
type DocstringMap:[Lookup] is String | String | String | String | String where
    r00000 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r00001 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r00002 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r00003 | "absolute" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00004 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r00005 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r00006 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r00007 | "absolute" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00008 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r00009 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r00010 | "absolute" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r00011 | "absolute" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00012 | "accented" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00013 | "accented" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00014 | "accented" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00015 | "accented" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00016 | "accented" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00017 | "accented" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00018 | "accept" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00019 | "accept" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00020 | "accept" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00021 | "accept" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00022 | "accumulate" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r00023 | "accumulate" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r00024 | "add" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00025 | "add" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00026 | "add" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r00027 | "add" | "Time" | "date" | "derives" | "Add days to a date"
    r00028 | "add" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00029 | "add" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00030 | "add" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r00031 | "add" | "Time" | "date" | "derives" | "Add days to a date"
    r00032 | "add" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00033 | "add" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00034 | "add" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r00035 | "add" | "Time" | "date" | "derives" | "Add days to a date"
    r00036 | "add" | "Simple_math" | "add" | "transforms" | "add(1, 2) == 3"
    r00037 | "added" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r00038 | "added" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r00039 | "added" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r00040 | "added" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r00041 | "added" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r00042 | "added" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r00043 | "added" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r00044 | "added" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r00045 | "added" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r00046 | "added" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r00047 | "added" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r00048 | "added" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r00049 | "afterward" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r00050 | "afterward" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r00051 | "afterward" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r00052 | "against" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r00053 | "against" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r00054 | "against" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r00055 | "algorithm" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r00056 | "algorithm" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r00057 | "algorithm" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r00058 | "all" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r00059 | "all" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r00060 | "all" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00061 | "all" | "Store" | "version" | "inputs" | "List all versions of a table"
    r00062 | "all" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r00063 | "all" | "Table" | "values" | "derives" | "Get all values in a table"
    r00064 | "all" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r00065 | "all" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r00066 | "all" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r00067 | "all" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r00068 | "all" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r00069 | "all" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r00070 | "all" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00071 | "all" | "Store" | "version" | "inputs" | "List all versions of a table"
    r00072 | "all" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r00073 | "all" | "Table" | "values" | "derives" | "Get all values in a table"
    r00074 | "all" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r00075 | "all" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r00076 | "all" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r00077 | "all" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00078 | "all" | "Store" | "version" | "inputs" | "List all versions of a table"
    r00079 | "all" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r00080 | "all" | "Table" | "values" | "derives" | "Get all values in a table"
    r00081 | "all" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r00082 | "alphabetic" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00083 | "alphabetic" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00084 | "alphabetic" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00085 | "alphanumeric" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00086 | "alphanumeric" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00087 | "alphanumeric" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00088 | "always" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00089 | "always" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00090 | "always" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00091 | "and" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00092 | "and" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00093 | "and" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r00094 | "and" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r00095 | "and" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00096 | "and" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r00097 | "and" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r00098 | "and" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00099 | "and" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r00100 | "and" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r00101 | "and" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r00102 | "and" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r00103 | "and" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r00104 | "and" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00105 | "and" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00106 | "and" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00107 | "and" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00108 | "and" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r00109 | "and" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r00110 | "and" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r00111 | "and" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r00112 | "and" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r00113 | "and" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r00114 | "and" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00115 | "and" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00116 | "and" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00117 | "and" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00118 | "and" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r00119 | "and" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00120 | "and" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00121 | "and" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00122 | "and" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r00123 | "and" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r00124 | "and" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00125 | "and" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r00126 | "and" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00127 | "and" | "Main" | "parse_char" | "creates" | "Parse one BF character and emit opcodes"
    r00128 | "and" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r00129 | "and" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00130 | "and" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r00131 | "and" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r00132 | "and" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r00133 | "and" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00134 | "and" | "Main" | "try_json" | "transforms" | "Parse JSON and return as a Value string (failable)"
    r00135 | "and" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00136 | "and" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00137 | "and" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r00138 | "and" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r00139 | "and" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00140 | "and" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r00141 | "and" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r00142 | "and" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00143 | "and" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r00144 | "and" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r00145 | "and" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r00146 | "and" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r00147 | "and" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r00148 | "and" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00149 | "and" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00150 | "and" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00151 | "and" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00152 | "and" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r00153 | "and" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r00154 | "and" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r00155 | "and" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r00156 | "and" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r00157 | "and" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r00158 | "and" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00159 | "and" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00160 | "and" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00161 | "and" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00162 | "and" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r00163 | "and" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00164 | "and" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00165 | "and" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00166 | "and" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r00167 | "and" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r00168 | "and" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00169 | "and" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00170 | "and" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00171 | "and" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r00172 | "and" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r00173 | "and" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00174 | "and" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r00175 | "and" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r00176 | "and" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00177 | "and" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r00178 | "and" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r00179 | "and" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r00180 | "and" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r00181 | "and" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r00182 | "and" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00183 | "and" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00184 | "and" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00185 | "and" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00186 | "and" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r00187 | "and" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r00188 | "and" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r00189 | "and" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r00190 | "and" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r00191 | "and" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r00192 | "and" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00193 | "and" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00194 | "and" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00195 | "and" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00196 | "and" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r00197 | "and" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00198 | "and" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00199 | "and" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00200 | "and" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r00201 | "and" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r00202 | "and" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00203 | "angle" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r00204 | "angle" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r00205 | "angle" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r00206 | "angle" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r00207 | "angle" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r00208 | "angle" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r00209 | "angle" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r00210 | "angle" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r00211 | "angle" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r00212 | "anonymous" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00213 | "anonymous" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r00214 | "anonymous" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00215 | "anonymous" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r00216 | "anonymous" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00217 | "anonymous" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r00218 | "ansi" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r00219 | "ansi" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r00220 | "ansi" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r00221 | "any" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00222 | "any" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00223 | "any" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00224 | "append" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r00225 | "append" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r00226 | "append" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00227 | "append" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r00228 | "append" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r00229 | "append" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00230 | "append" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r00231 | "append" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r00232 | "append" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00233 | "appending" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r00234 | "appending" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r00235 | "appending" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r00236 | "apply" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r00237 | "apply" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r00238 | "apply" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r00239 | "apply" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r00240 | "apply" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r00241 | "apply" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r00242 | "arc" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r00243 | "arc" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r00244 | "arc" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r00245 | "arc" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r00246 | "arc" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r00247 | "arc" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r00248 | "arc" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r00249 | "arc" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r00250 | "arc" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r00251 | "are" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00252 | "are" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00253 | "are" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00254 | "are" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00255 | "are" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00256 | "are" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00257 | "are" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00258 | "are" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00259 | "are" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00260 | "args" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00261 | "args" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00262 | "args" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00263 | "argument" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r00264 | "argument" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r00265 | "argument" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r00266 | "arguments" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00267 | "arguments" | "System" | "process" | "inputs" | "Get command line arguments"
    r00268 | "arguments" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00269 | "arguments" | "System" | "process" | "inputs" | "Get command line arguments"
    r00270 | "arguments" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r00271 | "arguments" | "System" | "process" | "inputs" | "Get command line arguments"
    r00272 | "arm" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00273 | "arm" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00274 | "arm" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00275 | "array" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00276 | "array" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00277 | "array" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00278 | "array" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00279 | "array" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00280 | "array" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r00281 | "array" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00282 | "array" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00283 | "array" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00284 | "array" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r00285 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00286 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00287 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00288 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00289 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00290 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00291 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00292 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00293 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00294 | "array" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r00295 | "array" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00296 | "array" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r00297 | "array" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r00298 | "array" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r00299 | "array" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r00300 | "array" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r00301 | "array" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r00302 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00303 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00304 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00305 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00306 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00307 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00308 | "array" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r00309 | "array" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r00310 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00311 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00312 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00313 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00314 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00315 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00316 | "array" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r00317 | "array" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r00318 | "array" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00319 | "array" | "Main" | "count_ops" | "creates" | "Count BF opcodes in source for array sizing"
    r00320 | "array" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00321 | "array" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00322 | "array" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00323 | "array" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00324 | "array" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00325 | "array" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r00326 | "array" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00327 | "array" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00328 | "array" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00329 | "array" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r00330 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00331 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00332 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00333 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00334 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00335 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00336 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00337 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00338 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00339 | "array" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r00340 | "array" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00341 | "array" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r00342 | "array" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r00343 | "array" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r00344 | "array" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r00345 | "array" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r00346 | "array" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r00347 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00348 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00349 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00350 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00351 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00352 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00353 | "array" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r00354 | "array" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r00355 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00356 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00357 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00358 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00359 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00360 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00361 | "array" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r00362 | "array" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r00363 | "array" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00364 | "array" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00365 | "array" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00366 | "array" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00367 | "array" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00368 | "array" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00369 | "array" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r00370 | "array" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00371 | "array" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00372 | "array" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00373 | "array" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r00374 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00375 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00376 | "array" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r00377 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00378 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00379 | "array" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r00380 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00381 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00382 | "array" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00383 | "array" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r00384 | "array" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00385 | "array" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r00386 | "array" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r00387 | "array" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r00388 | "array" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r00389 | "array" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r00390 | "array" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r00391 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00392 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00393 | "array" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r00394 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00395 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00396 | "array" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r00397 | "array" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r00398 | "array" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r00399 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00400 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00401 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00402 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00403 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00404 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00405 | "array" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r00406 | "array" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r00407 | "array" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00408 | "arrays" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00409 | "arrays" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00410 | "arrays" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00411 | "ascii" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00412 | "ascii" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00413 | "ascii" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00414 | "ascii" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00415 | "ascii" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00416 | "ascii" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00417 | "atexit" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00418 | "atexit" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00419 | "atexit" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00420 | "atkin" | "Main" | "atkin_step1_y" | "creates" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r00421 | "atkin" | "Main" | "atkin_step1_x" | "creates" | "Sieve of Atkin step 1: iterate x"
    r00422 | "atkin" | "Main" | "atkin_step2_y" | "creates" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r00423 | "atkin" | "Main" | "atkin_step2_x" | "creates" | "Sieve of Atkin step 2: iterate x"
    r00424 | "atkin" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r00425 | "atkin" | "Main" | "atkin_step3_x" | "creates" | "Sieve of Atkin step 3: iterate x"
    r00426 | "automatically" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00427 | "automatically" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00428 | "automatically" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00429 | "await" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00430 | "back" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r00431 | "back" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00432 | "back" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r00433 | "back" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r00434 | "back" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00435 | "back" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r00436 | "back" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00437 | "back" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r00438 | "back" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r00439 | "back" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r00440 | "back" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00441 | "back" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r00442 | "back" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r00443 | "backed" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00444 | "backed" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00445 | "backed" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00446 | "bar" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r00447 | "bar" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r00448 | "bar" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r00449 | "base" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r00450 | "base" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r00451 | "base" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r00452 | "base" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00453 | "base" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00454 | "base" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r00455 | "base" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r00456 | "base" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r00457 | "base" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r00458 | "base" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r00459 | "base" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00460 | "base" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00461 | "base" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r00462 | "base" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r00463 | "base" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r00464 | "base" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r00465 | "base" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r00466 | "base" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00467 | "base" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00468 | "base" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r00469 | "base" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r00470 | "based" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r00471 | "based" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r00472 | "based" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r00473 | "based" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r00474 | "based" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r00475 | "based" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r00476 | "based" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r00477 | "based" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r00478 | "based" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r00479 | "before" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00480 | "before" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00481 | "before" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00482 | "before" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00483 | "before" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00484 | "before" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00485 | "begin" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00486 | "begin" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00487 | "begin" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00488 | "between" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r00489 | "between" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r00490 | "between" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r00491 | "between" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r00492 | "between" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r00493 | "between" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00494 | "between" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r00495 | "between" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r00496 | "between" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r00497 | "between" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r00498 | "between" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r00499 | "between" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r00500 | "between" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r00501 | "between" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00502 | "between" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r00503 | "between" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r00504 | "between" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r00505 | "between" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r00506 | "between" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r00507 | "between" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r00508 | "between" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r00509 | "between" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00510 | "between" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r00511 | "between" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r00512 | "bigrams" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r00513 | "bigrams" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r00514 | "bigrams" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r00515 | "binary" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r00516 | "binary" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r00517 | "binary" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r00518 | "bind" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00519 | "bind" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00520 | "bind" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00521 | "bits" | "Main" | "atkin_step1_y" | "creates" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r00522 | "bits" | "Main" | "atkin_step2_y" | "creates" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r00523 | "bits" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r00524 | "blake" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00525 | "blake" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r00526 | "blake" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00527 | "blake" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00528 | "blake" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r00529 | "blake" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00530 | "blake" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00531 | "blake" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r00532 | "blake" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00533 | "blank" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00534 | "blocking" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00535 | "blue" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r00536 | "blue" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r00537 | "blue" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r00538 | "boolean" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00539 | "boolean" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00540 | "boolean" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00541 | "boolean" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r00542 | "boolean" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00543 | "boolean" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r00544 | "boolean" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r00545 | "boolean" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r00546 | "boolean" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r00547 | "boolean" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r00548 | "boolean" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00549 | "boolean" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00550 | "boolean" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r00551 | "boolean" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r00552 | "boolean" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00553 | "boolean" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00554 | "boolean" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00555 | "boolean" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r00556 | "boolean" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00557 | "boolean" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r00558 | "boolean" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r00559 | "boolean" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r00560 | "boolean" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r00561 | "boolean" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r00562 | "boolean" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00563 | "boolean" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00564 | "boolean" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r00565 | "boolean" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r00566 | "boolean" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00567 | "boolean" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00568 | "boolean" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00569 | "boolean" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r00570 | "boolean" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00571 | "boolean" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r00572 | "boolean" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r00573 | "boolean" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r00574 | "boolean" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r00575 | "boolean" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r00576 | "boolean" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00577 | "boolean" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00578 | "boolean" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r00579 | "boolean" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r00580 | "both" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r00581 | "both" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r00582 | "both" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r00583 | "bounds" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r00584 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00585 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00586 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00587 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00588 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00589 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00590 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00591 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00592 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00593 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00594 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00595 | "bounds" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r00596 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00597 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00598 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00599 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00600 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00601 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00602 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00603 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00604 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00605 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00606 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00607 | "bounds" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r00608 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00609 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00610 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00611 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00612 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00613 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00614 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00615 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00616 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00617 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00618 | "bounds" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r00619 | "brackets" | "Main" | "parse_char_bracket" | "creates" | "Continue parsing: check for open/close brackets"
    r00620 | "branch" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r00621 | "branch" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r00622 | "branch" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r00623 | "buffering" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00624 | "buffering" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00625 | "buffering" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00626 | "buffering" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00627 | "buffering" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00628 | "buffering" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00629 | "build" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r00630 | "build" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r00631 | "build" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r00632 | "build" | "Commands" | "build" | "dispatches" | "Run the build command"
    r00633 | "build" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r00634 | "builder" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00635 | "builder" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r00636 | "builder" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00637 | "builder" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r00638 | "builder" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r00639 | "builder" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00640 | "builder" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r00641 | "builder" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00642 | "builder" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r00643 | "builder" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r00644 | "builder" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00645 | "builder" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r00646 | "builder" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00647 | "builder" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r00648 | "builder" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r00649 | "button" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r00650 | "button" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r00651 | "button" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r00652 | "byte" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00653 | "byte" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00654 | "byte" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00655 | "byte" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r00656 | "byte" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00657 | "byte" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00658 | "byte" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00659 | "byte" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r00660 | "byte" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00661 | "byte" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00662 | "byte" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00663 | "byte" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r00664 | "byte" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r00665 | "byte" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00666 | "byte" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00667 | "byte" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00668 | "byte" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r00669 | "byte" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00670 | "byte" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00671 | "byte" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00672 | "byte" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r00673 | "byte" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00674 | "byte" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00675 | "byte" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00676 | "byte" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r00677 | "byte" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r00678 | "byte" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00679 | "byte" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00680 | "byte" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00681 | "byte" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r00682 | "byte" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00683 | "byte" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00684 | "byte" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00685 | "byte" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r00686 | "byte" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00687 | "byte" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r00688 | "byte" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00689 | "byte" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r00690 | "byte" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r00691 | "bytearray" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00692 | "bytearray" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00693 | "bytearray" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r00694 | "bytearray" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00695 | "bytearray" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00696 | "bytearray" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00697 | "bytearray" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r00698 | "bytearray" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00699 | "bytearray" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00700 | "bytearray" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00701 | "bytearray" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r00702 | "bytearray" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00703 | "bytes" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00704 | "bytes" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00705 | "bytes" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00706 | "bytes" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00707 | "bytes" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00708 | "bytes" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r00709 | "bytes" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00710 | "call" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r00711 | "call" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00712 | "call" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r00713 | "call" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00714 | "call" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r00715 | "call" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r00716 | "called" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00717 | "called" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00718 | "called" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00719 | "called" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00720 | "calls" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00721 | "calls" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00722 | "calls" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r00723 | "case" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00724 | "case" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00725 | "case" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00726 | "ceiling" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r00727 | "ceiling" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r00728 | "ceiling" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r00729 | "cells" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00730 | "cells" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00731 | "cells" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00732 | "cells" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00733 | "cells" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00734 | "cells" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00735 | "centers" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r00736 | "centers" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r00737 | "centers" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r00738 | "character" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00739 | "character" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00740 | "character" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00741 | "character" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00742 | "character" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00743 | "character" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00744 | "character" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r00745 | "character" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00746 | "character" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00747 | "character" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00748 | "character" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00749 | "character" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00750 | "character" | "Types" | "string" | "creates" | "Convert a character to a string"
    r00751 | "character" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r00752 | "character" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00753 | "character" | "Main" | "parse_char" | "creates" | "Parse one BF character and emit opcodes"
    r00754 | "character" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00755 | "character" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00756 | "character" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00757 | "character" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00758 | "character" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00759 | "character" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00760 | "character" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r00761 | "character" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00762 | "character" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00763 | "character" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00764 | "character" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00765 | "character" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00766 | "character" | "Types" | "string" | "creates" | "Convert a character to a string"
    r00767 | "character" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r00768 | "character" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00769 | "character" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00770 | "character" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00771 | "character" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00772 | "character" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00773 | "character" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00774 | "character" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00775 | "character" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r00776 | "character" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r00777 | "character" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r00778 | "character" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r00779 | "character" | "Text" | "char" | "derives" | "Append a character to a builder"
    r00780 | "character" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00781 | "character" | "Types" | "string" | "creates" | "Convert a character to a string"
    r00782 | "character" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r00783 | "character" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00784 | "characters" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00785 | "characters" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00786 | "characters" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00787 | "characters" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00788 | "characters" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00789 | "characters" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00790 | "check" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00791 | "check" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00792 | "check" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00793 | "check" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r00794 | "check" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r00795 | "check" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00796 | "check" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00797 | "check" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00798 | "check" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00799 | "check" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00800 | "check" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00801 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00802 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00803 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00804 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r00805 | "check" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r00806 | "check" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r00807 | "check" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r00808 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r00809 | "check" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00810 | "check" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r00811 | "check" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r00812 | "check" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00813 | "check" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00814 | "check" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r00815 | "check" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r00816 | "check" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00817 | "check" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00818 | "check" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r00819 | "check" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00820 | "check" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00821 | "check" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00822 | "check" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r00823 | "check" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00824 | "check" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00825 | "check" | "Types" | "string" | "validates" | "Check if a value is a string"
    r00826 | "check" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r00827 | "check" | "Types" | "float" | "validates" | "Check if a value is a float"
    r00828 | "check" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00829 | "check" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00830 | "check" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00831 | "check" | "Types" | "text" | "validates" | "Check if a Value is text"
    r00832 | "check" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r00833 | "check" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00834 | "check" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00835 | "check" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00836 | "check" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r00837 | "check" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r00838 | "check" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r00839 | "check" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00840 | "check" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00841 | "check" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r00842 | "check" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r00843 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r00844 | "check" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r00845 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r00846 | "check" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00847 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r00848 | "check" | "Main" | "parse_char_minus" | "creates" | "Continue parsing: check for minus"
    r00849 | "check" | "Main" | "parse_char_right" | "creates" | "Continue parsing: check for right"
    r00850 | "check" | "Main" | "parse_char_left" | "creates" | "Continue parsing: check for left"
    r00851 | "check" | "Main" | "parse_char_dot" | "creates" | "Continue parsing: check for dot (print)"
    r00852 | "check" | "Main" | "parse_char_bracket" | "creates" | "Continue parsing: check for open/close brackets"
    r00853 | "check" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00854 | "check" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00855 | "check" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00856 | "check" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r00857 | "check" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r00858 | "check" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00859 | "check" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00860 | "check" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00861 | "check" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00862 | "check" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00863 | "check" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00864 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00865 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00866 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00867 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r00868 | "check" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r00869 | "check" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r00870 | "check" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r00871 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r00872 | "check" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00873 | "check" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r00874 | "check" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r00875 | "check" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00876 | "check" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00877 | "check" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r00878 | "check" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r00879 | "check" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00880 | "check" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00881 | "check" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r00882 | "check" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00883 | "check" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00884 | "check" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00885 | "check" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r00886 | "check" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00887 | "check" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00888 | "check" | "Types" | "string" | "validates" | "Check if a value is a string"
    r00889 | "check" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r00890 | "check" | "Types" | "float" | "validates" | "Check if a value is a float"
    r00891 | "check" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00892 | "check" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00893 | "check" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00894 | "check" | "Types" | "text" | "validates" | "Check if a Value is text"
    r00895 | "check" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r00896 | "check" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00897 | "check" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00898 | "check" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00899 | "check" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r00900 | "check" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r00901 | "check" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r00902 | "check" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00903 | "check" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00904 | "check" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r00905 | "check" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r00906 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r00907 | "check" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r00908 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r00909 | "check" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00910 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r00911 | "check" | "Commands" | "check" | "dispatches" | "Run the check command"
    r00912 | "check" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00913 | "check" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00914 | "check" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00915 | "check" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r00916 | "check" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r00917 | "check" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00918 | "check" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00919 | "check" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00920 | "check" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00921 | "check" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00922 | "check" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00923 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00924 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00925 | "check" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r00926 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r00927 | "check" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r00928 | "check" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r00929 | "check" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r00930 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r00931 | "check" | "Sequence" | "contains" | "validates" | "Check if a boolean is in an array"
    r00932 | "check" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r00933 | "check" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r00934 | "check" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00935 | "check" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00936 | "check" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r00937 | "check" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r00938 | "check" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00939 | "check" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00940 | "check" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r00941 | "check" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00942 | "check" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00943 | "check" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00944 | "check" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r00945 | "check" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00946 | "check" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00947 | "check" | "Types" | "string" | "validates" | "Check if a value is a string"
    r00948 | "check" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r00949 | "check" | "Types" | "float" | "validates" | "Check if a value is a float"
    r00950 | "check" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00951 | "check" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00952 | "check" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00953 | "check" | "Types" | "text" | "validates" | "Check if a Value is text"
    r00954 | "check" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r00955 | "check" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00956 | "check" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00957 | "check" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00958 | "check" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r00959 | "check" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r00960 | "check" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r00961 | "check" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00962 | "check" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00963 | "check" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r00964 | "check" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r00965 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r00966 | "check" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r00967 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r00968 | "check" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r00969 | "check" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r00970 | "checkbox" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00971 | "checkbox" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00972 | "checkbox" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00973 | "checked" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00974 | "checked" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00975 | "checked" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r00976 | "checks" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00977 | "checks" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00978 | "checks" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00979 | "checks" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00980 | "checks" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00981 | "checks" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00982 | "checks" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00983 | "checks" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00984 | "checks" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00985 | "checks" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00986 | "checks" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00987 | "checks" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00988 | "checks" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00989 | "checks" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00990 | "checks" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00991 | "checks" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00992 | "checks" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00993 | "checks" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00994 | "child" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00995 | "child" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r00996 | "child" | "Prove" | "count" | "creates" | "Number of child nodes."
    r00997 | "child" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r00998 | "child" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r00999 | "child" | "Prove" | "count" | "creates" | "Number of child nodes."
    r01000 | "child" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r01001 | "child" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r01002 | "child" | "Prove" | "count" | "creates" | "Number of child nodes."
    r01003 | "children" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r01004 | "children" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r01005 | "children" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r01006 | "chunk" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01007 | "chunk" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01008 | "chunk" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01009 | "chunks" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01010 | "clamp" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r01011 | "clamp" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r01012 | "clamp" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r01013 | "clamp" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r01014 | "clamp" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r01015 | "clamp" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r01016 | "clamp" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r01017 | "clamp" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r01018 | "clamp" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r01019 | "clear" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r01020 | "clear" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01021 | "clear" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r01022 | "clear" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01023 | "clear" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r01024 | "clear" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01025 | "click" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01026 | "click" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01027 | "click" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01028 | "clickable" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01029 | "clickable" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01030 | "clickable" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01031 | "clicked" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01032 | "clicked" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01033 | "clicked" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r01034 | "clicks" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01035 | "clicks" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01036 | "clicks" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01037 | "clock" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01038 | "clock" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r01039 | "clock" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01040 | "clock" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r01041 | "clock" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r01042 | "clock" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01043 | "clock" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r01044 | "clock" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01045 | "clock" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r01046 | "clock" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r01047 | "clock" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01048 | "clock" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r01049 | "clock" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01050 | "clock" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r01051 | "clock" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r01052 | "close" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r01053 | "close" | "Network" | "socket" | "outputs" | "Close a connection"
    r01054 | "close" | "System" | "close" | "outputs" | "Close an open file handle"
    r01055 | "close" | "Main" | "parse_char_bracket" | "creates" | "Continue parsing: check for open/close brackets"
    r01056 | "close" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r01057 | "close" | "Network" | "socket" | "outputs" | "Close a connection"
    r01058 | "close" | "System" | "close" | "outputs" | "Close an open file handle"
    r01059 | "close" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r01060 | "close" | "Network" | "socket" | "outputs" | "Close a connection"
    r01061 | "close" | "System" | "close" | "outputs" | "Close an open file handle"
    r01062 | "code" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r01063 | "code" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01064 | "code" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01065 | "code" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r01066 | "code" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01067 | "code" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r01068 | "code" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01069 | "code" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01070 | "code" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r01071 | "code" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01072 | "code" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r01073 | "code" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01074 | "code" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01075 | "code" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r01076 | "code" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01077 | "coefficient" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r01078 | "collect" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r01079 | "collect" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r01080 | "color" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r01081 | "color" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r01082 | "color" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r01083 | "color" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r01084 | "color" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r01085 | "color" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r01086 | "column" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r01087 | "column" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r01088 | "column" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01089 | "column" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01090 | "column" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r01091 | "column" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r01092 | "column" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01093 | "column" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01094 | "column" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r01095 | "column" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r01096 | "column" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01097 | "column" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01098 | "columns" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01099 | "columns" | "Main" | "columns" | "creates" | "Generate matrix: fill row by iterating columns"
    r01100 | "columns" | "Main" | "transpose_columns" | "creates" | "Transpose: copy columns of src into rows of dst"
    r01101 | "columns" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01102 | "columns" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01103 | "combined" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01104 | "combined" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01105 | "combined" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01106 | "comma" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r01107 | "comma" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r01108 | "command" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r01109 | "command" | "System" | "system" | "inputs" | "Execute a system command"
    r01110 | "command" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r01111 | "command" | "System" | "process" | "inputs" | "Get command line arguments"
    r01112 | "command" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r01113 | "command" | "System" | "system" | "inputs" | "Execute a system command"
    r01114 | "command" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r01115 | "command" | "System" | "process" | "inputs" | "Get command line arguments"
    r01116 | "command" | "Commands" | "build" | "dispatches" | "Run the build command"
    r01117 | "command" | "Commands" | "check" | "dispatches" | "Run the check command"
    r01118 | "command" | "Commands" | "format" | "dispatches" | "Run the format command"
    r01119 | "command" | "Commands" | "lsp" | "dispatches" | "Run the lsp command"
    r01120 | "command" | "Commands" | "new" | "dispatches" | "Run the new command"
    r01121 | "command" | "Commands" | "test" | "dispatches" | "Run the test command"
    r01122 | "command" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r01123 | "command" | "System" | "system" | "inputs" | "Execute a system command"
    r01124 | "command" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r01125 | "command" | "System" | "process" | "inputs" | "Get command line arguments"
    r01126 | "common" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r01127 | "common" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r01128 | "common" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r01129 | "common" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r01130 | "common" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r01131 | "common" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r01132 | "common" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r01133 | "common" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r01134 | "common" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r01135 | "compile" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01136 | "compile" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01137 | "compile" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01138 | "compiled" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r01139 | "compiled" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r01140 | "compiled" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r01141 | "component" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r01142 | "component" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r01143 | "component" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r01144 | "component" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r01145 | "component" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r01146 | "component" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r01147 | "component" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r01148 | "component" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r01149 | "component" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r01150 | "components" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r01151 | "components" | "Path" | "join" | "derives" | "Join path components together"
    r01152 | "components" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01153 | "components" | "Time" | "date" | "creates" | "Create a date from components"
    r01154 | "components" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01155 | "components" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r01156 | "components" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r01157 | "components" | "Path" | "join" | "derives" | "Join path components together"
    r01158 | "components" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01159 | "components" | "Time" | "date" | "creates" | "Create a date from components"
    r01160 | "components" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01161 | "components" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r01162 | "components" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r01163 | "components" | "Path" | "join" | "derives" | "Join path components together"
    r01164 | "components" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01165 | "components" | "Time" | "date" | "creates" | "Create a date from components"
    r01166 | "components" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01167 | "components" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r01168 | "compute" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01169 | "compute" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r01170 | "compute" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r01171 | "compute" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01172 | "compute" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r01173 | "compute" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r01174 | "compute" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r01175 | "compute" | "Main" | "signal_power" | "derives" | "Compute signal power from filter state"
    r01176 | "compute" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01177 | "compute" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r01178 | "compute" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r01179 | "compute" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01180 | "compute" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r01181 | "compute" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r01182 | "compute" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r01183 | "compute" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01184 | "compute" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r01185 | "compute" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r01186 | "compute" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01187 | "compute" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r01188 | "compute" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r01189 | "compute" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r01190 | "concatenate" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01191 | "concatenate" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01192 | "concatenate" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01193 | "concurrency" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01194 | "concurrency" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01195 | "concurrency" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r01196 | "config" | "Config" | "struct" | "transforms" | "Parse the toml data to Config struct"
    r01197 | "conflict" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r01198 | "conflict" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r01199 | "conflict" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r01200 | "conflict" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r01201 | "conflict" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r01202 | "conflict" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r01203 | "conflict" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r01204 | "conflict" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r01205 | "conflict" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r01206 | "conflict" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r01207 | "conflict" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r01208 | "conflict" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r01209 | "conflict" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r01210 | "conflict" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r01211 | "conflict" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r01212 | "conflicted" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r01213 | "conflicted" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r01214 | "conflicted" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r01215 | "conflicts" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01216 | "conflicts" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r01217 | "conflicts" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01218 | "conflicts" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r01219 | "conflicts" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01220 | "conflicts" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r01221 | "connected" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r01222 | "connected" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r01223 | "connected" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r01224 | "connection" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r01225 | "connection" | "Network" | "socket" | "outputs" | "Close a connection"
    r01226 | "connection" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r01227 | "connection" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01228 | "connection" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r01229 | "connection" | "Network" | "socket" | "outputs" | "Close a connection"
    r01230 | "connection" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r01231 | "connection" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01232 | "connection" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r01233 | "connection" | "Network" | "socket" | "outputs" | "Close a connection"
    r01234 | "connection" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r01235 | "connection" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01236 | "connections" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r01237 | "console" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r01238 | "console" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r01239 | "console" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r01240 | "console" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r01241 | "console" | "System" | "console" | "outputs" | "Print text to the console"
    r01242 | "console" | "System" | "console" | "inputs" | "Read a line from the console"
    r01243 | "console" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01244 | "console" | "System" | "console" | "validates" | "Validate console input"
    r01245 | "console" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01246 | "console" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r01247 | "console" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r01248 | "console" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r01249 | "console" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r01250 | "console" | "System" | "console" | "outputs" | "Print text to the console"
    r01251 | "console" | "System" | "console" | "inputs" | "Read a line from the console"
    r01252 | "console" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01253 | "console" | "System" | "console" | "validates" | "Validate console input"
    r01254 | "console" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r01255 | "console" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r01256 | "console" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r01257 | "console" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r01258 | "console" | "System" | "console" | "outputs" | "Print text to the console"
    r01259 | "console" | "System" | "console" | "inputs" | "Read a line from the console"
    r01260 | "console" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01261 | "console" | "System" | "console" | "validates" | "Validate console input"
    r01262 | "constant" | "Math" | "pi" | "derives" | "The constant pi (3.14159...)"
    r01263 | "constant" | "Math" | "e" | "derives" | "The constant e (2.71828...)"
    r01264 | "constant" | "Math" | "pi" | "derives" | "The constant pi (3.14159...)"
    r01265 | "constant" | "Math" | "e" | "derives" | "The constant e (2.71828...)"
    r01266 | "constant" | "Math" | "pi" | "derives" | "The constant pi (3.14159...)"
    r01267 | "constant" | "Math" | "e" | "derives" | "The constant e (2.71828...)"
    r01268 | "construct" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01269 | "construct" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01270 | "construct" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01271 | "consumed" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01272 | "contains" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r01273 | "contains" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r01274 | "contains" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r01275 | "content" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r01276 | "content" | "System" | "file" | "outputs" | "Write content to a file"
    r01277 | "content" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r01278 | "content" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r01279 | "content" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r01280 | "content" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r01281 | "content" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r01282 | "content" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01283 | "content" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r01284 | "content" | "System" | "file" | "outputs" | "Write content to a file"
    r01285 | "content" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r01286 | "content" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r01287 | "content" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r01288 | "content" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r01289 | "content" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r01290 | "content" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r01291 | "content" | "System" | "file" | "outputs" | "Write content to a file"
    r01292 | "content" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r01293 | "content" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r01294 | "content" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r01295 | "content" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r01296 | "content" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r01297 | "contents" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01298 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01299 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01300 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01301 | "contents" | "System" | "file" | "inputs" | "Read contents from a file"
    r01302 | "contents" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01303 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01304 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01305 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01306 | "contents" | "System" | "file" | "inputs" | "Read contents from a file"
    r01307 | "contents" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01308 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01309 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01310 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01311 | "contents" | "System" | "file" | "inputs" | "Read contents from a file"
    r01312 | "continue" | "Main" | "parse_char_minus" | "creates" | "Continue parsing: check for minus"
    r01313 | "continue" | "Main" | "parse_char_right" | "creates" | "Continue parsing: check for right"
    r01314 | "continue" | "Main" | "parse_char_left" | "creates" | "Continue parsing: check for left"
    r01315 | "continue" | "Main" | "parse_char_dot" | "creates" | "Continue parsing: check for dot (print)"
    r01316 | "continue" | "Main" | "parse_char_bracket" | "creates" | "Continue parsing: check for open/close brackets"
    r01317 | "convert" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r01318 | "convert" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r01319 | "convert" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r01320 | "convert" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r01321 | "convert" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01322 | "convert" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r01323 | "convert" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r01324 | "convert" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r01325 | "convert" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r01326 | "convert" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01327 | "convert" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r01328 | "convert" | "Types" | "string" | "creates" | "Convert a float to a string"
    r01329 | "convert" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r01330 | "convert" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r01331 | "convert" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r01332 | "convert" | "Types" | "string" | "creates" | "Convert a character to a string"
    r01333 | "convert" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r01334 | "convert" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r01335 | "convert" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r01336 | "convert" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r01337 | "convert" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r01338 | "convert" | "Types" | "string" | "creates" | "Convert a value to a string"
    r01339 | "convert" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r01340 | "convert" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r01341 | "convert" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r01342 | "convert" | "Main" | "to_dbm" | "derives" | "Convert power to dBm (600 Ohm impedance)"
    r01343 | "convert" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r01344 | "convert" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r01345 | "convert" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r01346 | "convert" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r01347 | "convert" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01348 | "convert" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r01349 | "convert" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r01350 | "convert" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r01351 | "convert" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r01352 | "convert" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01353 | "convert" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r01354 | "convert" | "Types" | "string" | "creates" | "Convert a float to a string"
    r01355 | "convert" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r01356 | "convert" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r01357 | "convert" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r01358 | "convert" | "Types" | "string" | "creates" | "Convert a character to a string"
    r01359 | "convert" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r01360 | "convert" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r01361 | "convert" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r01362 | "convert" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r01363 | "convert" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r01364 | "convert" | "Types" | "string" | "creates" | "Convert a value to a string"
    r01365 | "convert" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r01366 | "convert" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r01367 | "convert" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r01368 | "convert" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r01369 | "convert" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r01370 | "convert" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r01371 | "convert" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r01372 | "convert" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01373 | "convert" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r01374 | "convert" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r01375 | "convert" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r01376 | "convert" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r01377 | "convert" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01378 | "convert" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r01379 | "convert" | "Types" | "string" | "creates" | "Convert a float to a string"
    r01380 | "convert" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r01381 | "convert" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r01382 | "convert" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r01383 | "convert" | "Types" | "string" | "creates" | "Convert a character to a string"
    r01384 | "convert" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r01385 | "convert" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r01386 | "convert" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r01387 | "convert" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r01388 | "convert" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r01389 | "convert" | "Types" | "string" | "creates" | "Convert a value to a string"
    r01390 | "convert" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r01391 | "convert" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r01392 | "convert" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r01393 | "converts" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r01394 | "converts" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r01395 | "converts" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r01396 | "converts" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r01397 | "converts" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r01398 | "converts" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r01399 | "converts" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r01400 | "converts" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r01401 | "converts" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r01402 | "cooked" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01403 | "cooked" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r01404 | "cooked" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01405 | "cooked" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r01406 | "cooked" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01407 | "cooked" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r01408 | "cooperative" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r01409 | "coordinates" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01410 | "coordinates" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01411 | "coordinates" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01412 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r01413 | "copy" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r01414 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01415 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01416 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01417 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01418 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01419 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01420 | "copy" | "Main" | "transpose_columns" | "creates" | "Transpose: copy columns of src into rows of dst"
    r01421 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r01422 | "copy" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r01423 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01424 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01425 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01426 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01427 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01428 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01429 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r01430 | "copy" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r01431 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01432 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01433 | "copy" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01434 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01435 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01436 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01437 | "corner" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01438 | "corner" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01439 | "corner" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r01440 | "cosine" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r01441 | "cosine" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r01442 | "cosine" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r01443 | "cosine" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r01444 | "cosine" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r01445 | "cosine" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r01446 | "count" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r01447 | "count" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01448 | "count" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r01449 | "count" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r01450 | "count" | "Main" | "count_ops" | "creates" | "Count BF opcodes in source for array sizing"
    r01451 | "count" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r01452 | "count" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01453 | "count" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r01454 | "count" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r01455 | "count" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01456 | "count" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r01457 | "create" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01458 | "create" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01459 | "create" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r01460 | "create" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r01461 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01462 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01463 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01464 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01465 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01466 | "create" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01467 | "create" | "System" | "dir" | "outputs" | "Create a directory"
    r01468 | "create" | "Table" | "new" | "creates" | "Create a new empty table"
    r01469 | "create" | "Text" | "builder" | "creates" | "Create a new string builder"
    r01470 | "create" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01471 | "create" | "Time" | "date" | "creates" | "Create a date from components"
    r01472 | "create" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01473 | "create" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01474 | "create" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01475 | "create" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01476 | "create" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01477 | "create" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r01478 | "create" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r01479 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01480 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01481 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01482 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01483 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01484 | "create" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01485 | "create" | "System" | "dir" | "outputs" | "Create a directory"
    r01486 | "create" | "Table" | "new" | "creates" | "Create a new empty table"
    r01487 | "create" | "Text" | "builder" | "creates" | "Create a new string builder"
    r01488 | "create" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01489 | "create" | "Time" | "date" | "creates" | "Create a date from components"
    r01490 | "create" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01491 | "create" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01492 | "create" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01493 | "create" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01494 | "create" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01495 | "create" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r01496 | "create" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r01497 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01498 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01499 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01500 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01501 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01502 | "create" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01503 | "create" | "System" | "dir" | "outputs" | "Create a directory"
    r01504 | "create" | "Table" | "new" | "creates" | "Create a new empty table"
    r01505 | "create" | "Text" | "builder" | "creates" | "Create a new string builder"
    r01506 | "create" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01507 | "create" | "Time" | "date" | "creates" | "Create a date from components"
    r01508 | "create" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01509 | "create" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01510 | "create" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01511 | "csv" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r01512 | "csv" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r01513 | "csv" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r01514 | "csv" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r01515 | "csv" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r01516 | "csv" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r01517 | "csv" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r01518 | "csv" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r01519 | "csv" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r01520 | "csv" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r01521 | "csv" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r01522 | "csv" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r01523 | "ctrl" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01524 | "current" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01525 | "current" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01526 | "current" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r01527 | "current" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r01528 | "current" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r01529 | "current" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01530 | "current" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01531 | "current" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r01532 | "current" | "Time" | "time" | "inputs" | "Get current time"
    r01533 | "current" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01534 | "current" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01535 | "current" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r01536 | "current" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r01537 | "current" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r01538 | "current" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01539 | "current" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01540 | "current" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r01541 | "current" | "Time" | "time" | "inputs" | "Get current time"
    r01542 | "current" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01543 | "current" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01544 | "current" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r01545 | "current" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r01546 | "current" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r01547 | "current" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01548 | "current" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01549 | "current" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r01550 | "current" | "Time" | "time" | "inputs" | "Get current time"
    r01551 | "cursor" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01552 | "cursor" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r01553 | "cursor" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01554 | "cursor" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01555 | "cursor" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r01556 | "cursor" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01557 | "cursor" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01558 | "cursor" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r01559 | "cursor" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01560 | "data" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01561 | "data" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01562 | "data" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01563 | "data" | "Network" | "message" | "inputs" | "Read data from a socket"
    r01564 | "data" | "Network" | "message" | "outputs" | "Write data to a socket"
    r01565 | "data" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r01566 | "data" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01567 | "data" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01568 | "data" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01569 | "data" | "Network" | "message" | "inputs" | "Read data from a socket"
    r01570 | "data" | "Network" | "message" | "outputs" | "Write data to a socket"
    r01571 | "data" | "Config" | "struct" | "transforms" | "Parse the toml data to Config struct"
    r01572 | "data" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01573 | "data" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01574 | "data" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01575 | "data" | "Network" | "message" | "inputs" | "Read data from a socket"
    r01576 | "data" | "Network" | "message" | "outputs" | "Write data to a socket"
    r01577 | "date" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r01578 | "date" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r01579 | "date" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01580 | "date" | "Time" | "date" | "creates" | "Extract date from a time"
    r01581 | "date" | "Time" | "date" | "creates" | "Create a date from components"
    r01582 | "date" | "Time" | "date" | "validates" | "Check if a date is valid"
    r01583 | "date" | "Time" | "date" | "derives" | "Add days to a date"
    r01584 | "date" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01585 | "date" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r01586 | "date" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r01587 | "date" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r01588 | "date" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r01589 | "date" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r01590 | "date" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01591 | "date" | "Time" | "date" | "creates" | "Extract date from a time"
    r01592 | "date" | "Time" | "date" | "creates" | "Create a date from components"
    r01593 | "date" | "Time" | "date" | "validates" | "Check if a date is valid"
    r01594 | "date" | "Time" | "date" | "derives" | "Add days to a date"
    r01595 | "date" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01596 | "date" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r01597 | "date" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r01598 | "date" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r01599 | "date" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r01600 | "date" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r01601 | "date" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01602 | "date" | "Time" | "date" | "creates" | "Extract date from a time"
    r01603 | "date" | "Time" | "date" | "creates" | "Create a date from components"
    r01604 | "date" | "Time" | "date" | "validates" | "Check if a date is valid"
    r01605 | "date" | "Time" | "date" | "derives" | "Add days to a date"
    r01606 | "date" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01607 | "date" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r01608 | "date" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r01609 | "date" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r01610 | "datetime" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r01611 | "datetime" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r01612 | "datetime" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01613 | "datetime" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r01614 | "datetime" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01615 | "datetime" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r01616 | "datetime" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r01617 | "datetime" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r01618 | "datetime" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r01619 | "datetime" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r01620 | "datetime" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01621 | "datetime" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r01622 | "datetime" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01623 | "datetime" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r01624 | "datetime" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r01625 | "datetime" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r01626 | "datetime" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r01627 | "datetime" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r01628 | "datetime" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01629 | "datetime" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r01630 | "datetime" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01631 | "datetime" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r01632 | "datetime" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r01633 | "datetime" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r01634 | "days" | "Time" | "date" | "derives" | "Add days to a date"
    r01635 | "days" | "Time" | "days" | "derives" | "Get number of days in a month"
    r01636 | "days" | "Time" | "date" | "derives" | "Add days to a date"
    r01637 | "days" | "Time" | "days" | "derives" | "Get number of days in a month"
    r01638 | "days" | "Time" | "date" | "derives" | "Add days to a date"
    r01639 | "days" | "Time" | "days" | "derives" | "Get number of days in a month"
    r01640 | "dbm" | "Main" | "to_dbm" | "derives" | "Convert power to dBm (600 Ohm impedance)"
    r01641 | "debug" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r01642 | "debug" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r01643 | "debug" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r01644 | "decide" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r01645 | "decide" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r01646 | "decide" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r01647 | "decimal" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r01648 | "decimal" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r01649 | "decimal" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r01650 | "decimal" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r01651 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r01652 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r01653 | "decimal" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r01654 | "decimal" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r01655 | "decimal" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r01656 | "decimal" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r01657 | "decimal" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r01658 | "decimal" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r01659 | "decimal" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r01660 | "decimal" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r01661 | "decimal" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01662 | "decimal" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r01663 | "decimal" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r01664 | "decimal" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r01665 | "decimal" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r01666 | "decimal" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r01667 | "decimal" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r01668 | "decimal" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r01669 | "decimal" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r01670 | "decimal" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r01671 | "decimal" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r01672 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r01673 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r01674 | "decimal" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r01675 | "decimal" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r01676 | "decimal" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r01677 | "decimal" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r01678 | "decimal" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r01679 | "decimal" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r01680 | "decimal" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r01681 | "decimal" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r01682 | "decimal" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01683 | "decimal" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r01684 | "decimal" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r01685 | "decimal" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r01686 | "decimal" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r01687 | "decimal" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r01688 | "decimal" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r01689 | "decimal" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r01690 | "decimal" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r01691 | "decimal" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r01692 | "decimal" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r01693 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r01694 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r01695 | "decimal" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r01696 | "decimal" | "Sequence" | "contains" | "validates" | "Check if a decimal is in an array"
    r01697 | "decimal" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r01698 | "decimal" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r01699 | "decimal" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r01700 | "decimal" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r01701 | "decimal" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r01702 | "decimal" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r01703 | "decimal" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01704 | "decimal" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r01705 | "decimal" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r01706 | "decimal" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r01707 | "decimal" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r01708 | "decimal" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r01709 | "decimal" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r01710 | "decimals" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r01711 | "decimals" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r01712 | "decimals" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r01713 | "decimals" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r01714 | "decimals" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r01715 | "decimals" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r01716 | "decimals" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r01717 | "decimals" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r01718 | "decimals" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r01719 | "decimals" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r01720 | "decimals" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r01721 | "decimals" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r01722 | "decimals" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r01723 | "decimals" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r01724 | "decimals" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r01725 | "decimals" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r01726 | "decimals" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r01727 | "decimals" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r01728 | "decode" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r01729 | "decode" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r01730 | "decode" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r01731 | "decode" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r01732 | "decode" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r01733 | "decode" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r01734 | "decode" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r01735 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01736 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01737 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01738 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r01739 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r01740 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r01741 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r01742 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r01743 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01744 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01745 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01746 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r01747 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r01748 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r01749 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r01750 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r01751 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01752 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01753 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01754 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r01755 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r01756 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r01757 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r01758 | "default" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r01759 | "definition" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r01760 | "definition" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r01761 | "definition" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r01762 | "delivered" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01763 | "delivered" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01764 | "delivered" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01765 | "descriptions" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r01766 | "descriptions" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r01767 | "descriptions" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r01768 | "detect" | "Main" | "detect" | "creates" | "Run Goertzel: detect target_freq power in a signal of signal_freq"
    r01769 | "diff" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r01770 | "diff" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r01771 | "diff" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r01772 | "diff" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r01773 | "diff" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r01774 | "diff" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r01775 | "difference" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r01776 | "difference" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r01777 | "difference" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r01778 | "diffs" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r01779 | "diffs" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r01780 | "diffs" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r01781 | "digest" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r01782 | "digest" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r01783 | "digest" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r01784 | "digest" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r01785 | "digest" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r01786 | "digest" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r01787 | "digest" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r01788 | "digest" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r01789 | "digest" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r01790 | "digit" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r01791 | "digit" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r01792 | "digit" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r01793 | "digits" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r01794 | "dimensions" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01795 | "dimensions" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01796 | "dimensions" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01797 | "dimensions" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01798 | "dimensions" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01799 | "dimensions" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r01800 | "directory" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r01801 | "directory" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01802 | "directory" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01803 | "directory" | "System" | "dir" | "inputs" | "List entries in a directory"
    r01804 | "directory" | "System" | "dir" | "outputs" | "Create a directory"
    r01805 | "directory" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r01806 | "directory" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r01807 | "directory" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r01808 | "directory" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01809 | "directory" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01810 | "directory" | "System" | "dir" | "inputs" | "List entries in a directory"
    r01811 | "directory" | "System" | "dir" | "outputs" | "Create a directory"
    r01812 | "directory" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r01813 | "directory" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r01814 | "directory" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r01815 | "directory" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01816 | "directory" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01817 | "directory" | "System" | "dir" | "inputs" | "List entries in a directory"
    r01818 | "directory" | "System" | "dir" | "outputs" | "Create a directory"
    r01819 | "directory" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r01820 | "directory" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r01821 | "disables" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01822 | "disables" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01823 | "disables" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01824 | "dispatch" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r01825 | "display" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r01826 | "display" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r01827 | "display" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r01828 | "distance" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01829 | "distance" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01830 | "distance" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01831 | "does" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r01832 | "does" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01833 | "does" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01834 | "does" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r01835 | "does" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01836 | "does" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01837 | "does" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r01838 | "does" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r01839 | "does" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r01840 | "dot" | "Main" | "parse_char_dot" | "creates" | "Continue parsing: check for dot (print)"
    r01841 | "dot" | "Main" | "dot" | "creates" | "Dot product of row i of a with row j of bt (transposed b)"
    r01842 | "double" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01843 | "double" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01844 | "double" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r01845 | "double" | "Simple_math" | "double" | "transforms" | "double(5) == 10"
    r01846 | "doubles" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r01847 | "drags" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r01848 | "drags" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r01849 | "drags" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r01850 | "draw" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01851 | "draw" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01852 | "draw" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r01853 | "driven" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r01854 | "driven" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r01855 | "driven" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r01856 | "dst" | "Main" | "transpose_columns" | "creates" | "Transpose: copy columns of src into rows of dst"
    r01857 | "duration" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r01858 | "duration" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r01859 | "duration" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01860 | "duration" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r01861 | "duration" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r01862 | "duration" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r01863 | "duration" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r01864 | "duration" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r01865 | "duration" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01866 | "duration" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r01867 | "duration" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r01868 | "duration" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r01869 | "duration" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r01870 | "duration" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r01871 | "duration" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01872 | "duration" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r01873 | "duration" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r01874 | "duration" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r01875 | "each" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01876 | "each" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01877 | "each" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01878 | "each" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r01879 | "each" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01880 | "each" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01881 | "each" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r01882 | "echo" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01883 | "echo" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r01884 | "echo" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01885 | "echo" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r01886 | "echo" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01887 | "echo" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01888 | "echo" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r01889 | "echo" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r01890 | "echo" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r01891 | "edit" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01892 | "edit" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01893 | "edit" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r01894 | "editable" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01895 | "editable" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01896 | "editable" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r01897 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01898 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01899 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r01900 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r01901 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r01902 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01903 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01904 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01905 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r01906 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r01907 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r01908 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01909 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01910 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01911 | "element" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r01912 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01913 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01914 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01915 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01916 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01917 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01918 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01919 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01920 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01921 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01922 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r01923 | "element" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r01924 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01925 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01926 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01927 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01928 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01929 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01930 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01931 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01932 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01933 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01934 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01935 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01936 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01937 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01938 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01939 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01940 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01941 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r01942 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r01943 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r01944 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01945 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01946 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01947 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r01948 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r01949 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r01950 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01951 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01952 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01953 | "element" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r01954 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01955 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01956 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01957 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01958 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01959 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01960 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01961 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01962 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01963 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01964 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r01965 | "element" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r01966 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01967 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01968 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01969 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01970 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01971 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01972 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01973 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01974 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01975 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01976 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01977 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01978 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01979 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01980 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r01981 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01982 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01983 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r01984 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r01985 | "element" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r01986 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01987 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01988 | "element" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r01989 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r01990 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r01991 | "element" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r01992 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01993 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01994 | "element" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r01995 | "element" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r01996 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01997 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01998 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r01999 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02000 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02001 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02002 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02003 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02004 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02005 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02006 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r02007 | "element" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r02008 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02009 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02010 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02011 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02012 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02013 | "element" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02014 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02015 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02016 | "element" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02017 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02018 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02019 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02020 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02021 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02022 | "element" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02023 | "eliminate" | "Main" | "eliminate_multiples" | "creates" | "Eliminate squares of primes: mark multiples of n*n"
    r02024 | "eliminate" | "Main" | "eliminate_squares" | "creates" | "Eliminate squares of primes: iterate over sieve"
    r02025 | "emit" | "Main" | "parse_char" | "creates" | "Parse one BF character and emit opcodes"
    r02026 | "empty" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r02027 | "empty" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r02028 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02029 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02030 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02031 | "empty" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02032 | "empty" | "Table" | "new" | "creates" | "Create a new empty table"
    r02033 | "empty" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02034 | "empty" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r02035 | "empty" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r02036 | "empty" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r02037 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02038 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02039 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02040 | "empty" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02041 | "empty" | "Table" | "new" | "creates" | "Create a new empty table"
    r02042 | "empty" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02043 | "empty" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r02044 | "empty" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r02045 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02046 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02047 | "empty" | "Sequence" | "empty" | "validates" | "Check if an array is empty"
    r02048 | "empty" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02049 | "empty" | "Table" | "new" | "creates" | "Create a new empty table"
    r02050 | "empty" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02051 | "enable" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r02052 | "enable" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r02053 | "enable" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r02054 | "enables" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r02055 | "enables" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r02056 | "enables" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r02057 | "encode" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r02058 | "encode" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r02059 | "encode" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r02060 | "encode" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r02061 | "encode" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r02062 | "encode" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r02063 | "encode" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r02064 | "encoded" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r02065 | "encoded" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r02066 | "end" | "Language" | "end" | "creates" | "Get the end position of a token"
    r02067 | "end" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r02068 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02069 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02070 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02071 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02072 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02073 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02074 | "end" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02075 | "end" | "Language" | "end" | "creates" | "Get the end position of a token"
    r02076 | "end" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r02077 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02078 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02079 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02080 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02081 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02082 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02083 | "end" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02084 | "end" | "Language" | "end" | "creates" | "Get the end position of a token"
    r02085 | "end" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r02086 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02087 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02088 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02089 | "end" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02090 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02091 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02092 | "end" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02093 | "ends" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r02094 | "ends" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r02095 | "ends" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r02096 | "ends" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r02097 | "ends" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r02098 | "ends" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r02099 | "english" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r02100 | "english" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r02101 | "english" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r02102 | "ensures" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02103 | "ensures" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02104 | "ensures" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02105 | "enter" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02106 | "enter" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02107 | "enter" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02108 | "entire" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r02109 | "entire" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r02110 | "entire" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r02111 | "entries" | "System" | "dir" | "inputs" | "List entries in a directory"
    r02112 | "entries" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r02113 | "entries" | "System" | "dir" | "inputs" | "List entries in a directory"
    r02114 | "entries" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r02115 | "entries" | "System" | "dir" | "inputs" | "List entries in a directory"
    r02116 | "entries" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r02117 | "eof" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02118 | "eof" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r02119 | "eof" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02120 | "eof" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02121 | "equivalents" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r02122 | "equivalents" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r02123 | "equivalents" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r02124 | "error" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02125 | "error" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r02126 | "error" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r02127 | "error" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r02128 | "error" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02129 | "error" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r02130 | "error" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r02131 | "error" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r02132 | "error" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02133 | "error" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r02134 | "error" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r02135 | "error" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r02136 | "escape" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02137 | "escape" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02138 | "escape" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02139 | "euler" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r02140 | "euler" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r02141 | "euler" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r02142 | "even" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02143 | "even" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02144 | "even" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02145 | "event" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02146 | "event" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02147 | "event" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02148 | "events" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r02149 | "events" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r02150 | "events" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r02151 | "events" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r02152 | "events" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r02153 | "events" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r02154 | "events" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r02155 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02156 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02157 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02158 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02159 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02160 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02161 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02162 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02163 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02164 | "exactly" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02165 | "exactly" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02166 | "exactly" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02167 | "exactly" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02168 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02169 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02170 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02171 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02172 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02173 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02174 | "exclusive" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02175 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02176 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02177 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02178 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02179 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02180 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02181 | "exclusive" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02182 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02183 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02184 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02185 | "exclusive" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02186 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02187 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02188 | "exclusive" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02189 | "execute" | "System" | "system" | "inputs" | "Execute a system command"
    r02190 | "execute" | "System" | "system" | "inputs" | "Execute a system command"
    r02191 | "execute" | "System" | "system" | "inputs" | "Execute a system command"
    r02192 | "exist" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r02193 | "exist" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r02194 | "exist" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r02195 | "exists" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r02196 | "exists" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r02197 | "exists" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02198 | "exists" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02199 | "exists" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r02200 | "exists" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r02201 | "exists" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r02202 | "exists" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r02203 | "exists" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r02204 | "exists" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02205 | "exists" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02206 | "exists" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r02207 | "exists" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r02208 | "exists" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r02209 | "exists" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r02210 | "exists" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r02211 | "exists" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02212 | "exists" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02213 | "exists" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r02214 | "exists" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r02215 | "exists" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r02216 | "exit" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02217 | "exit" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r02218 | "exit" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r02219 | "exit" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r02220 | "exit" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02221 | "exit" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02222 | "exit" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r02223 | "exit" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r02224 | "exit" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02225 | "exit" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r02226 | "exit" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r02227 | "exits" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r02228 | "exits" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r02229 | "exits" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02230 | "exp" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r02231 | "exp" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r02232 | "exp" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r02233 | "expected" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r02234 | "expected" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r02235 | "expected" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r02236 | "expected" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r02237 | "expected" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r02238 | "expected" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r02239 | "expected" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r02240 | "expected" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r02241 | "expected" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r02242 | "expression" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r02243 | "expression" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r02244 | "expression" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r02245 | "extension" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02246 | "extension" | "Path" | "extension" | "derives" | "Get the file extension"
    r02247 | "extension" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02248 | "extension" | "Path" | "extension" | "derives" | "Get the file extension"
    r02249 | "extension" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02250 | "extension" | "Path" | "extension" | "derives" | "Get the file extension"
    r02251 | "extract" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r02252 | "extract" | "Language" | "words" | "creates" | "Extract individual words from text"
    r02253 | "extract" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r02254 | "extract" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r02255 | "extract" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02256 | "extract" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r02257 | "extract" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r02258 | "extract" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r02259 | "extract" | "Time" | "date" | "creates" | "Extract date from a time"
    r02260 | "extract" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r02261 | "extract" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r02262 | "extract" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02263 | "extract" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r02264 | "extract" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02265 | "extract" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r02266 | "extract" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r02267 | "extract" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r02268 | "extract" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r02269 | "extract" | "Language" | "words" | "creates" | "Extract individual words from text"
    r02270 | "extract" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r02271 | "extract" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r02272 | "extract" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02273 | "extract" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r02274 | "extract" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r02275 | "extract" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r02276 | "extract" | "Time" | "date" | "creates" | "Extract date from a time"
    r02277 | "extract" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r02278 | "extract" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r02279 | "extract" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02280 | "extract" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r02281 | "extract" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02282 | "extract" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r02283 | "extract" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r02284 | "extract" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r02285 | "extract" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r02286 | "extract" | "Language" | "words" | "creates" | "Extract individual words from text"
    r02287 | "extract" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r02288 | "extract" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r02289 | "extract" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02290 | "extract" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r02291 | "extract" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r02292 | "extract" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r02293 | "extract" | "Time" | "date" | "creates" | "Extract date from a time"
    r02294 | "extract" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r02295 | "extract" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r02296 | "extract" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02297 | "extract" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r02298 | "extract" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02299 | "extract" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r02300 | "extract" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r02301 | "extract" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r02302 | "failable" | "Main" | "try_json" | "transforms" | "Parse JSON and return as a Value string (failable)"
    r02303 | "fails" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02304 | "fails" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02305 | "fails" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02306 | "fall" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02307 | "fall" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02308 | "fall" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02309 | "falls" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r02310 | "falls" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r02311 | "falls" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r02312 | "falls" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r02313 | "falls" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r02314 | "falls" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r02315 | "false" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r02316 | "false" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02317 | "false" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r02318 | "false" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r02319 | "false" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r02320 | "false" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r02321 | "false" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02322 | "false" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r02323 | "false" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r02324 | "false" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r02325 | "false" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r02326 | "false" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02327 | "false" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r02328 | "false" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r02329 | "false" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r02330 | "field" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r02331 | "field" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r02332 | "field" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r02333 | "field" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r02334 | "field" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r02335 | "field" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r02336 | "file" | "Path" | "extension" | "derives" | "Get the file extension"
    r02337 | "file" | "System" | "file" | "inputs" | "Read contents from a file"
    r02338 | "file" | "System" | "file" | "outputs" | "Write content to a file"
    r02339 | "file" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02340 | "file" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02341 | "file" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02342 | "file" | "System" | "close" | "outputs" | "Close an open file handle"
    r02343 | "file" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r02344 | "file" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r02345 | "file" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02346 | "file" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r02347 | "file" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r02348 | "file" | "Path" | "extension" | "derives" | "Get the file extension"
    r02349 | "file" | "System" | "file" | "inputs" | "Read contents from a file"
    r02350 | "file" | "System" | "file" | "outputs" | "Write content to a file"
    r02351 | "file" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02352 | "file" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02353 | "file" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02354 | "file" | "System" | "close" | "outputs" | "Close an open file handle"
    r02355 | "file" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r02356 | "file" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r02357 | "file" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02358 | "file" | "Path" | "extension" | "derives" | "Get the file extension"
    r02359 | "file" | "System" | "file" | "inputs" | "Read contents from a file"
    r02360 | "file" | "System" | "file" | "outputs" | "Write content to a file"
    r02361 | "file" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02362 | "file" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02363 | "file" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02364 | "file" | "System" | "close" | "outputs" | "Close an open file handle"
    r02365 | "file" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r02366 | "file" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r02367 | "file" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r02368 | "filename" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r02369 | "filename" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02370 | "filename" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r02371 | "filename" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02372 | "filename" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r02373 | "filename" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02374 | "fill" | "Main" | "columns" | "creates" | "Generate matrix: fill row by iterating columns"
    r02375 | "fill" | "Main" | "mul_columns" | "creates" | "Multiply: fill one row of result"
    r02376 | "filter" | "Main" | "signal_power" | "derives" | "Compute signal power from filter state"
    r02377 | "final" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r02378 | "final" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r02379 | "final" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r02380 | "find" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r02381 | "find" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r02382 | "find" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r02383 | "find" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r02384 | "find" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r02385 | "find" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r02386 | "find" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r02387 | "find" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r02388 | "find" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r02389 | "find" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r02390 | "find" | "Text" | "index" | "creates" | "Find the index of a substring"
    r02391 | "find" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r02392 | "find" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r02393 | "find" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r02394 | "find" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r02395 | "find" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r02396 | "find" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r02397 | "find" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r02398 | "find" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r02399 | "find" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r02400 | "find" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r02401 | "find" | "Text" | "index" | "creates" | "Find the index of a substring"
    r02402 | "find" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r02403 | "find" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r02404 | "find" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r02405 | "find" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r02406 | "find" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r02407 | "find" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r02408 | "find" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r02409 | "find" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r02410 | "find" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r02411 | "find" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r02412 | "find" | "Text" | "index" | "creates" | "Find the index of a substring"
    r02413 | "fire" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r02414 | "first" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r02415 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r02416 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02417 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r02418 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02419 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02420 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02421 | "first" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r02422 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r02423 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02424 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r02425 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02426 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02427 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02428 | "first" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r02429 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r02430 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02431 | "first" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r02432 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02433 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02434 | "first" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02435 | "flip" | "Main" | "atkin_step1_y" | "creates" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r02436 | "flip" | "Main" | "atkin_step2_y" | "creates" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r02437 | "flip" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r02438 | "float" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r02439 | "float" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r02440 | "float" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r02441 | "float" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r02442 | "float" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r02443 | "float" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r02444 | "float" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r02445 | "float" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r02446 | "float" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r02447 | "float" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r02448 | "float" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r02449 | "float" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r02450 | "float" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r02451 | "float" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r02452 | "float" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r02453 | "float" | "Types" | "float" | "validates" | "Check if a value is a float"
    r02454 | "float" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r02455 | "float" | "Types" | "float" | "creates" | "Parse a string as a float"
    r02456 | "float" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r02457 | "float" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r02458 | "float" | "Types" | "string" | "creates" | "Convert a float to a string"
    r02459 | "float" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r02460 | "float" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r02461 | "float" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r02462 | "float" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r02463 | "float" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r02464 | "float" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r02465 | "float" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r02466 | "float" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r02467 | "float" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r02468 | "float" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r02469 | "float" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r02470 | "float" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r02471 | "float" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r02472 | "float" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r02473 | "float" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r02474 | "float" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r02475 | "float" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r02476 | "float" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r02477 | "float" | "Types" | "float" | "validates" | "Check if a value is a float"
    r02478 | "float" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r02479 | "float" | "Types" | "float" | "creates" | "Parse a string as a float"
    r02480 | "float" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r02481 | "float" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r02482 | "float" | "Types" | "string" | "creates" | "Convert a float to a string"
    r02483 | "float" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r02484 | "float" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r02485 | "float" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r02486 | "float" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r02487 | "float" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r02488 | "float" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r02489 | "float" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r02490 | "float" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r02491 | "float" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r02492 | "float" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r02493 | "float" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r02494 | "float" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r02495 | "float" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r02496 | "float" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r02497 | "float" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r02498 | "float" | "Sequence" | "contains" | "validates" | "Check if a float is in an array"
    r02499 | "float" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r02500 | "float" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r02501 | "float" | "Types" | "float" | "validates" | "Check if a value is a float"
    r02502 | "float" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r02503 | "float" | "Types" | "float" | "creates" | "Parse a string as a float"
    r02504 | "float" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r02505 | "float" | "Types" | "float" | "creates" | "Convert a decimal to a float"
    r02506 | "float" | "Types" | "string" | "creates" | "Convert a float to a string"
    r02507 | "float" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r02508 | "float" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r02509 | "float" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r02510 | "floating" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02511 | "floating" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02512 | "floating" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02513 | "floats" | "Math" | "min" | "derives" | "Minimum of two floats"
    r02514 | "floats" | "Math" | "max" | "derives" | "Maximum of two floats"
    r02515 | "floats" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02516 | "floats" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r02517 | "floats" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r02518 | "floats" | "Math" | "min" | "derives" | "Minimum of two floats"
    r02519 | "floats" | "Math" | "max" | "derives" | "Maximum of two floats"
    r02520 | "floats" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02521 | "floats" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r02522 | "floats" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r02523 | "floats" | "Math" | "min" | "derives" | "Minimum of two floats"
    r02524 | "floats" | "Math" | "max" | "derives" | "Maximum of two floats"
    r02525 | "floats" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02526 | "floats" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r02527 | "floats" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r02528 | "floor" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r02529 | "floor" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r02530 | "floor" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r02531 | "fly" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r02532 | "folding" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r02533 | "folding" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r02534 | "folding" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r02535 | "for" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r02536 | "for" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02537 | "for" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r02538 | "for" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r02539 | "for" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r02540 | "for" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r02541 | "for" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r02542 | "for" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r02543 | "for" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r02544 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02545 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02546 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02547 | "for" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02548 | "for" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r02549 | "for" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r02550 | "for" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02551 | "for" | "Main" | "parse_char_minus" | "creates" | "Continue parsing: check for minus"
    r02552 | "for" | "Main" | "parse_char_right" | "creates" | "Continue parsing: check for right"
    r02553 | "for" | "Main" | "parse_char_left" | "creates" | "Continue parsing: check for left"
    r02554 | "for" | "Main" | "parse_char_dot" | "creates" | "Continue parsing: check for dot (print)"
    r02555 | "for" | "Main" | "parse_char_bracket" | "creates" | "Continue parsing: check for open/close brackets"
    r02556 | "for" | "Main" | "count_ops" | "creates" | "Count BF opcodes in source for array sizing"
    r02557 | "for" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r02558 | "for" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r02559 | "for" | "Main" | "atkin_step1_y" | "creates" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r02560 | "for" | "Main" | "atkin_step2_y" | "creates" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r02561 | "for" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r02562 | "for" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r02563 | "for" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02564 | "for" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r02565 | "for" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r02566 | "for" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r02567 | "for" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r02568 | "for" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r02569 | "for" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r02570 | "for" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r02571 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02572 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02573 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02574 | "for" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02575 | "for" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r02576 | "for" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r02577 | "for" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02578 | "for" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r02579 | "for" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r02580 | "for" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r02581 | "for" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r02582 | "for" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r02583 | "for" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r02584 | "for" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r02585 | "for" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r02586 | "for" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r02587 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02588 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02589 | "for" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02590 | "for" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02591 | "for" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r02592 | "for" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r02593 | "for" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r02594 | "forget" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r02595 | "form" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r02596 | "form" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r02597 | "form" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r02598 | "format" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r02599 | "format" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r02600 | "format" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r02601 | "format" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r02602 | "format" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r02603 | "format" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r02604 | "format" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r02605 | "format" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r02606 | "format" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r02607 | "format" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r02608 | "format" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r02609 | "format" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r02610 | "format" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r02611 | "format" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r02612 | "format" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r02613 | "format" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r02614 | "format" | "Commands" | "format" | "dispatches" | "Run the format command"
    r02615 | "format" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r02616 | "format" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r02617 | "format" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r02618 | "format" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r02619 | "format" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r02620 | "format" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r02621 | "format" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r02622 | "format" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r02623 | "formats" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r02624 | "formats" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r02625 | "formats" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r02626 | "formats" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r02627 | "formats" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r02628 | "formats" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r02629 | "found" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02630 | "found" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r02631 | "found" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02632 | "found" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02633 | "frame" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r02634 | "frame" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r02635 | "frame" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r02636 | "frame" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02637 | "frame" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r02638 | "frame" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r02639 | "frame" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r02640 | "frame" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r02641 | "frame" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02642 | "frame" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r02643 | "frame" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r02644 | "frame" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r02645 | "frame" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r02646 | "frame" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r02647 | "frame" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r02648 | "framed" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02649 | "freq" | "Main" | "detect" | "creates" | "Run Goertzel: detect target_freq power in a signal of signal_freq"
    r02650 | "frequencies" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r02651 | "frequencies" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r02652 | "frequencies" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r02653 | "frequency" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r02654 | "frequent" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02655 | "frequent" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02656 | "frequent" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02657 | "from" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r02658 | "from" | "Language" | "words" | "creates" | "Extract individual words from text"
    r02659 | "from" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r02660 | "from" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r02661 | "from" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r02662 | "from" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02663 | "from" | "Network" | "message" | "inputs" | "Read data from a socket"
    r02664 | "from" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r02665 | "from" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r02666 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r02667 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02668 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02669 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02670 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02671 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02672 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02673 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02674 | "from" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r02675 | "from" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r02676 | "from" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r02677 | "from" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r02678 | "from" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r02679 | "from" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r02680 | "from" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r02681 | "from" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r02682 | "from" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r02683 | "from" | "System" | "console" | "inputs" | "Read a line from the console"
    r02684 | "from" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02685 | "from" | "System" | "file" | "inputs" | "Read contents from a file"
    r02686 | "from" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02687 | "from" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r02688 | "from" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r02689 | "from" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02690 | "from" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r02691 | "from" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r02692 | "from" | "Time" | "duration" | "creates" | "Create a duration from components"
    r02693 | "from" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r02694 | "from" | "Time" | "date" | "creates" | "Extract date from a time"
    r02695 | "from" | "Time" | "date" | "creates" | "Create a date from components"
    r02696 | "from" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r02697 | "from" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r02698 | "from" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r02699 | "from" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r02700 | "from" | "Time" | "clock" | "creates" | "Create a clock from components"
    r02701 | "from" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02702 | "from" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r02703 | "from" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02704 | "from" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r02705 | "from" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r02706 | "from" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r02707 | "from" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r02708 | "from" | "Main" | "signal_power" | "derives" | "Compute signal power from filter state"
    r02709 | "from" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r02710 | "from" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02711 | "from" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r02712 | "from" | "Language" | "words" | "creates" | "Extract individual words from text"
    r02713 | "from" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r02714 | "from" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r02715 | "from" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r02716 | "from" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02717 | "from" | "Network" | "message" | "inputs" | "Read data from a socket"
    r02718 | "from" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r02719 | "from" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r02720 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r02721 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02722 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02723 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02724 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02725 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02726 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02727 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02728 | "from" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r02729 | "from" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r02730 | "from" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r02731 | "from" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r02732 | "from" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r02733 | "from" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r02734 | "from" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r02735 | "from" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r02736 | "from" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r02737 | "from" | "System" | "console" | "inputs" | "Read a line from the console"
    r02738 | "from" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02739 | "from" | "System" | "file" | "inputs" | "Read contents from a file"
    r02740 | "from" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02741 | "from" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r02742 | "from" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r02743 | "from" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02744 | "from" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r02745 | "from" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r02746 | "from" | "Time" | "duration" | "creates" | "Create a duration from components"
    r02747 | "from" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r02748 | "from" | "Time" | "date" | "creates" | "Extract date from a time"
    r02749 | "from" | "Time" | "date" | "creates" | "Create a date from components"
    r02750 | "from" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r02751 | "from" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r02752 | "from" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r02753 | "from" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r02754 | "from" | "Time" | "clock" | "creates" | "Create a clock from components"
    r02755 | "from" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02756 | "from" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r02757 | "from" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02758 | "from" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r02759 | "from" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r02760 | "from" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r02761 | "from" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r02762 | "from" | "Language" | "words" | "creates" | "Extract individual words from text"
    r02763 | "from" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r02764 | "from" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r02765 | "from" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r02766 | "from" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r02767 | "from" | "Network" | "message" | "inputs" | "Read data from a socket"
    r02768 | "from" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r02769 | "from" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r02770 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r02771 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02772 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02773 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02774 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02775 | "from" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02776 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02777 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02778 | "from" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r02779 | "from" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r02780 | "from" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r02781 | "from" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r02782 | "from" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r02783 | "from" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r02784 | "from" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r02785 | "from" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r02786 | "from" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r02787 | "from" | "System" | "console" | "inputs" | "Read a line from the console"
    r02788 | "from" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02789 | "from" | "System" | "file" | "inputs" | "Read contents from a file"
    r02790 | "from" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02791 | "from" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r02792 | "from" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r02793 | "from" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02794 | "from" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r02795 | "from" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r02796 | "from" | "Time" | "duration" | "creates" | "Create a duration from components"
    r02797 | "from" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r02798 | "from" | "Time" | "date" | "creates" | "Extract date from a time"
    r02799 | "from" | "Time" | "date" | "creates" | "Create a date from components"
    r02800 | "from" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r02801 | "from" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r02802 | "from" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r02803 | "from" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r02804 | "from" | "Time" | "clock" | "creates" | "Create a clock from components"
    r02805 | "from" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r02806 | "from" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r02807 | "from" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02808 | "from" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r02809 | "from" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r02810 | "from" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r02811 | "full" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02812 | "full" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r02813 | "full" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02814 | "full" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r02815 | "fully" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r02816 | "function" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r02817 | "function" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r02818 | "function" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r02819 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r02820 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r02821 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r02822 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r02823 | "generate" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r02824 | "generate" | "Main" | "sine_sample" | "derives" | "Generate a sine wave sample at a given index"
    r02825 | "generate" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r02826 | "generate" | "Main" | "columns" | "creates" | "Generate matrix: fill row by iterating columns"
    r02827 | "generate" | "Main" | "rows" | "creates" | "Generate matrix: iterate rows"
    r02828 | "generate" | "Main" | "generate" | "creates" | "Generate an n x n matrix with seed scaling"
    r02829 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r02830 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r02831 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r02832 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r02833 | "generate" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r02834 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r02835 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r02836 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r02837 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r02838 | "generate" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r02839 | "get" | "Language" | "start" | "creates" | "Get the start position of a token"
    r02840 | "get" | "Language" | "end" | "creates" | "Get the end position of a token"
    r02841 | "get" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r02842 | "get" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r02843 | "get" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r02844 | "get" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r02845 | "get" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02846 | "get" | "Path" | "extension" | "derives" | "Get the file extension"
    r02847 | "get" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r02848 | "get" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r02849 | "get" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r02850 | "get" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r02851 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r02852 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02853 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r02854 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02855 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02856 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02857 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r02858 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r02859 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r02860 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02861 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02862 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02863 | "get" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r02864 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02865 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02866 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02867 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02868 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02869 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02870 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02871 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02872 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02873 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02874 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02875 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02876 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02877 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02878 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02879 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02880 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02881 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02882 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02883 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02884 | "get" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r02885 | "get" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r02886 | "get" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r02887 | "get" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r02888 | "get" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r02889 | "get" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r02890 | "get" | "System" | "process" | "inputs" | "Get command line arguments"
    r02891 | "get" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r02892 | "get" | "Table" | "get" | "derives" | "Get a value by key"
    r02893 | "get" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r02894 | "get" | "Table" | "values" | "derives" | "Get all values in a table"
    r02895 | "get" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r02896 | "get" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r02897 | "get" | "Text" | "length" | "creates" | "Get the length of a string"
    r02898 | "get" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02899 | "get" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r02900 | "get" | "Time" | "time" | "inputs" | "Get current time"
    r02901 | "get" | "Time" | "days" | "derives" | "Get number of days in a month"
    r02902 | "get" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r02903 | "get" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r02904 | "get" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r02905 | "get" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r02906 | "get" | "Language" | "start" | "creates" | "Get the start position of a token"
    r02907 | "get" | "Language" | "end" | "creates" | "Get the end position of a token"
    r02908 | "get" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r02909 | "get" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r02910 | "get" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r02911 | "get" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r02912 | "get" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02913 | "get" | "Path" | "extension" | "derives" | "Get the file extension"
    r02914 | "get" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r02915 | "get" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r02916 | "get" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r02917 | "get" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r02918 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r02919 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02920 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r02921 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02922 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02923 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02924 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r02925 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r02926 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r02927 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02928 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02929 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02930 | "get" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r02931 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02932 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02933 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02934 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02935 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02936 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02937 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02938 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02939 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02940 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02941 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r02942 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02943 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02944 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r02945 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02946 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02947 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02948 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02949 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02950 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r02951 | "get" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r02952 | "get" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r02953 | "get" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r02954 | "get" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r02955 | "get" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r02956 | "get" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r02957 | "get" | "System" | "process" | "inputs" | "Get command line arguments"
    r02958 | "get" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r02959 | "get" | "Table" | "get" | "derives" | "Get a value by key"
    r02960 | "get" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r02961 | "get" | "Table" | "values" | "derives" | "Get all values in a table"
    r02962 | "get" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r02963 | "get" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r02964 | "get" | "Text" | "length" | "creates" | "Get the length of a string"
    r02965 | "get" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r02966 | "get" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r02967 | "get" | "Time" | "time" | "inputs" | "Get current time"
    r02968 | "get" | "Time" | "days" | "derives" | "Get number of days in a month"
    r02969 | "get" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r02970 | "get" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r02971 | "get" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r02972 | "get" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r02973 | "get" | "Language" | "start" | "creates" | "Get the start position of a token"
    r02974 | "get" | "Language" | "end" | "creates" | "Get the end position of a token"
    r02975 | "get" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r02976 | "get" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r02977 | "get" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r02978 | "get" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r02979 | "get" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r02980 | "get" | "Path" | "extension" | "derives" | "Get the file extension"
    r02981 | "get" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r02982 | "get" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r02983 | "get" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r02984 | "get" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r02985 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r02986 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r02987 | "get" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r02988 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02989 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02990 | "get" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r02991 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r02992 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r02993 | "get" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r02994 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02995 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02996 | "get" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r02997 | "get" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r02998 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r02999 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03000 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03001 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03002 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03003 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03004 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03005 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03006 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03007 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03008 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r03009 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r03010 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r03011 | "get" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r03012 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03013 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03014 | "get" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03015 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03016 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03017 | "get" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03018 | "get" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r03019 | "get" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r03020 | "get" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r03021 | "get" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r03022 | "get" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r03023 | "get" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r03024 | "get" | "System" | "process" | "inputs" | "Get command line arguments"
    r03025 | "get" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r03026 | "get" | "Table" | "get" | "derives" | "Get a value by key"
    r03027 | "get" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r03028 | "get" | "Table" | "values" | "derives" | "Get all values in a table"
    r03029 | "get" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r03030 | "get" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r03031 | "get" | "Text" | "length" | "creates" | "Get the length of a string"
    r03032 | "get" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r03033 | "get" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r03034 | "get" | "Time" | "time" | "inputs" | "Get current time"
    r03035 | "get" | "Time" | "days" | "derives" | "Get number of days in a month"
    r03036 | "get" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r03037 | "get" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r03038 | "get" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r03039 | "get" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r03040 | "gets" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r03041 | "gets" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r03042 | "gets" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r03043 | "given" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r03044 | "given" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03045 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03046 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03047 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03048 | "given" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r03049 | "given" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r03050 | "given" | "Main" | "sine_sample" | "derives" | "Generate a sine wave sample at a given index"
    r03051 | "given" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r03052 | "given" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03053 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03054 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03055 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03056 | "given" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r03057 | "given" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r03058 | "given" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03059 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03060 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03061 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03062 | "given" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r03063 | "goertzel" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r03064 | "goertzel" | "Main" | "detect" | "creates" | "Run Goertzel: detect target_freq power in a signal of signal_freq"
    r03065 | "grams" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r03066 | "grams" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r03067 | "grams" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r03068 | "handle" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r03069 | "handle" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03070 | "handle" | "System" | "close" | "outputs" | "Close an open file handle"
    r03071 | "handle" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r03072 | "handle" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03073 | "handle" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r03074 | "handle" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03075 | "handle" | "System" | "close" | "outputs" | "Close an open file handle"
    r03076 | "handle" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r03077 | "handle" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03078 | "handle" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r03079 | "handle" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03080 | "handle" | "System" | "close" | "outputs" | "Close an open file handle"
    r03081 | "handle" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r03082 | "handle" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03083 | "hash" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r03084 | "hash" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r03085 | "hash" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03086 | "hash" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r03087 | "hash" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r03088 | "hash" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03089 | "hash" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r03090 | "hash" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r03091 | "hash" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03092 | "hash" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r03093 | "hash" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r03094 | "hash" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r03095 | "hash" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03096 | "hash" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r03097 | "hash" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r03098 | "hash" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03099 | "hash" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r03100 | "hash" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r03101 | "hash" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03102 | "hash" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r03103 | "hash" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r03104 | "hash" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r03105 | "hash" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03106 | "hash" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r03107 | "hash" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r03108 | "hash" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03109 | "hash" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r03110 | "hash" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r03111 | "hash" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03112 | "hash" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r03113 | "have" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r03114 | "have" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r03115 | "have" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r03116 | "have" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r03117 | "have" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r03118 | "have" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r03119 | "header" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r03120 | "headings" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03121 | "headings" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03122 | "headings" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03123 | "height" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r03124 | "height" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r03125 | "height" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r03126 | "hex" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r03127 | "hex" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r03128 | "hex" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r03129 | "hex" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r03130 | "hex" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r03131 | "hex" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r03132 | "hex" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r03133 | "hex" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r03134 | "hex" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r03135 | "hexadecimal" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r03136 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r03137 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r03138 | "hexadecimal" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r03139 | "hexadecimal" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r03140 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r03141 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r03142 | "hexadecimal" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r03143 | "hexadecimal" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r03144 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r03145 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r03146 | "hexadecimal" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r03147 | "hmac" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r03148 | "hmac" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r03149 | "hmac" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r03150 | "hmac" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r03151 | "hmac" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r03152 | "hmac" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r03153 | "horizontal" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03154 | "horizontal" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03155 | "horizontal" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03156 | "host" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r03157 | "host" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r03158 | "host" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r03159 | "host" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r03160 | "host" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r03161 | "host" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r03162 | "host" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r03163 | "host" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r03164 | "host" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r03165 | "identical" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03166 | "identical" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03167 | "identical" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03168 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03169 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03170 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03171 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03172 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03173 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03174 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03175 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03176 | "immutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03177 | "impedance" | "Main" | "to_dbm" | "derives" | "Convert power to dBm (600 Ohm impedance)"
    r03178 | "including" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r03179 | "including" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r03180 | "including" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r03181 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r03182 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r03183 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r03184 | "inclusive" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03185 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r03186 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r03187 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r03188 | "inclusive" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03189 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r03190 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp a float between lo and hi (inclusive)"
    r03191 | "inclusive" | "Math" | "clamp" | "derives" | "Clamp a decimal between lo and hi (inclusive)"
    r03192 | "inclusive" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03193 | "incoming" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r03194 | "incoming" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r03195 | "incoming" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r03196 | "index" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r03197 | "index" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r03198 | "index" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r03199 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03200 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03201 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03202 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03203 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03204 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03205 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03206 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03207 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03208 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03209 | "index" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r03210 | "index" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r03211 | "index" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r03212 | "index" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r03213 | "index" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r03214 | "index" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r03215 | "index" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r03216 | "index" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r03217 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r03218 | "index" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r03219 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03220 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03221 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03222 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03223 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03224 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03225 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03226 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03227 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03228 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03229 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03230 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03231 | "index" | "Text" | "index" | "creates" | "Find the index of a substring"
    r03232 | "index" | "Main" | "sine_sample" | "derives" | "Generate a sine wave sample at a given index"
    r03233 | "index" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r03234 | "index" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r03235 | "index" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r03236 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03237 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03238 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03239 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03240 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03241 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03242 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03243 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03244 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03245 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03246 | "index" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r03247 | "index" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r03248 | "index" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r03249 | "index" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r03250 | "index" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r03251 | "index" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r03252 | "index" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r03253 | "index" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r03254 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r03255 | "index" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r03256 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03257 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03258 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03259 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03260 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03261 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03262 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03263 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03264 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03265 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03266 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03267 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03268 | "index" | "Text" | "index" | "creates" | "Find the index of a substring"
    r03269 | "index" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r03270 | "index" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r03271 | "index" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r03272 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03273 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03274 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03275 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03276 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03277 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03278 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03279 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03280 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03281 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03282 | "index" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r03283 | "index" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r03284 | "index" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r03285 | "index" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r03286 | "index" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r03287 | "index" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r03288 | "index" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r03289 | "index" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r03290 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r03291 | "index" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r03292 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03293 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03294 | "index" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r03295 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03296 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03297 | "index" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03298 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03299 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03300 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03301 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03302 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r03303 | "index" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r03304 | "index" | "Text" | "index" | "creates" | "Find the index of a substring"
    r03305 | "indices" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r03306 | "individual" | "Language" | "words" | "creates" | "Extract individual words from text"
    r03307 | "individual" | "Language" | "words" | "creates" | "Extract individual words from text"
    r03308 | "individual" | "Language" | "words" | "creates" | "Extract individual words from text"
    r03309 | "info" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r03310 | "info" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r03311 | "info" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r03312 | "inlined" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r03313 | "input" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r03314 | "input" | "System" | "console" | "validates" | "Validate console input"
    r03315 | "input" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03316 | "input" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r03317 | "input" | "System" | "console" | "validates" | "Validate console input"
    r03318 | "input" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03319 | "input" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r03320 | "input" | "System" | "console" | "validates" | "Validate console input"
    r03321 | "input" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03322 | "insert" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r03323 | "insert" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r03324 | "inside" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r03325 | "inside" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r03326 | "inside" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r03327 | "integer" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r03328 | "integer" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r03329 | "integer" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r03330 | "integer" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03331 | "integer" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r03332 | "integer" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r03333 | "integer" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r03334 | "integer" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r03335 | "integer" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r03336 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r03337 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03338 | "integer" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r03339 | "integer" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r03340 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r03341 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r03342 | "integer" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r03343 | "integer" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r03344 | "integer" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r03345 | "integer" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r03346 | "integer" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r03347 | "integer" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r03348 | "integer" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r03349 | "integer" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r03350 | "integer" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r03351 | "integer" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r03352 | "integer" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r03353 | "integer" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r03354 | "integer" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r03355 | "integer" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r03356 | "integer" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r03357 | "integer" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r03358 | "integer" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03359 | "integer" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r03360 | "integer" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r03361 | "integer" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r03362 | "integer" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r03363 | "integer" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r03364 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r03365 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03366 | "integer" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r03367 | "integer" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r03368 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r03369 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r03370 | "integer" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r03371 | "integer" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r03372 | "integer" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r03373 | "integer" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r03374 | "integer" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r03375 | "integer" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r03376 | "integer" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r03377 | "integer" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r03378 | "integer" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r03379 | "integer" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r03380 | "integer" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r03381 | "integer" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r03382 | "integer" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r03383 | "integer" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r03384 | "integer" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r03385 | "integer" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03386 | "integer" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r03387 | "integer" | "Math" | "clamp" | "derives" | "Clamp an integer between lo and hi (inclusive)"
    r03388 | "integer" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r03389 | "integer" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r03390 | "integer" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r03391 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r03392 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03393 | "integer" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r03394 | "integer" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r03395 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r03396 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in an array"
    r03397 | "integer" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r03398 | "integer" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r03399 | "integer" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r03400 | "integer" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r03401 | "integer" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r03402 | "integer" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r03403 | "integer" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r03404 | "integer" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r03405 | "integer" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r03406 | "integer" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r03407 | "integer" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r03408 | "integer" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r03409 | "integers" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r03410 | "integers" | "Math" | "min" | "derives" | "Minimum of two integers"
    r03411 | "integers" | "Math" | "max" | "derives" | "Maximum of two integers"
    r03412 | "integers" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r03413 | "integers" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r03414 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r03415 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03416 | "integers" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r03417 | "integers" | "Math" | "min" | "derives" | "Minimum of two integers"
    r03418 | "integers" | "Math" | "max" | "derives" | "Maximum of two integers"
    r03419 | "integers" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r03420 | "integers" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r03421 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r03422 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03423 | "integers" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r03424 | "integers" | "Math" | "min" | "derives" | "Minimum of two integers"
    r03425 | "integers" | "Math" | "max" | "derives" | "Maximum of two integers"
    r03426 | "integers" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r03427 | "integers" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r03428 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r03429 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03430 | "integrity" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r03431 | "integrity" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r03432 | "integrity" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r03433 | "interactive" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03434 | "interactive" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03435 | "interactive" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r03436 | "interactive" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03437 | "interactive" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03438 | "interactive" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r03439 | "interactive" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03440 | "interactive" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03441 | "interactive" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r03442 | "into" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r03443 | "into" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r03444 | "into" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r03445 | "into" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03446 | "into" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r03447 | "into" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r03448 | "into" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03449 | "into" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r03450 | "into" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r03451 | "into" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r03452 | "into" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r03453 | "into" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r03454 | "into" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r03455 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03456 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03457 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03458 | "into" | "Main" | "transpose_columns" | "creates" | "Transpose: copy columns of src into rows of dst"
    r03459 | "into" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r03460 | "into" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r03461 | "into" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r03462 | "into" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r03463 | "into" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03464 | "into" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r03465 | "into" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r03466 | "into" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03467 | "into" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r03468 | "into" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r03469 | "into" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r03470 | "into" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r03471 | "into" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r03472 | "into" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r03473 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03474 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03475 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03476 | "into" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r03477 | "into" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r03478 | "into" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r03479 | "into" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03480 | "into" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r03481 | "into" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r03482 | "into" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03483 | "into" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r03484 | "into" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r03485 | "into" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r03486 | "into" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r03487 | "into" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r03488 | "into" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r03489 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03490 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03491 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03492 | "inverse" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r03493 | "inverse" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r03494 | "inverse" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r03495 | "inverse" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r03496 | "inverse" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r03497 | "inverse" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r03498 | "inverse" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r03499 | "inverse" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r03500 | "inverse" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r03501 | "inverse" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r03502 | "inverse" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r03503 | "inverse" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r03504 | "iso" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r03505 | "iso" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r03506 | "iso" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r03507 | "iso" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r03508 | "iso" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r03509 | "iso" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r03510 | "iso" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r03511 | "iso" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r03512 | "iso" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r03513 | "iterate" | "Main" | "rows" | "creates" | "Generate matrix: iterate rows"
    r03514 | "iterate" | "Main" | "transpose_rows" | "creates" | "Transpose: iterate rows"
    r03515 | "iterate" | "Main" | "mul_rows" | "creates" | "Multiply: iterate rows"
    r03516 | "iterate" | "Main" | "atkin_step1_x" | "creates" | "Sieve of Atkin step 1: iterate x"
    r03517 | "iterate" | "Main" | "atkin_step2_x" | "creates" | "Sieve of Atkin step 2: iterate x"
    r03518 | "iterate" | "Main" | "atkin_step3_x" | "creates" | "Sieve of Atkin step 3: iterate x"
    r03519 | "iterate" | "Main" | "eliminate_squares" | "creates" | "Eliminate squares of primes: iterate over sieve"
    r03520 | "iterate" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r03521 | "iterating" | "Main" | "columns" | "creates" | "Generate matrix: fill row by iterating columns"
    r03522 | "iterating" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r03523 | "iteration" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r03524 | "iteration" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r03525 | "iteration" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r03526 | "iterations" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r03527 | "iterations" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r03528 | "its" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r03529 | "its" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r03530 | "its" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r03531 | "its" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r03532 | "its" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r03533 | "its" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r03534 | "its" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r03535 | "join" | "Path" | "join" | "derives" | "Join path components together"
    r03536 | "join" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r03537 | "join" | "Path" | "join" | "derives" | "Join path components together"
    r03538 | "join" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r03539 | "join" | "Path" | "join" | "derives" | "Join path components together"
    r03540 | "join" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r03541 | "json" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03542 | "json" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r03543 | "json" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r03544 | "json" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r03545 | "json" | "Main" | "show" | "outputs" | "Show table operations on a parsed JSON object"
    r03546 | "json" | "Main" | "try_json" | "transforms" | "Parse JSON and return as a Value string (failable)"
    r03547 | "json" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03548 | "json" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r03549 | "json" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r03550 | "json" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r03551 | "json" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03552 | "json" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r03553 | "json" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r03554 | "json" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r03555 | "just" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r03556 | "just" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r03557 | "just" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r03558 | "kernel" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r03559 | "key" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r03560 | "key" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r03561 | "key" | "Table" | "get" | "derives" | "Get a value by key"
    r03562 | "key" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r03563 | "key" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r03564 | "key" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r03565 | "key" | "Table" | "get" | "derives" | "Get a value by key"
    r03566 | "key" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r03567 | "key" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r03568 | "key" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r03569 | "key" | "Table" | "get" | "derives" | "Get a value by key"
    r03570 | "key" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r03571 | "keydown" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03572 | "keydown" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03573 | "keydown" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03574 | "keys" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r03575 | "keys" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r03576 | "keys" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r03577 | "kind" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r03578 | "kind" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r03579 | "kind" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r03580 | "kind" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r03581 | "kind" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r03582 | "kind" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r03583 | "kind" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r03584 | "kind" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r03585 | "kind" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r03586 | "kind" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r03587 | "kwargs" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03588 | "kwargs" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03589 | "kwargs" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03590 | "label" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r03591 | "label" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03592 | "label" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r03593 | "label" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03594 | "label" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03595 | "label" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r03596 | "label" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03597 | "label" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r03598 | "label" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03599 | "label" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03600 | "label" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r03601 | "label" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03602 | "label" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r03603 | "label" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03604 | "label" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03605 | "labels" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03606 | "labels" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03607 | "labels" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r03608 | "largest" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r03609 | "largest" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r03610 | "largest" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r03611 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r03612 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r03613 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r03614 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03615 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03616 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03617 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r03618 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r03619 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r03620 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03621 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03622 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03623 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r03624 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r03625 | "last" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r03626 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03627 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03628 | "last" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r03629 | "leap" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r03630 | "leap" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r03631 | "leap" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r03632 | "leave" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03633 | "leave" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03634 | "leave" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03635 | "left" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r03636 | "left" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r03637 | "left" | "Main" | "parse_char_left" | "creates" | "Continue parsing: check for left"
    r03638 | "left" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r03639 | "left" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r03640 | "left" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r03641 | "left" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r03642 | "length" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r03643 | "length" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r03644 | "length" | "Text" | "length" | "creates" | "Get the length of a string"
    r03645 | "length" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r03646 | "length" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r03647 | "length" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r03648 | "length" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r03649 | "length" | "Text" | "length" | "creates" | "Get the length of a string"
    r03650 | "length" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r03651 | "length" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r03652 | "length" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r03653 | "length" | "Text" | "length" | "creates" | "Get the length of a string"
    r03654 | "length" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r03655 | "level" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r03656 | "level" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r03657 | "level" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r03658 | "level" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r03659 | "level" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r03660 | "level" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r03661 | "levenshtein" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r03662 | "levenshtein" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r03663 | "levenshtein" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r03664 | "like" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r03665 | "like" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r03666 | "like" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r03667 | "line" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03668 | "line" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r03669 | "line" | "System" | "console" | "inputs" | "Read a line from the console"
    r03670 | "line" | "System" | "process" | "inputs" | "Get command line arguments"
    r03671 | "line" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03672 | "line" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03673 | "line" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03674 | "line" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03675 | "line" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r03676 | "line" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r03677 | "line" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r03678 | "line" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r03679 | "line" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r03680 | "line" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03681 | "line" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r03682 | "line" | "System" | "console" | "inputs" | "Read a line from the console"
    r03683 | "line" | "System" | "process" | "inputs" | "Get command line arguments"
    r03684 | "line" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03685 | "line" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03686 | "line" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03687 | "line" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03688 | "line" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r03689 | "line" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r03690 | "line" | "System" | "console" | "inputs" | "Read a line from the console"
    r03691 | "line" | "System" | "process" | "inputs" | "Get command line arguments"
    r03692 | "line" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03693 | "line" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03694 | "line" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03695 | "line" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03696 | "lines" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r03697 | "list" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r03698 | "list" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r03699 | "list" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r03700 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r03701 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r03702 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r03703 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r03704 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r03705 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r03706 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r03707 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r03708 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r03709 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r03710 | "list" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r03711 | "list" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r03712 | "list" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r03713 | "list" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r03714 | "list" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r03715 | "list" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r03716 | "list" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r03717 | "list" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r03718 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03719 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03720 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03721 | "list" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r03722 | "list" | "Store" | "version" | "inputs" | "List all versions of a table"
    r03723 | "list" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r03724 | "list" | "System" | "dir" | "inputs" | "List entries in a directory"
    r03725 | "list" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r03726 | "list" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r03727 | "list" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r03728 | "list" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r03729 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r03730 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r03731 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r03732 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r03733 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r03734 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r03735 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r03736 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r03737 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r03738 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r03739 | "list" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r03740 | "list" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r03741 | "list" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r03742 | "list" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r03743 | "list" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r03744 | "list" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r03745 | "list" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r03746 | "list" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r03747 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03748 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03749 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03750 | "list" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r03751 | "list" | "Store" | "version" | "inputs" | "List all versions of a table"
    r03752 | "list" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r03753 | "list" | "System" | "dir" | "inputs" | "List entries in a directory"
    r03754 | "list" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r03755 | "list" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r03756 | "list" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r03757 | "list" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r03758 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r03759 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r03760 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r03761 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r03762 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r03763 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r03764 | "list" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r03765 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r03766 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r03767 | "list" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r03768 | "list" | "Sequence" | "contains" | "validates" | "Check if a float is in a list"
    r03769 | "list" | "Sequence" | "contains" | "validates" | "Check if a decimal is in a list"
    r03770 | "list" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r03771 | "list" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r03772 | "list" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r03773 | "list" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r03774 | "list" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r03775 | "list" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r03776 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03777 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03778 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r03779 | "list" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r03780 | "list" | "Store" | "version" | "inputs" | "List all versions of a table"
    r03781 | "list" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r03782 | "list" | "System" | "dir" | "inputs" | "List entries in a directory"
    r03783 | "list" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r03784 | "listen" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r03785 | "listen" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r03786 | "listen" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r03787 | "listening" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r03788 | "listening" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r03789 | "listening" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r03790 | "lists" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r03791 | "lists" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r03792 | "lists" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r03793 | "load" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r03794 | "load" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r03795 | "load" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r03796 | "load" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r03797 | "load" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r03798 | "load" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r03799 | "loading" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03800 | "loading" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03801 | "loading" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03802 | "local" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r03803 | "local" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r03804 | "local" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r03805 | "logarithm" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r03806 | "logarithm" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r03807 | "logarithm" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r03808 | "logarithm" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r03809 | "logarithm" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r03810 | "logarithm" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r03811 | "logarithm" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r03812 | "logarithm" | "Math" | "log10" | "derives" | "Base-10 logarithm of a float"
    r03813 | "logarithm" | "Math" | "log2" | "derives" | "Base-2 logarithm of a float"
    r03814 | "logs" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r03815 | "lookup" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r03816 | "lookup" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r03817 | "lookup" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r03818 | "lookup" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r03819 | "lookup" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r03820 | "lookup" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r03821 | "lookup" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r03822 | "lookup" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r03823 | "lookup" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r03824 | "lookup" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r03825 | "lookup" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r03826 | "lookup" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r03827 | "loop" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r03828 | "loop" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r03829 | "loop" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r03830 | "loop" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r03831 | "loop" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r03832 | "loop" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r03833 | "loop" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r03834 | "loop" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r03835 | "lowercase" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r03836 | "lowercase" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r03837 | "lowercase" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r03838 | "lowercase" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r03839 | "lowercase" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r03840 | "lowercase" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r03841 | "lowercasing" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r03842 | "lowercasing" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r03843 | "lowercasing" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r03844 | "lsp" | "Commands" | "lsp" | "dispatches" | "Run the lsp command"
    r03845 | "main" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r03846 | "many" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r03847 | "mark" | "Main" | "eliminate_multiples" | "creates" | "Eliminate squares of primes: mark multiples of n*n"
    r03848 | "match" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r03849 | "match" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r03850 | "match" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r03851 | "match" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r03852 | "match" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r03853 | "match" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r03854 | "match" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r03855 | "match" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r03856 | "match" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r03857 | "match" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r03858 | "match" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r03859 | "match" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r03860 | "match" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r03861 | "match" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r03862 | "match" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r03863 | "match" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r03864 | "match" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r03865 | "match" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r03866 | "matched" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r03867 | "matched" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r03868 | "matched" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r03869 | "matched" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r03870 | "matched" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r03871 | "matched" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r03872 | "matches" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03873 | "matches" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03874 | "matches" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03875 | "matches" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r03876 | "matches" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r03877 | "matches" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r03878 | "matches" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r03879 | "matches" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r03880 | "matches" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03881 | "matches" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03882 | "matches" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03883 | "matches" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r03884 | "matches" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r03885 | "matches" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r03886 | "matches" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r03887 | "matches" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r03888 | "matches" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03889 | "matches" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03890 | "matches" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03891 | "matches" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r03892 | "matches" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r03893 | "matches" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r03894 | "matches" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r03895 | "matches" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r03896 | "matrices" | "Main" | "multiply" | "creates" | "Multiply two matrices: a * b with transpose optimization"
    r03897 | "matrix" | "Main" | "columns" | "creates" | "Generate matrix: fill row by iterating columns"
    r03898 | "matrix" | "Main" | "rows" | "creates" | "Generate matrix: iterate rows"
    r03899 | "matrix" | "Main" | "generate" | "creates" | "Generate an n x n matrix with seed scaling"
    r03900 | "matrix" | "Main" | "transpose" | "creates" | "Transpose a matrix"
    r03901 | "max" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03902 | "max" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03903 | "max" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03904 | "max" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03905 | "max" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03906 | "max" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r03907 | "maximum" | "Math" | "max" | "derives" | "Maximum of two integers"
    r03908 | "maximum" | "Math" | "max" | "derives" | "Maximum of two floats"
    r03909 | "maximum" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r03910 | "maximum" | "Math" | "max" | "derives" | "Maximum of two integers"
    r03911 | "maximum" | "Math" | "max" | "derives" | "Maximum of two floats"
    r03912 | "maximum" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r03913 | "maximum" | "Math" | "max" | "derives" | "Maximum of two integers"
    r03914 | "maximum" | "Math" | "max" | "derives" | "Maximum of two floats"
    r03915 | "maximum" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r03916 | "menu" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r03917 | "menu" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r03918 | "menu" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r03919 | "merge" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r03920 | "merge" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r03921 | "merge" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r03922 | "merge" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r03923 | "merge" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r03924 | "merge" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r03925 | "merge" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r03926 | "merge" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r03927 | "merge" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r03928 | "merge" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r03929 | "merge" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r03930 | "merge" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r03931 | "merge" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r03932 | "merge" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r03933 | "merge" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r03934 | "merged" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r03935 | "merged" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r03936 | "merged" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r03937 | "message" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r03938 | "message" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r03939 | "message" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r03940 | "message" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r03941 | "message" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r03942 | "message" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r03943 | "message" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r03944 | "message" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r03945 | "message" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r03946 | "message" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r03947 | "message" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r03948 | "message" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r03949 | "message" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r03950 | "message" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r03951 | "message" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r03952 | "message" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r03953 | "message" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r03954 | "messages" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03955 | "messages" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03956 | "messages" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r03957 | "metaphone" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r03958 | "metaphone" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r03959 | "metaphone" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r03960 | "min" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03961 | "min" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03962 | "min" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r03963 | "minimum" | "Math" | "min" | "derives" | "Minimum of two integers"
    r03964 | "minimum" | "Math" | "min" | "derives" | "Minimum of two floats"
    r03965 | "minimum" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r03966 | "minimum" | "Math" | "min" | "derives" | "Minimum of two integers"
    r03967 | "minimum" | "Math" | "min" | "derives" | "Minimum of two floats"
    r03968 | "minimum" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r03969 | "minimum" | "Math" | "min" | "derives" | "Minimum of two integers"
    r03970 | "minimum" | "Math" | "min" | "derives" | "Minimum of two floats"
    r03971 | "minimum" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r03972 | "minus" | "Main" | "parse_char_minus" | "creates" | "Continue parsing: check for minus"
    r03973 | "mode" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r03974 | "mode" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03975 | "mode" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03976 | "mode" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r03977 | "mode" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03978 | "mode" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03979 | "mode" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r03980 | "mode" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r03981 | "mode" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r03982 | "month" | "Time" | "days" | "derives" | "Get number of days in a month"
    r03983 | "month" | "Time" | "days" | "derives" | "Get number of days in a month"
    r03984 | "month" | "Time" | "days" | "derives" | "Get number of days in a month"
    r03985 | "most" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r03986 | "most" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r03987 | "most" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r03988 | "move" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r03989 | "move" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r03990 | "move" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r03991 | "move" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r03992 | "move" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r03993 | "move" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r03994 | "multiples" | "Main" | "eliminate_multiples" | "creates" | "Eliminate squares of primes: mark multiples of n*n"
    r03995 | "multiply" | "Main" | "mul_columns" | "creates" | "Multiply: fill one row of result"
    r03996 | "multiply" | "Main" | "mul_rows" | "creates" | "Multiply: iterate rows"
    r03997 | "multiply" | "Main" | "multiply" | "creates" | "Multiply two matrices: a * b with transpose optimization"
    r03998 | "must" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r03999 | "must" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04000 | "must" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04001 | "must" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04002 | "must" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04003 | "must" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04004 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04005 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04006 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04007 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04008 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04009 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04010 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04011 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04012 | "mutable" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04013 | "name" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r04014 | "name" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r04015 | "name" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04016 | "name" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r04017 | "name" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r04018 | "name" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r04019 | "name" | "System" | "process" | "validates" | "Validate a process name"
    r04020 | "name" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r04021 | "name" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r04022 | "name" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r04023 | "name" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04024 | "name" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r04025 | "name" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r04026 | "name" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r04027 | "name" | "System" | "process" | "validates" | "Validate a process name"
    r04028 | "name" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r04029 | "name" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r04030 | "name" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r04031 | "name" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04032 | "name" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r04033 | "name" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r04034 | "name" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r04035 | "name" | "System" | "process" | "validates" | "Validate a process name"
    r04036 | "name" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r04037 | "named" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04038 | "named" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04039 | "named" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04040 | "named" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04041 | "named" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04042 | "named" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04043 | "named" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04044 | "named" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04045 | "named" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04046 | "names" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r04047 | "names" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r04048 | "names" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r04049 | "natural" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r04050 | "natural" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r04051 | "natural" | "Math" | "log" | "derives" | "Natural logarithm of a float"
    r04052 | "navigate" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r04053 | "nearest" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r04054 | "nearest" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r04055 | "nearest" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r04056 | "new" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04057 | "new" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r04058 | "new" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r04059 | "new" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r04060 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04061 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04062 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04063 | "new" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r04064 | "new" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r04065 | "new" | "Table" | "new" | "creates" | "Create a new empty table"
    r04066 | "new" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r04067 | "new" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r04068 | "new" | "Text" | "builder" | "creates" | "Create a new string builder"
    r04069 | "new" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04070 | "new" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r04071 | "new" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r04072 | "new" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r04073 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04074 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04075 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04076 | "new" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r04077 | "new" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r04078 | "new" | "Table" | "new" | "creates" | "Create a new empty table"
    r04079 | "new" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r04080 | "new" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r04081 | "new" | "Text" | "builder" | "creates" | "Create a new string builder"
    r04082 | "new" | "Commands" | "new" | "dispatches" | "Run the new command"
    r04083 | "new" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04084 | "new" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r04085 | "new" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r04086 | "new" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r04087 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04088 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04089 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r04090 | "new" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r04091 | "new" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r04092 | "new" | "Table" | "new" | "creates" | "Create a new empty table"
    r04093 | "new" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r04094 | "new" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r04095 | "new" | "Text" | "builder" | "creates" | "Create a new string builder"
    r04096 | "next" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04097 | "next" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04098 | "next" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04099 | "next" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04100 | "next" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04101 | "next" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r04102 | "next" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04103 | "next" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04104 | "next" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04105 | "next" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04106 | "next" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04107 | "next" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04108 | "next" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04109 | "next" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04110 | "next" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04111 | "next" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04112 | "node" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r04113 | "node" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r04114 | "node" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04115 | "node" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r04116 | "node" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r04117 | "node" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r04118 | "node" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r04119 | "node" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r04120 | "node" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r04121 | "node" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r04122 | "node" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r04123 | "node" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04124 | "node" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r04125 | "node" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r04126 | "node" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r04127 | "node" | "Prove" | "kind" | "creates" | "Node kind name (e.g. \"function_definition\", \"call_expression\")."
    r04128 | "node" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04129 | "node" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r04130 | "node" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r04131 | "nodes" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r04132 | "nodes" | "Prove" | "count" | "creates" | "Number of child nodes."
    r04133 | "nodes" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r04134 | "nodes" | "Prove" | "count" | "creates" | "Number of child nodes."
    r04135 | "nodes" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r04136 | "nodes" | "Prove" | "count" | "creates" | "Number of child nodes."
    r04137 | "non" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r04138 | "non" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04139 | "non" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r04140 | "non" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r04141 | "non" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04142 | "non" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r04143 | "non" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r04144 | "non" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04145 | "non" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r04146 | "none" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04147 | "none" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04148 | "none" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04149 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04150 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04151 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04152 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04153 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04154 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04155 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04156 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04157 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04158 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04159 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04160 | "none" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04161 | "none" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04162 | "none" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04163 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04164 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04165 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04166 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04167 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04168 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04169 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04170 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04171 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04172 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04173 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04174 | "none" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04175 | "none" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04176 | "none" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04177 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04178 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04179 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04180 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04181 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04182 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04183 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04184 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04185 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04186 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04187 | "none" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04188 | "normalize" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r04189 | "normalize" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04190 | "normalize" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r04191 | "normalize" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04192 | "normalize" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r04193 | "normalize" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04194 | "normalized" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r04195 | "normalized" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r04196 | "normalized" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r04197 | "not" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04198 | "not" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04199 | "not" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04200 | "not" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r04201 | "not" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04202 | "not" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r04203 | "not" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r04204 | "not" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r04205 | "not" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r04206 | "not" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04207 | "not" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04208 | "not" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04209 | "not" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r04210 | "not" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04211 | "not" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r04212 | "not" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r04213 | "not" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r04214 | "not" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r04215 | "not" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04216 | "not" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r04217 | "not" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04218 | "not" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r04219 | "not" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04220 | "not" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r04221 | "not" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r04222 | "not" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r04223 | "not" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r04224 | "note" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04225 | "note" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04226 | "note" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04227 | "number" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r04228 | "number" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04229 | "number" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r04230 | "number" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r04231 | "number" | "Prove" | "count" | "creates" | "Number of child nodes."
    r04232 | "number" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r04233 | "number" | "Time" | "days" | "derives" | "Get number of days in a month"
    r04234 | "number" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r04235 | "number" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r04236 | "number" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r04237 | "number" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04238 | "number" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r04239 | "number" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r04240 | "number" | "Prove" | "count" | "creates" | "Number of child nodes."
    r04241 | "number" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r04242 | "number" | "Time" | "days" | "derives" | "Get number of days in a month"
    r04243 | "number" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r04244 | "number" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r04245 | "number" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04246 | "number" | "Prove" | "line" | "creates" | "Line number (1-based)."
    r04247 | "number" | "Prove" | "column" | "creates" | "Column number (0-based)."
    r04248 | "number" | "Prove" | "count" | "creates" | "Number of child nodes."
    r04249 | "number" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r04250 | "number" | "Time" | "days" | "derives" | "Get number of days in a month"
    r04251 | "number" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r04252 | "numeric" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r04253 | "numeric" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r04254 | "numeric" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r04255 | "object" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r04256 | "object" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r04257 | "object" | "Main" | "show" | "outputs" | "Show table operations on a parsed JSON object"
    r04258 | "object" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r04259 | "object" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r04260 | "object" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r04261 | "object" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r04262 | "occurrences" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04263 | "occurrences" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r04264 | "occurrences" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04265 | "occurrences" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r04266 | "occurrences" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04267 | "occurrences" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r04268 | "octal" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r04269 | "octal" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r04270 | "octal" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r04271 | "ohm" | "Main" | "to_dbm" | "derives" | "Convert power to dBm (600 Ohm impedance)"
    r04272 | "once" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04273 | "once" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04274 | "once" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04275 | "one" | "Main" | "parse_char" | "creates" | "Parse one BF character and emit opcodes"
    r04276 | "one" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r04277 | "one" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r04278 | "one" | "Main" | "mul_columns" | "creates" | "Multiply: fill one row of result"
    r04279 | "one" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r04280 | "one" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r04281 | "only" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04282 | "only" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04283 | "only" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04284 | "opcodes" | "Main" | "parse_char" | "creates" | "Parse one BF character and emit opcodes"
    r04285 | "opcodes" | "Main" | "count_ops" | "creates" | "Count BF opcodes in source for array sizing"
    r04286 | "opcodes" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r04287 | "open" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04288 | "open" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r04289 | "open" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04290 | "open" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04291 | "open" | "System" | "close" | "outputs" | "Close an open file handle"
    r04292 | "open" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r04293 | "open" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r04294 | "open" | "Main" | "parse_char_bracket" | "creates" | "Continue parsing: check for open/close brackets"
    r04295 | "open" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04296 | "open" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r04297 | "open" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04298 | "open" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04299 | "open" | "System" | "close" | "outputs" | "Close an open file handle"
    r04300 | "open" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r04301 | "open" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r04302 | "open" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04303 | "open" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r04304 | "open" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04305 | "open" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04306 | "open" | "System" | "close" | "outputs" | "Close an open file handle"
    r04307 | "open" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r04308 | "open" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r04309 | "operations" | "Main" | "show" | "outputs" | "Show table operations on a parsed JSON object"
    r04310 | "optimistic" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r04311 | "optimistic" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r04312 | "optimistic" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r04313 | "optimization" | "Main" | "multiply" | "creates" | "Multiply two matrices: a * b with transpose optimization"
    r04314 | "option" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r04315 | "option" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r04316 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r04317 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r04318 | "option" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r04319 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r04320 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r04321 | "option" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r04322 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r04323 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r04324 | "option" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r04325 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r04326 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r04327 | "option" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r04328 | "option" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r04329 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r04330 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r04331 | "option" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r04332 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r04333 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r04334 | "option" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r04335 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r04336 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r04337 | "option" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r04338 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r04339 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r04340 | "option" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r04341 | "option" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r04342 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r04343 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r04344 | "option" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r04345 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r04346 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r04347 | "option" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r04348 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r04349 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r04350 | "option" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r04351 | "option" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r04352 | "option" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r04353 | "otherwise" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r04354 | "otherwise" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r04355 | "otherwise" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r04356 | "out" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04357 | "out" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04358 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04359 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04360 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04361 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04362 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04363 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04364 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04365 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04366 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04367 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04368 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04369 | "out" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04370 | "out" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04371 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04372 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04373 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04374 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04375 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04376 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04377 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04378 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04379 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04380 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04381 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04382 | "out" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04383 | "out" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04384 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04385 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04386 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04387 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04388 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04389 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04390 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04391 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04392 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04393 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r04394 | "out" | "Sequence" | "get_safe" | "derives" | "Get element at index, or None if out of bounds"
    r04395 | "output" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04396 | "output" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04397 | "output" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04398 | "output" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04399 | "output" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04400 | "output" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04401 | "over" | "Main" | "eliminate_squares" | "creates" | "Eliminate squares of primes: iterate over sieve"
    r04402 | "pads" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r04403 | "pads" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r04404 | "pads" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r04405 | "pads" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r04406 | "pads" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r04407 | "pads" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r04408 | "pair" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r04409 | "pair" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r04410 | "pair" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r04411 | "paired" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04412 | "paired" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04413 | "paired" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04414 | "parameters" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r04415 | "parameters" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r04416 | "parameters" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r04417 | "parent" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r04418 | "parent" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04419 | "parent" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r04420 | "parent" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04421 | "parent" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r04422 | "parent" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r04423 | "parse" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r04424 | "parse" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r04425 | "parse" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r04426 | "parse" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r04427 | "parse" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r04428 | "parse" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r04429 | "parse" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r04430 | "parse" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r04431 | "parse" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r04432 | "parse" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r04433 | "parse" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r04434 | "parse" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r04435 | "parse" | "Types" | "float" | "creates" | "Parse a string as a float"
    r04436 | "parse" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r04437 | "parse" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r04438 | "parse" | "Main" | "parse_char" | "creates" | "Parse one BF character and emit opcodes"
    r04439 | "parse" | "Main" | "try_json" | "transforms" | "Parse JSON and return as a Value string (failable)"
    r04440 | "parse" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r04441 | "parse" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r04442 | "parse" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r04443 | "parse" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r04444 | "parse" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r04445 | "parse" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r04446 | "parse" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r04447 | "parse" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r04448 | "parse" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r04449 | "parse" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r04450 | "parse" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r04451 | "parse" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r04452 | "parse" | "Types" | "float" | "creates" | "Parse a string as a float"
    r04453 | "parse" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r04454 | "parse" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r04455 | "parse" | "Config" | "struct" | "transforms" | "Parse the toml data to Config struct"
    r04456 | "parse" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r04457 | "parse" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r04458 | "parse" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r04459 | "parse" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r04460 | "parse" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r04461 | "parse" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r04462 | "parse" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r04463 | "parse" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r04464 | "parse" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r04465 | "parse" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r04466 | "parse" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r04467 | "parse" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r04468 | "parse" | "Types" | "float" | "creates" | "Parse a string as a float"
    r04469 | "parse" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r04470 | "parse" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r04471 | "parsed" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r04472 | "parsed" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r04473 | "parsed" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r04474 | "parsed" | "Main" | "show" | "outputs" | "Show table operations on a parsed JSON object"
    r04475 | "parsed" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r04476 | "parsed" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r04477 | "parsed" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r04478 | "parsed" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r04479 | "parsed" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r04480 | "parsed" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r04481 | "parses" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r04482 | "parsing" | "Main" | "parse_char_minus" | "creates" | "Continue parsing: check for minus"
    r04483 | "parsing" | "Main" | "parse_char_right" | "creates" | "Continue parsing: check for right"
    r04484 | "parsing" | "Main" | "parse_char_left" | "creates" | "Continue parsing: check for left"
    r04485 | "parsing" | "Main" | "parse_char_dot" | "creates" | "Continue parsing: check for dot (print)"
    r04486 | "parsing" | "Main" | "parse_char_bracket" | "creates" | "Continue parsing: check for open/close brackets"
    r04487 | "pass" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04488 | "pass" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04489 | "pass" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04490 | "pass" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04491 | "pass" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04492 | "pass" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04493 | "pass" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04494 | "pass" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04495 | "pass" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04496 | "past" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r04497 | "past" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r04498 | "past" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r04499 | "path" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r04500 | "path" | "Path" | "join" | "derives" | "Join path components together"
    r04501 | "path" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r04502 | "path" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r04503 | "path" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r04504 | "path" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04505 | "path" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r04506 | "path" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r04507 | "path" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r04508 | "path" | "Path" | "join" | "derives" | "Join path components together"
    r04509 | "path" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r04510 | "path" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r04511 | "path" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r04512 | "path" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04513 | "path" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r04514 | "path" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r04515 | "path" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r04516 | "path" | "Path" | "join" | "derives" | "Join path components together"
    r04517 | "path" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r04518 | "path" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r04519 | "path" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r04520 | "path" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04521 | "path" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r04522 | "path" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r04523 | "pattern" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r04524 | "pattern" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r04525 | "pattern" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r04526 | "pattern" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r04527 | "pattern" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r04528 | "pattern" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r04529 | "pattern" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r04530 | "pattern" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r04531 | "pattern" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r04532 | "pattern" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r04533 | "pattern" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r04534 | "pattern" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r04535 | "pattern" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04536 | "pattern" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r04537 | "pattern" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r04538 | "pattern" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r04539 | "pattern" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r04540 | "pattern" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r04541 | "pattern" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r04542 | "pattern" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r04543 | "pattern" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r04544 | "pattern" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r04545 | "pattern" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r04546 | "pattern" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r04547 | "pattern" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r04548 | "pattern" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r04549 | "pattern" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04550 | "pattern" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r04551 | "pattern" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r04552 | "pattern" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r04553 | "pattern" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r04554 | "pattern" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r04555 | "pattern" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r04556 | "pattern" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r04557 | "pattern" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r04558 | "pattern" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r04559 | "pattern" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r04560 | "pattern" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r04561 | "pattern" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r04562 | "pattern" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r04563 | "pattern" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04564 | "pattern" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r04565 | "per" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04566 | "per" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04567 | "per" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04568 | "phonetic" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r04569 | "phonetic" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r04570 | "phonetic" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r04571 | "phonetic" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r04572 | "phonetic" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r04573 | "phonetic" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r04574 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r04575 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r04576 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r04577 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r04578 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r04579 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r04580 | "piped" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04581 | "piped" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04582 | "piped" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04583 | "pixels" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04584 | "pixels" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04585 | "pixels" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04586 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04587 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04588 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04589 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04590 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04591 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04592 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04593 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04594 | "place" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r04595 | "places" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r04596 | "places" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r04597 | "places" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r04598 | "places" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r04599 | "places" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r04600 | "places" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r04601 | "plain" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04602 | "plain" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04603 | "plain" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04604 | "point" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r04605 | "point" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r04606 | "point" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r04607 | "point" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r04608 | "point" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r04609 | "point" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r04610 | "point" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r04611 | "point" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r04612 | "point" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r04613 | "port" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04614 | "port" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r04615 | "port" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04616 | "port" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04617 | "port" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r04618 | "port" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04619 | "port" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04620 | "port" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r04621 | "port" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04622 | "porter" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r04623 | "porter" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r04624 | "porter" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r04625 | "position" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04626 | "position" | "Language" | "start" | "creates" | "Get the start position of a token"
    r04627 | "position" | "Language" | "end" | "creates" | "Get the end position of a token"
    r04628 | "position" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r04629 | "position" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r04630 | "position" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04631 | "position" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r04632 | "position" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r04633 | "position" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04634 | "position" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r04635 | "position" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r04636 | "position" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04637 | "position" | "Language" | "start" | "creates" | "Get the start position of a token"
    r04638 | "position" | "Language" | "end" | "creates" | "Get the end position of a token"
    r04639 | "position" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r04640 | "position" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r04641 | "position" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04642 | "position" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r04643 | "position" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r04644 | "position" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04645 | "position" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r04646 | "position" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r04647 | "position" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04648 | "position" | "Language" | "start" | "creates" | "Get the start position of a token"
    r04649 | "position" | "Language" | "end" | "creates" | "Get the end position of a token"
    r04650 | "position" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r04651 | "position" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r04652 | "position" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r04653 | "position" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r04654 | "position" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r04655 | "position" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04656 | "position" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r04657 | "position" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r04658 | "positive" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r04659 | "positive" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r04660 | "positive" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r04661 | "power" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r04662 | "power" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04663 | "power" | "Main" | "signal_power" | "derives" | "Compute signal power from filter state"
    r04664 | "power" | "Main" | "to_dbm" | "derives" | "Convert power to dBm (600 Ohm impedance)"
    r04665 | "power" | "Main" | "detect" | "creates" | "Run Goertzel: detect target_freq power in a signal of signal_freq"
    r04666 | "power" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r04667 | "power" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04668 | "power" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r04669 | "power" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04670 | "prefix" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r04671 | "prefix" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r04672 | "prefix" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r04673 | "prefix" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r04674 | "preserve" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04675 | "preserve" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04676 | "preserve" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04677 | "preserve" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04678 | "preserve" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04679 | "preserve" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04680 | "preserve" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04681 | "preserve" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04682 | "preserve" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04683 | "preserving" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r04684 | "preserving" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r04685 | "preserving" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r04686 | "previous" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r04687 | "previous" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r04688 | "previous" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r04689 | "prime" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r04690 | "primes" | "Main" | "eliminate_multiples" | "creates" | "Eliminate squares of primes: mark multiples of n*n"
    r04691 | "primes" | "Main" | "eliminate_squares" | "creates" | "Eliminate squares of primes: iterate over sieve"
    r04692 | "primes" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r04693 | "primes" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r04694 | "primes" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r04695 | "print" | "System" | "console" | "outputs" | "Print text to the console"
    r04696 | "print" | "Main" | "parse_char_dot" | "creates" | "Continue parsing: check for dot (print)"
    r04697 | "print" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r04698 | "print" | "System" | "console" | "outputs" | "Print text to the console"
    r04699 | "print" | "System" | "console" | "outputs" | "Print text to the console"
    r04700 | "printing" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r04701 | "process" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r04702 | "process" | "System" | "process" | "validates" | "Validate a process name"
    r04703 | "process" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r04704 | "process" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r04705 | "process" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r04706 | "process" | "System" | "process" | "validates" | "Validate a process name"
    r04707 | "process" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r04708 | "process" | "System" | "process" | "validates" | "Validate a process name"
    r04709 | "processes" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r04710 | "processing" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04711 | "processing" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04712 | "processing" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04713 | "producing" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r04714 | "producing" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r04715 | "producing" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r04716 | "product" | "Main" | "dot" | "creates" | "Dot product of row i of a with row j of bt (transposed b)"
    r04717 | "programmatically" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04718 | "programmatically" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04719 | "programmatically" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04720 | "progress" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04721 | "progress" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04722 | "progress" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04723 | "prompt" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r04724 | "prove" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r04725 | "prove" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r04726 | "prove" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r04727 | "punctuation" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04728 | "punctuation" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04729 | "punctuation" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r04730 | "query" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r04731 | "query" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r04732 | "query" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r04733 | "quit" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04734 | "quit" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04735 | "quit" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04736 | "radians" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r04737 | "radians" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r04738 | "radians" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r04739 | "radians" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r04740 | "radians" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r04741 | "radians" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r04742 | "radians" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r04743 | "radians" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r04744 | "radians" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r04745 | "radians" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r04746 | "radians" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r04747 | "radians" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r04748 | "radians" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r04749 | "radians" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r04750 | "radians" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r04751 | "radians" | "Math" | "cos" | "derives" | "Cosine of an angle in radians"
    r04752 | "radians" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r04753 | "radians" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r04754 | "radians" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r04755 | "radians" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r04756 | "radians" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r04757 | "raised" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r04758 | "raised" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04759 | "raised" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r04760 | "raised" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04761 | "raised" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r04762 | "raised" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r04763 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r04764 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r04765 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r04766 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r04767 | "random" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r04768 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r04769 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r04770 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r04771 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r04772 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r04773 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r04774 | "random" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r04775 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r04776 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r04777 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r04778 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r04779 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r04780 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r04781 | "random" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r04782 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r04783 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r04784 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r04785 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r04786 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r04787 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r04788 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r04789 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r04790 | "range" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04791 | "range" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04792 | "range" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r04793 | "range" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r04794 | "range" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r04795 | "range" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04796 | "range" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04797 | "range" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r04798 | "range" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r04799 | "range" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r04800 | "range" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04801 | "range" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04802 | "range" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r04803 | "range" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r04804 | "range" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r04805 | "rate" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r04806 | "raw" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04807 | "raw" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04808 | "raw" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04809 | "raw" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04810 | "raw" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04811 | "raw" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r04812 | "reach" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r04813 | "reach" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r04814 | "reach" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r04815 | "reach" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r04816 | "reach" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r04817 | "reach" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r04818 | "read" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r04819 | "read" | "Network" | "message" | "inputs" | "Read data from a socket"
    r04820 | "read" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04821 | "read" | "System" | "console" | "inputs" | "Read a line from the console"
    r04822 | "read" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r04823 | "read" | "System" | "file" | "inputs" | "Read contents from a file"
    r04824 | "read" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04825 | "read" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r04826 | "read" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r04827 | "read" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r04828 | "read" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r04829 | "read" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r04830 | "read" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r04831 | "read" | "Network" | "message" | "inputs" | "Read data from a socket"
    r04832 | "read" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04833 | "read" | "System" | "console" | "inputs" | "Read a line from the console"
    r04834 | "read" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r04835 | "read" | "System" | "file" | "inputs" | "Read contents from a file"
    r04836 | "read" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04837 | "read" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r04838 | "read" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r04839 | "read" | "Network" | "message" | "inputs" | "Read data from a socket"
    r04840 | "read" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r04841 | "read" | "System" | "console" | "inputs" | "Read a line from the console"
    r04842 | "read" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r04843 | "read" | "System" | "file" | "inputs" | "Read contents from a file"
    r04844 | "read" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r04845 | "read" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r04846 | "readable" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r04847 | "readable" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r04848 | "readable" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r04849 | "reading" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04850 | "reading" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04851 | "reading" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r04852 | "reads" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r04853 | "recursive" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r04854 | "redirected" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04855 | "redirected" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04856 | "redirected" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r04857 | "regex" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r04858 | "regex" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r04859 | "regex" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r04860 | "regex" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r04861 | "regex" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r04862 | "regex" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r04863 | "regex" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r04864 | "regex" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r04865 | "regex" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r04866 | "remaining" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04867 | "remaining" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04868 | "remaining" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04869 | "remote" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04870 | "remote" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r04871 | "remote" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04872 | "remote" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r04873 | "remote" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r04874 | "remote" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r04875 | "remove" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04876 | "remove" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r04877 | "remove" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r04878 | "remove" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04879 | "remove" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r04880 | "remove" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r04881 | "remove" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04882 | "remove" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r04883 | "remove" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r04884 | "render" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04885 | "render" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r04886 | "render" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r04887 | "render" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04888 | "render" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04889 | "render" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04890 | "render" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04891 | "render" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04892 | "render" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04893 | "render" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r04894 | "render" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r04895 | "render" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04896 | "render" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04897 | "render" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04898 | "render" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04899 | "render" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04900 | "render" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r04901 | "render" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r04902 | "render" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r04903 | "render" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r04904 | "render" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r04905 | "render" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r04906 | "render" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r04907 | "render" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r04908 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r04909 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r04910 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r04911 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r04912 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r04913 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r04914 | "repeat" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r04915 | "repeat" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r04916 | "repeat" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r04917 | "replace" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04918 | "replace" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r04919 | "replace" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04920 | "replace" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r04921 | "replace" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04922 | "replace" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r04923 | "replacement" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04924 | "replacement" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04925 | "replacement" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r04926 | "repositions" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04927 | "repositions" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04928 | "repositions" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r04929 | "represents" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r04930 | "represents" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r04931 | "represents" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r04932 | "reset" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r04933 | "reset" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r04934 | "reset" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r04935 | "resize" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r04936 | "resize" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r04937 | "resize" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r04938 | "resolve" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04939 | "resolve" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04940 | "resolve" | "Path" | "normalize" | "derives" | "Normalize a path (resolve .. and .)"
    r04941 | "resolver" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r04942 | "resolver" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r04943 | "resolver" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r04944 | "restore" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r04945 | "restore" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r04946 | "restore" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r04947 | "result" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r04948 | "result" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r04949 | "result" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r04950 | "result" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r04951 | "result" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r04952 | "result" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r04953 | "result" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r04954 | "result" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r04955 | "result" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r04956 | "result" | "Main" | "mul_columns" | "creates" | "Multiply: fill one row of result"
    r04957 | "result" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r04958 | "result" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r04959 | "result" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r04960 | "result" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r04961 | "result" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r04962 | "result" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r04963 | "result" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r04964 | "result" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r04965 | "result" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r04966 | "result" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r04967 | "result" | "Math" | "acos" | "derives" | "Inverse cosine (arc cosine), result in radians"
    r04968 | "result" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r04969 | "result" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r04970 | "result" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r04971 | "result" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r04972 | "result" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r04973 | "result" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r04974 | "result" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r04975 | "return" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04976 | "return" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r04977 | "return" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r04978 | "return" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04979 | "return" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r04980 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r04981 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r04982 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r04983 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r04984 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r04985 | "return" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r04986 | "return" | "Main" | "try_json" | "transforms" | "Parse JSON and return as a Value string (failable)"
    r04987 | "return" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04988 | "return" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r04989 | "return" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r04990 | "return" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r04991 | "return" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r04992 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r04993 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r04994 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r04995 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r04996 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r04997 | "return" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r04998 | "return" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r04999 | "return" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r05000 | "return" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r05001 | "return" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r05002 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r05003 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r05004 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r05005 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r05006 | "return" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r05007 | "returned" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05008 | "returned" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05009 | "returned" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05010 | "returned" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05011 | "returned" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05012 | "returned" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05013 | "returned" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05014 | "returned" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05015 | "returned" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05016 | "returning" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05017 | "returning" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05018 | "returning" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05019 | "returns" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r05020 | "returns" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05021 | "returns" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05022 | "returns" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05023 | "returns" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r05024 | "returns" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r05025 | "returns" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r05026 | "returns" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r05027 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05028 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05029 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05030 | "returns" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r05031 | "returns" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r05032 | "returns" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r05033 | "returns" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r05034 | "returns" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r05035 | "returns" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r05036 | "returns" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05037 | "returns" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r05038 | "returns" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r05039 | "returns" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05040 | "returns" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05041 | "returns" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05042 | "returns" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r05043 | "returns" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r05044 | "returns" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r05045 | "returns" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r05046 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05047 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05048 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05049 | "returns" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r05050 | "returns" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r05051 | "returns" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r05052 | "returns" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r05053 | "returns" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r05054 | "returns" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r05055 | "returns" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05056 | "returns" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r05057 | "returns" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r05058 | "returns" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05059 | "returns" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05060 | "returns" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05061 | "returns" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r05062 | "returns" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r05063 | "returns" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r05064 | "returns" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r05065 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05066 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05067 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05068 | "returns" | "System" | "reader" | "inputs" | "Open a file for reading (returns a file handle, or fails if not found)"
    r05069 | "returns" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r05070 | "returns" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r05071 | "returns" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r05072 | "returns" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r05073 | "returns" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r05074 | "returns" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05075 | "returns" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r05076 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse a sequence"
    r05077 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05078 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05079 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05080 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse a sequence"
    r05081 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05082 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05083 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05084 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse a sequence"
    r05085 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05086 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05087 | "reverse" | "Sequence" | "reverse" | "derives" | "Reverse an array"
    r05088 | "right" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05089 | "right" | "Main" | "parse_char_right" | "creates" | "Continue parsing: check for right"
    r05090 | "right" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05091 | "right" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05092 | "rollback" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r05093 | "rollback" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r05094 | "rollback" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r05095 | "root" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05096 | "root" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r05097 | "root" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r05098 | "root" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r05099 | "root" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05100 | "root" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r05101 | "root" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r05102 | "root" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r05103 | "root" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05104 | "root" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r05105 | "root" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r05106 | "root" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r05107 | "round" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r05108 | "round" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r05109 | "round" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r05110 | "row" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05111 | "row" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05112 | "row" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r05113 | "row" | "Main" | "columns" | "creates" | "Generate matrix: fill row by iterating columns"
    r05114 | "row" | "Main" | "dot" | "creates" | "Dot product of row i of a with row j of bt (transposed b)"
    r05115 | "row" | "Main" | "mul_columns" | "creates" | "Multiply: fill one row of result"
    r05116 | "row" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05117 | "row" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05118 | "row" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r05119 | "row" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05120 | "row" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05121 | "row" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r05122 | "rows" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r05123 | "rows" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r05124 | "rows" | "Main" | "rows" | "creates" | "Generate matrix: iterate rows"
    r05125 | "rows" | "Main" | "transpose_columns" | "creates" | "Transpose: copy columns of src into rows of dst"
    r05126 | "rows" | "Main" | "transpose_rows" | "creates" | "Transpose: iterate rows"
    r05127 | "rows" | "Main" | "mul_rows" | "creates" | "Multiply: iterate rows"
    r05128 | "rows" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r05129 | "rows" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r05130 | "rows" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r05131 | "rows" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r05132 | "rule" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r05133 | "rule" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r05134 | "rule" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r05135 | "rules" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05136 | "rules" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05137 | "rules" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05138 | "run" | "Main" | "detect" | "creates" | "Run Goertzel: detect target_freq power in a signal of signal_freq"
    r05139 | "run" | "Commands" | "build" | "dispatches" | "Run the build command"
    r05140 | "run" | "Commands" | "check" | "dispatches" | "Run the check command"
    r05141 | "run" | "Commands" | "format" | "dispatches" | "Run the format command"
    r05142 | "run" | "Commands" | "lsp" | "dispatches" | "Run the lsp command"
    r05143 | "run" | "Commands" | "new" | "dispatches" | "Run the new command"
    r05144 | "run" | "Commands" | "test" | "dispatches" | "Run the test command"
    r05145 | "safety" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05146 | "safety" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05147 | "safety" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05148 | "sample" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r05149 | "sample" | "Main" | "sine_sample" | "derives" | "Generate a sine wave sample at a given index"
    r05150 | "samples" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r05151 | "save" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05152 | "save" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05153 | "save" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05154 | "scaling" | "Main" | "generate" | "creates" | "Generate an n x n matrix with seed scaling"
    r05155 | "scheme" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r05156 | "scheme" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r05157 | "scheme" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r05158 | "screen" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05159 | "screen" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r05160 | "screen" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r05161 | "screen" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05162 | "screen" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r05163 | "screen" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r05164 | "screen" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05165 | "screen" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r05166 | "screen" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r05167 | "search" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r05168 | "search" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r05169 | "search" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r05170 | "seconds" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r05171 | "seconds" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r05172 | "seconds" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r05173 | "see" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05174 | "see" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05175 | "see" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05176 | "seed" | "Main" | "generate" | "creates" | "Generate an n x n matrix with seed scaling"
    r05177 | "send" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05178 | "send" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05179 | "send" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05180 | "send" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05181 | "send" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05182 | "send" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05183 | "send" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05184 | "send" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05185 | "send" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05186 | "send" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05187 | "send" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05188 | "send" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05189 | "sentences" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r05190 | "sentences" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r05191 | "sentences" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r05192 | "separated" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r05193 | "separated" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r05194 | "separator" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05195 | "separator" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r05196 | "separator" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r05197 | "separator" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05198 | "separator" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r05199 | "separator" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05200 | "separator" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r05201 | "sequence" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r05202 | "sequence" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r05203 | "sequence" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r05204 | "sequence" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r05205 | "sequence" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r05206 | "sequence" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r05207 | "sequence" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r05208 | "sequence" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05209 | "sequence" | "Sequence" | "reverse" | "derives" | "Reverse a sequence"
    r05210 | "sequence" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r05211 | "sequence" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05212 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r05213 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05214 | "sequence" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05215 | "sequence" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r05216 | "sequence" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r05217 | "sequence" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r05218 | "sequence" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r05219 | "sequence" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r05220 | "sequence" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r05221 | "sequence" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r05222 | "sequence" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05223 | "sequence" | "Sequence" | "reverse" | "derives" | "Reverse a sequence"
    r05224 | "sequence" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r05225 | "sequence" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05226 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r05227 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05228 | "sequence" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05229 | "sequence" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r05230 | "sequence" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r05231 | "sequence" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r05232 | "sequence" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r05233 | "sequence" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r05234 | "sequence" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r05235 | "sequence" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r05236 | "sequence" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05237 | "sequence" | "Sequence" | "reverse" | "derives" | "Reverse a sequence"
    r05238 | "sequence" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r05239 | "sequence" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05240 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r05241 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05242 | "sequence" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05243 | "serialization" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r05244 | "serialization" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r05245 | "serialization" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r05246 | "serialization" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r05247 | "serialization" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r05248 | "serialization" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r05249 | "serialize" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r05250 | "serialize" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r05251 | "serialize" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r05252 | "serialize" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r05253 | "serialize" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r05254 | "serialize" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r05255 | "serialize" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r05256 | "serialize" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r05257 | "serialize" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r05258 | "set" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r05259 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r05260 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05261 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05262 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05263 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05264 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05265 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05266 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05267 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05268 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05269 | "set" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r05270 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r05271 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05272 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05273 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05274 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05275 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05276 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05277 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05278 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05279 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05280 | "set" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r05281 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r05282 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05283 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05284 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05285 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05286 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05287 | "set" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r05288 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05289 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05290 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r05291 | "severity" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r05292 | "severity" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05293 | "severity" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05294 | "severity" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05295 | "severity" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05296 | "severity" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r05297 | "severity" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05298 | "severity" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05299 | "severity" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05300 | "severity" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05301 | "severity" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r05302 | "severity" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05303 | "severity" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05304 | "severity" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05305 | "severity" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05306 | "sgr" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05307 | "sgr" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05308 | "sgr" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05309 | "sha" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r05310 | "sha" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r05311 | "sha" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r05312 | "sha" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r05313 | "sha" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r05314 | "sha" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r05315 | "sha" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r05316 | "sha" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r05317 | "sha" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r05318 | "sha" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r05319 | "sha" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r05320 | "sha" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r05321 | "sha" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r05322 | "sha" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r05323 | "sha" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r05324 | "sha" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r05325 | "sha" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r05326 | "sha" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r05327 | "sha" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r05328 | "sha" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r05329 | "sha" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r05330 | "sha" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r05331 | "sha" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r05332 | "sha" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r05333 | "show" | "Main" | "show" | "outputs" | "Show table operations on a parsed JSON object"
    r05334 | "showing" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r05335 | "showing" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r05336 | "showing" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r05337 | "shutdown" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r05338 | "shutdown" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r05339 | "shutdown" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r05340 | "sieve" | "Main" | "atkin_step1_y" | "creates" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r05341 | "sieve" | "Main" | "atkin_step1_x" | "creates" | "Sieve of Atkin step 1: iterate x"
    r05342 | "sieve" | "Main" | "atkin_step2_y" | "creates" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r05343 | "sieve" | "Main" | "atkin_step2_x" | "creates" | "Sieve of Atkin step 2: iterate x"
    r05344 | "sieve" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r05345 | "sieve" | "Main" | "atkin_step3_x" | "creates" | "Sieve of Atkin step 3: iterate x"
    r05346 | "sieve" | "Main" | "eliminate_squares" | "creates" | "Eliminate squares of primes: iterate over sieve"
    r05347 | "sieve" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r05348 | "signal" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05349 | "signal" | "Main" | "signal_power" | "derives" | "Compute signal power from filter state"
    r05350 | "signal" | "Main" | "detect" | "creates" | "Run Goertzel: detect target_freq power in a signal of signal_freq"
    r05351 | "signal" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05352 | "signal" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r05353 | "signals" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r05354 | "signals" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r05355 | "signals" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r05356 | "signature" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r05357 | "signature" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r05358 | "signature" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r05359 | "signature" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r05360 | "signature" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r05361 | "signature" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r05362 | "similarity" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r05363 | "similarity" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r05364 | "similarity" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r05365 | "sine" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r05366 | "sine" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r05367 | "sine" | "Main" | "sine_sample" | "derives" | "Generate a sine wave sample at a given index"
    r05368 | "sine" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r05369 | "sine" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r05370 | "sine" | "Math" | "sin" | "derives" | "Sine of an angle in radians"
    r05371 | "sine" | "Math" | "asin" | "derives" | "Inverse sine (arc sine), result in radians"
    r05372 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05373 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05374 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05375 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05376 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05377 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05378 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05379 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05380 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r05381 | "sizes" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r05382 | "sizing" | "Main" | "count_ops" | "creates" | "Count BF opcodes in source for array sizing"
    r05383 | "skipping" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r05384 | "skipping" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r05385 | "skipping" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r05386 | "skips" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r05387 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05388 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05389 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05390 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05391 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05392 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05393 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05394 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05395 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05396 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05397 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05398 | "slice" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05399 | "slider" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05400 | "slider" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05401 | "slider" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r05402 | "smallest" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r05403 | "smallest" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r05404 | "smallest" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r05405 | "socket" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r05406 | "socket" | "Network" | "message" | "inputs" | "Read data from a socket"
    r05407 | "socket" | "Network" | "message" | "outputs" | "Write data to a socket"
    r05408 | "socket" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r05409 | "socket" | "Network" | "message" | "inputs" | "Read data from a socket"
    r05410 | "socket" | "Network" | "message" | "outputs" | "Write data to a socket"
    r05411 | "socket" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r05412 | "socket" | "Network" | "message" | "inputs" | "Read data from a socket"
    r05413 | "socket" | "Network" | "message" | "outputs" | "Write data to a socket"
    r05414 | "sort" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r05415 | "sort" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05416 | "sort" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r05417 | "sort" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r05418 | "sort" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r05419 | "sort" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r05420 | "sort" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r05421 | "sort" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05422 | "sort" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r05423 | "sort" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r05424 | "sort" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r05425 | "sort" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r05426 | "sort" | "Sequence" | "sort" | "derives" | "Sort a sequence of integers"
    r05427 | "sort" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05428 | "sort" | "Sequence" | "sort" | "derives" | "Sort a list of floats"
    r05429 | "sort" | "Sequence" | "sort" | "derives" | "Sort a list of decimals"
    r05430 | "sort" | "Sequence" | "sort" | "derives" | "Sort an array of integers"
    r05431 | "sort" | "Sequence" | "sort" | "derives" | "Sort an array of decimals"
    r05432 | "soundex" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r05433 | "soundex" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r05434 | "soundex" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r05435 | "source" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r05436 | "source" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05437 | "source" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r05438 | "source" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r05439 | "source" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05440 | "source" | "Types" | "value" | "validates" | "Validate a source string"
    r05441 | "source" | "Main" | "count_ops" | "creates" | "Count BF opcodes in source for array sizing"
    r05442 | "source" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r05443 | "source" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05444 | "source" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r05445 | "source" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r05446 | "source" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05447 | "source" | "Types" | "value" | "validates" | "Validate a source string"
    r05448 | "source" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r05449 | "source" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r05450 | "source" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r05451 | "source" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r05452 | "source" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05453 | "source" | "Types" | "value" | "validates" | "Validate a source string"
    r05454 | "spanned" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05455 | "spanned" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05456 | "spanned" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05457 | "spawn" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r05458 | "specific" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r05459 | "specific" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05460 | "specific" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r05461 | "specific" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05462 | "specific" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r05463 | "specific" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r05464 | "specified" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r05465 | "specified" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r05466 | "specified" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05467 | "specified" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r05468 | "specified" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r05469 | "specified" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r05470 | "specified" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r05471 | "specified" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r05472 | "specified" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05473 | "specified" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r05474 | "specified" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r05475 | "specified" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r05476 | "specified" | "Bytes" | "at" | "creates" | "Read byte at a specified index in a ByteArray"
    r05477 | "specified" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r05478 | "specified" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05479 | "specified" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r05480 | "specified" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r05481 | "specified" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r05482 | "split" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r05483 | "split" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r05484 | "split" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05485 | "split" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r05486 | "split" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r05487 | "split" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05488 | "split" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r05489 | "split" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r05490 | "split" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05491 | "square" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r05492 | "square" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r05493 | "square" | "Math" | "sqrt" | "derives" | "Square root of a float"
    r05494 | "squares" | "Main" | "eliminate_multiples" | "creates" | "Eliminate squares of primes: mark multiples of n*n"
    r05495 | "squares" | "Main" | "eliminate_squares" | "creates" | "Eliminate squares of primes: iterate over sieve"
    r05496 | "src" | "Main" | "transpose_columns" | "creates" | "Transpose: copy columns of src into rows of dst"
    r05497 | "start" | "Language" | "start" | "creates" | "Get the start position of a token"
    r05498 | "start" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r05499 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05500 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05501 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05502 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05503 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r05504 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05505 | "start" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r05506 | "start" | "Language" | "start" | "creates" | "Get the start position of a token"
    r05507 | "start" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r05508 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05509 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05510 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05511 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05512 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r05513 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05514 | "start" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r05515 | "start" | "Language" | "start" | "creates" | "Get the start position of a token"
    r05516 | "start" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r05517 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of a sequence from start to end (exclusive)"
    r05518 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05519 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05520 | "start" | "Sequence" | "slice" | "derives" | "Get a slice of an array from start to end (exclusive)"
    r05521 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r05522 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05523 | "start" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r05524 | "starts" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r05525 | "starts" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r05526 | "starts" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r05527 | "state" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05528 | "state" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r05529 | "state" | "Main" | "signal_power" | "derives" | "Compute signal power from filter state"
    r05530 | "state" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05531 | "state" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r05532 | "state" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r05533 | "state" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r05534 | "static" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r05535 | "static" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r05536 | "static" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r05537 | "status" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r05538 | "status" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r05539 | "status" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r05540 | "status" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r05541 | "status" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r05542 | "status" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r05543 | "stdin" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r05544 | "stdin" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r05545 | "stdin" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r05546 | "stdout" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05547 | "stdout" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05548 | "stdout" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05549 | "stdout" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05550 | "stdout" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r05551 | "stdout" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05552 | "stdout" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05553 | "stdout" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05554 | "stdout" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05555 | "stdout" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r05556 | "stdout" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r05557 | "stdout" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r05558 | "stdout" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r05559 | "stdout" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r05560 | "stdout" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r05561 | "stemming" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r05562 | "stemming" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r05563 | "stemming" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r05564 | "step" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05565 | "step" | "Main" | "atkin_step1_y" | "creates" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r05566 | "step" | "Main" | "atkin_step1_x" | "creates" | "Sieve of Atkin step 1: iterate x"
    r05567 | "step" | "Main" | "atkin_step2_y" | "creates" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r05568 | "step" | "Main" | "atkin_step2_x" | "creates" | "Sieve of Atkin step 2: iterate x"
    r05569 | "step" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r05570 | "step" | "Main" | "atkin_step3_x" | "creates" | "Sieve of Atkin step 3: iterate x"
    r05571 | "step" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05572 | "step" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r05573 | "stopwords" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r05574 | "stopwords" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r05575 | "stopwords" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r05576 | "stopwords" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r05577 | "stopwords" | "Language" | "stopwords" | "derives" | "Return a list of common English stopwords"
    r05578 | "stopwords" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r05579 | "store" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r05580 | "store" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r05581 | "store" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r05582 | "store" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05583 | "store" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r05584 | "store" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05585 | "store" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r05586 | "store" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r05587 | "store" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r05588 | "store" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05589 | "store" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r05590 | "store" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05591 | "store" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r05592 | "store" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r05593 | "store" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r05594 | "store" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05595 | "store" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r05596 | "store" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05597 | "string" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r05598 | "string" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r05599 | "string" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05600 | "string" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r05601 | "string" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r05602 | "string" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r05603 | "string" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r05604 | "string" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r05605 | "string" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r05606 | "string" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r05607 | "string" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r05608 | "string" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05609 | "string" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r05610 | "string" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r05611 | "string" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r05612 | "string" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r05613 | "string" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r05614 | "string" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r05615 | "string" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r05616 | "string" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r05617 | "string" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r05618 | "string" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r05619 | "string" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r05620 | "string" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r05621 | "string" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r05622 | "string" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r05623 | "string" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r05624 | "string" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r05625 | "string" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r05626 | "string" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r05627 | "string" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r05628 | "string" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r05629 | "string" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r05630 | "string" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r05631 | "string" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r05632 | "string" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r05633 | "string" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r05634 | "string" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r05635 | "string" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r05636 | "string" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r05637 | "string" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r05638 | "string" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r05639 | "string" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r05640 | "string" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r05641 | "string" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05642 | "string" | "Text" | "length" | "creates" | "Get the length of a string"
    r05643 | "string" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r05644 | "string" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r05645 | "string" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r05646 | "string" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05647 | "string" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r05648 | "string" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r05649 | "string" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r05650 | "string" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r05651 | "string" | "Text" | "builder" | "creates" | "Create a new string builder"
    r05652 | "string" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r05653 | "string" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r05654 | "string" | "Types" | "string" | "validates" | "Check if a value is a string"
    r05655 | "string" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r05656 | "string" | "Types" | "float" | "creates" | "Parse a string as a float"
    r05657 | "string" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r05658 | "string" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r05659 | "string" | "Types" | "string" | "creates" | "Convert a float to a string"
    r05660 | "string" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r05661 | "string" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r05662 | "string" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r05663 | "string" | "Types" | "string" | "creates" | "Convert a character to a string"
    r05664 | "string" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r05665 | "string" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r05666 | "string" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r05667 | "string" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r05668 | "string" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r05669 | "string" | "Types" | "string" | "creates" | "Convert a value to a string"
    r05670 | "string" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r05671 | "string" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r05672 | "string" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r05673 | "string" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r05674 | "string" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r05675 | "string" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r05676 | "string" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r05677 | "string" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r05678 | "string" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r05679 | "string" | "Types" | "value" | "validates" | "Validate a source string"
    r05680 | "string" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r05681 | "string" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r05682 | "string" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r05683 | "string" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r05684 | "string" | "Main" | "try_json" | "transforms" | "Parse JSON and return as a Value string (failable)"
    r05685 | "string" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r05686 | "string" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r05687 | "string" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05688 | "string" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r05689 | "string" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r05690 | "string" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r05691 | "string" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r05692 | "string" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r05693 | "string" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r05694 | "string" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r05695 | "string" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r05696 | "string" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05697 | "string" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r05698 | "string" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r05699 | "string" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r05700 | "string" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r05701 | "string" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r05702 | "string" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r05703 | "string" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r05704 | "string" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r05705 | "string" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r05706 | "string" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r05707 | "string" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r05708 | "string" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r05709 | "string" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r05710 | "string" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r05711 | "string" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r05712 | "string" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r05713 | "string" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r05714 | "string" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r05715 | "string" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r05716 | "string" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r05717 | "string" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r05718 | "string" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r05719 | "string" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r05720 | "string" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r05721 | "string" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r05722 | "string" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r05723 | "string" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r05724 | "string" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r05725 | "string" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r05726 | "string" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r05727 | "string" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r05728 | "string" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r05729 | "string" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05730 | "string" | "Text" | "length" | "creates" | "Get the length of a string"
    r05731 | "string" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r05732 | "string" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r05733 | "string" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r05734 | "string" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05735 | "string" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r05736 | "string" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r05737 | "string" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r05738 | "string" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r05739 | "string" | "Text" | "builder" | "creates" | "Create a new string builder"
    r05740 | "string" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r05741 | "string" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r05742 | "string" | "Types" | "string" | "validates" | "Check if a value is a string"
    r05743 | "string" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r05744 | "string" | "Types" | "float" | "creates" | "Parse a string as a float"
    r05745 | "string" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r05746 | "string" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r05747 | "string" | "Types" | "string" | "creates" | "Convert a float to a string"
    r05748 | "string" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r05749 | "string" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r05750 | "string" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r05751 | "string" | "Types" | "string" | "creates" | "Convert a character to a string"
    r05752 | "string" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r05753 | "string" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r05754 | "string" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r05755 | "string" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r05756 | "string" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r05757 | "string" | "Types" | "string" | "creates" | "Convert a value to a string"
    r05758 | "string" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r05759 | "string" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r05760 | "string" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r05761 | "string" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r05762 | "string" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r05763 | "string" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r05764 | "string" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r05765 | "string" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r05766 | "string" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r05767 | "string" | "Types" | "value" | "validates" | "Validate a source string"
    r05768 | "string" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r05769 | "string" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r05770 | "string" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r05771 | "string" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r05772 | "string" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r05773 | "string" | "Format" | "octal" | "creates" | "Converts an integer to octal string."
    r05774 | "string" | "Format" | "hexadecimal" | "creates" | "Converts an integer to hexadecimal string."
    r05775 | "string" | "Format" | "bin" | "creates" | "Converts an integer to binary string."
    r05776 | "string" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r05777 | "string" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r05778 | "string" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r05779 | "string" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r05780 | "string" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r05781 | "string" | "Hash" | "sha256" | "creates" | "Hash a string to SHA-256 hex string"
    r05782 | "string" | "Hash" | "sha512" | "creates" | "Hash a string to SHA-512 hex string"
    r05783 | "string" | "Hash" | "blake3" | "creates" | "Hash a string to BLAKE3 hex string"
    r05784 | "string" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r05785 | "string" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r05786 | "string" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r05787 | "string" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r05788 | "string" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r05789 | "string" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r05790 | "string" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r05791 | "string" | "Parse" | "hexadecimal" | "creates" | "Encode a byte array as a hexadecimal string"
    r05792 | "string" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r05793 | "string" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r05794 | "string" | "Parse" | "base64" | "creates" | "Decode a Base64 string into a byte array"
    r05795 | "string" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r05796 | "string" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r05797 | "string" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r05798 | "string" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r05799 | "string" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r05800 | "string" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r05801 | "string" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r05802 | "string" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r05803 | "string" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r05804 | "string" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r05805 | "string" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r05806 | "string" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r05807 | "string" | "Pattern" | "split" | "creates" | "Split a string by a regex pattern"
    r05808 | "string" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r05809 | "string" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r05810 | "string" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r05811 | "string" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r05812 | "string" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r05813 | "string" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r05814 | "string" | "Text" | "length" | "creates" | "Get the length of a string"
    r05815 | "string" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r05816 | "string" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r05817 | "string" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r05818 | "string" | "Text" | "split" | "creates" | "Split a string by a separator"
    r05819 | "string" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r05820 | "string" | "Text" | "lower" | "derives" | "Convert a string to lowercase"
    r05821 | "string" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r05822 | "string" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r05823 | "string" | "Text" | "builder" | "creates" | "Create a new string builder"
    r05824 | "string" | "Text" | "string_builder" | "derives" | "Append a string to a builder"
    r05825 | "string" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r05826 | "string" | "Types" | "string" | "validates" | "Check if a value is a string"
    r05827 | "string" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r05828 | "string" | "Types" | "float" | "creates" | "Parse a string as a float"
    r05829 | "string" | "Types" | "decimal" | "creates" | "Parse a string as a decimal"
    r05830 | "string" | "Types" | "string" | "creates" | "Convert an integer to a string"
    r05831 | "string" | "Types" | "string" | "creates" | "Convert a float to a string"
    r05832 | "string" | "Types" | "string" | "creates" | "Convert a decimal to a string"
    r05833 | "string" | "Types" | "string" | "creates" | "Convert a boolean to a string"
    r05834 | "string" | "Types" | "string" | "creates" | "Convert a byte to a string"
    r05835 | "string" | "Types" | "string" | "creates" | "Convert a character to a string"
    r05836 | "string" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r05837 | "string" | "Types" | "string" | "creates" | "Convert a date to an ISO 8601 string"
    r05838 | "string" | "Types" | "string" | "creates" | "Convert a datetime to an ISO 8601 string"
    r05839 | "string" | "Types" | "string" | "creates" | "Convert a clock to a string"
    r05840 | "string" | "Types" | "string" | "creates" | "Convert a duration to a string"
    r05841 | "string" | "Types" | "string" | "creates" | "Convert a value to a string"
    r05842 | "string" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r05843 | "string" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r05844 | "string" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r05845 | "string" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r05846 | "string" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r05847 | "string" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r05848 | "string" | "Types" | "string" | "creates" | "Convert a Position to a string (e.g. \"80x24\")"
    r05849 | "string" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r05850 | "string" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r05851 | "string" | "Types" | "value" | "validates" | "Validate a source string"
    r05852 | "string" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r05853 | "strings" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r05854 | "strings" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r05855 | "strings" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05856 | "strings" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r05857 | "strings" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r05858 | "strings" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r05859 | "strings" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05860 | "strings" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r05861 | "strings" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r05862 | "strings" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r05863 | "strings" | "Sequence" | "sort" | "derives" | "Sort a sequence of strings"
    r05864 | "strings" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r05865 | "strip" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05866 | "strip" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05867 | "strip" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05868 | "struct" | "Config" | "struct" | "transforms" | "Parse the toml data to Config struct"
    r05869 | "structural" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r05870 | "structural" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r05871 | "structural" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r05872 | "sub" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r05873 | "sub" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r05874 | "sub" | "Bytes" | "bytearray" | "derives" | "Extract a sub-range of bytes and return a new ByteArray"
    r05875 | "subsequent" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r05876 | "subsequent" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r05877 | "subsequent" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r05878 | "subsequent" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r05879 | "subsequent" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r05880 | "subsequent" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r05881 | "substring" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r05882 | "substring" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r05883 | "substring" | "Text" | "index" | "creates" | "Find the index of a substring"
    r05884 | "substring" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r05885 | "substring" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r05886 | "substring" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r05887 | "substring" | "Text" | "index" | "creates" | "Find the index of a substring"
    r05888 | "substring" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r05889 | "substring" | "Text" | "slice" | "derives" | "Get a substring from start to end (exclusive)"
    r05890 | "substring" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r05891 | "substring" | "Text" | "index" | "creates" | "Find the index of a substring"
    r05892 | "substring" | "Text" | "replace" | "derives" | "Replace all occurrences of a substring"
    r05893 | "succeeded" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r05894 | "succeeded" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r05895 | "succeeded" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r05896 | "success" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r05897 | "successful" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r05898 | "successful" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r05899 | "successful" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r05900 | "suffix" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r05901 | "suffix" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r05902 | "suffix" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r05903 | "suffixes" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05904 | "suffixes" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05905 | "suffixes" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r05906 | "syntax" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r05907 | "syntax" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r05908 | "syntax" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05909 | "syntax" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r05910 | "syntax" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r05911 | "syntax" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05912 | "syntax" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r05913 | "syntax" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r05914 | "syntax" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r05915 | "system" | "System" | "system" | "inputs" | "Execute a system command"
    r05916 | "system" | "System" | "system" | "inputs" | "Execute a system command"
    r05917 | "system" | "System" | "system" | "inputs" | "Execute a system command"
    r05918 | "table" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r05919 | "table" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r05920 | "table" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05921 | "table" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r05922 | "table" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r05923 | "table" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r05924 | "table" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r05925 | "table" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r05926 | "table" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r05927 | "table" | "Store" | "version" | "inputs" | "List all versions of a table"
    r05928 | "table" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r05929 | "table" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05930 | "table" | "Table" | "new" | "creates" | "Create a new empty table"
    r05931 | "table" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r05932 | "table" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r05933 | "table" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r05934 | "table" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r05935 | "table" | "Table" | "values" | "derives" | "Get all values in a table"
    r05936 | "table" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r05937 | "table" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r05938 | "table" | "Main" | "show" | "outputs" | "Show table operations on a parsed JSON object"
    r05939 | "table" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r05940 | "table" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r05941 | "table" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05942 | "table" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r05943 | "table" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r05944 | "table" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r05945 | "table" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r05946 | "table" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r05947 | "table" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r05948 | "table" | "Store" | "version" | "inputs" | "List all versions of a table"
    r05949 | "table" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r05950 | "table" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05951 | "table" | "Table" | "new" | "creates" | "Create a new empty table"
    r05952 | "table" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r05953 | "table" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r05954 | "table" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r05955 | "table" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r05956 | "table" | "Table" | "values" | "derives" | "Get all values in a table"
    r05957 | "table" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r05958 | "table" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r05959 | "table" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r05960 | "table" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r05961 | "table" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r05962 | "table" | "Store" | "patch" | "derives" | "Apply a diff to a table producing a new table"
    r05963 | "table" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r05964 | "table" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r05965 | "table" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r05966 | "table" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r05967 | "table" | "Store" | "version" | "inputs" | "List all versions of a table"
    r05968 | "table" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r05969 | "table" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r05970 | "table" | "Table" | "new" | "creates" | "Create a new empty table"
    r05971 | "table" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r05972 | "table" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r05973 | "table" | "Table" | "remove" | "derives" | "Remove a key from a table (returns new table)"
    r05974 | "table" | "Table" | "keys" | "creates" | "Get all keys in a table"
    r05975 | "table" | "Table" | "values" | "derives" | "Get all values in a table"
    r05976 | "table" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r05977 | "tables" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r05978 | "tables" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r05979 | "tables" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r05980 | "tag" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r05981 | "tag" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r05982 | "tag" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r05983 | "tag" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r05984 | "tag" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r05985 | "tag" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r05986 | "tag" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r05987 | "tag" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r05988 | "tag" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r05989 | "tag" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r05990 | "tag" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r05991 | "tag" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r05992 | "tag" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r05993 | "tag" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r05994 | "tag" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r05995 | "tangent" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r05996 | "tangent" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r05997 | "tangent" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r05998 | "tangent" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r05999 | "tangent" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r06000 | "tangent" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r06001 | "tangent" | "Math" | "tan" | "derives" | "Tangent of an angle in radians"
    r06002 | "tangent" | "Math" | "atan" | "derives" | "Inverse tangent (arc tangent), result in radians"
    r06003 | "tangent" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r06004 | "target" | "Main" | "coefficient" | "derives" | "Goertzel coefficient for a target frequency at a given sample rate"
    r06005 | "target" | "Main" | "detect" | "creates" | "Run Goertzel: detect target_freq power in a signal of signal_freq"
    r06006 | "task" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06007 | "task" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06008 | "task" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06009 | "tco" | "Main" | "eval_dispatch" | "creates" | "Main dispatch loop — all opcodes inlined for full TCO"
    r06010 | "tcp" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r06011 | "tcp" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r06012 | "tcp" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r06013 | "tcp" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r06014 | "tcp" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r06015 | "tcp" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r06016 | "terminal" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r06017 | "terminal" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06018 | "terminal" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06019 | "terminal" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r06020 | "terminal" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r06021 | "terminal" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06022 | "terminal" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06023 | "terminal" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r06024 | "terminal" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06025 | "terminal" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06026 | "termination" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r06027 | "test" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r06028 | "test" | "Example" | "user" | "creates" | "test"
    r06029 | "test" | "Example" | "ensure_user" | "creates" | "test2"
    r06030 | "test" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r06031 | "test" | "Commands" | "test" | "dispatches" | "Run the test command"
    r06032 | "test" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r06033 | "text" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06034 | "text" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r06035 | "text" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06036 | "text" | "Language" | "words" | "creates" | "Extract individual words from text"
    r06037 | "text" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r06038 | "text" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r06039 | "text" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r06040 | "text" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r06041 | "text" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r06042 | "text" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r06043 | "text" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06044 | "text" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06045 | "text" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r06046 | "text" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r06047 | "text" | "System" | "console" | "outputs" | "Print text to the console"
    r06048 | "text" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r06049 | "text" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r06050 | "text" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06051 | "text" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06052 | "text" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r06053 | "text" | "Types" | "text" | "validates" | "Check if a Value is text"
    r06054 | "text" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06055 | "text" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r06056 | "text" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06057 | "text" | "Language" | "words" | "creates" | "Extract individual words from text"
    r06058 | "text" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r06059 | "text" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r06060 | "text" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r06061 | "text" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r06062 | "text" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r06063 | "text" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r06064 | "text" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06065 | "text" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06066 | "text" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r06067 | "text" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r06068 | "text" | "System" | "console" | "outputs" | "Print text to the console"
    r06069 | "text" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r06070 | "text" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r06071 | "text" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06072 | "text" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06073 | "text" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r06074 | "text" | "Types" | "text" | "validates" | "Check if a Value is text"
    r06075 | "text" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06076 | "text" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r06077 | "text" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06078 | "text" | "Language" | "words" | "creates" | "Extract individual words from text"
    r06079 | "text" | "Language" | "sentences" | "creates" | "Split text into sentences"
    r06080 | "text" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r06081 | "text" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r06082 | "text" | "Language" | "normalize" | "derives" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r06083 | "text" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r06084 | "text" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r06085 | "text" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06086 | "text" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06087 | "text" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r06088 | "text" | "Pattern" | "find_all" | "creates" | "Find all matches of a pattern in text"
    r06089 | "text" | "System" | "console" | "outputs" | "Print text to the console"
    r06090 | "text" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r06091 | "text" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r06092 | "text" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06093 | "text" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06094 | "text" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r06095 | "text" | "Types" | "text" | "validates" | "Check if a Value is text"
    r06096 | "that" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r06097 | "that" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06098 | "that" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r06099 | "that" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r06100 | "that" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r06101 | "that" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r06102 | "that" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06103 | "that" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r06104 | "that" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r06105 | "that" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r06106 | "that" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06107 | "that" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r06108 | "that" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r06109 | "the" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r06110 | "the" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r06111 | "the" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r06112 | "the" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r06113 | "the" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r06114 | "the" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r06115 | "the" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r06116 | "the" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06117 | "the" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06118 | "the" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06119 | "the" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06120 | "the" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06121 | "the" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r06122 | "the" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r06123 | "the" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r06124 | "the" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r06125 | "the" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r06126 | "the" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06127 | "the" | "Language" | "start" | "creates" | "Get the start position of a token"
    r06128 | "the" | "Language" | "end" | "creates" | "Get the end position of a token"
    r06129 | "the" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r06130 | "the" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r06131 | "the" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r06132 | "the" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r06133 | "the" | "Math" | "pi" | "derives" | "The constant pi (3.14159...)"
    r06134 | "the" | "Math" | "e" | "derives" | "The constant e (2.71828...)"
    r06135 | "the" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06136 | "the" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r06137 | "the" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r06138 | "the" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r06139 | "the" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r06140 | "the" | "Path" | "extension" | "derives" | "Get the file extension"
    r06141 | "the" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r06142 | "the" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r06143 | "the" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r06144 | "the" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r06145 | "the" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r06146 | "the" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r06147 | "the" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r06148 | "the" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r06149 | "the" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r06150 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r06151 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r06152 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r06153 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06154 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06155 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06156 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r06157 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r06158 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r06159 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06160 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06161 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06162 | "the" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r06163 | "the" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r06164 | "the" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r06165 | "the" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r06166 | "the" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r06167 | "the" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r06168 | "the" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r06169 | "the" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r06170 | "the" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r06171 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06172 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06173 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06174 | "the" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r06175 | "the" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r06176 | "the" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r06177 | "the" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06178 | "the" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r06179 | "the" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r06180 | "the" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r06181 | "the" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r06182 | "the" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r06183 | "the" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r06184 | "the" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r06185 | "the" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r06186 | "the" | "System" | "console" | "outputs" | "Print text to the console"
    r06187 | "the" | "System" | "console" | "inputs" | "Read a line from the console"
    r06188 | "the" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r06189 | "the" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r06190 | "the" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r06191 | "the" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r06192 | "the" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r06193 | "the" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06194 | "the" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r06195 | "the" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r06196 | "the" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r06197 | "the" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06198 | "the" | "Text" | "length" | "creates" | "Get the length of a string"
    r06199 | "the" | "Text" | "index" | "creates" | "Find the index of a substring"
    r06200 | "the" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r06201 | "the" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r06202 | "the" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r06203 | "the" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r06204 | "the" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06205 | "the" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r06206 | "the" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06207 | "the" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r06208 | "the" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r06209 | "the" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r06210 | "the" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r06211 | "the" | "Main" | "kernel" | "creates" | "Recursive kernel — generate and process samples on the fly"
    r06212 | "the" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r06213 | "the" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r06214 | "the" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r06215 | "the" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r06216 | "the" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r06217 | "the" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r06218 | "the" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r06219 | "the" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r06220 | "the" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r06221 | "the" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r06222 | "the" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r06223 | "the" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06224 | "the" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06225 | "the" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06226 | "the" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06227 | "the" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06228 | "the" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r06229 | "the" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r06230 | "the" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r06231 | "the" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r06232 | "the" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r06233 | "the" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06234 | "the" | "Language" | "start" | "creates" | "Get the start position of a token"
    r06235 | "the" | "Language" | "end" | "creates" | "Get the end position of a token"
    r06236 | "the" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r06237 | "the" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r06238 | "the" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r06239 | "the" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r06240 | "the" | "Math" | "pi" | "derives" | "The constant pi (3.14159...)"
    r06241 | "the" | "Math" | "e" | "derives" | "The constant e (2.71828...)"
    r06242 | "the" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06243 | "the" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r06244 | "the" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r06245 | "the" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r06246 | "the" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r06247 | "the" | "Path" | "extension" | "derives" | "Get the file extension"
    r06248 | "the" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r06249 | "the" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r06250 | "the" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r06251 | "the" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r06252 | "the" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r06253 | "the" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r06254 | "the" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r06255 | "the" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r06256 | "the" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r06257 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r06258 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r06259 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r06260 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06261 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06262 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06263 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r06264 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r06265 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r06266 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06267 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06268 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06269 | "the" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r06270 | "the" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r06271 | "the" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r06272 | "the" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r06273 | "the" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r06274 | "the" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r06275 | "the" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r06276 | "the" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r06277 | "the" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r06278 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06279 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06280 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06281 | "the" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r06282 | "the" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r06283 | "the" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r06284 | "the" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06285 | "the" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r06286 | "the" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r06287 | "the" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r06288 | "the" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r06289 | "the" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r06290 | "the" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r06291 | "the" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r06292 | "the" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r06293 | "the" | "System" | "console" | "outputs" | "Print text to the console"
    r06294 | "the" | "System" | "console" | "inputs" | "Read a line from the console"
    r06295 | "the" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r06296 | "the" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r06297 | "the" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r06298 | "the" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r06299 | "the" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r06300 | "the" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06301 | "the" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r06302 | "the" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r06303 | "the" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r06304 | "the" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06305 | "the" | "Text" | "length" | "creates" | "Get the length of a string"
    r06306 | "the" | "Text" | "index" | "creates" | "Find the index of a substring"
    r06307 | "the" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r06308 | "the" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r06309 | "the" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r06310 | "the" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r06311 | "the" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06312 | "the" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r06313 | "the" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06314 | "the" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r06315 | "the" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r06316 | "the" | "Commands" | "build" | "dispatches" | "Run the build command"
    r06317 | "the" | "Commands" | "check" | "dispatches" | "Run the check command"
    r06318 | "the" | "Commands" | "format" | "dispatches" | "Run the format command"
    r06319 | "the" | "Commands" | "lsp" | "dispatches" | "Run the lsp command"
    r06320 | "the" | "Commands" | "new" | "dispatches" | "Run the new command"
    r06321 | "the" | "Commands" | "test" | "dispatches" | "Run the test command"
    r06322 | "the" | "Config" | "struct" | "transforms" | "Parse the toml data to Config struct"
    r06323 | "the" | "Character" | "at" | "creates" | "Gets the character at a specific index in a string."
    r06324 | "the" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r06325 | "the" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r06326 | "the" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r06327 | "the" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r06328 | "the" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r06329 | "the" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r06330 | "the" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06331 | "the" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06332 | "the" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06333 | "the" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06334 | "the" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06335 | "the" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r06336 | "the" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r06337 | "the" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r06338 | "the" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r06339 | "the" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r06340 | "the" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06341 | "the" | "Language" | "start" | "creates" | "Get the start position of a token"
    r06342 | "the" | "Language" | "end" | "creates" | "Get the end position of a token"
    r06343 | "the" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r06344 | "the" | "Math" | "power" | "derives" | "Base raised to the power of exp"
    r06345 | "the" | "Math" | "round" | "creates" | "Round a float to the nearest integer"
    r06346 | "the" | "Math" | "exp" | "derives" | "Euler's number raised to the power of x (e^x)"
    r06347 | "the" | "Math" | "pi" | "derives" | "The constant pi (3.14159...)"
    r06348 | "the" | "Math" | "e" | "derives" | "The constant e (2.71828...)"
    r06349 | "the" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06350 | "the" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r06351 | "the" | "Path" | "parent" | "derives" | "Get the parent directory of a path"
    r06352 | "the" | "Path" | "name" | "derives" | "Get the filename component of a path"
    r06353 | "the" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r06354 | "the" | "Path" | "extension" | "derives" | "Get the file extension"
    r06355 | "the" | "Pattern" | "search" | "creates" | "Search for the first match of a pattern in text"
    r06356 | "the" | "Pattern" | "start" | "creates" | "Get the start position of a match"
    r06357 | "the" | "Pattern" | "end" | "creates" | "Get the end position of a match"
    r06358 | "the" | "Prove" | "parent" | "creates" | "Parent node. Returns None for the root node."
    r06359 | "the" | "Prove" | "child" | "creates" | "Named child by field name. Returns None if the field does not exist."
    r06360 | "the" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r06361 | "the" | "Log" | "severity_color" | "matches" | "match the color for message based on severity"
    r06362 | "the" | "Sequence" | "length" | "creates" | "Get the length of a sequence"
    r06363 | "the" | "Sequence" | "length" | "creates" | "Get the length of an array"
    r06364 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list"
    r06365 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list of floats"
    r06366 | "the" | "Sequence" | "first" | "derives" | "Get the first element of a list of decimals"
    r06367 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06368 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06369 | "the" | "Sequence" | "first" | "derives" | "Get the first element of an array"
    r06370 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list"
    r06371 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list of floats"
    r06372 | "the" | "Sequence" | "last" | "derives" | "Get the last element of a list of decimals"
    r06373 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06374 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06375 | "the" | "Sequence" | "last" | "derives" | "Get the last element of an array"
    r06376 | "the" | "Sequence" | "value" | "derives" | "Get the element at a position in a sequence, or None if out of bounds"
    r06377 | "the" | "Sequence" | "index" | "creates" | "Find the index of an integer in a sequence"
    r06378 | "the" | "Sequence" | "index" | "creates" | "Find the index of a string in a sequence"
    r06379 | "the" | "Sequence" | "index" | "creates" | "Find the index of a float in a list"
    r06380 | "the" | "Sequence" | "index" | "creates" | "Find the index of a decimal in a list"
    r06381 | "the" | "Sequence" | "index" | "creates" | "Find the index of an integer in an array"
    r06382 | "the" | "Sequence" | "index" | "creates" | "Find the index of a boolean in an array"
    r06383 | "the" | "Sequence" | "index" | "creates" | "Find the index of a float in an array"
    r06384 | "the" | "Sequence" | "index" | "creates" | "Find the index of a decimal in an array"
    r06385 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06386 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06387 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06388 | "the" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r06389 | "the" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r06390 | "the" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r06391 | "the" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06392 | "the" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r06393 | "the" | "Store" | "integrity" | "creates" | "Compute the integrity hash of a table"
    r06394 | "the" | "Store" | "merged" | "creates" | "Get the merged table from a successful merge result"
    r06395 | "the" | "Store" | "conflicts" | "creates" | "Get the list of conflicts from a conflicted merge result"
    r06396 | "the" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r06397 | "the" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r06398 | "the" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r06399 | "the" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r06400 | "the" | "System" | "console" | "outputs" | "Print text to the console"
    r06401 | "the" | "System" | "console" | "inputs" | "Read a line from the console"
    r06402 | "the" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r06403 | "the" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r06404 | "the" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r06405 | "the" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r06406 | "the" | "Table" | "length" | "creates" | "Get the number of entries in a table"
    r06407 | "the" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06408 | "the" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r06409 | "the" | "Terminal" | "clear" | "outputs" | "Clear the entire screen and reset cursor to (0, 0)."
    r06410 | "the" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r06411 | "the" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06412 | "the" | "Text" | "length" | "creates" | "Get the length of a string"
    r06413 | "the" | "Text" | "index" | "creates" | "Find the index of a substring"
    r06414 | "the" | "Text" | "build" | "creates" | "Build the final string from a builder"
    r06415 | "the" | "Text" | "length" | "creates" | "Get the current length of a builder"
    r06416 | "the" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r06417 | "the" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r06418 | "the" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06419 | "the" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r06420 | "the" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06421 | "the" | "Types" | "string" | "creates" | "Get the matched string from a match result"
    r06422 | "the" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r06423 | "them" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r06424 | "then" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r06425 | "this" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r06426 | "this" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r06427 | "this" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r06428 | "three" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r06429 | "three" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r06430 | "three" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r06431 | "three" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r06432 | "three" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r06433 | "three" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r06434 | "time" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r06435 | "time" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r06436 | "time" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r06437 | "time" | "Time" | "time" | "inputs" | "Get current time"
    r06438 | "time" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r06439 | "time" | "Time" | "date" | "creates" | "Extract date from a time"
    r06440 | "time" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r06441 | "time" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r06442 | "time" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r06443 | "time" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r06444 | "time" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r06445 | "time" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r06446 | "time" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r06447 | "time" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r06448 | "time" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r06449 | "time" | "Time" | "time" | "inputs" | "Get current time"
    r06450 | "time" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r06451 | "time" | "Time" | "date" | "creates" | "Extract date from a time"
    r06452 | "time" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r06453 | "time" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r06454 | "time" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r06455 | "time" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r06456 | "time" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r06457 | "time" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r06458 | "time" | "Time" | "time" | "inputs" | "Get current time"
    r06459 | "time" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r06460 | "time" | "Time" | "date" | "creates" | "Extract date from a time"
    r06461 | "time" | "Time" | "datetime" | "creates" | "Extract datetime from a time"
    r06462 | "time" | "Time" | "clock" | "creates" | "Extract clock from a time"
    r06463 | "time" | "Types" | "string" | "creates" | "Convert a time to an ISO 8601 string"
    r06464 | "times" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r06465 | "times" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r06466 | "times" | "Main" | "encode" | "creates" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r06467 | "times" | "Main" | "decode" | "derives" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r06468 | "times" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r06469 | "times" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r06470 | "times" | "Text" | "repeat" | "derives" | "Repeat a string count times"
    r06471 | "times" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r06472 | "timestamp" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r06473 | "timestamp" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r06474 | "timestamp" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r06475 | "timestamp" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r06476 | "timestamp" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r06477 | "timestamp" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r06478 | "timestamp" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r06479 | "timestamp" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r06480 | "timestamp" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r06481 | "timestamp" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r06482 | "timestamp" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r06483 | "timestamp" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r06484 | "timestamp" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r06485 | "timestamp" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r06486 | "timestamp" | "Time" | "datetime" | "creates" | "Convert datetime to timestamp"
    r06487 | "together" | "Path" | "join" | "derives" | "Join path components together"
    r06488 | "together" | "Path" | "join" | "derives" | "Join path components together"
    r06489 | "together" | "Path" | "join" | "derives" | "Join path components together"
    r06490 | "toggled" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06491 | "toggled" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06492 | "toggled" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06493 | "token" | "Language" | "start" | "creates" | "Get the start position of a token"
    r06494 | "token" | "Language" | "end" | "creates" | "Get the end position of a token"
    r06495 | "token" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r06496 | "token" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06497 | "token" | "Language" | "start" | "creates" | "Get the start position of a token"
    r06498 | "token" | "Language" | "end" | "creates" | "Get the end position of a token"
    r06499 | "token" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r06500 | "token" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06501 | "token" | "Language" | "start" | "creates" | "Get the start position of a token"
    r06502 | "token" | "Language" | "end" | "creates" | "Get the end position of a token"
    r06503 | "token" | "Language" | "kind" | "creates" | "Get the kind tag of a token"
    r06504 | "token" | "Types" | "string" | "creates" | "Extract the matched text of a token as a string"
    r06505 | "tokenization" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r06506 | "tokenization" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r06507 | "tokenization" | "Parse" | "rule" | "creates" | "Create a tokenization rule from a regex pattern and a kind tag"
    r06508 | "tokenize" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06509 | "tokenize" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06510 | "tokenize" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06511 | "tokens" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06512 | "tokens" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r06513 | "tokens" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r06514 | "tokens" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06515 | "tokens" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r06516 | "tokens" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r06517 | "tokens" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06518 | "tokens" | "Prove" | "children" | "creates" | "All child nodes (including anonymous tokens)."
    r06519 | "tokens" | "Prove" | "named_children" | "creates" | "Named children only (skipping anonymous tokens like punctuation)."
    r06520 | "toml" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r06521 | "toml" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r06522 | "toml" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r06523 | "toml" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r06524 | "toml" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r06525 | "toml" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r06526 | "toml" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r06527 | "toml" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r06528 | "toml" | "Config" | "struct" | "transforms" | "Parse the toml data to Config struct"
    r06529 | "toml" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r06530 | "toml" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r06531 | "toml" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r06532 | "toml" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r06533 | "top" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06534 | "top" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r06535 | "top" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06536 | "top" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r06537 | "top" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r06538 | "top" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r06539 | "total" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r06540 | "total" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r06541 | "total" | "Time" | "duration" | "derives" | "Read total seconds from a duration"
    r06542 | "transliterate" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r06543 | "transliterate" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r06544 | "transliterate" | "Language" | "transliterate" | "derives" | "Transliterate accented characters to ASCII equivalents preserving case"
    r06545 | "transpose" | "Main" | "transpose_columns" | "creates" | "Transpose: copy columns of src into rows of dst"
    r06546 | "transpose" | "Main" | "transpose_rows" | "creates" | "Transpose: iterate rows"
    r06547 | "transpose" | "Main" | "transpose" | "creates" | "Transpose a matrix"
    r06548 | "transpose" | "Main" | "multiply" | "creates" | "Multiply two matrices: a * b with transpose optimization"
    r06549 | "transposed" | "Main" | "dot" | "creates" | "Dot product of row i of a with row j of bt (transposed b)"
    r06550 | "tree" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r06551 | "tree" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r06552 | "tree" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06553 | "tree" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r06554 | "tree" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r06555 | "tree" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r06556 | "tree" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r06557 | "tree" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06558 | "tree" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r06559 | "tree" | "Parse" | "tree" | "creates" | "Parse Prove source into a syntax tree"
    r06560 | "tree" | "Prove" | "root" | "derives" | "Root node of a parsed tree."
    r06561 | "tree" | "Types" | "string" | "creates" | "Extract the full source text from a parsed tree"
    r06562 | "tree" | "Types" | "string" | "creates" | "Source text spanned by a syntax tree node"
    r06563 | "trie" | "Main" | "trie_insert_digits" | "creates" | "Insert a prime into the trie by iterating its digits"
    r06564 | "trie" | "Main" | "build_trie_iter" | "creates" | "Build the trie from sieve: iterate indices and insert primes"
    r06565 | "trie" | "Main" | "trie_find_prefix" | "creates" | "Navigate trie to the prefix node, return found node or empty table"
    r06566 | "trie" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r06567 | "trie" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r06568 | "triggers" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06569 | "triggers" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06570 | "triggers" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06571 | "trim" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r06572 | "trim" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r06573 | "trim" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r06574 | "true" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06575 | "true" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r06576 | "true" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r06577 | "true" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r06578 | "true" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06579 | "true" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r06580 | "true" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r06581 | "true" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r06582 | "true" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06583 | "true" | "Types" | "integer" | "creates" | "Convert a boolean to an integer (true = 1, false = 0)"
    r06584 | "true" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r06585 | "true" | "Types" | "boolean" | "creates" | "Parse a string as a boolean (\"true\" or \"false\")"
    r06586 | "truncates" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r06587 | "truncates" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r06588 | "truncates" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r06589 | "truncates" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r06590 | "truncates" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r06591 | "truncates" | "Types" | "integer" | "creates" | "Convert a decimal to an integer (truncates)"
    r06592 | "two" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r06593 | "two" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r06594 | "two" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r06595 | "two" | "Math" | "min" | "derives" | "Minimum of two integers"
    r06596 | "two" | "Math" | "min" | "derives" | "Minimum of two floats"
    r06597 | "two" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r06598 | "two" | "Math" | "max" | "derives" | "Maximum of two integers"
    r06599 | "two" | "Math" | "max" | "derives" | "Maximum of two floats"
    r06600 | "two" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r06601 | "two" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r06602 | "two" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r06603 | "two" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r06604 | "two" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r06605 | "two" | "Main" | "multiply" | "creates" | "Multiply two matrices: a * b with transpose optimization"
    r06606 | "two" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r06607 | "two" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r06608 | "two" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r06609 | "two" | "Math" | "min" | "derives" | "Minimum of two integers"
    r06610 | "two" | "Math" | "min" | "derives" | "Minimum of two floats"
    r06611 | "two" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r06612 | "two" | "Math" | "max" | "derives" | "Maximum of two integers"
    r06613 | "two" | "Math" | "max" | "derives" | "Maximum of two floats"
    r06614 | "two" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r06615 | "two" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r06616 | "two" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r06617 | "two" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r06618 | "two" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r06619 | "two" | "Bytes" | "bytearray" | "derives" | "Concatenate two byte arrays and return a new combined ByteArray"
    r06620 | "two" | "Language" | "distance" | "creates" | "Compute the Levenshtein edit distance between two strings"
    r06621 | "two" | "Language" | "similarity" | "creates" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r06622 | "two" | "Math" | "min" | "derives" | "Minimum of two integers"
    r06623 | "two" | "Math" | "min" | "derives" | "Minimum of two floats"
    r06624 | "two" | "Math" | "min" | "derives" | "Minimum of two decimals"
    r06625 | "two" | "Math" | "max" | "derives" | "Maximum of two integers"
    r06626 | "two" | "Math" | "max" | "derives" | "Maximum of two floats"
    r06627 | "two" | "Math" | "max" | "derives" | "Maximum of two decimals"
    r06628 | "two" | "Math" | "atan2" | "derives" | "Two-argument inverse tangent, result in radians (-pi to pi)"
    r06629 | "two" | "Store" | "diff" | "creates" | "Compute the structural diff between two tables"
    r06630 | "two" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r06631 | "two" | "Time" | "duration" | "creates" | "Compute difference between two times"
    r06632 | "type" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06633 | "type" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06634 | "type" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06635 | "types" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06636 | "types" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06637 | "types" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06638 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06639 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06640 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06641 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06642 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06643 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06644 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06645 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06646 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06647 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06648 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06649 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06650 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06651 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06652 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06653 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06654 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06655 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06656 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06657 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06658 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06659 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06660 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06661 | "unchecked" | "Sequence" | "get" | "derives" | "Get element at index (unchecked)"
    r06662 | "under" | "Main" | "collect_from_keys" | "creates" | "Collect all terminal primes under a trie node as a comma-separated string"
    r06663 | "under" | "Main" | "collect_primes" | "creates" | "Collect all primes under a trie node as comma-separated string"
    r06664 | "unicode" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r06665 | "unicode" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r06666 | "unicode" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r06667 | "unicode" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r06668 | "unicode" | "Types" | "code" | "creates" | "Get the Unicode code point of a character"
    r06669 | "unicode" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r06670 | "unique" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06671 | "unique" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06672 | "unique" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06673 | "unit" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r06674 | "unit" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r06675 | "unit" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r06676 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r06677 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r06678 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r06679 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r06680 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r06681 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r06682 | "unit" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r06683 | "unit" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r06684 | "unit" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r06685 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r06686 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r06687 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r06688 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r06689 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r06690 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r06691 | "unit" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r06692 | "unit" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r06693 | "unit" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r06694 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r06695 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Float> is a Unit"
    r06696 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r06697 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Decimal> is a Unit"
    r06698 | "unit" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r06699 | "unit" | "Types" | "unit" | "validates" | "Check if an Option<Boolean> is a Unit"
    r06700 | "unknown" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r06701 | "unknown" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r06702 | "unknown" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r06703 | "until" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r06704 | "unwrap" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r06705 | "unwrap" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r06706 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r06707 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r06708 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r06709 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r06710 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r06711 | "unwrap" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r06712 | "unwrap" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r06713 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r06714 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r06715 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r06716 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r06717 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r06718 | "unwrap" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r06719 | "unwrap" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r06720 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default integer"
    r06721 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option or return a default string"
    r06722 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Float> or return a default"
    r06723 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Decimal> or return a default"
    r06724 | "unwrap" | "Types" | "unwrap" | "derives" | "Unwrap an Option<Boolean> or return a default"
    r06725 | "updated" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06726 | "updated" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06727 | "updated" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06728 | "updated" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06729 | "updated" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06730 | "updated" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06731 | "updates" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06732 | "updates" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06733 | "updates" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r06734 | "upload" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06735 | "upload" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06736 | "upload" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06737 | "uppercase" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r06738 | "uppercase" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r06739 | "uppercase" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r06740 | "uppercase" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r06741 | "uppercase" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r06742 | "uppercase" | "Text" | "upper" | "derives" | "Convert a string to uppercase"
    r06743 | "url" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r06744 | "url" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r06745 | "url" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r06746 | "url" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r06747 | "url" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r06748 | "url" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r06749 | "url" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r06750 | "url" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r06751 | "url" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r06752 | "url" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r06753 | "url" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r06754 | "url" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r06755 | "url" | "Parse" | "url" | "creates" | "Parse a URL string into its components"
    r06756 | "url" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r06757 | "url" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r06758 | "url" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r06759 | "url" | "Parse" | "port" | "creates" | "Read the port component of a URL (-1 if not set)"
    r06760 | "url" | "Types" | "string" | "creates" | "Extract the host component of a URL as a string"
    r06761 | "usable" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06762 | "usable" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06763 | "usable" | "Terminal" | "cooked" | "outputs" | "Restore cooked mode: re-enables line buffering, echo, and signals.
Always call before exit to leave the terminal in a usable state."
    r06764 | "use" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06765 | "use" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06766 | "use" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06767 | "use" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r06768 | "use" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r06769 | "use" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06770 | "use" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06771 | "use" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06772 | "use" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r06773 | "use" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r06774 | "use" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r06775 | "use" | "Graphic" | "progress" | "outputs" | "Render a progress bar showing current out of max.
Non-interactive — use to display loading, upload, or task progress."
    r06776 | "use" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r06777 | "use" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r06778 | "use" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r06779 | "used" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r06780 | "used" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r06781 | "used" | "Graphic" | "label" | "outputs" | "Render a static text label. Non-interactive, used for headings,
descriptions, and status messages."
    r06782 | "user" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06783 | "user" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06784 | "user" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06785 | "user" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06786 | "user" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06787 | "user" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06788 | "user" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06789 | "user" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06790 | "user" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06791 | "using" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r06792 | "using" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r06793 | "using" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r06794 | "using" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r06795 | "using" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r06796 | "using" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r06797 | "using" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r06798 | "using" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r06799 | "using" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06800 | "using" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r06801 | "using" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r06802 | "using" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r06803 | "using" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r06804 | "using" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r06805 | "using" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r06806 | "using" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r06807 | "using" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r06808 | "using" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06809 | "using" | "Format" | "time" | "creates" | "Format a time as a string using a pattern."
    r06810 | "using" | "Format" | "date" | "creates" | "Format a date as a string using a pattern."
    r06811 | "using" | "Format" | "datetime" | "creates" | "Format a datetime as a string using a pattern."
    r06812 | "using" | "Format" | "duration" | "creates" | "Format a duration as a string using a pattern."
    r06813 | "using" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r06814 | "using" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r06815 | "using" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r06816 | "using" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r06817 | "using" | "Parse" | "tokens" | "creates" | "Tokenize source text using a list of rules, returning tokens"
    r06818 | "utf" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r06819 | "utf" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r06820 | "utf" | "Types" | "string" | "creates" | "Convert a byte array to a UTF-8 string"
    r06821 | "valid" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r06822 | "valid" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r06823 | "valid" | "Time" | "date" | "validates" | "Check if a date is valid"
    r06824 | "valid" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r06825 | "valid" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r06826 | "valid" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r06827 | "valid" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r06828 | "valid" | "Time" | "date" | "validates" | "Check if a date is valid"
    r06829 | "valid" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r06830 | "valid" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r06831 | "valid" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r06832 | "valid" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r06833 | "valid" | "Time" | "date" | "validates" | "Check if a date is valid"
    r06834 | "valid" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r06835 | "valid" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r06836 | "validate" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r06837 | "validate" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r06838 | "validate" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r06839 | "validate" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r06840 | "validate" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r06841 | "validate" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06842 | "validate" | "System" | "console" | "validates" | "Validate console input"
    r06843 | "validate" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r06844 | "validate" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r06845 | "validate" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r06846 | "validate" | "System" | "process" | "validates" | "Validate a process name"
    r06847 | "validate" | "Types" | "value" | "validates" | "Validate a source string"
    r06848 | "validate" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r06849 | "validate" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r06850 | "validate" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r06851 | "validate" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r06852 | "validate" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r06853 | "validate" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06854 | "validate" | "System" | "console" | "validates" | "Validate console input"
    r06855 | "validate" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r06856 | "validate" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r06857 | "validate" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r06858 | "validate" | "System" | "process" | "validates" | "Validate a process name"
    r06859 | "validate" | "Types" | "value" | "validates" | "Validate a source string"
    r06860 | "validate" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r06861 | "validate" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r06862 | "validate" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r06863 | "validate" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r06864 | "validate" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r06865 | "validate" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r06866 | "validate" | "System" | "console" | "validates" | "Validate console input"
    r06867 | "validate" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r06868 | "validate" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r06869 | "validate" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r06870 | "validate" | "System" | "process" | "validates" | "Validate a process name"
    r06871 | "validate" | "Types" | "value" | "validates" | "Validate a source string"
    r06872 | "value" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06873 | "value" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06874 | "value" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06875 | "value" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r06876 | "value" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r06877 | "value" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r06878 | "value" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r06879 | "value" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r06880 | "value" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r06881 | "value" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r06882 | "value" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r06883 | "value" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r06884 | "value" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r06885 | "value" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06886 | "value" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r06887 | "value" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r06888 | "value" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r06889 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06890 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06891 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06892 | "value" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r06893 | "value" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r06894 | "value" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r06895 | "value" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r06896 | "value" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r06897 | "value" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r06898 | "value" | "Table" | "get" | "derives" | "Get a value by key"
    r06899 | "value" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r06900 | "value" | "Types" | "string" | "validates" | "Check if a value is a string"
    r06901 | "value" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r06902 | "value" | "Types" | "float" | "validates" | "Check if a value is a float"
    r06903 | "value" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r06904 | "value" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r06905 | "value" | "Types" | "character" | "validates" | "Check if a value is a character"
    r06906 | "value" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r06907 | "value" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r06908 | "value" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r06909 | "value" | "Types" | "string" | "creates" | "Convert a value to a string"
    r06910 | "value" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r06911 | "value" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r06912 | "value" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r06913 | "value" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r06914 | "value" | "Types" | "text" | "validates" | "Check if a Value is text"
    r06915 | "value" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r06916 | "value" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r06917 | "value" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r06918 | "value" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r06919 | "value" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r06920 | "value" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r06921 | "value" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r06922 | "value" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r06923 | "value" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r06924 | "value" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r06925 | "value" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r06926 | "value" | "Main" | "try_json" | "transforms" | "Parse JSON and return as a Value string (failable)"
    r06927 | "value" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06928 | "value" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06929 | "value" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06930 | "value" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r06931 | "value" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r06932 | "value" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r06933 | "value" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r06934 | "value" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r06935 | "value" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r06936 | "value" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r06937 | "value" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r06938 | "value" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r06939 | "value" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r06940 | "value" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06941 | "value" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r06942 | "value" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r06943 | "value" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r06944 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06945 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06946 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06947 | "value" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r06948 | "value" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r06949 | "value" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r06950 | "value" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r06951 | "value" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r06952 | "value" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r06953 | "value" | "Table" | "get" | "derives" | "Get a value by key"
    r06954 | "value" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r06955 | "value" | "Types" | "string" | "validates" | "Check if a value is a string"
    r06956 | "value" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r06957 | "value" | "Types" | "float" | "validates" | "Check if a value is a float"
    r06958 | "value" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r06959 | "value" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r06960 | "value" | "Types" | "character" | "validates" | "Check if a value is a character"
    r06961 | "value" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r06962 | "value" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r06963 | "value" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r06964 | "value" | "Types" | "string" | "creates" | "Convert a value to a string"
    r06965 | "value" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r06966 | "value" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r06967 | "value" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r06968 | "value" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r06969 | "value" | "Types" | "text" | "validates" | "Check if a Value is text"
    r06970 | "value" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r06971 | "value" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r06972 | "value" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r06973 | "value" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r06974 | "value" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r06975 | "value" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r06976 | "value" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r06977 | "value" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r06978 | "value" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r06979 | "value" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r06980 | "value" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r06981 | "value" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r06982 | "value" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r06983 | "value" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r06984 | "value" | "Math" | "abs" | "derives" | "Absolute value of an integer"
    r06985 | "value" | "Math" | "abs" | "derives" | "Absolute value of a float"
    r06986 | "value" | "Math" | "abs" | "derives" | "Absolute value of a decimal"
    r06987 | "value" | "Math" | "floor" | "creates" | "Floor of a float (largest integer <= value)"
    r06988 | "value" | "Math" | "ceil" | "creates" | "Ceiling of a float (smallest integer >= value)"
    r06989 | "value" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r06990 | "value" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Value"
    r06991 | "value" | "Parse" | "toml" | "creates" | "Tag a Value as TOML for serialization"
    r06992 | "value" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r06993 | "value" | "Parse" | "json" | "creates" | "Tag a Value as JSON for serialization"
    r06994 | "value" | "Parse" | "tag" | "creates" | "Get the tag (type name) of a Value"
    r06995 | "value" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r06996 | "value" | "Parse" | "csv" | "creates" | "Parse a CSV string into a Value"
    r06997 | "value" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r06998 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r06999 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07000 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07001 | "value" | "Sequence" | "list" | "creates" | "Extract rows from a parsed CSV value as a list of lists"
    r07002 | "value" | "Sequence" | "list" | "creates" | "Extract array content from a Value"
    r07003 | "value" | "Store" | "column" | "creates" | "Get the column name from a value conflict"
    r07004 | "value" | "Store" | "local_value" | "creates" | "Get the local value from a value conflict"
    r07005 | "value" | "Store" | "remote_value" | "creates" | "Get the remote value from a value conflict"
    r07006 | "value" | "Table" | "add" | "derives" | "Add a key-value pair to a table (returns new table)"
    r07007 | "value" | "Table" | "get" | "derives" | "Get a value by key"
    r07008 | "value" | "Table" | "table" | "creates" | "Extract object content from a Value"
    r07009 | "value" | "Types" | "string" | "validates" | "Check if a value is a string"
    r07010 | "value" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r07011 | "value" | "Types" | "float" | "validates" | "Check if a value is a float"
    r07012 | "value" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r07013 | "value" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r07014 | "value" | "Types" | "character" | "validates" | "Check if a value is a character"
    r07015 | "value" | "Types" | "integer" | "creates" | "Get the numeric content of a Value"
    r07016 | "value" | "Types" | "float" | "creates" | "Extract floating-point content from a Value"
    r07017 | "value" | "Types" | "decimal" | "creates" | "Extract decimal content from a Value"
    r07018 | "value" | "Types" | "string" | "creates" | "Convert a value to a string"
    r07019 | "value" | "Types" | "string" | "creates" | "Serialize a JSON value to a string"
    r07020 | "value" | "Types" | "string" | "creates" | "Serialize a TOML value to a string"
    r07021 | "value" | "Types" | "string" | "creates" | "Serialize a CSV value to a string"
    r07022 | "value" | "Types" | "boolean" | "creates" | "Extract boolean content from a Value"
    r07023 | "value" | "Types" | "text" | "validates" | "Check if a Value is text"
    r07024 | "value" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r07025 | "value" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r07026 | "value" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r07027 | "value" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r07028 | "value" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r07029 | "value" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r07030 | "value" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r07031 | "value" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r07032 | "value" | "Types" | "value" | "validates" | "Check if an Option<Float> is a Value and not Unit"
    r07033 | "value" | "Types" | "value" | "validates" | "Check if an Option<Decimal> is a Value and not Unit"
    r07034 | "value" | "Types" | "value" | "validates" | "Check if an Option<Boolean> is a Value and not Unit"
    r07035 | "values" | "Table" | "values" | "derives" | "Get all values in a table"
    r07036 | "values" | "Table" | "values" | "derives" | "Get all values in a table"
    r07037 | "values" | "Table" | "values" | "derives" | "Get all values in a table"
    r07038 | "variant" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r07039 | "variant" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r07040 | "variant" | "Store" | "variant" | "creates" | "Get the variant name from a conflict"
    r07041 | "verify" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r07042 | "verify" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r07043 | "verify" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r07044 | "verify" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r07045 | "verify" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r07046 | "verify" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r07047 | "verify" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r07048 | "verify" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r07049 | "verify" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r07050 | "verify" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r07051 | "verify" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r07052 | "verify" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r07053 | "version" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r07054 | "version" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r07055 | "version" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r07056 | "versions" | "Store" | "version" | "inputs" | "List all versions of a table"
    r07057 | "versions" | "Store" | "version" | "inputs" | "List all versions of a table"
    r07058 | "versions" | "Store" | "version" | "inputs" | "List all versions of a table"
    r07059 | "via" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r07060 | "via" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r07061 | "via" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r07062 | "waiting" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r07063 | "walk" | "Main" | "walk" | "outputs" | "Walk a node tree, printing each node's kind and line number"
    r07064 | "warning" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r07065 | "warning" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r07066 | "warning" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r07067 | "wave" | "Main" | "sine_sample" | "derives" | "Generate a sine wave sample at a given index"
    r07068 | "way" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r07069 | "way" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r07070 | "way" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r07071 | "way" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r07072 | "way" | "Store" | "merge" | "creates" | "Three-way merge of two diffs against a base table"
    r07073 | "way" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r07074 | "weekday" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r07075 | "weekday" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r07076 | "weekday" | "Time" | "weekday" | "creates" | "Get weekday from a date"
    r07077 | "weekend" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r07078 | "weekend" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r07079 | "weekend" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r07080 | "when" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r07081 | "when" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r07082 | "when" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r07083 | "when" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r07084 | "when" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r07085 | "when" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r07086 | "when" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r07087 | "when" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r07088 | "where" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r07089 | "where" | "Main" | "atkin_step1_y" | "creates" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r07090 | "where" | "Main" | "atkin_step2_y" | "creates" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r07091 | "where" | "Main" | "atkin_step3_y" | "creates" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r07092 | "where" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r07093 | "where" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r07094 | "whether" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r07095 | "whether" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r07096 | "whether" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r07097 | "whether" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r07098 | "whether" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r07099 | "whether" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r07100 | "whether" | "Prove" | "error" | "validates" | "Whether the node represents a syntax error."
    r07101 | "whether" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r07102 | "whether" | "Terminal" | "terminal" | "validates" | "Check if stdout is connected to an interactive terminal.
Returns false when output is piped or redirected to a file.
Use to decide whether to enter raw mode or fall back to plain output."
    r07103 | "whitespace" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r07104 | "whitespace" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r07105 | "whitespace" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r07106 | "whitespace" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r07107 | "whitespace" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r07108 | "whitespace" | "Text" | "trim" | "derives" | "Trim whitespace from both ends of a string"
    r07109 | "widget" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07110 | "widget" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07111 | "widget" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07112 | "width" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r07113 | "width" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r07114 | "width" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r07115 | "width" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r07116 | "width" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r07117 | "width" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r07118 | "width" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r07119 | "width" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r07120 | "width" | "Format" | "pad_left" | "derives" | "Pads a string on the left to reach the specified width."
    r07121 | "width" | "Format" | "pad_right" | "derives" | "Pads a string on the right to reach the specified width."
    r07122 | "width" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r07123 | "width" | "Terminal" | "size" | "derives" | "Get the current terminal dimensions in character cells.
Returns a Position where x = columns (width) and y = rows (height).
Updates automatically on Resize events."
    r07124 | "window" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07125 | "window" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r07126 | "window" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07127 | "window" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r07128 | "window" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07129 | "window" | "Graphic" | "quit" | "outputs" | "Programmatically close the window and exit the render loop.
Triggers the Exit event. Use for menu-driven quit or error shutdown."
    r07130 | "with" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r07131 | "with" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r07132 | "with" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07133 | "with" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r07134 | "with" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r07135 | "with" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r07136 | "with" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r07137 | "with" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r07138 | "with" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r07139 | "with" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r07140 | "with" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r07141 | "with" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r07142 | "with" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r07143 | "with" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r07144 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07145 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07146 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07147 | "with" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r07148 | "with" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r07149 | "with" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r07150 | "with" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r07151 | "with" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r07152 | "with" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r07153 | "with" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r07154 | "with" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r07155 | "with" | "Main" | "generate" | "creates" | "Generate an n x n matrix with seed scaling"
    r07156 | "with" | "Main" | "dot" | "creates" | "Dot product of row i of a with row j of bt (transposed b)"
    r07157 | "with" | "Main" | "multiply" | "creates" | "Multiply two matrices: a * b with transpose optimization"
    r07158 | "with" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r07159 | "with" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r07160 | "with" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r07161 | "with" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07162 | "with" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r07163 | "with" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r07164 | "with" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r07165 | "with" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r07166 | "with" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r07167 | "with" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r07168 | "with" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r07169 | "with" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r07170 | "with" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r07171 | "with" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r07172 | "with" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r07173 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07174 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07175 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07176 | "with" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r07177 | "with" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r07178 | "with" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r07179 | "with" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r07180 | "with" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r07181 | "with" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r07182 | "with" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r07183 | "with" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r07184 | "with" | "Format" | "decimal" | "creates" | "Formats a number with the specified decimal places."
    r07185 | "with" | "Format" | "decimal" | "creates" | "Formats a float with the specified decimal places."
    r07186 | "with" | "Graphic" | "window" | "outputs" | "Create or begin a named window with the given dimensions in pixels.
Must be called once per frame in the Draw arm before any widget calls.
Subsequent widget calls render inside this window."
    r07187 | "with" | "Graphic" | "button" | "outputs" | "Render a clickable button with a text label.
Returns true on the frame the button is clicked, false otherwise.
Use in a match to branch on click."
    r07188 | "with" | "Graphic" | "text_input" | "outputs" | "Render an editable text field with a label and current value.
Returns the current string contents (updated as user types).
Pass the returned value back on the next frame to preserve input."
    r07189 | "with" | "Graphic" | "checkbox" | "outputs" | "Render a checkbox with a unique integer ID, label, and checked state.
The ID ensures each checkbox is unique even if labels are identical.
Returns the current boolean state (toggled when user clicks).
Pass the returned value back on the next frame to preserve state."
    r07190 | "with" | "Graphic" | "slider" | "outputs" | "Render a horizontal slider with a label, min/max range, and current value.
Returns the current float value (updated as user drags).
Pass the returned value back on the next frame to preserve position."
    r07191 | "with" | "Parse" | "arguments" | "creates" | "Parse command line arguments into a Value with args and kwargs"
    r07192 | "with" | "Pattern" | "replace" | "derives" | "Replace all occurrences of a pattern with a replacement string"
    r07193 | "with" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message
with severity and timestamp added"
    r07194 | "with" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message
with severity and timestamp added"
    r07195 | "with" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message
with severity and timestamp added"
    r07196 | "with" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message
with severity and timestamp added"
    r07197 | "with" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r07198 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07199 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07200 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r07201 | "with" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r07202 | "with" | "Store" | "merge" | "creates" | "Three-way merge with a conflict resolver"
    r07203 | "with" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r07204 | "with" | "Terminal" | "raw" | "outputs" | "Enable raw mode: disables line buffering, echo, and signal processing.
Input is delivered character-by-character via KeyDown events.
Must be paired with cooked() — see atexit safety note."
    r07205 | "with" | "Terminal" | "ansi" | "derives" | "Convert a Color name to its ANSI SGR escape sequence.
Use with UI Color lookup: ansi(Color:Blue) returns \"\\033[34m\".
Returns empty string for unknown names."
    r07206 | "with" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r07207 | "with" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r07208 | "with" | "Text" | "join" | "creates" | "Join a list of strings with a separator"
    r07209 | "within" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r07210 | "within" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r07211 | "within" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r07212 | "within" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r07213 | "within" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r07214 | "within" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r07215 | "within" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r07216 | "within" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r07217 | "within" | "Format" | "center" | "derives" | "Centers a string within the specified width."
    r07218 | "within" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r07219 | "within" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r07220 | "within" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r07221 | "without" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r07222 | "without" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r07223 | "without" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r07224 | "without" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r07225 | "without" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r07226 | "without" | "Path" | "stem" | "derives" | "Get the filename without extension"
    r07227 | "without" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r07228 | "word" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r07229 | "word" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r07230 | "word" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r07231 | "word" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r07232 | "word" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r07233 | "word" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r07234 | "word" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r07235 | "word" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r07236 | "word" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r07237 | "word" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r07238 | "word" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r07239 | "word" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r07240 | "word" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r07241 | "word" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r07242 | "word" | "Language" | "stem" | "derives" | "Apply the Porter stemming algorithm to a word"
    r07243 | "word" | "Language" | "root" | "derives" | "Strip common suffixes to find the root form of a word"
    r07244 | "word" | "Language" | "soundex" | "derives" | "Compute the Soundex phonetic code for a word"
    r07245 | "word" | "Language" | "metaphone" | "derives" | "Compute the Double Metaphone phonetic code for a word"
    r07246 | "word" | "Language" | "ngrams" | "creates" | "Extract word-level n-grams from text"
    r07247 | "word" | "Language" | "bigrams" | "creates" | "Extract word-level bigrams from text"
    r07248 | "word" | "Language" | "frequency" | "creates" | "Count word frequencies in text and return a table of word to count"
    r07249 | "words" | "Language" | "words" | "creates" | "Extract individual words from text"
    r07250 | "words" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r07251 | "words" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r07252 | "words" | "Language" | "words" | "creates" | "Extract individual words from text"
    r07253 | "words" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r07254 | "words" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r07255 | "words" | "Language" | "words" | "creates" | "Extract individual words from text"
    r07256 | "words" | "Language" | "without_stopwords" | "creates" | "Remove common stopwords from text and return remaining words"
    r07257 | "words" | "Language" | "keywords" | "creates" | "Extract the top N most frequent words from text"
    r07258 | "working" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r07259 | "working" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r07260 | "working" | "System" | "cwd" | "inputs" | "Get the current working directory"
    r07261 | "write" | "Network" | "message" | "outputs" | "Write data to a socket"
    r07262 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r07263 | "write" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r07264 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07265 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07266 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07267 | "write" | "System" | "file" | "outputs" | "Write content to a file"
    r07268 | "write" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r07269 | "write" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r07270 | "write" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r07271 | "write" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r07272 | "write" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r07273 | "write" | "Network" | "message" | "outputs" | "Write data to a socket"
    r07274 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r07275 | "write" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r07276 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07277 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07278 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07279 | "write" | "System" | "file" | "outputs" | "Write content to a file"
    r07280 | "write" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r07281 | "write" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r07282 | "write" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r07283 | "write" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r07284 | "write" | "Network" | "message" | "outputs" | "Write data to a socket"
    r07285 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write: returns new list)"
    r07286 | "write" | "Sequence" | "remove" | "derives" | "Remove element at index (copy-on-write: returns new list)"
    r07287 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07288 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07289 | "write" | "Sequence" | "set" | "derives" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r07290 | "write" | "System" | "file" | "outputs" | "Write content to a file"
    r07291 | "write" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r07292 | "write" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r07293 | "write" | "Terminal" | "terminal" | "outputs" | "Write text at a specific screen position (column x, row y).
Top-left corner is (0, 0). Coordinates are in character cells."
    r07294 | "write" | "Terminal" | "cursor" | "outputs" | "Move the cursor to screen position (column x, row y).
Does not clear or write — just repositions for the next output."
    r07295 | "writes" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r07296 | "writes" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r07297 | "writes" | "Terminal" | "terminal" | "outputs" | "Write text at the current cursor position.
Does not move the cursor afterward — subsequent writes append."
    r07298 | "writing" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r07299 | "writing" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r07300 | "writing" | "System" | "writer" | "inputs" | "Open a file for appending (returns a file handle for writing)"
    r07301 | "year" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r07302 | "year" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r07303 | "year" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r07304 | "zero" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r07305 | "zero" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"
    r07306 | "zero" | "Types" | "boolean" | "creates" | "Convert an integer to a boolean (0 = false, non-zero = true)"

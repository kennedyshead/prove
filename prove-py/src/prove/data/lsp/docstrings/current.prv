// Docstring knowledge base for LSP intent completions
// Auto-generated by prove-py/scripts/build_stores.py
type DocstringMap:[Lookup] is String String String String String where
    r00000 | "absolute" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r00001 | "absolute" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r00002 | "absolute" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00003 | "absolute" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r00004 | "absolute" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r00005 | "absolute" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00006 | "accented" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00007 | "accented" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00008 | "accented" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00009 | "accented" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00010 | "accept" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00011 | "accept" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00012 | "accept" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00013 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00014 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00015 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00016 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00017 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00018 | "accumulate" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00019 | "accumulate" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r00020 | "accumulate" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r00021 | "add" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00022 | "add" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00023 | "add" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r00024 | "add" | "Time" | "date" | "transforms" | "Add days to a date"
    r00025 | "add" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r00026 | "add" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00027 | "add" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r00028 | "add" | "Time" | "date" | "transforms" | "Add days to a date"
    r00029 | "add" | "Simple_math" | "add" | "transforms" | "add(1, 2) == 3"
    r00030 | "added" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00031 | "added" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00032 | "added" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00033 | "added" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00034 | "added" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00035 | "added" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00036 | "added" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00037 | "added" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00038 | "against" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00039 | "against" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00040 | "algorithm" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r00041 | "algorithm" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r00042 | "all" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r00043 | "all" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r00044 | "all" | "Store" | "version" | "inputs" | "List all versions of a table"
    r00045 | "all" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r00046 | "all" | "Table" | "values" | "reads" | "Get all values in a table"
    r00047 | "all" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r00048 | "all" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r00049 | "all" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r00050 | "all" | "Store" | "version" | "inputs" | "List all versions of a table"
    r00051 | "all" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r00052 | "all" | "Table" | "values" | "reads" | "Get all values in a table"
    r00053 | "all" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r00054 | "all" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r00055 | "all" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r00056 | "all" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r00057 | "alphabetic" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00058 | "alphabetic" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00059 | "alphanumeric" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00060 | "alphanumeric" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00061 | "and" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00062 | "and" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00063 | "and" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r00064 | "and" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00065 | "and" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r00066 | "and" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r00067 | "and" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00068 | "and" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00069 | "and" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00070 | "and" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00071 | "and" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00072 | "and" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r00073 | "and" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00074 | "and" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00075 | "and" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00076 | "and" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00077 | "and" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00078 | "and" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00079 | "and" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00080 | "and" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00081 | "and" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r00082 | "and" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00083 | "and" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00084 | "and" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00085 | "and" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00086 | "and" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00087 | "and" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r00088 | "and" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00089 | "and" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r00090 | "and" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r00091 | "and" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00092 | "and" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00093 | "and" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00094 | "and" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00095 | "and" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00096 | "and" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r00097 | "and" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00098 | "and" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00099 | "and" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00100 | "and" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00101 | "and" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00102 | "and" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r00103 | "and" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00104 | "and" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00105 | "and" | "Main" | "parse_char" | "transforms" | "Parse one BF character and emit opcodes"
    r00106 | "and" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r00107 | "and" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r00108 | "append" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r00109 | "append" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00110 | "append" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r00111 | "append" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00112 | "appending" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r00113 | "appending" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r00114 | "apply" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r00115 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00116 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00117 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00118 | "apply" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00119 | "apply" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r00120 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00121 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00122 | "apply" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00123 | "apply" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00124 | "are" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00125 | "are" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00126 | "arguments" | "System" | "process" | "inputs" | "Get command line arguments"
    r00127 | "arguments" | "System" | "process" | "inputs" | "Get command line arguments"
    r00128 | "array" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00129 | "array" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00130 | "array" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00131 | "array" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00132 | "array" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00133 | "array" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r00134 | "array" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00135 | "array" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00136 | "array" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00137 | "array" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00138 | "array" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r00139 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00140 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00141 | "array" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00142 | "array" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00143 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00144 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00145 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00146 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00147 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00148 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00149 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00150 | "array" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00151 | "array" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00152 | "array" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00153 | "array" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00154 | "array" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00155 | "array" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00156 | "array" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r00157 | "array" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00158 | "array" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00159 | "array" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00160 | "array" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00161 | "array" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r00162 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00163 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00164 | "array" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00165 | "array" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00166 | "array" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00167 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00168 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00169 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00170 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00171 | "array" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00172 | "array" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00173 | "array" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00174 | "array" | "Main" | "count_ops" | "reads" | "Count BF opcodes in source for array sizing"
    r00175 | "arrays" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00176 | "arrays" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00177 | "ascii" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00178 | "ascii" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00179 | "ascii" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00180 | "ascii" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00181 | "atkin" | "Main" | "atkin_step1_y" | "transforms" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r00182 | "atkin" | "Main" | "atkin_step1_x" | "transforms" | "Sieve of Atkin step 1: iterate x"
    r00183 | "atkin" | "Main" | "atkin_step2_y" | "transforms" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r00184 | "atkin" | "Main" | "atkin_step2_x" | "transforms" | "Sieve of Atkin step 2: iterate x"
    r00185 | "atkin" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r00186 | "atkin" | "Main" | "atkin_step3_x" | "transforms" | "Sieve of Atkin step 3: iterate x"
    r00187 | "await" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00188 | "back" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00189 | "backed" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00190 | "backed" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r00191 | "base" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r00192 | "base" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00193 | "base" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00194 | "base" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r00195 | "base" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00196 | "base" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r00197 | "base" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00198 | "base" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00199 | "base" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r00200 | "base" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00201 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00202 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00203 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00204 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00205 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00206 | "because" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00207 | "between" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00208 | "between" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r00209 | "between" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00210 | "between" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00211 | "between" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00212 | "between" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00213 | "between" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00214 | "between" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00215 | "between" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r00216 | "between" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00217 | "between" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00218 | "between" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00219 | "between" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00220 | "between" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00221 | "bigrams" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r00222 | "bigrams" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r00223 | "binary" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r00224 | "binary" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r00225 | "bind" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00226 | "bind" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r00227 | "bits" | "Main" | "atkin_step1_y" | "transforms" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r00228 | "bits" | "Main" | "atkin_step2_y" | "transforms" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r00229 | "bits" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r00230 | "blake" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00231 | "blake" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r00232 | "blake" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00233 | "blake" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00234 | "blake" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r00235 | "blake" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00236 | "blank" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00237 | "blocking" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00238 | "boolean" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r00239 | "boolean" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00240 | "boolean" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00241 | "boolean" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r00242 | "boolean" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00243 | "boolean" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r00244 | "boolean" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r00245 | "boolean" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00246 | "boolean" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r00247 | "boolean" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00248 | "both" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r00249 | "both" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r00250 | "bounds" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00251 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00252 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00253 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00254 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00255 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00256 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00257 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00258 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00259 | "bounds" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00260 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00261 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00262 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00263 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00264 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00265 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00266 | "bounds" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00267 | "bounds" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00268 | "brackets" | "Main" | "parse_char_bracket" | "transforms" | "Continue parsing: check for open/close brackets"
    r00269 | "build" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00270 | "build" | "Commands" | "build" | "outputs" | "Run the build command"
    r00271 | "build" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00272 | "build" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r00273 | "builder" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00274 | "builder" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r00275 | "builder" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00276 | "builder" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00277 | "builder" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00278 | "builder" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00279 | "builder" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r00280 | "builder" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00281 | "builder" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r00282 | "builder" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00283 | "byte" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00284 | "byte" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00285 | "byte" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00286 | "byte" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r00287 | "byte" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00288 | "byte" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00289 | "byte" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00290 | "byte" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00291 | "byte" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00292 | "byte" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00293 | "byte" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00294 | "byte" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00295 | "byte" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00296 | "byte" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00297 | "byte" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r00298 | "byte" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00299 | "byte" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00300 | "byte" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00301 | "byte" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00302 | "byte" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00303 | "byte" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00304 | "byte" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00305 | "bytearray" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00306 | "bytearray" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00307 | "bytearray" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r00308 | "bytearray" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00309 | "bytearray" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00310 | "bytearray" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00311 | "bytearray" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r00312 | "bytearray" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00313 | "bytes" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00314 | "bytes" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00315 | "bytes" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00316 | "bytes" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r00317 | "bytes" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00318 | "called" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00319 | "case" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00320 | "case" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00321 | "ceiling" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r00322 | "ceiling" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r00323 | "centers" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r00324 | "centers" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r00325 | "character" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00326 | "character" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00327 | "character" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00328 | "character" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00329 | "character" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00330 | "character" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00331 | "character" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r00332 | "character" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00333 | "character" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00334 | "character" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00335 | "character" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00336 | "character" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00337 | "character" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00338 | "character" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00339 | "character" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00340 | "character" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00341 | "character" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00342 | "character" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r00343 | "character" | "Text" | "char" | "transforms" | "Append a character to a builder"
    r00344 | "character" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00345 | "character" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00346 | "character" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00347 | "character" | "Main" | "parse_char" | "transforms" | "Parse one BF character and emit opcodes"
    r00348 | "characters" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00349 | "characters" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00350 | "characters" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r00351 | "characters" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00352 | "check" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00353 | "check" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00354 | "check" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00355 | "check" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r00356 | "check" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r00357 | "check" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00358 | "check" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00359 | "check" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00360 | "check" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00361 | "check" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00362 | "check" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00363 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r00364 | "check" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r00365 | "check" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00366 | "check" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00367 | "check" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r00368 | "check" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00369 | "check" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00370 | "check" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r00371 | "check" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00372 | "check" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00373 | "check" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00374 | "check" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r00375 | "check" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00376 | "check" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00377 | "check" | "Types" | "string" | "validates" | "Check if a value is a string"
    r00378 | "check" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r00379 | "check" | "Types" | "float" | "validates" | "Check if a value is a float"
    r00380 | "check" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00381 | "check" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00382 | "check" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00383 | "check" | "Types" | "text" | "validates" | "Check if a Value is text"
    r00384 | "check" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r00385 | "check" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00386 | "check" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00387 | "check" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00388 | "check" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r00389 | "check" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r00390 | "check" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r00391 | "check" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00392 | "check" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00393 | "check" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r00394 | "check" | "Commands" | "check" | "outputs" | "Run the check command"
    r00395 | "check" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00396 | "check" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r00397 | "check" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00398 | "check" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r00399 | "check" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r00400 | "check" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r00401 | "check" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00402 | "check" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00403 | "check" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r00404 | "check" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r00405 | "check" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00406 | "check" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r00407 | "check" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r00408 | "check" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00409 | "check" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r00410 | "check" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r00411 | "check" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00412 | "check" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00413 | "check" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r00414 | "check" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00415 | "check" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00416 | "check" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00417 | "check" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r00418 | "check" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00419 | "check" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00420 | "check" | "Types" | "string" | "validates" | "Check if a value is a string"
    r00421 | "check" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r00422 | "check" | "Types" | "float" | "validates" | "Check if a value is a float"
    r00423 | "check" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00424 | "check" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r00425 | "check" | "Types" | "character" | "validates" | "Check if a value is a character"
    r00426 | "check" | "Types" | "text" | "validates" | "Check if a Value is text"
    r00427 | "check" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r00428 | "check" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00429 | "check" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r00430 | "check" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r00431 | "check" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r00432 | "check" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r00433 | "check" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r00434 | "check" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00435 | "check" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r00436 | "check" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r00437 | "check" | "Main" | "parse_char_minus" | "transforms" | "Continue parsing: check for minus"
    r00438 | "check" | "Main" | "parse_char_right" | "transforms" | "Continue parsing: check for right"
    r00439 | "check" | "Main" | "parse_char_left" | "transforms" | "Continue parsing: check for left"
    r00440 | "check" | "Main" | "parse_char_dot" | "transforms" | "Continue parsing: check for dot (print)"
    r00441 | "check" | "Main" | "parse_char_bracket" | "transforms" | "Continue parsing: check for open/close brackets"
    r00442 | "checks" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00443 | "checks" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00444 | "checks" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00445 | "checks" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00446 | "checks" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00447 | "checks" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00448 | "checks" | "Character" | "alphabetic" | "validates" | "Checks if a character is alphabetic (a-z, A-Z)."
    r00449 | "checks" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00450 | "checks" | "Character" | "alphanumeric" | "validates" | "Checks if a character is alphanumeric (a-z, A-Z, 0-9)."
    r00451 | "checks" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r00452 | "checks" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r00453 | "checks" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r00454 | "chunk" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00455 | "chunk" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00456 | "chunk" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00457 | "chunks" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00458 | "clamp" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00459 | "clamp" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00460 | "clamp" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r00461 | "clamp" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r00462 | "clock" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00463 | "clock" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r00464 | "clock" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00465 | "clock" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00466 | "clock" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00467 | "clock" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r00468 | "clock" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00469 | "clock" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00470 | "close" | "Network" | "socket" | "outputs" | "Close a connection"
    r00471 | "close" | "System" | "close" | "outputs" | "Close an open file handle"
    r00472 | "close" | "Network" | "socket" | "outputs" | "Close a connection"
    r00473 | "close" | "System" | "close" | "outputs" | "Close an open file handle"
    r00474 | "close" | "Main" | "parse_char_bracket" | "transforms" | "Continue parsing: check for open/close brackets"
    r00475 | "code" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r00476 | "code" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r00477 | "code" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r00478 | "code" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00479 | "code" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00480 | "code" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r00481 | "code" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r00482 | "code" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r00483 | "code" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r00484 | "code" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00485 | "collect" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r00486 | "collect" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r00487 | "column" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00488 | "column" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00489 | "columns" | "Main" | "gen_cols" | "transforms" | "Generate matrix: fill row by iterating columns"
    r00490 | "columns" | "Main" | "transpose_cols" | "transforms" | "Transpose: copy columns of src into rows of dst"
    r00491 | "combined" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00492 | "combined" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00493 | "comma" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r00494 | "comma" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r00495 | "command" | "System" | "system" | "inputs" | "Execute a system command"
    r00496 | "command" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r00497 | "command" | "System" | "process" | "inputs" | "Get command line arguments"
    r00498 | "command" | "Commands" | "build" | "outputs" | "Run the build command"
    r00499 | "command" | "Commands" | "check" | "outputs" | "Run the check command"
    r00500 | "command" | "Commands" | "compiler" | "outputs" | "Run the compiler command"
    r00501 | "command" | "Commands" | "format" | "outputs" | "Run the format command"
    r00502 | "command" | "Commands" | "lsp" | "outputs" | "Run the lsp command"
    r00503 | "command" | "Commands" | "new" | "outputs" | "Run the new command"
    r00504 | "command" | "Commands" | "test" | "outputs" | "Run the test command"
    r00505 | "command" | "Commands" | "view" | "outputs" | "Run the view command"
    r00506 | "command" | "System" | "system" | "inputs" | "Execute a system command"
    r00507 | "command" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r00508 | "command" | "System" | "process" | "inputs" | "Get command line arguments"
    r00509 | "common" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r00510 | "common" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r00511 | "common" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r00512 | "common" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r00513 | "common" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r00514 | "common" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r00515 | "compile" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r00516 | "compile" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r00517 | "compiled" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r00518 | "compiled" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r00519 | "compiler" | "Commands" | "compiler" | "outputs" | "Run the compiler command"
    r00520 | "component" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r00521 | "component" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r00522 | "component" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r00523 | "component" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r00524 | "component" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r00525 | "component" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r00526 | "components" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r00527 | "components" | "Path" | "join" | "transforms" | "Join path components together"
    r00528 | "components" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00529 | "components" | "Time" | "date" | "creates" | "Create a date from components"
    r00530 | "components" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00531 | "components" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00532 | "components" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r00533 | "components" | "Path" | "join" | "transforms" | "Join path components together"
    r00534 | "components" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00535 | "components" | "Time" | "date" | "creates" | "Create a date from components"
    r00536 | "components" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00537 | "components" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r00538 | "compute" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00539 | "compute" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r00540 | "compute" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r00541 | "compute" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r00542 | "compute" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00543 | "compute" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r00544 | "compute" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00545 | "compute" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00546 | "compute" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r00547 | "compute" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r00548 | "compute" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r00549 | "compute" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00550 | "compute" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r00551 | "compute" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00552 | "concatenate" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00553 | "concatenate" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r00554 | "concurrency" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r00555 | "concurrency" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r00556 | "config" | "Config" | "config" | "transforms" | "Parse the toml data to Config struct"
    r00557 | "conflict" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r00558 | "conflict" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r00559 | "conflict" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00560 | "conflict" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r00561 | "conflict" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r00562 | "conflict" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r00563 | "conflict" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r00564 | "conflict" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r00565 | "conflict" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r00566 | "conflict" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r00567 | "conflicted" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00568 | "conflicted" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00569 | "conflicts" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00570 | "conflicts" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00571 | "conflicts" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r00572 | "conflicts" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r00573 | "connection" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00574 | "connection" | "Network" | "socket" | "outputs" | "Close a connection"
    r00575 | "connection" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00576 | "connection" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00577 | "connection" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r00578 | "connection" | "Network" | "socket" | "outputs" | "Close a connection"
    r00579 | "connection" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r00580 | "connection" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r00581 | "connections" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00582 | "console" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00583 | "console" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00584 | "console" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00585 | "console" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00586 | "console" | "System" | "console" | "outputs" | "Print text to the console"
    r00587 | "console" | "System" | "console" | "inputs" | "Read a line from the console"
    r00588 | "console" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00589 | "console" | "System" | "console" | "validates" | "Validate console input"
    r00590 | "console" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00591 | "console" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00592 | "console" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r00593 | "console" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r00594 | "console" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00595 | "console" | "System" | "console" | "outputs" | "Print text to the console"
    r00596 | "console" | "System" | "console" | "inputs" | "Read a line from the console"
    r00597 | "console" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00598 | "console" | "System" | "console" | "validates" | "Validate console input"
    r00599 | "construct" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00600 | "construct" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r00601 | "consumed" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00602 | "contains" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00603 | "contains" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r00604 | "content" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r00605 | "content" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r00606 | "content" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r00607 | "content" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r00608 | "content" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r00609 | "content" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r00610 | "content" | "System" | "file" | "outputs" | "Write content to a file"
    r00611 | "content" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r00612 | "content" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r00613 | "content" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r00614 | "content" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r00615 | "content" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r00616 | "content" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r00617 | "content" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r00618 | "content" | "System" | "file" | "outputs" | "Write content to a file"
    r00619 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00620 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00621 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00622 | "contents" | "System" | "file" | "inputs" | "Read contents from a file"
    r00623 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00624 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00625 | "contents" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00626 | "contents" | "System" | "file" | "inputs" | "Read contents from a file"
    r00627 | "continue" | "Main" | "parse_char_minus" | "transforms" | "Continue parsing: check for minus"
    r00628 | "continue" | "Main" | "parse_char_right" | "transforms" | "Continue parsing: check for right"
    r00629 | "continue" | "Main" | "parse_char_left" | "transforms" | "Continue parsing: check for left"
    r00630 | "continue" | "Main" | "parse_char_dot" | "transforms" | "Continue parsing: check for dot (print)"
    r00631 | "continue" | "Main" | "parse_char_bracket" | "transforms" | "Continue parsing: check for open/close brackets"
    r00632 | "convert" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r00633 | "convert" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r00634 | "convert" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r00635 | "convert" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r00636 | "convert" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r00637 | "convert" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r00638 | "convert" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r00639 | "convert" | "Types" | "string" | "reads" | "Convert a float to a string"
    r00640 | "convert" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r00641 | "convert" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r00642 | "convert" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r00643 | "convert" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r00644 | "convert" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r00645 | "convert" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r00646 | "convert" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r00647 | "convert" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r00648 | "convert" | "Types" | "string" | "reads" | "Convert a float to a string"
    r00649 | "convert" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r00650 | "converts" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r00651 | "converts" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r00652 | "converts" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r00653 | "converts" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r00654 | "converts" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r00655 | "converts" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r00656 | "cooperative" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r00657 | "copy" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00658 | "copy" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00659 | "copy" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00660 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00661 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00662 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00663 | "copy" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00664 | "copy" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00665 | "copy" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00666 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00667 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00668 | "copy" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r00669 | "copy" | "Main" | "transpose_cols" | "transforms" | "Transpose: copy columns of src into rows of dst"
    r00670 | "count" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r00671 | "count" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00672 | "count" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r00673 | "count" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r00674 | "count" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r00675 | "count" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r00676 | "count" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r00677 | "count" | "Main" | "count_ops" | "reads" | "Count BF opcodes in source for array sizing"
    r00678 | "create" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00679 | "create" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r00680 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00681 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00682 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00683 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00684 | "create" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00685 | "create" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00686 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00687 | "create" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00688 | "create" | "System" | "dir" | "outputs" | "Create a directory"
    r00689 | "create" | "Table" | "new" | "creates" | "Create a new empty table"
    r00690 | "create" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00691 | "create" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00692 | "create" | "Time" | "date" | "creates" | "Create a date from components"
    r00693 | "create" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00694 | "create" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00695 | "create" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00696 | "create" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r00697 | "create" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r00698 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00699 | "create" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00700 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00701 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00702 | "create" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00703 | "create" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00704 | "create" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00705 | "create" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00706 | "create" | "System" | "dir" | "outputs" | "Create a directory"
    r00707 | "create" | "Table" | "new" | "creates" | "Create a new empty table"
    r00708 | "create" | "Text" | "builder" | "creates" | "Create a new string builder"
    r00709 | "create" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00710 | "create" | "Time" | "date" | "creates" | "Create a date from components"
    r00711 | "create" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00712 | "create" | "Time" | "clock" | "creates" | "Create a clock from components"
    r00713 | "create" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r00714 | "csv" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r00715 | "csv" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r00716 | "csv" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r00717 | "csv" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r00718 | "csv" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r00719 | "csv" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r00720 | "ctrl" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00721 | "current" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00722 | "current" | "Time" | "time" | "inputs" | "Get current time"
    r00723 | "current" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r00724 | "current" | "Time" | "time" | "inputs" | "Get current time"
    r00725 | "data" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r00726 | "data" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r00727 | "data" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00728 | "data" | "Network" | "message" | "inputs" | "Read data from a socket"
    r00729 | "data" | "Network" | "message" | "outputs" | "Write data to a socket"
    r00730 | "data" | "Config" | "config" | "transforms" | "Parse the toml data to Config struct"
    r00731 | "data" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r00732 | "data" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r00733 | "data" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r00734 | "data" | "Network" | "message" | "inputs" | "Read data from a socket"
    r00735 | "data" | "Network" | "message" | "outputs" | "Write data to a socket"
    r00736 | "data" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r00737 | "date" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r00738 | "date" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r00739 | "date" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00740 | "date" | "Time" | "date" | "reads" | "Extract date from a time"
    r00741 | "date" | "Time" | "date" | "creates" | "Create a date from components"
    r00742 | "date" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00743 | "date" | "Time" | "date" | "transforms" | "Add days to a date"
    r00744 | "date" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00745 | "date" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r00746 | "date" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00747 | "date" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r00748 | "date" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r00749 | "date" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r00750 | "date" | "Time" | "date" | "reads" | "Extract date from a time"
    r00751 | "date" | "Time" | "date" | "creates" | "Create a date from components"
    r00752 | "date" | "Time" | "date" | "validates" | "Check if a date is valid"
    r00753 | "date" | "Time" | "date" | "transforms" | "Add days to a date"
    r00754 | "date" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00755 | "date" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r00756 | "date" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r00757 | "datetime" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r00758 | "datetime" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r00759 | "datetime" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00760 | "datetime" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r00761 | "datetime" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00762 | "datetime" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00763 | "datetime" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r00764 | "datetime" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r00765 | "datetime" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r00766 | "datetime" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r00767 | "datetime" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r00768 | "datetime" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r00769 | "datetime" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r00770 | "datetime" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r00771 | "days" | "Time" | "date" | "transforms" | "Add days to a date"
    r00772 | "days" | "Time" | "days" | "reads" | "Get number of days in a month"
    r00773 | "days" | "Time" | "date" | "transforms" | "Add days to a date"
    r00774 | "days" | "Time" | "days" | "reads" | "Get number of days in a month"
    r00775 | "debug" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00776 | "debug" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r00777 | "decimal" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r00778 | "decimal" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r00779 | "decimal" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r00780 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00781 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r00782 | "decimal" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00783 | "decimal" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r00784 | "decimal" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00785 | "decimal" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r00786 | "decimal" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r00787 | "decimal" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r00788 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r00789 | "decimal" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r00790 | "decimal" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r00791 | "decimal" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r00792 | "decimal" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r00793 | "decode" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00794 | "decode" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00795 | "decode" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r00796 | "decode" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r00797 | "decode" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r00798 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00799 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00800 | "default" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00801 | "default" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00802 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00803 | "default" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r00804 | "default" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r00805 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00806 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00807 | "default" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00808 | "default" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00809 | "default" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00810 | "default" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r00811 | "default" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r00812 | "diff" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00813 | "diff" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00814 | "diff" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r00815 | "diff" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r00816 | "difference" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00817 | "difference" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r00818 | "diffs" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00819 | "diffs" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r00820 | "digest" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00821 | "digest" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00822 | "digest" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00823 | "digest" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r00824 | "digest" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r00825 | "digest" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r00826 | "digit" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00827 | "digit" | "Character" | "digit" | "validates" | "Checks if a character is a digit (0-9)."
    r00828 | "digits" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r00829 | "directory" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r00830 | "directory" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00831 | "directory" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r00832 | "directory" | "System" | "dir" | "inputs" | "List entries in a directory"
    r00833 | "directory" | "System" | "dir" | "outputs" | "Create a directory"
    r00834 | "directory" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r00835 | "directory" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r00836 | "directory" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r00837 | "directory" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r00838 | "directory" | "System" | "dir" | "inputs" | "List entries in a directory"
    r00839 | "directory" | "System" | "dir" | "outputs" | "Create a directory"
    r00840 | "directory" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r00841 | "dispatch" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r00842 | "distance" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00843 | "distance" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00844 | "dot" | "Main" | "parse_char_dot" | "transforms" | "Continue parsing: check for dot (print)"
    r00845 | "dot" | "Main" | "dot" | "reads" | "Dot product of row i of a with row j of bt (transposed b)"
    r00846 | "double" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r00847 | "double" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r00848 | "double" | "Simple_math" | "double" | "transforms" | "double(5) == 10"
    r00849 | "doubles" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r00850 | "dst" | "Main" | "transpose_cols" | "transforms" | "Transpose: copy columns of src into rows of dst"
    r00851 | "duration" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r00852 | "duration" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r00853 | "duration" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00854 | "duration" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r00855 | "duration" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00856 | "duration" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r00857 | "duration" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r00858 | "duration" | "Time" | "duration" | "creates" | "Create a duration from components"
    r00859 | "duration" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r00860 | "duration" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r00861 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00862 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00863 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00864 | "each" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00865 | "each" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00866 | "each" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00867 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00868 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00869 | "each" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00870 | "echo" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r00871 | "echo" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r00872 | "echo" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r00873 | "edit" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00874 | "edit" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r00875 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00876 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r00877 | "element" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r00878 | "element" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r00879 | "element" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00880 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00881 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00882 | "element" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00883 | "element" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00884 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00885 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00886 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00887 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00888 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00889 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00890 | "element" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00891 | "element" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00892 | "element" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00893 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00894 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00895 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00896 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00897 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00898 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00899 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00900 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00901 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00902 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00903 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00904 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00905 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00906 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00907 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r00908 | "element" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r00909 | "element" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r00910 | "element" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r00911 | "element" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r00912 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00913 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00914 | "element" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00915 | "element" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r00916 | "element" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r00917 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00918 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00919 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00920 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r00921 | "element" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r00922 | "element" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00923 | "element" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00924 | "element" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r00925 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00926 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00927 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00928 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r00929 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00930 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00931 | "element" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r00932 | "element" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r00933 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00934 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00935 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00936 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00937 | "element" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r00938 | "element" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r00939 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00940 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00941 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00942 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00943 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00944 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00945 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00946 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00947 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00948 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00949 | "elements" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r00950 | "elements" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r00951 | "eliminate" | "Main" | "eliminate_multiples" | "transforms" | "Eliminate squares of primes: mark multiples of n*n"
    r00952 | "eliminate" | "Main" | "eliminate_squares" | "transforms" | "Eliminate squares of primes: iterate over sieve"
    r00953 | "emit" | "Main" | "parse_char" | "transforms" | "Parse one BF character and emit opcodes"
    r00954 | "empty" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00955 | "empty" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00956 | "empty" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00957 | "empty" | "Table" | "new" | "creates" | "Create a new empty table"
    r00958 | "empty" | "Bytes" | "bytearray" | "validates" | "Check if a byte array is empty"
    r00959 | "empty" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r00960 | "empty" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00961 | "empty" | "Table" | "new" | "creates" | "Create a new empty table"
    r00962 | "empty" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r00963 | "encode" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00964 | "encode" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00965 | "encode" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r00966 | "encode" | "Parse" | "base64" | "creates" | "Encode a byte array as Base64"
    r00967 | "encode" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r00968 | "encoded" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r00969 | "encoded" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r00970 | "end" | "Language" | "end" | "reads" | "Get the end position of a token"
    r00971 | "end" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r00972 | "end" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r00973 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00974 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00975 | "end" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r00976 | "end" | "Language" | "end" | "reads" | "Get the end position of a token"
    r00977 | "end" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r00978 | "end" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r00979 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r00980 | "end" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r00981 | "end" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r00982 | "ends" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00983 | "ends" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r00984 | "ends" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r00985 | "ends" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r00986 | "english" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r00987 | "english" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r00988 | "entries" | "System" | "dir" | "inputs" | "List entries in a directory"
    r00989 | "entries" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r00990 | "entries" | "System" | "dir" | "inputs" | "List entries in a directory"
    r00991 | "entries" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r00992 | "eof" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00993 | "eof" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r00994 | "eof" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r00995 | "equivalents" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00996 | "equivalents" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r00997 | "error" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r00998 | "error" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r00999 | "error" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01000 | "error" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r01001 | "events" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r01002 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01003 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01004 | "every" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01005 | "every" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01006 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01007 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01008 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01009 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01010 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01011 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01012 | "every" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01013 | "every" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01014 | "every" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01015 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01016 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01017 | "every" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01018 | "exactly" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01019 | "exactly" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01020 | "exactly" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01021 | "exclusive" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01022 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01023 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01024 | "exclusive" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01025 | "exclusive" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01026 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01027 | "exclusive" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01028 | "exclusive" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01029 | "execute" | "System" | "system" | "inputs" | "Execute a system command"
    r01030 | "execute" | "System" | "system" | "inputs" | "Execute a system command"
    r01031 | "exists" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r01032 | "exists" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01033 | "exists" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r01034 | "exists" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r01035 | "exists" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r01036 | "exists" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r01037 | "exists" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r01038 | "exists" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r01039 | "exists" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r01040 | "exists" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r01041 | "exists" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r01042 | "exists" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r01043 | "exists" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r01044 | "exists" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r01045 | "exit" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01046 | "exit" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r01047 | "exit" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01048 | "exit" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r01049 | "exits" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01050 | "exits" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01051 | "exits" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01052 | "exp" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r01053 | "exp" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r01054 | "expected" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01055 | "expected" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01056 | "expected" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01057 | "expected" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01058 | "expected" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01059 | "expected" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01060 | "extension" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01061 | "extension" | "Path" | "extension" | "reads" | "Get the file extension"
    r01062 | "extension" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01063 | "extension" | "Path" | "extension" | "reads" | "Get the file extension"
    r01064 | "extract" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r01065 | "extract" | "Language" | "words" | "reads" | "Extract individual words from text"
    r01066 | "extract" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01067 | "extract" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r01068 | "extract" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01069 | "extract" | "Time" | "date" | "reads" | "Extract date from a time"
    r01070 | "extract" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r01071 | "extract" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r01072 | "extract" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r01073 | "extract" | "Language" | "words" | "reads" | "Extract individual words from text"
    r01074 | "extract" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01075 | "extract" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r01076 | "extract" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01077 | "extract" | "Time" | "date" | "reads" | "Extract date from a time"
    r01078 | "extract" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r01079 | "extract" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r01080 | "fails" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01081 | "fails" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01082 | "falls" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r01083 | "falls" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r01084 | "falls" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r01085 | "falls" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r01086 | "fields" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r01087 | "fields" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r01088 | "fields" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r01089 | "fields" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r01090 | "file" | "Path" | "extension" | "reads" | "Get the file extension"
    r01091 | "file" | "System" | "file" | "inputs" | "Read contents from a file"
    r01092 | "file" | "System" | "file" | "outputs" | "Write content to a file"
    r01093 | "file" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r01094 | "file" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01095 | "file" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01096 | "file" | "System" | "close" | "outputs" | "Close an open file handle"
    r01097 | "file" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01098 | "file" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r01099 | "file" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01100 | "file" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01101 | "file" | "Path" | "extension" | "reads" | "Get the file extension"
    r01102 | "file" | "System" | "file" | "inputs" | "Read contents from a file"
    r01103 | "file" | "System" | "file" | "outputs" | "Write content to a file"
    r01104 | "file" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r01105 | "file" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01106 | "file" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01107 | "file" | "System" | "close" | "outputs" | "Close an open file handle"
    r01108 | "file" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01109 | "file" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r01110 | "filename" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r01111 | "filename" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01112 | "filename" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r01113 | "filename" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01114 | "fill" | "Main" | "gen_cols" | "transforms" | "Generate matrix: fill row by iterating columns"
    r01115 | "fill" | "Main" | "mul_cols" | "transforms" | "Multiply: fill one row of result"
    r01116 | "final" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r01117 | "final" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r01118 | "find" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r01119 | "find" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r01120 | "find" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01121 | "find" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r01122 | "find" | "Text" | "index" | "reads" | "Find the index of a substring"
    r01123 | "find" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r01124 | "find" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r01125 | "find" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01126 | "find" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r01127 | "find" | "Text" | "index" | "reads" | "Find the index of a substring"
    r01128 | "fire" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r01129 | "first" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01130 | "first" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r01131 | "first" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01132 | "first" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r01133 | "flip" | "Main" | "atkin_step1_y" | "transforms" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r01134 | "flip" | "Main" | "atkin_step2_y" | "transforms" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r01135 | "flip" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r01136 | "float" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r01137 | "float" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r01138 | "float" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r01139 | "float" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r01140 | "float" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01141 | "float" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r01142 | "float" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01143 | "float" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r01144 | "float" | "Types" | "float" | "validates" | "Check if a value is a float"
    r01145 | "float" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01146 | "float" | "Types" | "float" | "creates" | "Parse a string as a float"
    r01147 | "float" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r01148 | "float" | "Types" | "string" | "reads" | "Convert a float to a string"
    r01149 | "float" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r01150 | "float" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r01151 | "float" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r01152 | "float" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r01153 | "float" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01154 | "float" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r01155 | "float" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01156 | "float" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r01157 | "float" | "Types" | "float" | "validates" | "Check if a value is a float"
    r01158 | "float" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01159 | "float" | "Types" | "float" | "creates" | "Parse a string as a float"
    r01160 | "float" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r01161 | "float" | "Types" | "string" | "reads" | "Convert a float to a string"
    r01162 | "floats" | "Math" | "min" | "reads" | "Minimum of two floats"
    r01163 | "floats" | "Math" | "max" | "reads" | "Maximum of two floats"
    r01164 | "floats" | "Math" | "min" | "reads" | "Minimum of two floats"
    r01165 | "floats" | "Math" | "max" | "reads" | "Maximum of two floats"
    r01166 | "floor" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01167 | "floor" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01168 | "folding" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r01169 | "folding" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r01170 | "for" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r01171 | "for" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r01172 | "for" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01173 | "for" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01174 | "for" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01175 | "for" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01176 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01177 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01178 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01179 | "for" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01180 | "for" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01181 | "for" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r01182 | "for" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r01183 | "for" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01184 | "for" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01185 | "for" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01186 | "for" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01187 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01188 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01189 | "for" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01190 | "for" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01191 | "for" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01192 | "for" | "Main" | "parse_char_minus" | "transforms" | "Continue parsing: check for minus"
    r01193 | "for" | "Main" | "parse_char_right" | "transforms" | "Continue parsing: check for right"
    r01194 | "for" | "Main" | "parse_char_left" | "transforms" | "Continue parsing: check for left"
    r01195 | "for" | "Main" | "parse_char_dot" | "transforms" | "Continue parsing: check for dot (print)"
    r01196 | "for" | "Main" | "parse_char_bracket" | "transforms" | "Continue parsing: check for open/close brackets"
    r01197 | "for" | "Main" | "count_ops" | "reads" | "Count BF opcodes in source for array sizing"
    r01198 | "for" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r01199 | "for" | "Main" | "atkin_step1_y" | "transforms" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r01200 | "for" | "Main" | "atkin_step2_y" | "transforms" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r01201 | "for" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r01202 | "forget" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r01203 | "form" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r01204 | "form" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r01205 | "format" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r01206 | "format" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r01207 | "format" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r01208 | "format" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r01209 | "format" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r01210 | "format" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01211 | "format" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01212 | "format" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01213 | "format" | "Commands" | "format" | "outputs" | "Run the format command"
    r01214 | "format" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r01215 | "format" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r01216 | "format" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r01217 | "format" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r01218 | "format" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r01219 | "format" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01220 | "format" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01221 | "format" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01222 | "formats" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r01223 | "formats" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r01224 | "formats" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r01225 | "formats" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r01226 | "found" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01227 | "found" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01228 | "found" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r01229 | "framed" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01230 | "frequencies" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r01231 | "frequencies" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r01232 | "frequent" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01233 | "frequent" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01234 | "from" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01235 | "from" | "Language" | "words" | "reads" | "Extract individual words from text"
    r01236 | "from" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01237 | "from" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r01238 | "from" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r01239 | "from" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01240 | "from" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r01241 | "from" | "Network" | "message" | "inputs" | "Read data from a socket"
    r01242 | "from" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01243 | "from" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01244 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01245 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01246 | "from" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01247 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01248 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01249 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01250 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01251 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01252 | "from" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01253 | "from" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01254 | "from" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01255 | "from" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01256 | "from" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01257 | "from" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01258 | "from" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r01259 | "from" | "System" | "console" | "inputs" | "Read a line from the console"
    r01260 | "from" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01261 | "from" | "System" | "file" | "inputs" | "Read contents from a file"
    r01262 | "from" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01263 | "from" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01264 | "from" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01265 | "from" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r01266 | "from" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r01267 | "from" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01268 | "from" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r01269 | "from" | "Time" | "date" | "reads" | "Extract date from a time"
    r01270 | "from" | "Time" | "date" | "creates" | "Create a date from components"
    r01271 | "from" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r01272 | "from" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01273 | "from" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r01274 | "from" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r01275 | "from" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01276 | "from" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01277 | "from" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01278 | "from" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01279 | "from" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01280 | "from" | "Language" | "words" | "reads" | "Extract individual words from text"
    r01281 | "from" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01282 | "from" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r01283 | "from" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r01284 | "from" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01285 | "from" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r01286 | "from" | "Network" | "message" | "inputs" | "Read data from a socket"
    r01287 | "from" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01288 | "from" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01289 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01290 | "from" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01291 | "from" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01292 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01293 | "from" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01294 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01295 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01296 | "from" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01297 | "from" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01298 | "from" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01299 | "from" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01300 | "from" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01301 | "from" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01302 | "from" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01303 | "from" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r01304 | "from" | "System" | "console" | "inputs" | "Read a line from the console"
    r01305 | "from" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r01306 | "from" | "System" | "file" | "inputs" | "Read contents from a file"
    r01307 | "from" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01308 | "from" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01309 | "from" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01310 | "from" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r01311 | "from" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r01312 | "from" | "Time" | "duration" | "creates" | "Create a duration from components"
    r01313 | "from" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r01314 | "from" | "Time" | "date" | "reads" | "Extract date from a time"
    r01315 | "from" | "Time" | "date" | "creates" | "Create a date from components"
    r01316 | "from" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r01317 | "from" | "Time" | "datetime" | "creates" | "Create a datetime from date and clock"
    r01318 | "from" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r01319 | "from" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r01320 | "from" | "Time" | "clock" | "creates" | "Create a clock from components"
    r01321 | "from" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r01322 | "from" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r01323 | "full" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r01324 | "fully" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01325 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01326 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01327 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01328 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01329 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01330 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01331 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01332 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01333 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01334 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01335 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01336 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01337 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01338 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01339 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01340 | "function" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01341 | "function" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01342 | "function" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r01343 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r01344 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r01345 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r01346 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r01347 | "generate" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r01348 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r01349 | "generate" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r01350 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r01351 | "generate" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r01352 | "generate" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r01353 | "generate" | "Main" | "gen_cols" | "transforms" | "Generate matrix: fill row by iterating columns"
    r01354 | "generate" | "Main" | "gen_rows" | "transforms" | "Generate matrix: iterate rows"
    r01355 | "generate" | "Main" | "gen" | "transforms" | "Generate an n x n matrix with seed scaling"
    r01356 | "get" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r01357 | "get" | "Language" | "start" | "reads" | "Get the start position of a token"
    r01358 | "get" | "Language" | "end" | "reads" | "Get the end position of a token"
    r01359 | "get" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r01360 | "get" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01361 | "get" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r01362 | "get" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r01363 | "get" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r01364 | "get" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r01365 | "get" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r01366 | "get" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r01367 | "get" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r01368 | "get" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r01369 | "get" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01370 | "get" | "Path" | "extension" | "reads" | "Get the file extension"
    r01371 | "get" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01372 | "get" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r01373 | "get" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r01374 | "get" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r01375 | "get" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r01376 | "get" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r01377 | "get" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r01378 | "get" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r01379 | "get" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01380 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01381 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01382 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01383 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01384 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01385 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01386 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01387 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01388 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01389 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01390 | "get" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01391 | "get" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01392 | "get" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01393 | "get" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01394 | "get" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01395 | "get" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r01396 | "get" | "System" | "process" | "inputs" | "Get command line arguments"
    r01397 | "get" | "Table" | "get" | "reads" | "Get a value by key"
    r01398 | "get" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r01399 | "get" | "Table" | "values" | "reads" | "Get all values in a table"
    r01400 | "get" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r01401 | "get" | "Text" | "length" | "reads" | "Get the length of a string"
    r01402 | "get" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01403 | "get" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r01404 | "get" | "Time" | "time" | "inputs" | "Get current time"
    r01405 | "get" | "Time" | "days" | "reads" | "Get number of days in a month"
    r01406 | "get" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r01407 | "get" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r01408 | "get" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r01409 | "get" | "Language" | "start" | "reads" | "Get the start position of a token"
    r01410 | "get" | "Language" | "end" | "reads" | "Get the end position of a token"
    r01411 | "get" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r01412 | "get" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01413 | "get" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r01414 | "get" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r01415 | "get" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r01416 | "get" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r01417 | "get" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r01418 | "get" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r01419 | "get" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r01420 | "get" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r01421 | "get" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r01422 | "get" | "Path" | "extension" | "reads" | "Get the file extension"
    r01423 | "get" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01424 | "get" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r01425 | "get" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r01426 | "get" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r01427 | "get" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r01428 | "get" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r01429 | "get" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r01430 | "get" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r01431 | "get" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r01432 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01433 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01434 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01435 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01436 | "get" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01437 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01438 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01439 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01440 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01441 | "get" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01442 | "get" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01443 | "get" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01444 | "get" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01445 | "get" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01446 | "get" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01447 | "get" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r01448 | "get" | "System" | "process" | "inputs" | "Get command line arguments"
    r01449 | "get" | "Table" | "get" | "reads" | "Get a value by key"
    r01450 | "get" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r01451 | "get" | "Table" | "values" | "reads" | "Get all values in a table"
    r01452 | "get" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r01453 | "get" | "Text" | "length" | "reads" | "Get the length of a string"
    r01454 | "get" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r01455 | "get" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r01456 | "get" | "Time" | "time" | "inputs" | "Get current time"
    r01457 | "get" | "Time" | "days" | "reads" | "Get number of days in a month"
    r01458 | "get" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r01459 | "get" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r01460 | "gets" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r01461 | "gets" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r01462 | "given" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01463 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01464 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01465 | "given" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01466 | "given" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01467 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01468 | "given" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01469 | "given" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01470 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01471 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01472 | "given" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01473 | "given" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01474 | "given" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r01475 | "given" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01476 | "grams" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01477 | "grams" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01478 | "handle" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01479 | "handle" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01480 | "handle" | "System" | "close" | "outputs" | "Close an open file handle"
    r01481 | "handle" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01482 | "handle" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r01483 | "handle" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r01484 | "handle" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01485 | "handle" | "System" | "close" | "outputs" | "Close an open file handle"
    r01486 | "handle" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r01487 | "handle" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r01488 | "hash" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r01489 | "hash" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r01490 | "hash" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01491 | "hash" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r01492 | "hash" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r01493 | "hash" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01494 | "hash" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r01495 | "hash" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r01496 | "hash" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01497 | "hash" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r01498 | "hash" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r01499 | "hash" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r01500 | "hash" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01501 | "hash" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r01502 | "hash" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r01503 | "hash" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01504 | "hash" | "Hash" | "blake3" | "creates" | "Hash a byte array to BLAKE3 digest"
    r01505 | "hash" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r01506 | "hash" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01507 | "hash" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r01508 | "have" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r01509 | "have" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r01510 | "have" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r01511 | "have" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r01512 | "header" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01513 | "hex" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r01514 | "hex" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r01515 | "hex" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r01516 | "hex" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r01517 | "hex" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r01518 | "hex" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r01519 | "hexadecimal" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r01520 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r01521 | "hexadecimal" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r01522 | "hexadecimal" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r01523 | "hexadecimal" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r01524 | "hexadecimal" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r01525 | "hexadecimal" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r01526 | "hexadecimal" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r01527 | "hmac" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r01528 | "hmac" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r01529 | "hmac" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r01530 | "hmac" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r01531 | "host" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r01532 | "host" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01533 | "host" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r01534 | "host" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r01535 | "host" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r01536 | "host" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r01537 | "immutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01538 | "immutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01539 | "immutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01540 | "immutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01541 | "immutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01542 | "immutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01543 | "inclusive" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r01544 | "inclusive" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r01545 | "inclusive" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r01546 | "inclusive" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r01547 | "inclusive" | "Math" | "clamp" | "transforms" | "Clamp a float between lo and hi (inclusive)"
    r01548 | "inclusive" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r01549 | "incoming" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01550 | "incoming" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01551 | "index" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r01552 | "index" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r01553 | "index" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r01554 | "index" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01555 | "index" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r01556 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01557 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01558 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01559 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01560 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01561 | "index" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01562 | "index" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01563 | "index" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01564 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01565 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01566 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01567 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01568 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01569 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01570 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01571 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01572 | "index" | "Text" | "index" | "reads" | "Find the index of a substring"
    r01573 | "index" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r01574 | "index" | "Bytes" | "exist" | "validates" | "Check if index exists in a ByteArray"
    r01575 | "index" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r01576 | "index" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01577 | "index" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r01578 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01579 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01580 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01581 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01582 | "index" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r01583 | "index" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01584 | "index" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01585 | "index" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01586 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01587 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01588 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01589 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01590 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01591 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01592 | "index" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r01593 | "index" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r01594 | "index" | "Text" | "index" | "reads" | "Find the index of a substring"
    r01595 | "indices" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r01596 | "individual" | "Language" | "words" | "reads" | "Extract individual words from text"
    r01597 | "individual" | "Language" | "words" | "reads" | "Extract individual words from text"
    r01598 | "info" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01599 | "info" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01600 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01601 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01602 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01603 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01604 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01605 | "initial" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r01606 | "inlined" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r01607 | "inner" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r01608 | "inner" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r01609 | "input" | "System" | "console" | "validates" | "Validate console input"
    r01610 | "input" | "System" | "console" | "validates" | "Validate console input"
    r01611 | "insert" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r01612 | "insert" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r01613 | "integer" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r01614 | "integer" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r01615 | "integer" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r01616 | "integer" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r01617 | "integer" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01618 | "integer" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r01619 | "integer" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01620 | "integer" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r01621 | "integer" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r01622 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r01623 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r01624 | "integer" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01625 | "integer" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r01626 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r01627 | "integer" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01628 | "integer" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r01629 | "integer" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r01630 | "integer" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01631 | "integer" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r01632 | "integer" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01633 | "integer" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r01634 | "integer" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r01635 | "integer" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r01636 | "integer" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r01637 | "integer" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r01638 | "integer" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r01639 | "integer" | "Math" | "clamp" | "transforms" | "Clamp an integer between lo and hi (inclusive)"
    r01640 | "integer" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01641 | "integer" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r01642 | "integer" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01643 | "integer" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r01644 | "integer" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r01645 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r01646 | "integer" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r01647 | "integer" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01648 | "integer" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r01649 | "integer" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r01650 | "integer" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r01651 | "integer" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r01652 | "integer" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r01653 | "integer" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r01654 | "integer" | "Types" | "float" | "creates" | "Convert an integer to a float"
    r01655 | "integer" | "Types" | "decimal" | "creates" | "Convert an integer to a decimal"
    r01656 | "integer" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r01657 | "integer" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r01658 | "integers" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01659 | "integers" | "Math" | "min" | "reads" | "Minimum of two integers"
    r01660 | "integers" | "Math" | "max" | "reads" | "Maximum of two integers"
    r01661 | "integers" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r01662 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01663 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01664 | "integers" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01665 | "integers" | "Math" | "min" | "reads" | "Minimum of two integers"
    r01666 | "integers" | "Math" | "max" | "reads" | "Maximum of two integers"
    r01667 | "integers" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r01668 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r01669 | "integers" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r01670 | "integrity" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r01671 | "integrity" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r01672 | "into" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r01673 | "into" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r01674 | "into" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r01675 | "into" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r01676 | "into" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r01677 | "into" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r01678 | "into" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r01679 | "into" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r01680 | "into" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r01681 | "into" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r01682 | "into" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r01683 | "into" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r01684 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01685 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01686 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01687 | "into" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r01688 | "into" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r01689 | "into" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r01690 | "into" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r01691 | "into" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r01692 | "into" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r01693 | "into" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r01694 | "into" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r01695 | "into" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r01696 | "into" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r01697 | "into" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r01698 | "into" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r01699 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01700 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01701 | "into" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01702 | "into" | "Main" | "transpose_cols" | "transforms" | "Transpose: copy columns of src into rows of dst"
    r01703 | "into" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r01704 | "iterate" | "Main" | "gen_rows" | "transforms" | "Generate matrix: iterate rows"
    r01705 | "iterate" | "Main" | "transpose_rows" | "transforms" | "Transpose: iterate rows"
    r01706 | "iterate" | "Main" | "mul_rows" | "transforms" | "Multiply: iterate rows"
    r01707 | "iterate" | "Main" | "atkin_step1_x" | "transforms" | "Sieve of Atkin step 1: iterate x"
    r01708 | "iterate" | "Main" | "atkin_step2_x" | "transforms" | "Sieve of Atkin step 2: iterate x"
    r01709 | "iterate" | "Main" | "atkin_step3_x" | "transforms" | "Sieve of Atkin step 3: iterate x"
    r01710 | "iterate" | "Main" | "eliminate_squares" | "transforms" | "Eliminate squares of primes: iterate over sieve"
    r01711 | "iterate" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r01712 | "iterating" | "Main" | "gen_cols" | "transforms" | "Generate matrix: fill row by iterating columns"
    r01713 | "iterating" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r01714 | "iteration" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01715 | "iteration" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01716 | "iteration" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01717 | "iterations" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r01718 | "iterations" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r01719 | "its" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r01720 | "its" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r01721 | "its" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r01722 | "join" | "Path" | "join" | "transforms" | "Join path components together"
    r01723 | "join" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r01724 | "join" | "Path" | "join" | "transforms" | "Join path components together"
    r01725 | "join" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r01726 | "json" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r01727 | "json" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r01728 | "json" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r01729 | "json" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r01730 | "json" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r01731 | "json" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r01732 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01733 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01734 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01735 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01736 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01737 | "keep" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01738 | "key" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r01739 | "key" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r01740 | "key" | "Table" | "get" | "reads" | "Get a value by key"
    r01741 | "key" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01742 | "key" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r01743 | "key" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r01744 | "key" | "Table" | "get" | "reads" | "Get a value by key"
    r01745 | "key" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01746 | "keys" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r01747 | "keys" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r01748 | "kind" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r01749 | "kind" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r01750 | "kind" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r01751 | "kind" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r01752 | "largest" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01753 | "largest" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r01754 | "last" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r01755 | "last" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r01756 | "leap" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r01757 | "leap" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r01758 | "left" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r01759 | "left" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r01760 | "left" | "Main" | "parse_char_left" | "transforms" | "Continue parsing: check for left"
    r01761 | "length" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r01762 | "length" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r01763 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01764 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01765 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01766 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01767 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01768 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01769 | "length" | "Text" | "length" | "reads" | "Get the length of a string"
    r01770 | "length" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r01771 | "length" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01772 | "length" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r01773 | "length" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r01774 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01775 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01776 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01777 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01778 | "length" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01779 | "length" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01780 | "length" | "Text" | "length" | "reads" | "Get the length of a string"
    r01781 | "length" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r01782 | "level" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01783 | "level" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r01784 | "level" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r01785 | "level" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r01786 | "levenshtein" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r01787 | "levenshtein" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r01788 | "line" | "System" | "console" | "inputs" | "Read a line from the console"
    r01789 | "line" | "System" | "process" | "inputs" | "Get command line arguments"
    r01790 | "line" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01791 | "line" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r01792 | "line" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r01793 | "line" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r01794 | "line" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01795 | "line" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01796 | "line" | "System" | "console" | "inputs" | "Read a line from the console"
    r01797 | "line" | "System" | "process" | "inputs" | "Get command line arguments"
    r01798 | "line" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r01799 | "line" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r01800 | "lines" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r01801 | "list" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01802 | "list" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r01803 | "list" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r01804 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01805 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01806 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r01807 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r01808 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01809 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01810 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01811 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01812 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01813 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01814 | "list" | "Store" | "version" | "inputs" | "List all versions of a table"
    r01815 | "list" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01816 | "list" | "System" | "dir" | "inputs" | "List entries in a directory"
    r01817 | "list" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r01818 | "list" | "Bytes" | "bytearray" | "creates" | "Create a byte array from a list of integers"
    r01819 | "list" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r01820 | "list" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r01821 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r01822 | "list" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r01823 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r01824 | "list" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r01825 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01826 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01827 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01828 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01829 | "list" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01830 | "list" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01831 | "list" | "Store" | "version" | "inputs" | "List all versions of a table"
    r01832 | "list" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01833 | "list" | "System" | "dir" | "inputs" | "List entries in a directory"
    r01834 | "list" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r01835 | "listen" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r01836 | "listen" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r01837 | "listening" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01838 | "listening" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r01839 | "load" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01840 | "load" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r01841 | "load" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01842 | "load" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r01843 | "local" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01844 | "local" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r01845 | "logarithm" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r01846 | "logarithm" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r01847 | "logs" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r01848 | "lookup" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01849 | "lookup" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r01850 | "lookup" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r01851 | "lookup" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r01852 | "lookup" | "Store" | "lookup" | "inputs" | "Load a compiled lookup"
    r01853 | "lookup" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r01854 | "loop" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r01855 | "loop" | "Main" | "serve" | "streams" | "Accept and echo connections in a blocking loop."
    r01856 | "loop" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r01857 | "loop" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r01858 | "loop" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r01859 | "lowercase" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r01860 | "lowercase" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r01861 | "lowercase" | "Character" | "lowercase" | "validates" | "Checks if a character is lowercase (a-z)."
    r01862 | "lowercase" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r01863 | "lowercasing" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r01864 | "lowercasing" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r01865 | "lsp" | "Commands" | "lsp" | "outputs" | "Run the lsp command"
    r01866 | "main" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r01867 | "many" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01868 | "mark" | "Main" | "eliminate_multiples" | "transforms" | "Eliminate squares of primes: mark multiples of n*n"
    r01869 | "match" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01870 | "match" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01871 | "match" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r01872 | "match" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r01873 | "match" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r01874 | "match" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01875 | "match" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r01876 | "match" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r01877 | "matched" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r01878 | "matched" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01879 | "matched" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r01880 | "matched" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r01881 | "matches" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01882 | "matches" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01883 | "matches" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01884 | "matches" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r01885 | "matches" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01886 | "matches" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01887 | "matches" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r01888 | "matches" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r01889 | "matches" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r01890 | "matches" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r01891 | "matches" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r01892 | "matches" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r01893 | "matches" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r01894 | "matches" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r01895 | "matches" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r01896 | "matches" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r01897 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01898 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01899 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01900 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01901 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01902 | "matching" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r01903 | "matrices" | "Main" | "mul" | "transforms" | "Multiply two matrices: a * b with transpose optimization"
    r01904 | "matrix" | "Main" | "gen_cols" | "transforms" | "Generate matrix: fill row by iterating columns"
    r01905 | "matrix" | "Main" | "gen_rows" | "transforms" | "Generate matrix: iterate rows"
    r01906 | "matrix" | "Main" | "gen" | "transforms" | "Generate an n x n matrix with seed scaling"
    r01907 | "matrix" | "Main" | "transpose" | "transforms" | "Transpose a matrix"
    r01908 | "maximum" | "Math" | "max" | "reads" | "Maximum of two integers"
    r01909 | "maximum" | "Math" | "max" | "reads" | "Maximum of two floats"
    r01910 | "maximum" | "Math" | "max" | "reads" | "Maximum of two integers"
    r01911 | "maximum" | "Math" | "max" | "reads" | "Maximum of two floats"
    r01912 | "merge" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r01913 | "merge" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r01914 | "merge" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01915 | "merge" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01916 | "merge" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01917 | "merge" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r01918 | "merge" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r01919 | "merge" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r01920 | "merge" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01921 | "merge" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r01922 | "merged" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01923 | "merged" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r01924 | "message" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r01925 | "message" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01926 | "message" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01927 | "message" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01928 | "message" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r01929 | "message" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r01930 | "message" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r01931 | "message" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r01932 | "message" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r01933 | "message" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r01934 | "metaphone" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r01935 | "metaphone" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r01936 | "minimum" | "Math" | "min" | "reads" | "Minimum of two integers"
    r01937 | "minimum" | "Math" | "min" | "reads" | "Minimum of two floats"
    r01938 | "minimum" | "Math" | "min" | "reads" | "Minimum of two integers"
    r01939 | "minimum" | "Math" | "min" | "reads" | "Minimum of two floats"
    r01940 | "minus" | "Main" | "parse_char_minus" | "transforms" | "Continue parsing: check for minus"
    r01941 | "month" | "Time" | "days" | "reads" | "Get number of days in a month"
    r01942 | "month" | "Time" | "days" | "reads" | "Get number of days in a month"
    r01943 | "most" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01944 | "most" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r01945 | "multiples" | "Main" | "eliminate_multiples" | "transforms" | "Eliminate squares of primes: mark multiples of n*n"
    r01946 | "multiply" | "Main" | "mul_cols" | "transforms" | "Multiply: fill one row of result"
    r01947 | "multiply" | "Main" | "mul_rows" | "transforms" | "Multiply: iterate rows"
    r01948 | "multiply" | "Main" | "mul" | "transforms" | "Multiply two matrices: a * b with transpose optimization"
    r01949 | "mutable" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01950 | "mutable" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01951 | "mutable" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01952 | "mutable" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01953 | "mutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01954 | "mutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01955 | "mutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01956 | "mutable" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01957 | "mutable" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01958 | "mutable" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01959 | "mutable" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r01960 | "mutable" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01961 | "mutable" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r01962 | "mutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01963 | "mutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01964 | "mutable" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r01965 | "mutable" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01966 | "mutable" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r01967 | "name" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01968 | "name" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01969 | "name" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01970 | "name" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01971 | "name" | "System" | "process" | "validates" | "Validate a process name"
    r01972 | "name" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r01973 | "name" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r01974 | "name" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r01975 | "name" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r01976 | "name" | "System" | "process" | "validates" | "Validate a process name"
    r01977 | "natural" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r01978 | "natural" | "Math" | "log" | "reads" | "Natural logarithm of a float"
    r01979 | "navigate" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r01980 | "nearest" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01981 | "nearest" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r01982 | "new" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r01983 | "new" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01984 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01985 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01986 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01987 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01988 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r01989 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r01990 | "new" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r01991 | "new" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r01992 | "new" | "Table" | "new" | "creates" | "Create a new empty table"
    r01993 | "new" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r01994 | "new" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r01995 | "new" | "Text" | "builder" | "creates" | "Create a new string builder"
    r01996 | "new" | "Commands" | "new" | "outputs" | "Run the new command"
    r01997 | "new" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r01998 | "new" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r01999 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02000 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02001 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r02002 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r02003 | "new" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02004 | "new" | "Sequence" | "list" | "creates" | "Copy array contents into a new List"
    r02005 | "new" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r02006 | "new" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r02007 | "new" | "Table" | "new" | "creates" | "Create a new empty table"
    r02008 | "new" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r02009 | "new" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r02010 | "new" | "Text" | "builder" | "creates" | "Create a new string builder"
    r02011 | "next" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02012 | "next" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r02013 | "next" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02014 | "node" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r02015 | "node" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r02016 | "node" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r02017 | "none" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02018 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02019 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02020 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02021 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02022 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02023 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02024 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02025 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02026 | "none" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02027 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02028 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02029 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02030 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02031 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02032 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02033 | "none" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02034 | "none" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02035 | "normalize" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r02036 | "normalize" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r02037 | "normalize" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r02038 | "normalize" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r02039 | "normalized" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r02040 | "normalized" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r02041 | "not" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02042 | "not" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02043 | "not" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r02044 | "not" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02045 | "not" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02046 | "not" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r02047 | "number" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r02048 | "number" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r02049 | "number" | "Time" | "days" | "reads" | "Get number of days in a month"
    r02050 | "number" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r02051 | "number" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r02052 | "number" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r02053 | "number" | "Time" | "days" | "reads" | "Get number of days in a month"
    r02054 | "number" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r02055 | "numeric" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r02056 | "numeric" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r02057 | "object" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r02058 | "object" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r02059 | "object" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r02060 | "object" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r02061 | "occurrences" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02062 | "occurrences" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r02063 | "occurrences" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02064 | "occurrences" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r02065 | "octal" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r02066 | "octal" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r02067 | "one" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r02068 | "one" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r02069 | "one" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02070 | "one" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02071 | "one" | "Main" | "parse_char" | "transforms" | "Parse one BF character and emit opcodes"
    r02072 | "one" | "Main" | "mul_cols" | "transforms" | "Multiply: fill one row of result"
    r02073 | "opcodes" | "Main" | "parse_char" | "transforms" | "Parse one BF character and emit opcodes"
    r02074 | "opcodes" | "Main" | "count_ops" | "reads" | "Count BF opcodes in source for array sizing"
    r02075 | "opcodes" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r02076 | "open" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02077 | "open" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r02078 | "open" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02079 | "open" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02080 | "open" | "System" | "close" | "outputs" | "Close an open file handle"
    r02081 | "open" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r02082 | "open" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r02083 | "open" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02084 | "open" | "Network" | "socket" | "validates" | "Check if a connection is open"
    r02085 | "open" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02086 | "open" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02087 | "open" | "System" | "close" | "outputs" | "Close an open file handle"
    r02088 | "open" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r02089 | "open" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r02090 | "open" | "Main" | "parse_char_bracket" | "transforms" | "Continue parsing: check for open/close brackets"
    r02091 | "optimistic" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02092 | "optimistic" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02093 | "optimization" | "Main" | "mul" | "transforms" | "Multiply two matrices: a * b with transpose optimization"
    r02094 | "option" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r02095 | "option" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r02096 | "option" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r02097 | "option" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r02098 | "option" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r02099 | "option" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r02100 | "option" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r02101 | "option" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r02102 | "option" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r02103 | "option" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r02104 | "out" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02105 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02106 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02107 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02108 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02109 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02110 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02111 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02112 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02113 | "out" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02114 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02115 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02116 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02117 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r02118 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02119 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02120 | "out" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02121 | "out" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds"
    r02122 | "over" | "Main" | "eliminate_squares" | "transforms" | "Eliminate squares of primes: iterate over sieve"
    r02123 | "pads" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02124 | "pads" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02125 | "pads" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02126 | "pads" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02127 | "pair" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r02128 | "pair" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r02129 | "parameters" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r02130 | "parameters" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r02131 | "parent" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r02132 | "parent" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r02133 | "parse" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r02134 | "parse" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r02135 | "parse" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r02136 | "parse" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r02137 | "parse" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r02138 | "parse" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r02139 | "parse" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r02140 | "parse" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r02141 | "parse" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r02142 | "parse" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r02143 | "parse" | "Types" | "float" | "creates" | "Parse a string as a float"
    r02144 | "parse" | "Config" | "config" | "transforms" | "Parse the toml data to Config struct"
    r02145 | "parse" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r02146 | "parse" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r02147 | "parse" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r02148 | "parse" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r02149 | "parse" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r02150 | "parse" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r02151 | "parse" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r02152 | "parse" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r02153 | "parse" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r02154 | "parse" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r02155 | "parse" | "Types" | "float" | "creates" | "Parse a string as a float"
    r02156 | "parse" | "Main" | "parse_char" | "transforms" | "Parse one BF character and emit opcodes"
    r02157 | "parses" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02158 | "parsing" | "Main" | "parse_char_minus" | "transforms" | "Continue parsing: check for minus"
    r02159 | "parsing" | "Main" | "parse_char_right" | "transforms" | "Continue parsing: check for right"
    r02160 | "parsing" | "Main" | "parse_char_left" | "transforms" | "Continue parsing: check for left"
    r02161 | "parsing" | "Main" | "parse_char_dot" | "transforms" | "Continue parsing: check for dot (print)"
    r02162 | "parsing" | "Main" | "parse_char_bracket" | "transforms" | "Continue parsing: check for open/close brackets"
    r02163 | "past" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r02164 | "past" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r02165 | "path" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r02166 | "path" | "Path" | "join" | "transforms" | "Join path components together"
    r02167 | "path" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r02168 | "path" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r02169 | "path" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r02170 | "path" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r02171 | "path" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r02172 | "path" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r02173 | "path" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r02174 | "path" | "Path" | "join" | "transforms" | "Join path components together"
    r02175 | "path" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r02176 | "path" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r02177 | "path" | "Path" | "absolute" | "validates" | "Check if a path is absolute"
    r02178 | "path" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r02179 | "path" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r02180 | "path" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r02181 | "pattern" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r02182 | "pattern" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r02183 | "pattern" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r02184 | "pattern" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r02185 | "pattern" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r02186 | "pattern" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r02187 | "pattern" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r02188 | "pattern" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r02189 | "pattern" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02190 | "pattern" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r02191 | "pattern" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r02192 | "pattern" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02193 | "pattern" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02194 | "pattern" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r02195 | "pattern" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r02196 | "pattern" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r02197 | "pattern" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r02198 | "pattern" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r02199 | "pattern" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r02200 | "pattern" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r02201 | "pattern" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r02202 | "pattern" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02203 | "pattern" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r02204 | "pattern" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r02205 | "pattern" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02206 | "pattern" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02207 | "phonetic" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r02208 | "phonetic" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r02209 | "phonetic" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r02210 | "phonetic" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r02211 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r02212 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02213 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r02214 | "pick" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02215 | "place" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02216 | "place" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02217 | "place" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02218 | "place" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02219 | "place" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02220 | "place" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02221 | "places" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r02222 | "places" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r02223 | "places" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r02224 | "places" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r02225 | "point" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r02226 | "point" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r02227 | "point" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r02228 | "point" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r02229 | "port" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02230 | "port" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r02231 | "port" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02232 | "port" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02233 | "port" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r02234 | "port" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02235 | "porter" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r02236 | "porter" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r02237 | "position" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r02238 | "position" | "Language" | "start" | "reads" | "Get the start position of a token"
    r02239 | "position" | "Language" | "end" | "reads" | "Get the end position of a token"
    r02240 | "position" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r02241 | "position" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r02242 | "position" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02243 | "position" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r02244 | "position" | "Language" | "start" | "reads" | "Get the start position of a token"
    r02245 | "position" | "Language" | "end" | "reads" | "Get the end position of a token"
    r02246 | "position" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r02247 | "position" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r02248 | "position" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02249 | "positive" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r02250 | "positive" | "Time" | "duration" | "validates" | "Check if duration is positive"
    r02251 | "power" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r02252 | "power" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r02253 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02254 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02255 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02256 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02257 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02258 | "predicate" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02259 | "prefix" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r02260 | "prefix" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r02261 | "prefix" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r02262 | "preserving" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r02263 | "preserving" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r02264 | "previous" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r02265 | "previous" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r02266 | "prime" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r02267 | "primes" | "Main" | "eliminate_multiples" | "transforms" | "Eliminate squares of primes: mark multiples of n*n"
    r02268 | "primes" | "Main" | "eliminate_squares" | "transforms" | "Eliminate squares of primes: iterate over sieve"
    r02269 | "primes" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r02270 | "primes" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r02271 | "primes" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r02272 | "print" | "System" | "console" | "outputs" | "Print text to the console"
    r02273 | "print" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02274 | "print" | "System" | "console" | "outputs" | "Print text to the console"
    r02275 | "print" | "Main" | "parse_char_dot" | "transforms" | "Continue parsing: check for dot (print)"
    r02276 | "process" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r02277 | "process" | "System" | "process" | "validates" | "Validate a process name"
    r02278 | "process" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02279 | "process" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r02280 | "process" | "System" | "process" | "validates" | "Validate a process name"
    r02281 | "processes" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r02282 | "producing" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r02283 | "producing" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r02284 | "product" | "Main" | "dot" | "reads" | "Dot product of row i of a with row j of bt (transposed b)"
    r02285 | "prompt" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02286 | "query" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r02287 | "query" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r02288 | "raised" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r02289 | "raised" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r02290 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r02291 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r02292 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r02293 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r02294 | "random" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r02295 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r02296 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02297 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer"
    r02298 | "random" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r02299 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal between 0.0 and 1.0"
    r02300 | "random" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r02301 | "random" | "Random" | "boolean" | "inputs" | "Generate a random boolean"
    r02302 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from an integer list"
    r02303 | "random" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02304 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r02305 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r02306 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r02307 | "randomly" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r02308 | "range" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r02309 | "range" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r02310 | "range" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r02311 | "range" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r02312 | "range" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r02313 | "range" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r02314 | "range" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r02315 | "range" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r02316 | "reach" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02317 | "reach" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02318 | "reach" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02319 | "reach" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02320 | "read" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r02321 | "read" | "Network" | "message" | "inputs" | "Read data from a socket"
    r02322 | "read" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r02323 | "read" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02324 | "read" | "System" | "console" | "inputs" | "Read a line from the console"
    r02325 | "read" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02326 | "read" | "System" | "file" | "inputs" | "Read contents from a file"
    r02327 | "read" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02328 | "read" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r02329 | "read" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r02330 | "read" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r02331 | "read" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02332 | "read" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02333 | "read" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r02334 | "read" | "Network" | "message" | "inputs" | "Read data from a socket"
    r02335 | "read" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r02336 | "read" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02337 | "read" | "System" | "console" | "inputs" | "Read a line from the console"
    r02338 | "read" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r02339 | "read" | "System" | "file" | "inputs" | "Read contents from a file"
    r02340 | "read" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02341 | "read" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r02342 | "readable" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02343 | "readable" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02344 | "reading" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02345 | "reading" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02346 | "reads" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02347 | "regex" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02348 | "regex" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02349 | "regex" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02350 | "regex" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02351 | "remaining" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02352 | "remaining" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02353 | "remote" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02354 | "remote" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r02355 | "remote" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02356 | "remote" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r02357 | "remove" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02358 | "remove" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r02359 | "remove" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02360 | "remove" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r02361 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r02362 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r02363 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder an integer list"
    r02364 | "reorder" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r02365 | "repeat" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r02366 | "repeat" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r02367 | "replace" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02368 | "replace" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r02369 | "replace" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02370 | "replace" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r02371 | "replacement" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02372 | "replacement" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02373 | "resolve" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r02374 | "resolve" | "Path" | "normalize" | "transforms" | "Normalize a path (resolve .. and .)"
    r02375 | "resolver" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r02376 | "resolver" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r02377 | "result" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r02378 | "result" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r02379 | "result" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r02380 | "result" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r02381 | "result" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r02382 | "result" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r02383 | "result" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r02384 | "result" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r02385 | "result" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r02386 | "result" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r02387 | "result" | "Main" | "mul_cols" | "transforms" | "Multiply: fill one row of result"
    r02388 | "return" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r02389 | "return" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r02390 | "return" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r02391 | "return" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02392 | "return" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r02393 | "return" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r02394 | "return" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r02395 | "return" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r02396 | "return" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r02397 | "return" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r02398 | "return" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02399 | "return" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r02400 | "return" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r02401 | "return" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r02402 | "return" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r02403 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02404 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02405 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02406 | "returning" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r02407 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02408 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02409 | "returning" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02410 | "returning" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r02411 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02412 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02413 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02414 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02415 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02416 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02417 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02418 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02419 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02420 | "returns" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02421 | "returns" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02422 | "returns" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r02423 | "returns" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r02424 | "returns" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r02425 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02426 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02427 | "returns" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02428 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02429 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02430 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02431 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02432 | "returns" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02433 | "returns" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r02434 | "returns" | "System" | "reader" | "creates" | "Open a file for reading (returns a file handle, or fails if not found)"
    r02435 | "returns" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02436 | "returns" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r02437 | "returns" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r02438 | "returns" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r02439 | "reverse" | "Sequence" | "reverse" | "transforms" | "Reverse a sequence"
    r02440 | "reverse" | "Sequence" | "reverse" | "transforms" | "Reverse a sequence"
    r02441 | "right" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02442 | "right" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02443 | "right" | "Main" | "parse_char_right" | "transforms" | "Continue parsing: check for right"
    r02444 | "rollback" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r02445 | "rollback" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r02446 | "root" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r02447 | "root" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r02448 | "root" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r02449 | "root" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r02450 | "round" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r02451 | "round" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r02452 | "row" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r02453 | "row" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r02454 | "row" | "Main" | "gen_cols" | "transforms" | "Generate matrix: fill row by iterating columns"
    r02455 | "row" | "Main" | "dot" | "reads" | "Dot product of row i of a with row j of bt (transposed b)"
    r02456 | "row" | "Main" | "mul_cols" | "transforms" | "Multiply: fill one row of result"
    r02457 | "rows" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r02458 | "rows" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r02459 | "rows" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r02460 | "rows" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r02461 | "rows" | "Main" | "gen_rows" | "transforms" | "Generate matrix: iterate rows"
    r02462 | "rows" | "Main" | "transpose_cols" | "transforms" | "Transpose: copy columns of src into rows of dst"
    r02463 | "rows" | "Main" | "transpose_rows" | "transforms" | "Transpose: iterate rows"
    r02464 | "rows" | "Main" | "mul_rows" | "transforms" | "Multiply: iterate rows"
    r02465 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02466 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02467 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02468 | "run" | "Commands" | "build" | "outputs" | "Run the build command"
    r02469 | "run" | "Commands" | "check" | "outputs" | "Run the check command"
    r02470 | "run" | "Commands" | "compiler" | "outputs" | "Run the compiler command"
    r02471 | "run" | "Commands" | "format" | "outputs" | "Run the format command"
    r02472 | "run" | "Commands" | "lsp" | "outputs" | "Run the lsp command"
    r02473 | "run" | "Commands" | "new" | "outputs" | "Run the new command"
    r02474 | "run" | "Commands" | "test" | "outputs" | "Run the test command"
    r02475 | "run" | "Commands" | "view" | "outputs" | "Run the view command"
    r02476 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02477 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02478 | "run" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r02479 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02480 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02481 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02482 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02483 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02484 | "same" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r02485 | "save" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02486 | "save" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02487 | "scaling" | "Main" | "gen" | "transforms" | "Generate an n x n matrix with seed scaling"
    r02488 | "scheme" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r02489 | "scheme" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r02490 | "search" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r02491 | "search" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r02492 | "seconds" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r02493 | "seconds" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r02494 | "seed" | "Main" | "gen" | "transforms" | "Generate an n x n matrix with seed scaling"
    r02495 | "send" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r02496 | "send" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r02497 | "send" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r02498 | "send" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r02499 | "send" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r02500 | "send" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r02501 | "send" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r02502 | "send" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r02503 | "sentences" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r02504 | "sentences" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r02505 | "separated" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r02506 | "separated" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r02507 | "separator" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r02508 | "separator" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r02509 | "separator" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02510 | "separator" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r02511 | "separator" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r02512 | "sequence" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r02513 | "sequence" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r02514 | "sequence" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r02515 | "sequence" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02516 | "sequence" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r02517 | "sequence" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r02518 | "sequence" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r02519 | "sequence" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r02520 | "sequence" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r02521 | "sequence" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r02522 | "sequence" | "Sequence" | "reverse" | "transforms" | "Reverse a sequence"
    r02523 | "sequence" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r02524 | "sequence" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r02525 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02526 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02527 | "sequence" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r02528 | "sequence" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r02529 | "sequence" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r02530 | "sequence" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r02531 | "sequence" | "Sequence" | "empty" | "validates" | "Check if a sequence is empty"
    r02532 | "sequence" | "Sequence" | "contains" | "validates" | "Check if an integer is in a sequence"
    r02533 | "sequence" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r02534 | "sequence" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r02535 | "sequence" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r02536 | "sequence" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r02537 | "sequence" | "Sequence" | "reverse" | "transforms" | "Reverse a sequence"
    r02538 | "sequence" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r02539 | "sequence" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r02540 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02541 | "sequence" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02542 | "serialize" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r02543 | "serialize" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r02544 | "serialize" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r02545 | "serialize" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r02546 | "serialize" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r02547 | "serialize" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r02548 | "set" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02549 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02550 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02551 | "set" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02552 | "set" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02553 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02554 | "set" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02555 | "set" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02556 | "set" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02557 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02558 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02559 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02560 | "set" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r02561 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02562 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02563 | "set" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02564 | "set" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02565 | "set" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02566 | "set" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02567 | "set" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02568 | "set" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r02569 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02570 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02571 | "set" | "Sequence" | "set_safe" | "creates" | "Set element at index; returns None if out of bounds"
    r02572 | "severity" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r02573 | "severity" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r02574 | "severity" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r02575 | "severity" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r02576 | "severity" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r02577 | "severity" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r02578 | "severity" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r02579 | "severity" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r02580 | "sha" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r02581 | "sha" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r02582 | "sha" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r02583 | "sha" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r02584 | "sha" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r02585 | "sha" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r02586 | "sha" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r02587 | "sha" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r02588 | "sha" | "Hash" | "sha256" | "creates" | "Hash a byte array to SHA-256 digest"
    r02589 | "sha" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r02590 | "sha" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r02591 | "sha" | "Hash" | "sha512" | "creates" | "Hash a byte array to SHA-512 digest"
    r02592 | "sha" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r02593 | "sha" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r02594 | "sha" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r02595 | "sha" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r02596 | "sieve" | "Main" | "atkin_step1_y" | "transforms" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r02597 | "sieve" | "Main" | "atkin_step1_x" | "transforms" | "Sieve of Atkin step 1: iterate x"
    r02598 | "sieve" | "Main" | "atkin_step2_y" | "transforms" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r02599 | "sieve" | "Main" | "atkin_step2_x" | "transforms" | "Sieve of Atkin step 2: iterate x"
    r02600 | "sieve" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r02601 | "sieve" | "Main" | "atkin_step3_x" | "transforms" | "Sieve of Atkin step 3: iterate x"
    r02602 | "sieve" | "Main" | "eliminate_squares" | "transforms" | "Eliminate squares of primes: iterate over sieve"
    r02603 | "sieve" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r02604 | "signature" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r02605 | "signature" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r02606 | "signature" | "Hash" | "hmac" | "creates" | "Create an HMAC-SHA256 signature"
    r02607 | "signature" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r02608 | "similarity" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r02609 | "similarity" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r02610 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02611 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02612 | "size" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02613 | "size" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02614 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02615 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02616 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02617 | "size" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02618 | "size" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r02619 | "size" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r02620 | "sizes" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r02621 | "sizing" | "Main" | "count_ops" | "reads" | "Count BF opcodes in source for array sizing"
    r02622 | "skips" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02623 | "slice" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r02624 | "slice" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r02625 | "smallest" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r02626 | "smallest" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r02627 | "socket" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r02628 | "socket" | "Network" | "message" | "inputs" | "Read data from a socket"
    r02629 | "socket" | "Network" | "message" | "outputs" | "Write data to a socket"
    r02630 | "socket" | "Network" | "accept" | "inputs" | "Accept an incoming connection on a listening socket"
    r02631 | "socket" | "Network" | "message" | "inputs" | "Read data from a socket"
    r02632 | "socket" | "Network" | "message" | "outputs" | "Write data to a socket"
    r02633 | "sort" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r02634 | "sort" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r02635 | "sort" | "Sequence" | "sort" | "transforms" | "Sort a sequence of integers"
    r02636 | "sort" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r02637 | "soundex" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r02638 | "soundex" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r02639 | "source" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r02640 | "source" | "Types" | "value" | "validates" | "Validate a source string"
    r02641 | "source" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r02642 | "source" | "Types" | "value" | "validates" | "Validate a source string"
    r02643 | "source" | "Main" | "count_ops" | "reads" | "Count BF opcodes in source for array sizing"
    r02644 | "spawn" | "Main" | "double" | "attached" | "Spawn and await — doubles an integer."
    r02645 | "specific" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r02646 | "specific" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r02647 | "specified" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r02648 | "specified" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02649 | "specified" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02650 | "specified" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r02651 | "specified" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r02652 | "specified" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r02653 | "specified" | "Bytes" | "at" | "reads" | "Read byte at a specified index in a ByteArray"
    r02654 | "specified" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02655 | "specified" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02656 | "specified" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r02657 | "specified" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r02658 | "specified" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r02659 | "split" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r02660 | "split" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02661 | "split" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r02662 | "split" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r02663 | "split" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02664 | "split" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r02665 | "square" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r02666 | "square" | "Math" | "sqrt" | "reads" | "Square root of a float"
    r02667 | "squares" | "Main" | "eliminate_multiples" | "transforms" | "Eliminate squares of primes: mark multiples of n*n"
    r02668 | "squares" | "Main" | "eliminate_squares" | "transforms" | "Eliminate squares of primes: iterate over sieve"
    r02669 | "src" | "Main" | "transpose_cols" | "transforms" | "Transpose: copy columns of src into rows of dst"
    r02670 | "start" | "Language" | "start" | "reads" | "Get the start position of a token"
    r02671 | "start" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r02672 | "start" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r02673 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02674 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02675 | "start" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r02676 | "start" | "Language" | "start" | "reads" | "Get the start position of a token"
    r02677 | "start" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r02678 | "start" | "Sequence" | "slice" | "transforms" | "Get a slice of a sequence from start to end (exclusive)"
    r02679 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive)"
    r02680 | "start" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02681 | "start" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r02682 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r02683 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r02684 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r02685 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r02686 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r02687 | "starting" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r02688 | "starts" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r02689 | "starts" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r02690 | "status" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r02691 | "status" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r02692 | "stdin" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r02693 | "stdin" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02694 | "stdin" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02695 | "stdout" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r02696 | "stdout" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r02697 | "stdout" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r02698 | "stdout" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r02699 | "stdout" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r02700 | "stdout" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r02701 | "stdout" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r02702 | "stdout" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r02703 | "stemming" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r02704 | "stemming" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r02705 | "step" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02706 | "step" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r02707 | "step" | "Main" | "atkin_step1_y" | "transforms" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r02708 | "step" | "Main" | "atkin_step1_x" | "transforms" | "Sieve of Atkin step 1: iterate x"
    r02709 | "step" | "Main" | "atkin_step2_y" | "transforms" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r02710 | "step" | "Main" | "atkin_step2_x" | "transforms" | "Sieve of Atkin step 2: iterate x"
    r02711 | "step" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r02712 | "step" | "Main" | "atkin_step3_x" | "transforms" | "Sieve of Atkin step 3: iterate x"
    r02713 | "stopwords" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r02714 | "stopwords" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02715 | "stopwords" | "Language" | "stopwords" | "reads" | "Return a list of common English stopwords"
    r02716 | "stopwords" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02717 | "store" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r02718 | "store" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r02719 | "store" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r02720 | "store" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02721 | "store" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02722 | "store" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r02723 | "store" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r02724 | "store" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r02725 | "store" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r02726 | "store" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02727 | "store" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02728 | "store" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r02729 | "string" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r02730 | "string" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02731 | "string" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02732 | "string" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r02733 | "string" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r02734 | "string" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r02735 | "string" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r02736 | "string" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r02737 | "string" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r02738 | "string" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r02739 | "string" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r02740 | "string" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r02741 | "string" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r02742 | "string" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r02743 | "string" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r02744 | "string" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r02745 | "string" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r02746 | "string" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r02747 | "string" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r02748 | "string" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r02749 | "string" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r02750 | "string" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r02751 | "string" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r02752 | "string" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r02753 | "string" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r02754 | "string" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r02755 | "string" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r02756 | "string" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r02757 | "string" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r02758 | "string" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r02759 | "string" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r02760 | "string" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r02761 | "string" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r02762 | "string" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r02763 | "string" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r02764 | "string" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r02765 | "string" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r02766 | "string" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r02767 | "string" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02768 | "string" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02769 | "string" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02770 | "string" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02771 | "string" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r02772 | "string" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r02773 | "string" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r02774 | "string" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02775 | "string" | "Text" | "length" | "reads" | "Get the length of a string"
    r02776 | "string" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r02777 | "string" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r02778 | "string" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r02779 | "string" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r02780 | "string" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r02781 | "string" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r02782 | "string" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r02783 | "string" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r02784 | "string" | "Text" | "builder" | "creates" | "Create a new string builder"
    r02785 | "string" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r02786 | "string" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r02787 | "string" | "Types" | "string" | "validates" | "Check if a value is a string"
    r02788 | "string" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r02789 | "string" | "Types" | "float" | "creates" | "Parse a string as a float"
    r02790 | "string" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r02791 | "string" | "Types" | "string" | "reads" | "Convert a float to a string"
    r02792 | "string" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r02793 | "string" | "Types" | "value" | "validates" | "Validate a source string"
    r02794 | "string" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r02795 | "string" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r02796 | "string" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02797 | "string" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02798 | "string" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r02799 | "string" | "Format" | "octal" | "transforms" | "Converts an integer to octal string."
    r02800 | "string" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r02801 | "string" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r02802 | "string" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r02803 | "string" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r02804 | "string" | "Hash" | "sha256" | "reads" | "Hash a string to SHA-256 hex string"
    r02805 | "string" | "Hash" | "sha512" | "reads" | "Hash a string to SHA-512 hex string"
    r02806 | "string" | "Hash" | "blake3" | "reads" | "Hash a string to BLAKE3 hex string"
    r02807 | "string" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r02808 | "string" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r02809 | "string" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r02810 | "string" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r02811 | "string" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r02812 | "string" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r02813 | "string" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r02814 | "string" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r02815 | "string" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r02816 | "string" | "Parse" | "hexadecimal" | "reads" | "Encode a byte array as a hexadecimal string"
    r02817 | "string" | "Parse" | "hexadecimal" | "creates" | "Decode a hexadecimal string to a byte array"
    r02818 | "string" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r02819 | "string" | "Parse" | "base64" | "reads" | "Decode a Base64 string into a byte array"
    r02820 | "string" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r02821 | "string" | "Parse" | "csv" | "creates" | "Parse a CSV string into rows of fields"
    r02822 | "string" | "Parse" | "csv" | "reads" | "Serialize rows of fields to CSV string"
    r02823 | "string" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r02824 | "string" | "Parse" | "hexadecimal" | "transforms" | "Converts an integer to hexadecimal string."
    r02825 | "string" | "Parse" | "bin" | "transforms" | "Converts an integer to binary string."
    r02826 | "string" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r02827 | "string" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r02828 | "string" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r02829 | "string" | "Parse" | "date" | "validates" | "Check if a string matches a date format."
    r02830 | "string" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r02831 | "string" | "Parse" | "datetime" | "validates" | "Check if a string matches a datetime format."
    r02832 | "string" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r02833 | "string" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02834 | "string" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r02835 | "string" | "Pattern" | "split" | "transforms" | "Split a string by a regex pattern"
    r02836 | "string" | "Random" | "choice" | "inputs" | "Pick a random element from a string list"
    r02837 | "string" | "Random" | "shuffle" | "inputs" | "Randomly reorder a string list"
    r02838 | "string" | "Sequence" | "contains" | "validates" | "Check if a string is in a sequence"
    r02839 | "string" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r02840 | "string" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r02841 | "string" | "Text" | "length" | "reads" | "Get the length of a string"
    r02842 | "string" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r02843 | "string" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r02844 | "string" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r02845 | "string" | "Text" | "split" | "transforms" | "Split a string by a separator"
    r02846 | "string" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r02847 | "string" | "Text" | "lower" | "transforms" | "Convert a string to lowercase"
    r02848 | "string" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r02849 | "string" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r02850 | "string" | "Text" | "builder" | "creates" | "Create a new string builder"
    r02851 | "string" | "Text" | "string" | "transforms" | "Append a string to a builder"
    r02852 | "string" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r02853 | "string" | "Types" | "string" | "validates" | "Check if a value is a string"
    r02854 | "string" | "Types" | "integer" | "creates" | "Parse a string as an integer"
    r02855 | "string" | "Types" | "float" | "creates" | "Parse a string as a float"
    r02856 | "string" | "Types" | "string" | "reads" | "Convert an integer to a string"
    r02857 | "string" | "Types" | "string" | "reads" | "Convert a float to a string"
    r02858 | "string" | "Types" | "string" | "reads" | "Convert a boolean to a string"
    r02859 | "string" | "Types" | "value" | "validates" | "Validate a source string"
    r02860 | "string" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r02861 | "string" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r02862 | "string" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r02863 | "string" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r02864 | "strings" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r02865 | "strings" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r02866 | "strings" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r02867 | "strings" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r02868 | "strings" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r02869 | "strings" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r02870 | "strings" | "Sequence" | "sort" | "transforms" | "Sort a sequence of strings"
    r02871 | "strings" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r02872 | "strip" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r02873 | "strip" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r02874 | "struct" | "Config" | "config" | "transforms" | "Parse the toml data to Config struct"
    r02875 | "structural" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r02876 | "structural" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r02877 | "sub" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r02878 | "sub" | "Bytes" | "bytearray" | "reads" | "Extract a sub-range of bytes and return a new ByteArray"
    r02879 | "substring" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r02880 | "substring" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r02881 | "substring" | "Text" | "index" | "reads" | "Find the index of a substring"
    r02882 | "substring" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r02883 | "substring" | "Text" | "slice" | "transforms" | "Get a substring from start to end (exclusive)"
    r02884 | "substring" | "Text" | "contains" | "validates" | "Check if a string contains a substring"
    r02885 | "substring" | "Text" | "index" | "reads" | "Find the index of a substring"
    r02886 | "substring" | "Text" | "replace" | "transforms" | "Replace all occurrences of a substring"
    r02887 | "succeeded" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r02888 | "succeeded" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r02889 | "success" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r02890 | "successful" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r02891 | "successful" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r02892 | "suffix" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r02893 | "suffix" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r02894 | "suffixes" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r02895 | "suffixes" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r02896 | "system" | "System" | "system" | "inputs" | "Execute a system command"
    r02897 | "system" | "System" | "system" | "inputs" | "Execute a system command"
    r02898 | "table" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r02899 | "table" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r02900 | "table" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r02901 | "table" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02902 | "table" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02903 | "table" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r02904 | "table" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r02905 | "table" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r02906 | "table" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r02907 | "table" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r02908 | "table" | "Store" | "version" | "inputs" | "List all versions of a table"
    r02909 | "table" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r02910 | "table" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r02911 | "table" | "Table" | "new" | "creates" | "Create a new empty table"
    r02912 | "table" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r02913 | "table" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r02914 | "table" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r02915 | "table" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r02916 | "table" | "Table" | "values" | "reads" | "Get all values in a table"
    r02917 | "table" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r02918 | "table" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r02919 | "table" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r02920 | "table" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r02921 | "table" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r02922 | "table" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02923 | "table" | "Store" | "patch" | "transforms" | "Apply a diff to a table producing a new table"
    r02924 | "table" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r02925 | "table" | "Store" | "lookup" | "outputs" | "Compile a table to lookup format"
    r02926 | "table" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r02927 | "table" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r02928 | "table" | "Store" | "version" | "inputs" | "List all versions of a table"
    r02929 | "table" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r02930 | "table" | "Store" | "add" | "outputs" | "Add a row to a store-backed lookup table"
    r02931 | "table" | "Table" | "new" | "creates" | "Create a new empty table"
    r02932 | "table" | "Table" | "has" | "validates" | "Check if a key exists in a table"
    r02933 | "table" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r02934 | "table" | "Table" | "remove" | "transforms" | "Remove a key from a table (returns new table)"
    r02935 | "table" | "Table" | "keys" | "reads" | "Get all keys in a table"
    r02936 | "table" | "Table" | "values" | "reads" | "Get all values in a table"
    r02937 | "table" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r02938 | "table" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r02939 | "tables" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r02940 | "tables" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r02941 | "tag" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r02942 | "tag" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r02943 | "tco" | "Main" | "eval_dispatch" | "transforms" | "Main dispatch loop — all opcodes inlined for full TCO"
    r02944 | "tcp" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02945 | "tcp" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r02946 | "tcp" | "Network" | "socket" | "inputs" | "Open a TCP connection to a remote host and port"
    r02947 | "tcp" | "Network" | "server" | "inputs" | "Bind and listen on a TCP port"
    r02948 | "terminal" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r02949 | "termination" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r02950 | "test" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02951 | "test" | "Example" | "user" | "transforms" | "test"
    r02952 | "test" | "Example" | "ensure_user" | "matches" | "test2"
    r02953 | "test" | "Commands" | "test" | "outputs" | "Run the test command"
    r02954 | "test" | "Pattern" | "test" | "validates" | "Test if a string matches a regex pattern"
    r02955 | "text" | "Language" | "words" | "reads" | "Extract individual words from text"
    r02956 | "text" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r02957 | "text" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r02958 | "text" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r02959 | "text" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r02960 | "text" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r02961 | "text" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02962 | "text" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r02963 | "text" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r02964 | "text" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r02965 | "text" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r02966 | "text" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r02967 | "text" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r02968 | "text" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r02969 | "text" | "System" | "console" | "outputs" | "Print text to the console"
    r02970 | "text" | "Types" | "text" | "validates" | "Check if a Value is text"
    r02971 | "text" | "Language" | "words" | "reads" | "Extract individual words from text"
    r02972 | "text" | "Language" | "sentences" | "reads" | "Split text into sentences"
    r02973 | "text" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r02974 | "text" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r02975 | "text" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r02976 | "text" | "Language" | "normalize" | "transforms" | "Normalize text by lowercasing and folding accented characters to ASCII"
    r02977 | "text" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r02978 | "text" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r02979 | "text" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r02980 | "text" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r02981 | "text" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r02982 | "text" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r02983 | "text" | "Pattern" | "find_all" | "reads" | "Find all matches of a pattern in text"
    r02984 | "text" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r02985 | "text" | "System" | "console" | "outputs" | "Print text to the console"
    r02986 | "text" | "Types" | "text" | "validates" | "Check if a Value is text"
    r02987 | "that" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r02988 | "that" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02989 | "that" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02990 | "that" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r02991 | "that" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r02992 | "that" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r02993 | "that" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r02994 | "that" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r02995 | "that" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r02996 | "the" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r02997 | "the" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r02998 | "the" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r02999 | "the" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r03000 | "the" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r03001 | "the" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r03002 | "the" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r03003 | "the" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r03004 | "the" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r03005 | "the" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r03006 | "the" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r03007 | "the" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r03008 | "the" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r03009 | "the" | "Language" | "start" | "reads" | "Get the start position of a token"
    r03010 | "the" | "Language" | "end" | "reads" | "Get the end position of a token"
    r03011 | "the" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r03012 | "the" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r03013 | "the" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r03014 | "the" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r03015 | "the" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r03016 | "the" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r03017 | "the" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r03018 | "the" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r03019 | "the" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r03020 | "the" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r03021 | "the" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r03022 | "the" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r03023 | "the" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r03024 | "the" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r03025 | "the" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r03026 | "the" | "Path" | "extension" | "reads" | "Get the file extension"
    r03027 | "the" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r03028 | "the" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r03029 | "the" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r03030 | "the" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r03031 | "the" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r03032 | "the" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r03033 | "the" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r03034 | "the" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r03035 | "the" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r03036 | "the" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r03037 | "the" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r03038 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03039 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03040 | "the" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03041 | "the" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03042 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03043 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r03044 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r03045 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r03046 | "the" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r03047 | "the" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r03048 | "the" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r03049 | "the" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r03050 | "the" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r03051 | "the" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r03052 | "the" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r03053 | "the" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r03054 | "the" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r03055 | "the" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r03056 | "the" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r03057 | "the" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r03058 | "the" | "System" | "console" | "outputs" | "Print text to the console"
    r03059 | "the" | "System" | "console" | "inputs" | "Read a line from the console"
    r03060 | "the" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r03061 | "the" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r03062 | "the" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03063 | "the" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r03064 | "the" | "Text" | "length" | "reads" | "Get the length of a string"
    r03065 | "the" | "Text" | "index" | "reads" | "Find the index of a substring"
    r03066 | "the" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r03067 | "the" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r03068 | "the" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r03069 | "the" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r03070 | "the" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r03071 | "the" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r03072 | "the" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r03073 | "the" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r03074 | "the" | "Commands" | "build" | "outputs" | "Run the build command"
    r03075 | "the" | "Commands" | "check" | "outputs" | "Run the check command"
    r03076 | "the" | "Commands" | "compiler" | "outputs" | "Run the compiler command"
    r03077 | "the" | "Commands" | "format" | "outputs" | "Run the format command"
    r03078 | "the" | "Commands" | "lsp" | "outputs" | "Run the lsp command"
    r03079 | "the" | "Commands" | "new" | "outputs" | "Run the new command"
    r03080 | "the" | "Commands" | "test" | "outputs" | "Run the test command"
    r03081 | "the" | "Commands" | "view" | "outputs" | "Run the view command"
    r03082 | "the" | "Config" | "config" | "transforms" | "Parse the toml data to Config struct"
    r03083 | "the" | "Character" | "at" | "reads" | "Gets the character at a specific index in a string."
    r03084 | "the" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r03085 | "the" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r03086 | "the" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r03087 | "the" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r03088 | "the" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r03089 | "the" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r03090 | "the" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r03091 | "the" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r03092 | "the" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r03093 | "the" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r03094 | "the" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r03095 | "the" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r03096 | "the" | "Language" | "start" | "reads" | "Get the start position of a token"
    r03097 | "the" | "Language" | "end" | "reads" | "Get the end position of a token"
    r03098 | "the" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r03099 | "the" | "Math" | "power" | "reads" | "Base raised to the power of exp"
    r03100 | "the" | "Math" | "round" | "reads" | "Round a float to the nearest integer"
    r03101 | "the" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r03102 | "the" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r03103 | "the" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r03104 | "the" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r03105 | "the" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r03106 | "the" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r03107 | "the" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r03108 | "the" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r03109 | "the" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r03110 | "the" | "Path" | "parent" | "reads" | "Get the parent directory of a path"
    r03111 | "the" | "Path" | "name" | "reads" | "Get the filename component of a path"
    r03112 | "the" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r03113 | "the" | "Path" | "extension" | "reads" | "Get the file extension"
    r03114 | "the" | "Pattern" | "search" | "reads" | "Search for the first match of a pattern in text"
    r03115 | "the" | "Pattern" | "text" | "reads" | "Get the matched text from a match result"
    r03116 | "the" | "Pattern" | "start" | "reads" | "Get the start position of a match"
    r03117 | "the" | "Pattern" | "end" | "reads" | "Get the end position of a match"
    r03118 | "the" | "Sequence" | "length" | "reads" | "Get the length of a sequence"
    r03119 | "the" | "Sequence" | "length" | "reads" | "Get the length of an array"
    r03120 | "the" | "Sequence" | "first" | "reads" | "Get the first element of a sequence"
    r03121 | "the" | "Sequence" | "last" | "reads" | "Get the last element of a sequence"
    r03122 | "the" | "Sequence" | "value" | "reads" | "Get the element at a position in a sequence, or None if out of bounds"
    r03123 | "the" | "Sequence" | "index" | "reads" | "Find the index of an integer in a sequence"
    r03124 | "the" | "Sequence" | "index" | "reads" | "Find the index of a string in a sequence"
    r03125 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03126 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03127 | "the" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03128 | "the" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03129 | "the" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03130 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r03131 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r03132 | "the" | "Sequence" | "map" | "transforms" | "Apply function to every element, returning a new array of the same length"
    r03133 | "the" | "Store" | "store" | "outputs" | "Create a new store at the given directory path"
    r03134 | "the" | "Store" | "table" | "inputs" | "Load a table by name from the store"
    r03135 | "the" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r03136 | "the" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r03137 | "the" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r03138 | "the" | "Store" | "integrity" | "reads" | "Compute the integrity hash of a table"
    r03139 | "the" | "Store" | "merged" | "reads" | "Get the merged table from a successful merge result"
    r03140 | "the" | "Store" | "conflicts" | "reads" | "Get the list of conflicts from a conflicted merge result"
    r03141 | "the" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r03142 | "the" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r03143 | "the" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r03144 | "the" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r03145 | "the" | "System" | "console" | "outputs" | "Print text to the console"
    r03146 | "the" | "System" | "console" | "inputs" | "Read a line from the console"
    r03147 | "the" | "System" | "console" | "inputs" | "Read exactly count bytes from the console"
    r03148 | "the" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r03149 | "the" | "System" | "line" | "inputs" | "Read the next line from an open file handle (returns empty string at EOF)"
    r03150 | "the" | "Table" | "length" | "reads" | "Get the number of entries in a table"
    r03151 | "the" | "Text" | "length" | "reads" | "Get the length of a string"
    r03152 | "the" | "Text" | "index" | "reads" | "Find the index of a substring"
    r03153 | "the" | "Text" | "build" | "reads" | "Build the final string from a builder"
    r03154 | "the" | "Text" | "length" | "reads" | "Get the current length of a builder"
    r03155 | "the" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r03156 | "the" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r03157 | "the" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r03158 | "the" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r03159 | "the" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r03160 | "the" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r03161 | "them" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r03162 | "then" | "Main" | "read_message" | "inputs" | "Read one framed message from stdin.
Parses the Content-Length header, skips the blank separator line,
then reads exactly that many bytes from stdin."
    r03163 | "three" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r03164 | "three" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r03165 | "three" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r03166 | "three" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r03167 | "time" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r03168 | "time" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r03169 | "time" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r03170 | "time" | "Time" | "time" | "inputs" | "Get current time"
    r03171 | "time" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r03172 | "time" | "Time" | "date" | "reads" | "Extract date from a time"
    r03173 | "time" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r03174 | "time" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r03175 | "time" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r03176 | "time" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r03177 | "time" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r03178 | "time" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r03179 | "time" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r03180 | "time" | "Parse" | "time" | "validates" | "Check if a string matches a time format."
    r03181 | "time" | "Time" | "time" | "inputs" | "Get current time"
    r03182 | "time" | "Time" | "time" | "validates" | "Check if a time is in the past"
    r03183 | "time" | "Time" | "date" | "reads" | "Extract date from a time"
    r03184 | "time" | "Time" | "datetime" | "reads" | "Extract datetime from a time"
    r03185 | "time" | "Time" | "clock" | "reads" | "Extract clock from a time"
    r03186 | "times" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r03187 | "times" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r03188 | "times" | "Text" | "repeat" | "transforms" | "Repeat a string count times"
    r03189 | "times" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r03190 | "times" | "Main" | "encode_loop" | "reads" | "Encode loop: encode data ITERATIONS times, accumulate encoded sizes"
    r03191 | "times" | "Main" | "decode_loop" | "reads" | "Decode loop: decode encoded string ITERATIONS times, accumulate success count"
    r03192 | "timestamp" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r03193 | "timestamp" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r03194 | "timestamp" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r03195 | "timestamp" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r03196 | "timestamp" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r03197 | "timestamp" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r03198 | "timestamp" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r03199 | "timestamp" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r03200 | "timestamp" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r03201 | "timestamp" | "Time" | "datetime" | "transforms" | "Convert datetime to timestamp"
    r03202 | "together" | "Path" | "join" | "transforms" | "Join path components together"
    r03203 | "together" | "Path" | "join" | "transforms" | "Join path components together"
    r03204 | "token" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r03205 | "token" | "Language" | "start" | "reads" | "Get the start position of a token"
    r03206 | "token" | "Language" | "end" | "reads" | "Get the end position of a token"
    r03207 | "token" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r03208 | "token" | "Language" | "text" | "reads" | "Get the matched text from a token"
    r03209 | "token" | "Language" | "start" | "reads" | "Get the start position of a token"
    r03210 | "token" | "Language" | "end" | "reads" | "Get the end position of a token"
    r03211 | "token" | "Language" | "kind" | "reads" | "Get the kind of a token as an integer"
    r03212 | "tokenize" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r03213 | "tokenize" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r03214 | "tokens" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r03215 | "tokens" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r03216 | "toml" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r03217 | "toml" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r03218 | "toml" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r03219 | "toml" | "Config" | "config" | "transforms" | "Parse the toml data to Config struct"
    r03220 | "toml" | "Parse" | "toml" | "creates" | "Parse a TOML string into a Table"
    r03221 | "toml" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r03222 | "toml" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r03223 | "top" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r03224 | "top" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r03225 | "total" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r03226 | "total" | "Time" | "duration" | "reads" | "Read total seconds from a duration"
    r03227 | "transliterate" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r03228 | "transliterate" | "Language" | "transliterate" | "transforms" | "Transliterate accented characters to ASCII equivalents preserving case"
    r03229 | "transpose" | "Main" | "transpose_cols" | "transforms" | "Transpose: copy columns of src into rows of dst"
    r03230 | "transpose" | "Main" | "transpose_rows" | "transforms" | "Transpose: iterate rows"
    r03231 | "transpose" | "Main" | "transpose" | "transforms" | "Transpose a matrix"
    r03232 | "transpose" | "Main" | "mul" | "transforms" | "Multiply two matrices: a * b with transpose optimization"
    r03233 | "transposed" | "Main" | "dot" | "reads" | "Dot product of row i of a with row j of bt (transposed b)"
    r03234 | "trie" | "Main" | "trie_insert_digits" | "transforms" | "Insert a prime into the trie by iterating its digits"
    r03235 | "trie" | "Main" | "build_trie_iter" | "transforms" | "Build the trie from sieve: iterate indices and insert primes"
    r03236 | "trie" | "Main" | "trie_find_prefix" | "reads" | "Navigate trie to the prefix node, return found node or empty table"
    r03237 | "trie" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r03238 | "trie" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r03239 | "trim" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r03240 | "trim" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r03241 | "truncates" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r03242 | "truncates" | "Types" | "integer" | "creates" | "Convert a float to an integer (truncates)"
    r03243 | "two" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r03244 | "two" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r03245 | "two" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r03246 | "two" | "Math" | "min" | "reads" | "Minimum of two integers"
    r03247 | "two" | "Math" | "min" | "reads" | "Minimum of two floats"
    r03248 | "two" | "Math" | "max" | "reads" | "Maximum of two integers"
    r03249 | "two" | "Math" | "max" | "reads" | "Maximum of two floats"
    r03250 | "two" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r03251 | "two" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r03252 | "two" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r03253 | "two" | "Bytes" | "bytearray" | "transforms" | "Concatenate two byte arrays and return a new combined ByteArray"
    r03254 | "two" | "Language" | "distance" | "reads" | "Compute the Levenshtein edit distance between two strings"
    r03255 | "two" | "Language" | "similarity" | "reads" | "Compute normalized similarity between two strings (0.0 to 1.0)"
    r03256 | "two" | "Math" | "min" | "reads" | "Minimum of two integers"
    r03257 | "two" | "Math" | "min" | "reads" | "Minimum of two floats"
    r03258 | "two" | "Math" | "max" | "reads" | "Maximum of two integers"
    r03259 | "two" | "Math" | "max" | "reads" | "Maximum of two floats"
    r03260 | "two" | "Store" | "diff" | "transforms" | "Compute the structural diff between two tables"
    r03261 | "two" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r03262 | "two" | "Time" | "duration" | "transforms" | "Compute difference between two times"
    r03263 | "two" | "Main" | "mul" | "transforms" | "Multiply two matrices: a * b with transpose optimization"
    r03264 | "type" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r03265 | "type" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r03266 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r03267 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r03268 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03269 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03270 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r03271 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r03272 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r03273 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03274 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03275 | "unchecked" | "Sequence" | "get" | "reads" | "Get element at index (unchecked)"
    r03276 | "under" | "Main" | "collect_from_keys" | "transforms" | "Collect all terminal primes under a trie node as a comma-separated string"
    r03277 | "under" | "Main" | "collect_primes" | "transforms" | "Collect all primes under a trie node as comma-separated string"
    r03278 | "unicode" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r03279 | "unicode" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r03280 | "unicode" | "Types" | "code" | "reads" | "Get the Unicode code point of a character"
    r03281 | "unicode" | "Types" | "character" | "creates" | "Create a character from a Unicode code point"
    r03282 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r03283 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r03284 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r03285 | "unit" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r03286 | "unit" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r03287 | "unit" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r03288 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r03289 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r03290 | "unit" | "Sequence" | "each" | "outputs" | "Run function for each element; returns Unit"
    r03291 | "unit" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r03292 | "unit" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r03293 | "unit" | "Types" | "unit" | "validates" | "Check if an Option is a Unit"
    r03294 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r03295 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r03296 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r03297 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r03298 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r03299 | "unknown" | "Sequence" | "filter" | "creates" | "Keep elements matching predicate; returns List because length is unknown"
    r03300 | "until" | "Main" | "handler" | "listens" | "Cooperative loop — processes events until Exit."
    r03301 | "unwrap" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r03302 | "unwrap" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r03303 | "unwrap" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r03304 | "unwrap" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r03305 | "unwrap" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r03306 | "unwrap" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r03307 | "unwrap" | "Types" | "error" | "validates" | "Check if a Result have an Error to unwrap"
    r03308 | "unwrap" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default integer"
    r03309 | "unwrap" | "Types" | "unwrap" | "reads" | "Unwrap an Option or return a default string"
    r03310 | "unwrap" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r03311 | "uppercase" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r03312 | "uppercase" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r03313 | "uppercase" | "Character" | "uppercase" | "validates" | "Checks if a character is uppercase (A-Z)."
    r03314 | "uppercase" | "Text" | "upper" | "transforms" | "Convert a string to uppercase"
    r03315 | "url" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r03316 | "url" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r03317 | "url" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r03318 | "url" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r03319 | "url" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r03320 | "url" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r03321 | "url" | "Parse" | "url" | "reads" | "Parse a URL string into its components"
    r03322 | "url" | "Parse" | "url" | "creates" | "Construct a URL from scheme, host, and path"
    r03323 | "url" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r03324 | "url" | "Parse" | "url" | "transforms" | "Add query parameters to a URL"
    r03325 | "url" | "Parse" | "host" | "reads" | "Read the host component of a URL"
    r03326 | "url" | "Parse" | "port" | "reads" | "Read the port component of a URL (-1 if not set)"
    r03327 | "using" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r03328 | "using" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r03329 | "using" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r03330 | "using" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r03331 | "using" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r03332 | "using" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r03333 | "using" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r03334 | "using" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r03335 | "using" | "Format" | "time" | "transforms" | "Format a time as a string using a pattern."
    r03336 | "using" | "Format" | "date" | "transforms" | "Format a date as a string using a pattern."
    r03337 | "using" | "Format" | "datetime" | "transforms" | "Format a datetime as a string using a pattern."
    r03338 | "using" | "Format" | "duration" | "transforms" | "Format a duration as a string using a pattern."
    r03339 | "using" | "Parse" | "time" | "creates" | "Parse a string into a time using a pattern."
    r03340 | "using" | "Parse" | "date" | "creates" | "Parse a string into a date using a pattern."
    r03341 | "using" | "Parse" | "datetime" | "creates" | "Parse a string into a datetime using a pattern."
    r03342 | "using" | "Parse" | "duration" | "creates" | "Parse a string into a duration using a pattern."
    r03343 | "valid" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r03344 | "valid" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r03345 | "valid" | "Time" | "date" | "validates" | "Check if a date is valid"
    r03346 | "valid" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r03347 | "valid" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r03348 | "valid" | "Parse" | "url" | "validates" | "Check if a string is a valid URL"
    r03349 | "valid" | "Parse" | "hexadecimal" | "validates" | "Check if a string is valid hexadecimal"
    r03350 | "valid" | "Time" | "date" | "validates" | "Check if a date is valid"
    r03351 | "valid" | "Time" | "datetime" | "validates" | "Check if a datetime is valid"
    r03352 | "valid" | "Time" | "clock" | "validates" | "Check if clock components are valid"
    r03353 | "validate" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r03354 | "validate" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r03355 | "validate" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r03356 | "validate" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r03357 | "validate" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r03358 | "validate" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r03359 | "validate" | "System" | "console" | "validates" | "Validate console input"
    r03360 | "validate" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r03361 | "validate" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r03362 | "validate" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r03363 | "validate" | "System" | "process" | "validates" | "Validate a process name"
    r03364 | "validate" | "Types" | "value" | "validates" | "Validate a source string"
    r03365 | "validate" | "Parse" | "json" | "validates" | "Validate a JSON string"
    r03366 | "validate" | "Parse" | "toml" | "validates" | "Validate a TOML string"
    r03367 | "validate" | "Parse" | "base64" | "validates" | "Validate a Base64 string"
    r03368 | "validate" | "Parse" | "csv" | "validates" | "Validate a CSV string"
    r03369 | "validate" | "Store" | "store" | "validates" | "Validate that a store directory exists"
    r03370 | "validate" | "Store" | "table" | "validates" | "Validate that a table exists in the store"
    r03371 | "validate" | "System" | "console" | "validates" | "Validate console input"
    r03372 | "validate" | "System" | "file" | "validates" | "Validate that a file exists and is readable"
    r03373 | "validate" | "System" | "system" | "validates" | "Validate a command exists in PATH"
    r03374 | "validate" | "System" | "dir" | "validates" | "Validate that a directory exists"
    r03375 | "validate" | "System" | "process" | "validates" | "Validate a process name"
    r03376 | "validate" | "Types" | "value" | "validates" | "Validate a source string"
    r03377 | "value" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r03378 | "value" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r03379 | "value" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r03380 | "value" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r03381 | "value" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r03382 | "value" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r03383 | "value" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03384 | "value" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r03385 | "value" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r03386 | "value" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r03387 | "value" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r03388 | "value" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r03389 | "value" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r03390 | "value" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r03391 | "value" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r03392 | "value" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r03393 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03394 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03395 | "value" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03396 | "value" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03397 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03398 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03399 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03400 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03401 | "value" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r03402 | "value" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r03403 | "value" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r03404 | "value" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r03405 | "value" | "Table" | "get" | "reads" | "Get a value by key"
    r03406 | "value" | "Types" | "string" | "validates" | "Check if a value is a string"
    r03407 | "value" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r03408 | "value" | "Types" | "float" | "validates" | "Check if a value is a float"
    r03409 | "value" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r03410 | "value" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r03411 | "value" | "Types" | "character" | "validates" | "Check if a value is a character"
    r03412 | "value" | "Types" | "text" | "validates" | "Check if a Value is text"
    r03413 | "value" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r03414 | "value" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r03415 | "value" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r03416 | "value" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r03417 | "value" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r03418 | "value" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r03419 | "value" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r03420 | "value" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r03421 | "value" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r03422 | "value" | "Math" | "abs" | "reads" | "Absolute value of an integer"
    r03423 | "value" | "Math" | "abs" | "reads" | "Absolute value of a float"
    r03424 | "value" | "Math" | "floor" | "reads" | "Floor of a float (largest integer <= value)"
    r03425 | "value" | "Math" | "ceil" | "reads" | "Ceiling of a float (smallest integer >= value)"
    r03426 | "value" | "Parse" | "value" | "creates" | "Parse a source string into a Value"
    r03427 | "value" | "Parse" | "toml" | "reads" | "Serialize a Value to TOML string"
    r03428 | "value" | "Parse" | "json" | "creates" | "Parse a JSON string into a Value"
    r03429 | "value" | "Parse" | "json" | "reads" | "Serialize a Value to JSON string"
    r03430 | "value" | "Parse" | "tag" | "reads" | "Get the tag (type name) of a Value"
    r03431 | "value" | "Parse" | "text" | "reads" | "Get the text content of a Value"
    r03432 | "value" | "Parse" | "number" | "reads" | "Get the numeric content of a Value"
    r03433 | "value" | "Parse" | "decimal" | "reads" | "Get the decimal content of a Value"
    r03434 | "value" | "Parse" | "bool" | "reads" | "Get the boolean content of a Value"
    r03435 | "value" | "Parse" | "array" | "reads" | "Get the array content of a Value"
    r03436 | "value" | "Parse" | "object" | "reads" | "Get the object content of a Value"
    r03437 | "value" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r03438 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03439 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03440 | "value" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03441 | "value" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03442 | "value" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03443 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03444 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03445 | "value" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03446 | "value" | "Store" | "column" | "reads" | "Get the column name from a value conflict"
    r03447 | "value" | "Store" | "local_value" | "reads" | "Get the local value from a value conflict"
    r03448 | "value" | "Store" | "remote_value" | "reads" | "Get the remote value from a value conflict"
    r03449 | "value" | "Table" | "add" | "transforms" | "Add a key-value pair to a table (returns new table)"
    r03450 | "value" | "Table" | "get" | "reads" | "Get a value by key"
    r03451 | "value" | "Types" | "string" | "validates" | "Check if a value is a string"
    r03452 | "value" | "Types" | "integer" | "validates" | "Check if a value is an integer"
    r03453 | "value" | "Types" | "float" | "validates" | "Check if a value is a float"
    r03454 | "value" | "Types" | "decimal" | "validates" | "Check if a value is a decimal"
    r03455 | "value" | "Types" | "boolean" | "validates" | "Check if a value is a boolean"
    r03456 | "value" | "Types" | "character" | "validates" | "Check if a value is a character"
    r03457 | "value" | "Types" | "text" | "validates" | "Check if a Value is text"
    r03458 | "value" | "Types" | "number" | "validates" | "Check if a Value is a number"
    r03459 | "value" | "Types" | "decimal" | "validates" | "Check if a Value is a decimal"
    r03460 | "value" | "Types" | "boolean" | "validates" | "Check if a Value is a boolean"
    r03461 | "value" | "Types" | "array" | "validates" | "Check if a Value is an array"
    r03462 | "value" | "Types" | "object" | "validates" | "Check if a Value is an object"
    r03463 | "value" | "Types" | "unit" | "validates" | "Check if a Value is unit"
    r03464 | "value" | "Types" | "ok" | "validates" | "Check if a Result have a Value to unwrap"
    r03465 | "value" | "Types" | "value" | "validates" | "Check if an Option is a Value and not Unit"
    r03466 | "value" | "Types" | "unwrap" | "transforms" | "Unwrap an Option, returning the inner value"
    r03467 | "values" | "Table" | "values" | "reads" | "Get all values in a table"
    r03468 | "values" | "Table" | "values" | "reads" | "Get all values in a table"
    r03469 | "variant" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03470 | "variant" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03471 | "variant" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r03472 | "variant" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r03473 | "variant" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r03474 | "variant" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03475 | "variant" | "Sequence" | "get" | "reads" | "Get element at index (unchecked) - mutable variant"
    r03476 | "variant" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r03477 | "variant" | "Sequence" | "get_safe" | "reads" | "Get element at index, or None if out of bounds - mutable variant"
    r03478 | "variant" | "Store" | "variant" | "reads" | "Get the variant name from a conflict"
    r03479 | "verify" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03480 | "verify" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03481 | "verify" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03482 | "verify" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r03483 | "verify" | "Hash" | "sha256" | "validates" | "Verify data matches an expected SHA-256 hash"
    r03484 | "verify" | "Hash" | "sha512" | "validates" | "Verify data matches an expected SHA-512 hash"
    r03485 | "verify" | "Hash" | "blake3" | "validates" | "Verify data matches an expected BLAKE3 hash"
    r03486 | "verify" | "Hash" | "hmac" | "validates" | "Verify an HMAC-SHA256 signature"
    r03487 | "version" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r03488 | "version" | "Store" | "rollback" | "outputs" | "Rollback a table to a previous version"
    r03489 | "versions" | "Store" | "version" | "inputs" | "List all versions of a table"
    r03490 | "versions" | "Store" | "version" | "inputs" | "List all versions of a table"
    r03491 | "view" | "Commands" | "view" | "outputs" | "Run the view command"
    r03492 | "waiting" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r03493 | "warning" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r03494 | "warning" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r03495 | "way" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r03496 | "way" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r03497 | "way" | "Store" | "merge" | "transforms" | "Three-way merge of two diffs against a base table"
    r03498 | "way" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r03499 | "weekday" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r03500 | "weekday" | "Time" | "weekday" | "reads" | "Get weekday from a date"
    r03501 | "weekend" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r03502 | "weekend" | "Time" | "weekday" | "validates" | "Check if a date falls on a weekend"
    r03503 | "when" | "Main" | "read" | "streams" | "Read a file line by line, one chunk at a time.
Each iteration: read the next line chunk, echo it to console.
Exits when file is fully consumed."
    r03504 | "when" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r03505 | "where" | "Main" | "atkin_step1_y" | "transforms" | "Sieve of Atkin step 1: flip bits for n = 4*x*x + y*y where n % 12 == 1 or 5"
    r03506 | "where" | "Main" | "atkin_step2_y" | "transforms" | "Sieve of Atkin step 2: flip bits for n = 3*x*x + y*y where n % 12 == 7"
    r03507 | "where" | "Main" | "atkin_step3_y" | "transforms" | "Sieve of Atkin step 3: flip bits for n = 3*x*x - y*y where x > y and n % 12 == 11"
    r03508 | "whether" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r03509 | "whether" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r03510 | "whitespace" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r03511 | "whitespace" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r03512 | "whitespace" | "Character" | "whitespace" | "validates" | "Checks if a character is whitespace."
    r03513 | "whitespace" | "Text" | "trim" | "transforms" | "Trim whitespace from both ends of a string"
    r03514 | "width" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r03515 | "width" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r03516 | "width" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r03517 | "width" | "Format" | "pad_left" | "transforms" | "Pads a string on the left to reach the specified width."
    r03518 | "width" | "Format" | "pad_right" | "transforms" | "Pads a string on the right to reach the specified width."
    r03519 | "width" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r03520 | "with" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r03521 | "with" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r03522 | "with" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r03523 | "with" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r03524 | "with" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r03525 | "with" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r03526 | "with" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r03527 | "with" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r03528 | "with" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03529 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03530 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03531 | "with" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03532 | "with" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03533 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03534 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03535 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03536 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03537 | "with" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r03538 | "with" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r03539 | "with" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r03540 | "with" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r03541 | "with" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r03542 | "with" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r03543 | "with" | "Main" | "repl" | "streams" | "Read stdin lines and echo them back, one chunk at a time.
Each iteration: print prompt, read a line, echo it.
Exits when called with Exit or on process termination."
    r03544 | "with" | "Format" | "decimal" | "transforms" | "Formats a number with the specified decimal places."
    r03545 | "with" | "Format" | "decimal" | "transforms" | "Formats a float with the specified decimal places."
    r03546 | "with" | "Language" | "tokens" | "reads" | "Tokenize text into a list of tokens with position and kind"
    r03547 | "with" | "Pattern" | "replace" | "transforms" | "Replace all occurrences of a pattern with a replacement string"
    r03548 | "with" | "Log" | "debug" | "detached" | "Send a console message to stdout of a debug message 
with severity and timestamp added"
    r03549 | "with" | "Log" | "info" | "detached" | "Send a console message to stdout of a info message 
with severity and timestamp added"
    r03550 | "with" | "Log" | "warning" | "detached" | "Send a console message to stdout of a warning message 
with severity and timestamp added"
    r03551 | "with" | "Log" | "error" | "detached" | "Send a console message to stdout of a error message 
with severity and timestamp added"
    r03552 | "with" | "Sequence" | "range" | "creates" | "Create a sequence of integers from start to end (exclusive) with given step"
    r03553 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03554 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03555 | "with" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03556 | "with" | "Sequence" | "array" | "creates" | "Create a mutable array of given size with every element set to the default value"
    r03557 | "with" | "Sequence" | "array" | "creates" | "Create an array of given size with every element set to the default value"
    r03558 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03559 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03560 | "with" | "Sequence" | "reduce" | "reads" | "Accumulate elements with function, starting from initial value"
    r03561 | "with" | "Store" | "table" | "outputs" | "Save a table to the store with optimistic concurrency"
    r03562 | "with" | "Store" | "merge" | "transforms" | "Three-way merge with a conflict resolver"
    r03563 | "with" | "System" | "system" | "outputs" | "Exit the process with a status code"
    r03564 | "with" | "Text" | "starts" | "validates" | "Check if a string starts with a prefix"
    r03565 | "with" | "Text" | "ends" | "validates" | "Check if a string ends with a suffix"
    r03566 | "with" | "Text" | "join" | "transforms" | "Join a list of strings with a separator"
    r03567 | "with" | "Main" | "gen" | "transforms" | "Generate an n x n matrix with seed scaling"
    r03568 | "with" | "Main" | "dot" | "reads" | "Dot product of row i of a with row j of bt (transposed b)"
    r03569 | "with" | "Main" | "mul" | "transforms" | "Multiply two matrices: a * b with transpose optimization"
    r03570 | "within" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r03571 | "within" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03572 | "within" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r03573 | "within" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r03574 | "within" | "Format" | "center" | "transforms" | "Centers a string within the specified width."
    r03575 | "within" | "Random" | "integer" | "inputs" | "Generate a random integer within a range (inclusive)"
    r03576 | "within" | "Random" | "integer" | "validates" | "Check if a value falls within a range"
    r03577 | "within" | "Random" | "decimal" | "inputs" | "Generate a random decimal within a range"
    r03578 | "without" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r03579 | "without" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r03580 | "without" | "Main" | "fire" | "detached" | "Fire and forget — logs a message without waiting."
    r03581 | "without" | "Path" | "stem" | "reads" | "Get the filename without extension"
    r03582 | "without" | "Store" | "merged" | "validates" | "Check whether a merge succeeded without conflicts"
    r03583 | "word" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r03584 | "word" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r03585 | "word" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r03586 | "word" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r03587 | "word" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r03588 | "word" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r03589 | "word" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r03590 | "word" | "Language" | "stem" | "transforms" | "Apply the Porter stemming algorithm to a word"
    r03591 | "word" | "Language" | "root" | "transforms" | "Strip common suffixes to find the root form of a word"
    r03592 | "word" | "Language" | "soundex" | "reads" | "Compute the Soundex phonetic code for a word"
    r03593 | "word" | "Language" | "metaphone" | "reads" | "Compute the Double Metaphone phonetic code for a word"
    r03594 | "word" | "Language" | "ngrams" | "reads" | "Extract word-level n-grams from text"
    r03595 | "word" | "Language" | "bigrams" | "reads" | "Extract word-level bigrams from text"
    r03596 | "word" | "Language" | "frequency" | "reads" | "Count word frequencies in text and return a table of word to count"
    r03597 | "words" | "Language" | "words" | "reads" | "Extract individual words from text"
    r03598 | "words" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r03599 | "words" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r03600 | "words" | "Language" | "words" | "reads" | "Extract individual words from text"
    r03601 | "words" | "Language" | "without_stopwords" | "transforms" | "Remove common stopwords from text and return remaining words"
    r03602 | "words" | "Language" | "keywords" | "reads" | "Extract the top N most frequent words from text"
    r03603 | "write" | "Network" | "message" | "outputs" | "Write data to a socket"
    r03604 | "write" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03605 | "write" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03606 | "write" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03607 | "write" | "System" | "file" | "outputs" | "Write content to a file"
    r03608 | "write" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03609 | "write" | "Main" | "write" | "streams" | "Write stdin chunks to a file, one line at a time.
Each iteration: read a line from stdin, write it as a chunk to the file.
Exits on EOF (Ctrl+D)."
    r03610 | "write" | "Network" | "message" | "outputs" | "Write data to a socket"
    r03611 | "write" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03612 | "write" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03613 | "write" | "Sequence" | "set" | "transforms" | "Set element at index (copy-on-write for immutable, in-place for mutable)"
    r03614 | "write" | "System" | "file" | "outputs" | "Write content to a file"
    r03615 | "write" | "System" | "line" | "outputs" | "Write a line to an open file handle"
    r03616 | "writing" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r03617 | "writing" | "System" | "writer" | "creates" | "Open a file for appending (returns a file handle for writing)"
    r03618 | "year" | "Time" | "days" | "validates" | "Check if a year is a leap year"
    r03619 | "year" | "Time" | "days" | "validates" | "Check if a year is a leap year"

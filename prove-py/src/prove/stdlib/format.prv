module Format
  narrative: """String formatting and numeric display."""

transforms pad_left(s String, width Integer, fill Character) String
binary

transforms pad_right(s String, width Integer, fill Character) String
binary

transforms center(s String, width Integer, fill Character) String
binary

transforms hex(n Integer) String
binary

transforms bin(n Integer) String
binary

transforms octal(n Integer) String
binary

transforms decimal(x Float, places Integer) String
binary

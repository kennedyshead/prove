module Log
  narrative: """Console color constants for terminal output."""
  InputOutput outputs console

  RESET as String = "[0m"

  BOLD as String = "[1m"

  DIM as String = "[2m"

  RED as String = "[31m"

  GREEN as String = "[32m"

  YELLOW as String = "[33m"

  BLUE as String = "[34m"

  MAGENTA as String = "[35m"

  CYAN as String = "[36m"

  WHITE as String = "[37m"

detached debug(string String)
from
    console(f"{WHITE}[DEBUG]{string}{RESET}")

detached info(string String)
from
    console(f"{GREEN}[DEBUG]{string}{RESET}")

detached waring(string String)
from
    console(f"{YELLOW}[DEBUG]{string}{RESET}")

detached error(string String)
from
    console(f"{RED}[DEBUG]{string}{RESET}")

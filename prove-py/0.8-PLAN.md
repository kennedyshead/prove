# Prove v0.8 — Formatter Type Inference

## Goal

Make `prove format` type-aware: when a variable declaration has no
explicit type annotation and the right-hand side is a function call,
insert the function's return type as the annotation.

**Prerequisite:** v0.7 (IO, Parse, C FFI) must be complete.

---

## The Feature

### Before `prove format`

```prove
table = Table.new()
doc = Parse.toml(source)!
names = Table.keys(root)
trimmed = Text.trim(raw)
count = len(items)
```

### After `prove format`

```prove
table as Table<V> = Table.new()
doc as Result<Value, String> = Parse.toml(source)!
names as List<String> = Table.keys(root)
trimmed as String = Text.trim(raw)
count as Integer = len(items)
```

**Rule:** Only add a type annotation when `type_expr` is `None` (the
user did not write one). Never overwrite an existing annotation.

---

## Current State

The formatter (`formatter.py`) is purely syntactic. It walks the parsed
AST and emits source text — no type information, no symbol table access.

```
prove format:  lex → parse → format
prove check:   lex → parse → check → (format check)
prove build:   lex → parse → check → emit → compile
```

The checker already resolves function return types and registers them in
the symbol table. The formatter just needs access to that information.

---

## Implementation

### 1. Run checker before formatting

Update the `prove format` pipeline to optionally run the checker and
pass the resulting symbol table to the formatter. If the checker finds
errors, format without type inference (graceful degradation).

**File:** `cli.py` — `format_cmd` function

```
prove format:  lex → parse → [check] → format(symbols?)
```

The `--check` flag (dry-run) and `--stdin` mode also get this treatment.

### 2. Accept symbol table in formatter

Add an optional `symbols` parameter to `ProveFormatter`:

**File:** `formatter.py`

```python
class ProveFormatter:
    def __init__(self, symbols: SymbolTable | None = None):
        self._symbols = symbols
```

The formatter remains fully functional without symbols — it just skips
type inference for untyped variable declarations.

### 3. Infer types in `_format_var_decl`

When `vd.type_expr is None` and `self._symbols is not None`, inspect the
value expression to infer the type:

| Value expression | Lookup strategy |
|-----------------|-----------------|
| `Table.new()` | Module-qualified call: resolve `(module, verb, name, arity)` → return type |
| `len(items)` | Unqualified call: resolve `(name, arity)` → return type |
| `source!` | FailProp wrapping a call: unwrap the Result → inner type |
| Other | No inference — leave annotation empty |

**File:** `formatter.py` — new method `_infer_var_type`

### 4. Convert resolved types to type expressions

The symbol table stores resolved `Type` objects (`PrimitiveType`,
`GenericInstance`, `ListType`, etc.). The formatter needs to render
these as source-level type syntax (`String`, `Table<V>`, `List<String>`).

**File:** `formatter.py` — new method `_type_to_str`

| Resolved type | Output |
|---------------|--------|
| `PrimitiveType("String")` | `String` |
| `PrimitiveType("Integer")` | `Integer` |
| `ListType(PrimitiveType("String"))` | `List<String>` |
| `GenericInstance("Table", [TypeVariable("V")])` | `Table<V>` |
| `GenericInstance("Result", [T, E])` | `Result<T, E>` |
| `UnitType` | (skip — don't annotate Unit) |

### 5. Track current module context

For module-qualified calls like `Table.keys(root)`, the formatter
needs to know which module `Table` refers to. The existing
`SymbolTable.resolve_function(module, name, arity)` already handles
this. The formatter needs to track the current module context from
import declarations.

**File:** `formatter.py` — track imports during `_format_module_decl`

---

## Scope Boundaries

**In scope:**
- Variable declarations where RHS is a function call (direct or module-qualified)
- Variable declarations where RHS is a failable call (`expr!`)

**Out of scope (for now):**
- Literal inference (`x = 42` → `x as Integer = 42`)
- Complex expression inference (`x = a + b`)
- Lambda parameter type inference
- Narrowing generic type variables from usage context

---

## Files Changed

| File | Change |
|------|--------|
| `formatter.py` | Accept `SymbolTable`, add `_infer_var_type`, `_type_to_str` |
| `cli.py` | Run checker in `prove format` pipeline, pass symbols to formatter |
| Tests | New test cases for type inference formatting |

---

## Verification

Test with existing stdlib examples and the `read_toml` example project:

```bash
prove format examples/read_toml --check  # should pass after formatting
prove format examples/math --check
prove format examples/hello --check
```

Also verify:
- Existing type annotations are never modified
- Files with checker errors still format (without type inference)
- `prove format --stdin` works with type inference
- Round-trip: `prove format` twice produces identical output

---
---

# Prove v0.8.1 — Lint System Overhaul

## Context

The linting/diagnostic system has grown organically and now has several problems:
- Heavily overloaded codes (E100 covers 8+ lexer scenarios, E200 is ALL parser errors, E310/E311 each cover 4-7 unrelated situations)
- Messages are often terse/cryptic and don't follow natural English phrasing
- Almost no fix suggestions despite infrastructure supporting them (`Suggestion` class exists, never used)
- No documentation links despite docs existing at `prove.botwork.se/diagnostics`
- `Severity.NOTE` exists but is never emitted (no info-level messages)
- `diagnostics.md` only documents E360-E394 and W321-W326; E100-E352 are undocumented
- Some docs are out of sync with code (E365 docs say "ambiguous IO verb" but code does "matches verb requires algebraic"; E390 docs say "row count mismatch" but code says "ensures but no explain")
- W323 is documented but not implemented (E390 covers that scenario now)
- W326 is documented but not implemented in code

The user's `lints.md` establishes clear rules: natural English messages, include solutions (code examples where possible), link to documentation, errors only for things that won't compile, warnings for improvable code, info for everything else.

## Scope

This plan covers **only the lint/diagnostic improvements**. Once approved, this will be appended to `/workspace/prove-py/0.8-PLAN.md` as a post-v0.8 enhancement (v0.8.1).

---

## Phase 1: Infrastructure — `errors.py`, `lsp.py`, pre-commit hook

### 1a. Add `doc_url` to `Diagnostic` and create registry

**File: `src/prove/errors.py`**

- Add `doc_url: str | None = None` field to `Diagnostic`
- Add `DIAGNOSTIC_DOCS` dict mapping every code → URL fragment (`https://prove.botwork.se/diagnostics/#E100`)
- Add helper `make_diagnostic(code, severity, message, span, *, notes=None, suggestions=None)` that auto-populates `doc_url` from registry and builds the full `Diagnostic`
- Update `DiagnosticRenderer.render()` to append: `  = help: <url>` when `doc_url` is set

### 1b. Add INFO severity usage

- `Severity.NOTE` already exists — use it for info-level messages (rename display string to `"info"` in renderer output for clarity, keep enum name)
- Add `_info()` helper to checker alongside `_error()` and `_warning()`
- **Info = formatter-fixable.** Info-level diagnostics indicate things `prove format` can fix automatically. The diagnostic message should say so: append note `"Run 'prove format' to fix this."` to every info diagnostic. This makes info the "auto-fix" tier:
  - **Error** — won't compile, must be fixed by hand
  - **Warning** — compiles but should be improved by hand
  - **Info** — compiles, and `prove format` will fix it for you
- Examples of info-level diagnostics that the formatter handles:
  - **I201** (missing narrative) — formatter inserts a `narrative: "TODO"` placeholder
  - **W310 → I310** (implicitly typed variable) — already handled by v0.8 formatter type inference; downgrade from warning to info since `prove format` auto-inserts the type annotation
- When `prove format` runs, it should suppress info diagnostics for issues it just fixed (don't warn about something you're fixing in the same invocation)

### 1b2. Strict mode

- `--strict` flag (already exists on `prove check`) promotes all **warnings → errors**. Info stays info.
- This is the only behavioral difference in strict mode — no new flag logic needed beyond the severity promotion.

### 1c. LSP doc links

**File: `src/prove/lsp.py`**

- Use `lsp.CodeDescription(href=diag.doc_url)` in `_compile_diag()` so codes are clickable in editors

### 1d. Pre-commit link checker

**File: `scripts/check_doc_links.py`**

- Reads `DIAGNOSTIC_DOCS` from `errors.py`
- Parses headings in `docs/diagnostics.md`
- Verifies 1:1 mapping between registry and docs
- Exit non-zero on mismatch
- Wire into existing git hooks or document how to use

---

## Phase 2: Lexer — split E100 into E100-E110

**File: `src/prove/lexer.py`**

| Code | Situation | Message |
|------|-----------|---------|
| E100 | Tabs | "Tabs are not allowed in Prove source files, use spaces for indentation." |
| E101 | Unterminated string | "This string literal is missing its closing quote." |
| E102 | Unterminated triple-quoted string | "This triple-quoted string is missing its closing `\"\"\"`." |
| E103 | Unterminated character literal | "This character literal is missing its closing single quote." |
| E104 | Unterminated regex | "This regex literal is missing its closing `/`." |
| E105 | Unterminated raw string | "This raw string literal is missing its closing quote." |
| E106 | Unterminated f-string | "This f-string is missing its closing quote." |
| E107 | Unknown escape | "Unknown escape sequence '\\{ch}'. Valid escapes are: \\n, \\r, \\t, \\\\, \\\", \\0." |
| E108 | Unexpected end of escape | "Unexpected end of file inside an escape sequence." |
| E109 | Unexpected character | "Unexpected character {ch!r}." + note linking to syntax docs |
| E110 | Inconsistent indentation | "Inconsistent indentation: expected {n} spaces but found {m}." + note: "Prove uses significant indentation. Align this line with the block above." |

Update `tests/test_lexer.py` for new codes.

---

## Phase 3: Parser — split E200 into E200-E216, implement Rule 3

**File: `src/prove/parser.py`**

| Code | Situation | Message |
|------|-----------|---------|
| E200 | Missing module declaration | "Every Prove file must start with a module declaration." + suggestion template |
| E201 | Missing narrative on module | Downgrade to **info**: "Module '{name}' has no narrative. A narrative describes the module's purpose for documentation and tooling." |
| E210 | Unexpected token (generic) | "Expected {friendly_name} but found {actual}." — add `_TOKEN_NAMES` dict for human-readable token names |
| E211 | Expected declaration at top level | "Expected a declaration (transforms, validates, inputs, outputs, type, main) but found '{value}'." |
| E212 | Expected type body | "Expected a type body (fields or variants) after 'is' but found '{token}'." |
| E213 | Expected expression | "Expected an expression but found '{token}'." |
| E214 | Verb used as identifier | "'{value}' is a verb keyword and cannot be used as a name." |
| E215 | Expected pattern | "Expected a match pattern (variant name, literal, or _) but found '{token}'." |
| E216 | Duplicate verb in import | Already decent, formalize as own code |

**Rule 3 (error collapse):** When parse fails catastrophically, report only the first error + note: "Fix this error first, then re-check. If this message is unclear, report it at https://code.botwork.se/Botwork/prove/issues"

Move E200/E201 narrative check out of `checker.py` into parser (it's a parse-level concern). E201 becomes **info** — narrative is encouraged but doesn't block compilation (Rule 1). In strict mode (`--strict`), warnings are promoted to errors, but info stays info.

---

## Phase 4: Checker — split overloaded codes, improve messages, add new lints

**File: `src/prove/checker.py`**

### 4a. Split E310 (undefined name — 4 scenarios → 3 codes)

| Code | Situation | Message |
|------|-----------|---------|
| E310 | Undefined variable | "'{name}' is not defined." + fuzzy match (already exists) |
| E313 | Module not imported | "Module '{name}' is used but not imported. Add it to your imports." + suggestion with import line |
| E314 | Unknown module in import | "Unknown module '{name}'. Available standard library modules are: {list}." |

**Fix E310 severity:** Currently emitted as WARNING in some places despite being an E-code. Standardize: always ERROR.

### 4b. Split E311 (undefined function — 7 scenarios → 3 codes)

| Code | Situation | Message |
|------|-----------|---------|
| E311 | Undefined function (unqualified) | "Function '{name}' is not defined." + fuzzy match |
| E312 | Undefined function in module (qualified) | "Module '{module}' does not have a function '{name}' for verb '{verb}'." + note listing available functions |
| E315 | Function not found in stdlib import | "Module '{module}' does not export '{name}'." |

### 4c. Split W310 (2 unrelated warnings → 2 codes)

| Code | Situation | Message |
|------|-----------|---------|
| W310 | Implicitly typed variable | Keep current message (already has suggestion) |
| W311 | Intent without contracts | "Function '{name}' declares an intent but has no ensures or requires to validate it." |

### 4d. Improve E301/E302 — add line references

- **E301**: "'{name}' is already defined at line {n}." + secondary label pointing to original definition
- **E302**: "Variable '{name}' is already declared in this scope at line {n}." + secondary label
- Address lints.md question: E301 = top-level definitions (types, constants, functions same verb+name), E302 = variables within a scope. They are genuinely different — document why.
- **E301 verb check**: Verify duplicate detection considers verb — `foo` with `transforms` and `foo` with `validates` are different functions (channel dispatch), not duplicates.

### 4e. Improve all existing messages to follow rules

Key rewrites (biggest improvements):

| Code | Current | Improved |
|------|---------|----------|
| E300 | "undefined type '{name}'" | "Type '{name}' is not defined." + fuzzy match against known types |
| E320 | "type mismatch in binary expression" | "Cannot apply '{op}' to '{left_type}' and '{right_type}'." — show the actual operator and types |
| E325 | "f-string interpolation requires a stringable type, got {type}" | "Cannot interpolate '{type}' in an f-string. Supported types: String, Integer, Decimal, Float, Boolean, Character." |
| E340 | "no field '{field}' on type '{type}'" | "Type '{type}' has no field '{field}'." + fuzzy match against actual fields |
| E350 | "fail propagation in non-failable function" | "Cannot use '!' in '{name}' because it is not declared as failable. Add '!' after the return type." + suggestion |
| E360 | "validates has implicit Boolean return" | "A 'validates' function always returns Boolean. Remove the explicit return type." + suggestion |
| E361 | "pure function cannot be failable" | "Pure function '{name}' cannot use '!'. Functions with '{verb}' do not perform IO and cannot fail." |
| E362/E363 | "pure function cannot call IO function '{fname}'" | "Function '{name}' uses pure verb '{verb}' and cannot call IO function '{fname}'. Change the verb to 'inputs' or 'outputs' to allow IO." |
| E364 | already good | Add note: "Pass '{name}' as an extra parameter to the lambda instead." |
| E370 | "unknown variant '{name}'" | "'{name}' is not a variant of type '{type}'. Known variants: {list}." |

### 4f. Add `Suggestion` objects where code fixes are possible

- **E360**: suggestion removing return type from signature
- **E350**: suggestion adding `!` to return type
- **E371**: suggestion adding missing match arms
- **W310**: already has inline suggestion text, convert to proper `Suggestion`

### 4g. New lint rules

| Code | Rule | Message |
|------|------|---------|
| E316 | Name shadows builtin function | "'{name}' shadows the built-in function '{name}'. Choose a different name." |
| E317 | Name shadows builtin type | "'{name}' conflicts with the built-in type '{name}'. Choose a different name." |
| W302 | Unused import | "'{name}' is imported from '{module}' but never used." |
| W303 | Unused type definition | "Type '{name}' is defined but never used." |

For E316/E317, check against `BUILTINS` dict in `types.py` and the builtin function list in checker (println, print, readln, etc.).

---

## Phase 5: Prover — improve messages, add doc links

**File: `src/prove/prover.py`**

- Add doc links to all E366, E390-E394, W321-W325
- Improve W322: "Near-miss input is duplicated." → "This near-miss input is identical to one already declared on line {n}." + secondary label

---

## Phase 6: Documentation — full diagnostics.md rewrite

**File: `docs/diagnostics.md`**

Rewrite to cover ALL codes organized by category:

```
# Diagnostic Codes
## Lexer Errors (E100-E110)
## Parser Errors (E200-E216)
## Definition Errors (E300-E302, E316-E317)
## Name Resolution (E310-E315)
## Type Checking (E320-E331)
## Field Access (E340)
## Control Flow (E350-E352)
## Verb Enforcement (E360-E366)
## Pattern Matching (E370-E371)
## Contract Checking (E380-E386)
## Proof Verification (E390-E394)
## Warnings — Code Quality (W300-W303)
## Warnings — Style (W310-W311)
## Warnings — Contracts (W321-W326)
## Info Messages (I-codes)
```

Each entry: description, before/after code example, fix guidance. Matches existing format.

Fix out-of-sync docs:
- E365: docs say "ambiguous IO verb" but code does "matches requires algebraic" — update docs to match code
- E390: docs say "row count mismatch" but code says "ensures without explain" — update docs to match code
- W323: documented but not implemented — **implement** in `prover.py` (emit when function has `ensures` but no `explain`)
- W326: documented but not implemented — **implement** in `prover.py` (emit when `terminates` measure suggests O(n) call depth)

Run `mkdocs build --strict` after every edit.

---

## Phase 7: Update CLAUDE.md diagnostic codes section

**File: `CLAUDE.md`**

The current "Diagnostic Codes" section only lists verb enforcement (E360-E365), proof verification (E366, E390-E394), and warnings (W321-W325). After the overhaul it must reflect every code range — errors, warnings, and info messages.

Replace the entire `## Diagnostic Codes` section with:

```markdown
## Diagnostic Codes

### Lexer (lexer.py)

E100 (tabs), E101 (unterminated string), E102 (unterminated triple-quoted string),
E103 (unterminated char literal), E104 (unterminated regex), E105 (unterminated raw string),
E106 (unterminated f-string), E107 (unknown escape sequence), E108 (unexpected end of escape),
E109 (unexpected character), E110 (inconsistent indentation).

### Parser (parser.py)

E200 (missing module declaration), E201 (missing narrative — info),
E210 (unexpected token), E211 (expected declaration), E212 (expected type body),
E213 (expected expression), E214 (verb used as identifier), E215 (expected pattern),
E216 (duplicate verb in import).

### Definitions (checker.py)

E300 (undefined type), E301 (duplicate top-level definition), E302 (duplicate variable in scope),
E316 (name shadows builtin function), E317 (name shadows builtin type).

### Name resolution (checker.py)

E310 (undefined variable), E311 (undefined function — unqualified),
E312 (undefined function in module — qualified), E313 (module not imported),
E314 (unknown module in import), E315 (function not found in stdlib import).

### Type checking (checker.py)

E320 (type mismatch in binary expression), E321–E331 (various type errors),
E325 (f-string interpolation type), E340 (no field on type).

### Control flow (checker.py)

E350 (fail propagation in non-failable function), E351–E352 (control flow errors).

### Verb enforcement (checker.py)

E360 (`validates` has implicit Boolean return), E361 (pure function cannot be failable),
E362 (pure function cannot call IO builtin), E363 (pure function cannot call IO function),
E364 (lambda captures variable — closures not supported),
E365 (`matches` verb requires first parameter to be algebraic type).

### Pattern matching (checker.py)

E370 (unknown variant), E371 (non-exhaustive match).

### Proof verification (prover.py)

E366 (recursive function missing `terminates`),
E390 (ensures without explain), E391 (duplicate obligation),
E392 (obligations < ensures count), E393 (believe without ensures),
E394 (proof condition must be Boolean).

### Warnings — code quality

W302 (unused import), W303 (unused type definition).

### Warnings — style

W310 (implicitly typed variable), W311 (intent without contracts).

### Warnings — contracts

W321 (proof text missing references), W322 (duplicate near-miss),
W323 (ensures without explain), W324 (ensures without requires),
W325 (explain without ensures), W326 (recursion depth may be unbounded).

### Info messages

I201 (missing narrative — informational, not blocking).
```

Also update the `prover.py` line in the Project Structure table:
```
prover.py        — Structural proof verification (E366, E390-E394, W321-W326)
```

---

## Phase 8: Testing

- Update `tests/test_lexer.py` — new E101-E110 codes
- Update `tests/test_parser.py` — new E201-E216 codes
- Update `tests/test_checker.py` — new E312-E318, W302, W303, W311; updated messages for existing codes
- Update `tests/test_prover.py` — verify doc links
- New `tests/test_diagnostic_links.py` — verify every code in registry has docs and vice versa
- All 491+ existing tests must stay green throughout

---

## Phase Dependencies

```
Phase 1 (infrastructure)
  ├── Phase 2 (lexer)     ─┐
  ├── Phase 3 (parser)     ├── Phase 6 (docs) ── Phase 7 (CLAUDE.md)
  ├── Phase 4 (checker)   ─┤     │
  └── Phase 5 (prover)    ─┘     └── Phase 8 (testing)
```

Phases 2-5 can proceed in parallel after Phase 1.

---

## Summary of Code Changes

| New codes | ~20 (E101-E110, E201-E216, E312-E317, W302-W303, W311) |
|-----------|--------------------------------------------------------|
| Improved messages | ~40 existing diagnostic sites |
| New `Suggestion` usage | ~5 codes get code-fix suggestions |
| New infrastructure | doc_url field, DIAGNOSTIC_DOCS registry, make_diagnostic() helper, pre-commit hook |
| Files modified | errors.py, lexer.py, parser.py, checker.py, prover.py, lsp.py, diagnostics.md, 4 test files |
| New files | scripts/check_doc_links.py, tests/test_diagnostic_links.py |
